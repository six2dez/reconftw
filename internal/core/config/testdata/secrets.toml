# Simulated secrets.toml (sixth source) — overrides cli.toml on bare key collisions.
[notifications.slack]
webhook_url = "https://hooks.slack.com/services/test/secret/value"

[api_keys]
shodan = "test-shodan-key-from-secrets-toml"
