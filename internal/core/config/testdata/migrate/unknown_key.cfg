# unknown_key.cfg — fixture for the loud unknown-key warning test (CUT-06).
# THIS_IS_NOT_A_REAL_KEY has no v2-native mapping and is not in the drop-list,
# so the migrator must emit a `⚠ unknown key …` stderr line AND preserve it as a
# `# UNKNOWN …` comment — never silently drop it.
OSINT=true
THIS_IS_NOT_A_REAL_KEY=surprise
SUBDOMAINS_GENERAL=true
