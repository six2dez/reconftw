---
phase: 02-architecture-v2-design
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - .planning/decisions/0002-architecture-v2.md
autonomous: true
requirements:
  - ARCH-02

must_haves:
  truths:
    - "§2 TOML Configuration Schema exists in the ADR with every top-level section named (concurrency, subdomains, web, vulns, osint, notifications, axiom, mcp, advanced, legacy)"
    - "Every TOML block has inline comments showing the v1 flag it replaces"
    - "Per-key validation rules table exists for representative keys covering all validation categories (int ranges, path traversal, URL scheme, string allowlist)"
    - "[legacy] table section is shown with commented UPPER_CASE aliases and v2-native mapping notes"
    - "D-11 drop list is documented with migrator warning text per flag"
  artifacts:
    - path: ".planning/decisions/0002-architecture-v2.md"
      provides: "§2 TOML Configuration Schema section — fully populated"
      contains: "[concurrency]"
    - path: ".planning/decisions/0002-architecture-v2.md"
      provides: "Per-key validation rules"
      contains: "go-playground/validator"
  key_links:
    - from: "§2 TOML Schema"
      to: "ARCH-02"
      via: "requirement reference in section heading or body"
      pattern: "ARCH-02"
    - from: "[legacy] table"
      to: "v2-native keys"
      via: "inline comment mapping"
      pattern: "maps to"
---

<objective>
Fill §2 TOML Configuration Schema in the ADR — the longest and most detailed section.

Purpose: D-09 requires all ~290-310 v1 flags to have a documented TOML home. D-10 requires
both v2-native hierarchy AND `[legacy]` UPPER_CASE aliases. D-12 requires per-key validation
rules (type, range, regex, required/optional, default, mutex groups) locked in the ADR.
Phase 3 config loader implements the rules exactly as specified here.

Output: §2 of .planning/decisions/0002-architecture-v2.md fully populated with:
- Complete v2-native TOML schema (all sections with annotated samples)
- [legacy] table pattern and collision resolution policy
- Per-key validation rules table
- D-11 drop list with migrator warning texts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-architecture-v2-design/02-CONTEXT.md
@.planning/phases/02-architecture-v2-design/02-RESEARCH.md
@.planning/phases/02-architecture-v2-design/02-PATTERNS.md
@.planning/decisions/0001-language.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write §2 TOML Schema — v2-native hierarchy and [legacy] table</name>
  <files>.planning/decisions/0002-architecture-v2.md</files>
  <read_first>
    - .planning/phases/02-architecture-v2-design/02-RESEARCH.md §TOML Schema Design (lines ~239-337) — complete section mapping (v2-native → v1 approximate group), annotated TOML sample with inline comments, [legacy] table convention, section grouping order (operational first, then integration, then advanced/legacy last)
    - .planning/phases/02-architecture-v2-design/02-PATTERNS.md §TOML Sample Style — every key has inline comment showing v1 flag replaced, [legacy] entries shown as comments, section grouping order, default values shown in every sample
    - .planning/phases/02-architecture-v2-design/02-CONTEXT.md — D-09 (all ~323 flags 1:1), D-10 (v2-native + [legacy] aliases), D-11 (drop list), D-12 (per-key validation)
    - reconftw.cfg (reference — all v1 flags to cover; executor reads to verify no major section is missed)
    - .planning/decisions/0002-architecture-v2.md — current file to determine where §2 stub begins and append/replace it
  </read_first>
  <action>
    Replace the §2 TOML Configuration Schema stub with the full section content. The section MUST contain:

    SUBSECTION §2.1 — Schema Authoring Notes: one paragraph explaining the approach (D-09: all flags covered, D-10: v2-native + legacy aliases, D-11: drop list, D-12: validation rules locked here). Include the note: "NEVER use spf13/viper — key lowercasing breaks the legacy alias table. Use knadh/koanf/v2."

    SUBSECTION §2.2 — V2-Native TOML Schema: a single annotated ```toml code block covering ALL of the following top-level sections (each with representative keys; inline comments show the v1 flag each replaces):
    - `[concurrency]`: max_jobs (was PARALLEL_MAX_JOBS), heartbeat_seconds, log_mode, tail_lines, job_timeout_seconds, kill_grace_seconds
    - `[subdomains]`: enabled (was SUBDOMAINS_GENERAL); nested: [subdomains.passive] enabled + timeout_minutes, [subdomains.brute] enabled, [subdomains.permut] enabled + limit_bytes, [subdomains.takeover] enabled, [subdomains.crt] enabled + limit, [subdomains.dns_resolve] timeout_minutes (was DNS_RESOLVE_TIMEOUT), [subdomains.scope] only_resolved (was ONLY_RESOLVED)
    - `[web]`: nested: [web.probe] enabled + ports + rate_limit + timeout_seconds + threads, [web.fuzz] enabled + rate_limit + threads + max_time_seconds, [web.screenshots] enabled, [web.nuclei] enabled + rate_limit + severity, [web.js] enabled (was JSCHECKS), [web.urls] enabled (was URLCHECKS), [web.waf] enabled
    - `[vulns]`: enabled (was VULNS_GENERAL); nested: [vulns.xss] enabled, [vulns.sqli] enabled + engine (sqlmap|ghauri|both), [vulns.ssrf] enabled, [vulns.lfi] enabled, [vulns.ssti] enabled, [vulns.crlf] enabled, [vulns.smuggling] enabled, [vulns.cmdi] enabled, [vulns.cache] enabled
    - `[osint]`: enabled; nested: [osint.google_dorks] enabled, [osint.github] enabled + leaks_enabled + threads, [osint.cloud] enabled, [osint.emails] enabled, [osint.postman] enabled, [osint.swagger] enabled, [osint.spoofy] enabled, [osint.msft] enabled
    - `[notifications]`: enabled (was NOTIFICATION); nested: [notifications.slack] channel + webhook_url, [notifications.telegram] bot_token + chat_id, [notifications.discord] webhook_url; events = ["on-critical-finding", "on-scan-complete", "on-failure"]
    - `[axiom]`: enabled + fleet_name + fleet_count + fleet_regions + shutdown_on_end + auto_fix_hostkey
    - `[mcp]`: enabled (default false) + api_key + transport (stdio|http) + port
    - `[advanced]`: nested [advanced.tools.<toolname>] pattern with examples: subfinder (timeout_minutes), nuclei (rate_limit + severity + flags), ffuf (rate_limit + threads + flags + max_time_seconds), httpx (flags), puredns (resolvers_file), gf (patterns_dir)
    - `[legacy]`: shown as COMMENTED entries (not active assignments) with arrow annotations showing v2-native mapping target

    SUBSECTION §2.3 — Legacy Table Pattern and Collision Resolution: explain that [legacy] keys are generated by the migrator (not for new users), that when both legacy.KEY and the v2-native key are set, v2-native ALWAYS wins (koanf merge order), and that the config loader emits WARN on collision.

    SUBSECTION §2.4 — D-11 Drop List: table with columns (v1 Flag | Reason for Drop | Migrator Warning text). Include all entries from RESEARCH.md §Per-Key Validation Rules D-11 drop list: AVAILABLE_CORES, DEBUG_STD/DEBUG_ERROR, profile_shell, tools (path prefix), reconftw_version, _detected_shell.

    Every TOML key in the schema must have an inline comment. No key may be left uncommented.
  </action>
  <verify>
    <automated>grep -c '# was ' .planning/decisions/0002-architecture-v2.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep '## §2 TOML' .planning/decisions/0002-architecture-v2.md` exits 0
    - ADR contains `[STUB` nowhere in the §2 section (stub replaced)
    - `grep -c '# was ' .planning/decisions/0002-architecture-v2.md` outputs ≥ 20 (inline v1-flag comments)
    - `grep '\[legacy\]' .planning/decisions/0002-architecture-v2.md` exits 0
    - `grep 'AVAILABLE_CORES' .planning/decisions/0002-architecture-v2.md` exits 0 (D-11 drop list)
    - `grep 'koanf' .planning/decisions/0002-architecture-v2.md` exits 0 (library citation)
    - `grep 'mcp.*enabled' .planning/decisions/0002-architecture-v2.md` exits 0 (MCP section)
  </acceptance_criteria>
  <done>§2 stub replaced with full TOML schema — all top-level sections present with inline annotations and [legacy] table pattern.</done>
</task>

<task type="auto">
  <name>Task 2: Write §2 per-key validation rules and full v1 flag coverage audit</name>
  <files>.planning/decisions/0002-architecture-v2.md</files>
  <read_first>
    - .planning/phases/02-architecture-v2-design/02-RESEARCH.md §Per-Key Validation Rules (lines ~339-370) — validation table (key, type, validation tag, security reason), mutex group rules, D-11 drop list
    - .planning/phases/02-architecture-v2-design/02-RESEARCH.md §Common Pitfalls §Pitfall 1 (TOML key lowercasing trap), §Pitfall 5 (legacy table collision)
    - .planning/phases/02-architecture-v2-design/02-CONTEXT.md D-12 — per-key validation rules locked in Phase 2; type, range, regex, required/optional, default, mutex-exclusive groups
    - .planning/decisions/0002-architecture-v2.md — current state after Task 1
  </read_first>
  <action>
    Append SUBSECTION §2.5 — Per-Key Validation Rules to the §2 section.

    The subsection contains a markdown table with columns: Key | Type | Validation Tag | Default | Security Reason.
    Cover ALL of the following key categories (minimum 15 rows):
    - concurrency.max_jobs: int, min=1,max=64, 4, DoS prevention
    - concurrency.job_timeout_seconds: int, min=0,max=86400, 0 (disable), cap prevents accidental infinite hang
    - web.probe.rate_limit: int, min=0,max=10000, 150, 0=unlimited; cap prevents DoS vs target
    - web.fuzz.threads: int, min=0,max=500, 0 (auto), cap per XCUT-01 perf budget
    - web.nuclei.severity: string, oneof=info,low,medium,high,critical (comma-sep), "info,low,medium,high,critical", allowlist prevents injection via template filter
    - subdomains.permut.limit_bytes: int64, min=0,max=10737418240, 2147483648, 10GB hard cap; DoS prevention
    - subdomains.crt.limit: int, min=1,max=9999999, 999999, cap prevents memory exhaustion on large crt.sh results
    - axiom.fleet_count: int, min=1,max=1000, 10, fleet provisioning safety cap
    - Any file-path key (wordlist, resolver, config): string, file (must exist if non-empty) + custom `nopath_traversal` validator rejecting `..` segments, varies, path traversal prevention
    - Any URL key (webhook, proxy, collab): string, url+startswith=https OR startswith=http, "", scheme allowlist prevents file:// gopher:// etc.
    - API key keys (SHODAN_API_KEY, PDCP_API_KEY, etc.): string, omitempty + max=256, "", length cap only; opaque value; no structural validation
    - notifications.slack.webhook_url: string, omitempty+url+startswith=https, "", must be HTTPS; redacted in logs via Secret type
    - mcp.api_key: string, omitempty+min=16+max=256, "", min length enforces non-trivial key
    - concurrency.log_mode: string, oneof=summary,tail,full, "summary", allowlist only
    - advanced.tools.nuclei.rate_limit: int, min=0,max=10000, 150, per-tool cap

    After the table, add a MUTEX GROUPS subsection explaining:
    1. [legacy] key + v2-native key for same config: v2-native wins; WARN emitted
    2. vulns.enabled=false AND any [vulns.*] sub-enabled=true: sub-flags ignored; WARN at startup

    Add a PITFALLS NOTE with two items referencing RESEARCH.md pitfall sections:
    1. "Viper lowercasing trap: NEVER use spf13/viper — always koanf/v2 (RESEARCH.md §Pitfall 1)"
    2. "ConfigError.Message must NEVER include raw secret values — see RESEARCH.md §Pitfall 2"
  </action>
  <verify>
    <automated>grep -c '| min=' .planning/decisions/0002-architecture-v2.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep '§2.5' .planning/decisions/0002-architecture-v2.md` exits 0 (subsection exists)
    - `grep -c '| min=' .planning/decisions/0002-architecture-v2.md` outputs ≥ 8 (multiple int-range validation rows)
    - `grep 'nopath_traversal' .planning/decisions/0002-architecture-v2.md` exits 0 (path traversal custom validator named)
    - `grep 'startswith=https' .planning/decisions/0002-architecture-v2.md` exits 0 (URL scheme allowlist)
    - `grep 'MUTEX' .planning/decisions/0002-architecture-v2.md` exits 0 (mutex groups section)
    - `grep 'ARCH-02' .planning/decisions/0002-architecture-v2.md` exits 0 (requirement referenced)
  </acceptance_criteria>
  <done>§2 is complete: full TOML schema with v1-flag annotations, [legacy] table pattern, collision resolution policy, per-key validation rules, D-11 drop list, and mutex groups. D-09, D-10, D-11, D-12 are all implemented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reconftw.cfg (v1 input) → ADR §2 (spec) | v1 flag values read by executor to verify coverage; no execution of v1 flags |
| TOML schema spec → Phase 3 config loader | Phase 3 implements validation rules from this section; correctness here has security impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Tampering | TOML path-key validation (file paths) | mitigate | ADR must specify `nopath_traversal` custom validator rejecting `..` segments for all file-path keys; locked in §2.5 validation table |
| T-02-02-02 | Tampering | TOML URL-key validation (webhook_url, collab_server) | mitigate | ADR must specify `url+startswith=https` for all webhook/proxy/collaborator URL keys; scheme allowlist blocks file:// gopher:// injection |
| T-02-02-03 | Information Disclosure | Secret fields in TOML schema (API keys, webhook tokens) | mitigate | ADR must specify Secret type for all credential fields; NEVER structural validation on secret value content (opaque, max=256 only) |
| T-02-02-04 | Tampering | [legacy] table collision resolution | mitigate | ADR specifies explicit merge order (v2-native always wins) + WARN on collision; prevents silent misconfiguration |
| T-02-02-05 | DoS | Thread/concurrency limits in TOML | mitigate | Explicit max caps on all thread/job counts (max_jobs ≤ 64, fuzz.threads ≤ 500) prevent resource exhaustion via misconfigured config |
  T-02-02-SC | Tampering | npm/pip/cargo installs | accept | Phase 2 plan 02 installs no packages |
</threat_model>

<verification>
1. `grep '## §2 TOML' .planning/decisions/0002-architecture-v2.md` exits 0
2. `grep -c '# was ' .planning/decisions/0002-architecture-v2.md` outputs ≥ 20
3. `grep '\[legacy\]' .planning/decisions/0002-architecture-v2.md` exits 0
4. `grep 'AVAILABLE_CORES' .planning/decisions/0002-architecture-v2.md` exits 0
5. `grep -c '| min=' .planning/decisions/0002-architecture-v2.md` outputs ≥ 8
6. `grep 'nopath_traversal' .planning/decisions/0002-architecture-v2.md` exits 0
7. No `[STUB` marker remains in §2 section
</verification>

<success_criteria>
§2 TOML Configuration Schema is fully populated with: complete v2-native hierarchy, [legacy] alias pattern, D-11 drop list, and per-key validation rules for all security-critical key categories (file paths, URLs, integers, allowlists, API keys). ARCH-02 is satisfied.
</success_criteria>

<output>
Create `.planning/phases/02-architecture-v2-design/02-02-SUMMARY.md` when done.
</output>
