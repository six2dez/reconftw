---
phase: 02-architecture-v2-design
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/decisions/0002-architecture-v2.md
  - .planning/decisions/verify-0002.sh
  - interfaces_check/main.go
autonomous: true
requirements:
  - ARCH-01

must_haves:
  truths:
    - "ADR skeleton file exists at .planning/decisions/0002-architecture-v2.md with correct frontmatter (Status: Proposed)"
    - "All 12 numbered section headings (§1 through §12) exist as H2 stubs with placeholder text"
    - "verify-0002.sh exists, is executable, and runs without error (all 4 checks structured but not yet passing)"
    - "interfaces_check/main.go exists with the correct SPDX header and package main declaration"
  artifacts:
    - path: ".planning/decisions/0002-architecture-v2.md"
      provides: "ADR skeleton with frontmatter + 12 numbered section stubs"
      contains: "Status: Proposed"
    - path: ".planning/decisions/verify-0002.sh"
      provides: "Pre-sign verification gate script"
      contains: "set -euo pipefail"
    - path: "interfaces_check/main.go"
      provides: "Compile-check package for Go snippets"
      contains: "package main"
  key_links:
    - from: ".planning/decisions/verify-0002.sh"
      to: ".planning/decisions/0002-architecture-v2.md"
      via: "ADR= variable reference"
      pattern: 'ADR=".planning/decisions/0002-architecture-v2.md"'
---

<objective>
Scaffold the ADR 0002 skeleton and supporting verification infrastructure so all
Wave 2 content-fill plans can write to a pre-existing file with the correct structure.

Purpose: Wave 2 plans fill sections in parallel — they must start from a structurally
valid ADR file (frontmatter + 12 stubbed sections), not build the skeleton themselves.
This plan creates the three files that gate every subsequent plan.

Output:
- `.planning/decisions/0002-architecture-v2.md` (skeleton — Status: Proposed, all 12 section stubs)
- `.planning/decisions/verify-0002.sh` (pre-sign gate script — 4 checks)
- `interfaces_check/main.go` (compile-check package — package main)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-architecture-v2-design/02-CONTEXT.md
@.planning/phases/02-architecture-v2-design/02-PATTERNS.md
@.planning/phases/02-architecture-v2-design/02-RESEARCH.md
@.planning/decisions/0001-language.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write ADR 0002 skeleton with correct frontmatter and 12 section stubs</name>
  <files>.planning/decisions/0002-architecture-v2.md</files>
  <read_first>
    - .planning/decisions/0001-language.md — canonical ADR template shape: exact frontmatter bullet syntax (* Status: * Date: * Deciders: * Tags:), section heading pattern (## Context / ## Decision / ## Consequences / ## References / ## Signed), signed block format (**Signed by:** **Date:** **Git SHA:**)
    - .planning/phases/02-architecture-v2-design/02-PATTERNS.md — frontmatter rules, section heading sequence for ADR 0002 (12 numbered §-sections between ## Decision and ## Consequences), amendment block convention, code block language tag rules
    - .planning/phases/02-architecture-v2-design/02-CONTEXT.md — D-01 (single mega-ADR), D-04 (TL;DR + glossary + reading order at top), D-05 (BINDING annotation), D-15 (file location)
    - .planning/phases/02-architecture-v2-design/02-RESEARCH.md §Architecture Patterns — System Architecture Diagram (mermaid), Recommended Project Structure
  </read_first>
  <action>
    Create `.planning/decisions/0002-architecture-v2.md` with the following structure, honoring the 0001-language.md template exactly:

    FRONTMATTER (no YAML fence — uses * bullet syntax, no blank line between title and frontmatter):
    - Title line: `# 0002 — Architecture v2 Design for reconFTW`
    - `* Status: Proposed`
    - `* Date: [leave blank — filled at sign-off per D-13]`
    - `* Deciders: six2dez (solo maintainer, project owner)`
    - `* Tags: architecture, v2.0, go, toml, interfaces, output-tree, cli, testing, logging`

    SECTIONS IN ORDER:
    1. `## Context` — one-paragraph summary: Phase 2 locks all v2.0 contracts; this doc is the source of truth for Phase 3-12; references REQUIREMENTS.md ARCH-01..ARCH-12; language locked as Go per ADR 0001.
    2. `## Decision` — one paragraph: v2 architecture is specified in 12 numbered sections (§1-§12) below; after sign-off, these contracts are BINDING per D-05; amendments via inline amendment block (D-06).
    3. INSERT all 12 numbered sections as H2 stubs — each has a heading and a `[STUB — populated in Wave 2]` placeholder plus its ARCH-NN requirement reference:
       - `## §1 Overview & System Diagram` (ARCH-01)
       - `## §2 TOML Configuration Schema` (ARCH-02)
       - `## §3 Output Tree Layout` (ARCH-03)
       - `## §4 Compat Symlink Layer` (ARCH-04)
       - `## §5 Interface Signatures` (ARCH-05, ARCH-06, ARCH-07)
       - `## §6 Error Class Hierarchy` (ARCH-08)
       - `## §7 Failure Policy Model` (ARCH-09)
       - `## §8 CLI Surface` (ARCH-10)
       - `## §9 Test Ring Policy` (ARCH-11)
       - `## §10 Logging Policy & Secret Redaction` (ARCH-12)
       - `## §11 Pre-Sign Verification Gate` (ARCH-01 — process)
       - `## §12 Amendment Log` (D-06)
    4. `## Consequences` — two H3 subsections: `### Positive` and `### Negative`, each with `[STUB — populated in Wave 3]`.
    5. `## References` — stub list with key canonical refs: .planning/REQUIREMENTS.md, .planning/ROADMAP.md Phase 2, .planning/research/ARCHITECTURE.md, .planning/research/STACK.md, .planning/decisions/0001-language.md, .planning/phases/02-architecture-v2-design/02-RESEARCH.md.
    6. `## Signed` — use exact format from 0001-language.md:
       ```
       **Signed by:** six2dez (single maintainer, project owner)
       **Date:** [to be filled at sign-off]
       **Git SHA (this ADR):** <see git log -- .planning/decisions/0002-architecture-v2.md>
       ```

    All 12 section stubs must exist. Wave 2 plans replace the `[STUB]` lines with real content. Do NOT add content to any section beyond the stub — that is Wave 2's job.
  </action>
  <verify>
    <automated>grep -c '## §' .planning/decisions/0002-architecture-v2.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/decisions/0002-architecture-v2.md`
    - `grep -c '## §' .planning/decisions/0002-architecture-v2.md` outputs 12
    - `grep '^\* Status:' .planning/decisions/0002-architecture-v2.md` outputs `* Status: Proposed`
    - `grep '## Signed' .planning/decisions/0002-architecture-v2.md` exits 0
    - `grep 'ARCH-01' .planning/decisions/0002-architecture-v2.md` exits 0 (requirement referenced in stub)
    - File does NOT begin with `---` YAML front matter (uses * bullet syntax, not YAML)
  </acceptance_criteria>
  <done>ADR skeleton file exists with correct frontmatter bullet syntax, 12 section stub headings, Consequences/References/Signed sections — Wave 2 plans can fill sections in parallel without structural conflict.</done>
</task>

<task type="auto">
  <name>Task 2: Write verify-0002.sh and interfaces_check/main.go</name>
  <files>
    .planning/decisions/verify-0002.sh
    interfaces_check/main.go
  </files>
  <read_first>
    - .planning/phases/02-architecture-v2-design/02-RESEARCH.md §Pre-Sign Verification Gate (lines 1096-1137) — complete reference script; copy verbatim, adjust only ADR path variable if needed
    - .planning/phases/02-architecture-v2-design/02-PATTERNS.md §verify-0002.sh — shebang + error discipline (#!/usr/bin/env bash + set -euo pipefail), script variable convention (ADR=".planning/decisions/0002-architecture-v2.md"), check section format, 4 required checks
    - .planning/phases/02-architecture-v2-design/02-PATTERNS.md §interfaces_check/main.go — SPDX header, purpose comment lines, Source/reference back-links, `package main` declaration
    - spike/go/internal/passive/passive.go (lines 1-7) — Go file header convention (SPDX + purpose + package line)
  </read_first>
  <action>
    CREATE `.planning/decisions/verify-0002.sh`:
    Copy the reference script verbatim from RESEARCH.md §Pre-Sign Verification Gate (lines 1096-1137).
    The script checks 4 things: (1) ARCH-01..ARCH-12 grep coverage in ADR, (2) TOML blocks parse via tomljson, (3) `go build ./interfaces_check/...` compiles, (4) glossary terms present (AppContext, Backend, Task, Result, FailurePolicy, Secret, ToolError, ToolTimeout, OutOfScope, AxiomFailure, ConfigError, ScopeError, ChecksumMismatch).
    Make it executable: `chmod +x .planning/decisions/verify-0002.sh`.
    The ADR variable must be `ADR=".planning/decisions/0002-architecture-v2.md"` (relative path; script is run from repo root).

    CREATE `interfaces_check/main.go`:
    Use the adapted file header from PATTERNS.md §interfaces_check/main.go:
    - Line 1: `// SPDX-License-Identifier: MIT`
    - Lines 2-5: purpose comment block (compile-only build target for ADR 0002 pre-sign gate D-14; source reference; DO NOT import into production code warning)
    - `package main`
    - `import "fmt"` (minimal import to make it a valid compilable main package)
    - `func main() { fmt.Println("interfaces_check: compile gate — DO NOT RUN") }`

    NOTE: interfaces_check/main.go starts minimal. Wave 2 Plan C (Interface Signatures) populates it with the actual interface type imports once those types exist in the ADR Go blocks. The compile check itself is exercised in Wave 4 (verify gate).
  </action>
  <verify>
    <automated>bash -n .planning/decisions/verify-0002.sh && test -x .planning/decisions/verify-0002.sh && go build ./interfaces_check/...</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/decisions/verify-0002.sh` exists and `bash -n` (syntax check) passes
    - `test -x .planning/decisions/verify-0002.sh` exits 0 (file is executable)
    - Script contains `set -euo pipefail` on line 2
    - Script contains `ADR=".planning/decisions/0002-architecture-v2.md"`
    - Script contains all 4 check sections (=== Check 1 through === Check 4)
    - `interfaces_check/main.go` exists with `package main` declaration
    - `go build ./interfaces_check/...` exits 0 (compiles successfully)
    - `grep 'SPDX-License-Identifier: MIT' interfaces_check/main.go` exits 0
  </acceptance_criteria>
  <done>verify-0002.sh is executable with 4 checks; interfaces_check/main.go compiles; both files exist as the pre-sign infrastructure foundation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo root → verify-0002.sh | Script reads ADR file; must be run from repo root (relative ADR path) |
| interfaces_check → go toolchain | `go build` invoked on planner-authored stub; no external packages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | verify-0002.sh script | mitigate | Script uses `set -euo pipefail`; all commands exit-on-error; no user input processed |
| T-02-01-02 | Information Disclosure | ADR skeleton (Status: Proposed) | accept | ADR in .planning/decisions/ on `rewrite/v2` branch; low-value target (no secrets) |
| T-02-01-03 | Tampering | interfaces_check/main.go | accept | Compile-only check package; no production imports; `package main` with no logic |
| T-02-01-SC | Tampering | npm/pip/cargo installs | accept | Phase 2 installs no packages; Go build uses existing module cache |
</threat_model>

<verification>
1. `grep -c '## §' .planning/decisions/0002-architecture-v2.md` → outputs exactly 12
2. `grep '^\* Status:' .planning/decisions/0002-architecture-v2.md` → `* Status: Proposed`
3. `bash -n .planning/decisions/verify-0002.sh` → exits 0
4. `test -x .planning/decisions/verify-0002.sh` → exits 0
5. `go build ./interfaces_check/...` → exits 0
6. All three files committed to `rewrite/v2`
</verification>

<success_criteria>
ADR skeleton exists with 12 section stubs, correct ADR 0001-style frontmatter, and Status: Proposed.
verify-0002.sh is executable with 4 check sections and set -euo pipefail.
interfaces_check/main.go compiles with `go build ./interfaces_check/...`.
Wave 2 plans can start immediately in parallel.
</success_criteria>

<output>
Create `.planning/phases/02-architecture-v2-design/02-01-SUMMARY.md` when done.
</output>
