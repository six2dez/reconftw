---
phase: 02-architecture-v2-design
plan: 07
type: execute
wave: 4
depends_on:
  - "02-06"
autonomous: false
files_modified:
  - .planning/decisions/0002-architecture-v2.md
  - .planning/STATE.md
  - .planning/PROJECT.md
requirements:
  - ARCH-01

must_haves:
  truths:
    - "verify-0002.sh exits 0 (all 4 checks pass)"
    - "ADR Status field is 'Accepted' (not 'Proposed')"
    - "ADR Signed block has the real git SHA, signed-by name, and sign date"
    - "STATE.md Phase 2 status is 'Complete'"
    - "PROJECT.md Key Decisions table has a new row for ADR 0002 sign-off"
    - "All changes are committed to rewrite/v2"
  artifacts:
    - path: ".planning/decisions/0002-architecture-v2.md"
      provides: "Signed ADR 0002 (Status: Accepted)"
      contains: "Status: Accepted"
    - path: ".planning/STATE.md"
      provides: "Updated phase status"
      contains: "Phase 2.*Complete"
    - path: ".planning/PROJECT.md"
      provides: "Key Decision row for ADR 0002"
      contains: "ADR 0002"
  key_links:
    - from: "ADR 0002 Signed block"
      to: "git SHA of sign-off commit"
      via: "Git SHA field in ## Signed"
      pattern: "Git SHA"
---

<objective>
Run the pre-sign verification gate, sign the ADR, update STATE.md and PROJECT.md, and commit.

Purpose: D-13 (solo same-day sign-off) and D-14 (pre-sign programmatic check) gate the ADR
moving from Proposed to Accepted. This is the CHECKPOINT plan — it pauses for the maintainer
to run the verification gate, review the output, and explicitly approve signing. After approval,
all state tracking is updated and the contracts are locked.

Output:
- verify-0002.sh exits 0 (maintainer sees all 4 checks pass)
- .planning/decisions/0002-architecture-v2.md Status flipped to Accepted, Signed block filled
- .planning/STATE.md Phase 2 marked Complete
- .planning/PROJECT.md Key Decisions updated
- Git commit to rewrite/v2
- 02-07-SUMMARY.md written
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-architecture-v2-design/02-CONTEXT.md
@.planning/decisions/0002-architecture-v2.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run pre-sign verification gate (D-14)</name>
  <files>.planning/decisions/verify-0002.sh</files>
  <read_first>
    - .planning/decisions/verify-0002.sh — the verification script; read it to understand what each check does before running
    - .planning/decisions/0002-architecture-v2.md — current ADR state; verify it looks complete before running the gate
    - .planning/phases/02-architecture-v2-design/02-CONTEXT.md D-14 — what the 4 checks verify
  </read_first>
  <action>
    Run the pre-sign verification gate from the repository root:
    ```
    bash .planning/decisions/verify-0002.sh
    ```

    If any check fails, FIX the root cause before proceeding. The gate must exit 0 before signing.

    Fix categories:
    - Check 1 fails (ARCH-NN not found): add the missing requirement reference to the appropriate ADR section
    - Check 2 fails (TOML parse error): fix the malformed TOML block in §2
    - Check 3 fails (go build fails): fix the Go syntax error in interfaces_check/main.go or the ADR Go snippet
    - Check 4 fails (glossary term missing): add the missing term definition to the Glossary section

    After all 4 checks pass, proceed to Task 2.

    PREREQUISITE: `tomljson` must be installed. If not present, install it:
    `go install github.com/pelletier/go-toml/v2/cmd/tomljson@latest`
    (This is the only tool installation in Phase 2, for the D-14 gate only — not a production dependency.)
  </action>
  <verify>
    <automated>bash .planning/decisions/verify-0002.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash .planning/decisions/verify-0002.sh` exits 0 with "=== ALL CHECKS PASSED — safe to sign ===" in output
    - All 4 check sections emit "OK: ..." lines (no "FAIL:" lines)
    - Check 1: all 12 ARCH-NN IDs found
    - Check 2: all TOML blocks parse without error
    - Check 3: `go build ./interfaces_check/...` exits 0
    - Check 4: all 13 glossary terms found (AppContext, Backend, Task, Result, FailurePolicy, Secret, ToolError, ToolTimeout, OutOfScope, AxiomFailure, ConfigError, ScopeError, ChecksumMismatch)
  </acceptance_criteria>
  <done>All 4 verification checks pass; gate output shows "=== ALL CHECKS PASSED — safe to sign ===". ADR is verified complete and internally consistent.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Claude has run verify-0002.sh and all 4 checks passed. The ADR body is complete:
    - §1 System Overview with Mermaid diagram
    - §2 TOML Schema with all v1 flags, [legacy] table, and per-key validation rules
    - §3 Output Tree Layout with all artefact schemas
    - §4 Compat Symlink Layer with AtomicSymlink pattern and v1→v2 mapping
    - §5 Interface Signatures (Task, Backend, AppContext — all BINDING)
    - §6 Error Class Hierarchy (7 types)
    - §7 Failure Policy Model
    - §8 CLI Surface with deprecation pattern
    - §9 Test Ring Policy
    - §10 Logging Policy with Secret type + RedactingHandler
    - §11 Pre-Sign Verification Gate documentation
    - §12 Amendment Log
    - TL;DR + Glossary + Reading Order at the top

    All 4 verification checks pass. verify-0002.sh exits 0.
  </what-built>
  <how-to-verify>
    1. Read `.planning/decisions/0002-architecture-v2.md` — review the TL;DR and at least 3 sections that matter most to you (suggested: §5 Interfaces, §2 TOML Schema, §10 Logging Policy)
    2. Spot-check that interface signatures match your intent: Task has 6 methods, Backend has 4 methods, AppContext has 9 fields
    3. Spot-check that the TOML schema has the sections you care about (subdomains, web, vulns, axiom, mcp, notifications)
    4. Confirm verify-0002.sh still passes: `bash .planning/decisions/verify-0002.sh`
    5. If everything looks correct, type "approved" to proceed with signing
    6. If you see issues, describe them — Claude will fix before signing
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the sign-off ceremony, or describe any issues to fix first.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Sign the ADR and update STATE.md + PROJECT.md</name>
  <files>
    .planning/decisions/0002-architecture-v2.md
    .planning/STATE.md
    .planning/PROJECT.md
  </files>
  <read_first>
    - .planning/decisions/0002-architecture-v2.md — read current ## Signed block to understand what to replace
    - .planning/decisions/0001-language.md — ## Signed block format reference (Signed by, Date, Git SHA fields)
    - .planning/STATE.md — read current Phase 2 status row to understand what to update
    - .planning/PROJECT.md — read Key Decisions table to understand where to append
    - .planning/phases/02-architecture-v2-design/02-CONTEXT.md — D-13 (solo same-day), D-16 (sign-off triggers STATE.md + PROJECT.md only)
  </read_first>
  <action>
    STEP 1 — Flip ADR Status and fill Signed block:
    In .planning/decisions/0002-architecture-v2.md:
    - Change `* Status: Proposed` to `* Status: Accepted`
    - Fill `* Date:` with today's date in ISO 8601 format (YYYY-MM-DD)
    - In ## Signed block: fill **Date:** with today's date (same)
    - **Git SHA:** leave as `<see git log -- .planning/decisions/0002-architecture-v2.md>` for now — the real SHA is only available after the commit. Claude will update this AFTER the git commit below.

    STEP 2 — Update STATE.md:
    In .planning/STATE.md:
    - Change Phase 2 row status from "Not started" to "Complete" in the phase status table
    - Update `stopped_at` to "Phase 2 complete — ADR 0002 signed"
    - Update `last_updated` to today's date
    - Update `last_activity` to today's date
    - Add to ## Accumulated Context §Decisions: "Phase 2 ADR (2026-XX-XX): v2.0 architecture locked in ADR 0002 — TOML schema (~290-310 flags), workspaces/<target-id>/ output tree, Task/Backend/AppContext interfaces, 7-class error hierarchy, failure_policy model, CLI deprecation plan (removal at v2.2.0), 4-ring test policy, Secret+RedactingHandler logging."

    STEP 3 — Update PROJECT.md Key Decisions table:
    Add a new row to the Key Decisions table in .planning/PROJECT.md:
    | 2026-XX-XX | v2.0 architecture locked: ADR 0002 signed — TOML schema, interfaces (Task/Backend/AppContext), error hierarchy, failure_policy, CLI surface, test rings, logging policy | Phase 3 Foundation Kernel can now begin |

    STEP 4 — Git commit:
    Stage the three files and the supporting artifacts:
    ```
    git add .planning/decisions/0002-architecture-v2.md \
            .planning/decisions/verify-0002.sh \
            interfaces_check/main.go \
            .planning/STATE.md \
            .planning/PROJECT.md
    git commit -m "docs(phase-02): sign ADR 0002 — architecture v2 design locked"
    ```

    STEP 5 — Fill Git SHA in Signed block:
    Get the SHA of the just-created commit:
    ```
    git log -1 --format=%H -- .planning/decisions/0002-architecture-v2.md
    ```
    Update the **Git SHA (this ADR):** field in the ## Signed block with the real SHA.
    Commit this final update:
    ```
    git add .planning/decisions/0002-architecture-v2.md
    git commit -m "docs(phase-02): fill ADR 0002 git SHA in Signed block"
    ```
  </action>
  <verify>
    <automated>grep 'Status: Accepted' .planning/decisions/0002-architecture-v2.md && grep 'Complete' .planning/STATE.md && grep 'ADR 0002' .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep '^\* Status: Accepted' .planning/decisions/0002-architecture-v2.md` exits 0
    - `grep '^\* Date: 20' .planning/decisions/0002-architecture-v2.md` exits 0 (Date field filled with real date)
    - `grep 'Git SHA.*[0-9a-f]\{40\}' .planning/decisions/0002-architecture-v2.md` exits 0 (real SHA in Signed block)
    - `grep 'Phase 2.*Complete' .planning/STATE.md` exits 0
    - `grep 'ADR 0002' .planning/PROJECT.md` exits 0
    - `git log --oneline -3` shows two commits: the sign-off commit and the SHA-fill commit
    - `git status` shows working tree clean
  </acceptance_criteria>
  <done>ADR 0002 is signed (Status: Accepted) with real git SHA, date, and maintainer name. STATE.md and PROJECT.md updated. All changes committed to rewrite/v2. Phase 2 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verify-0002.sh output → signing decision | All 4 checks must pass; no bypass path exists; gate is the human-checkpoint trigger |
| signed ADR → Phase 3 implementation | After signing, interface signatures are BINDING; changes require D-06 amendment process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07-01 | Tampering | ADR content changed after signing without amendment block | mitigate | D-05 + D-06 specify amendment process; git history is audit trail; any diff to a section after sign-off is visible in git log |
| T-02-07-02 | Repudiation | Sign-off not recorded with maintainer identity | mitigate | ## Signed block includes **Signed by:** six2dez (solo maintainer), **Date:**, **Git SHA:** — all three provide non-repudiation evidence |
| T-02-07-03 | Tampering | verify-0002.sh passes despite corrupt ADR content | mitigate | 4 independent checks (coverage + TOML + Go compile + glossary); all 4 must pass; human checkpoint reviews ADR before signing |
  T-02-07-SC | Tampering | npm/pip/cargo installs | accept | Only `tomljson` installed (pre-sign gate tooling); verified package from pelletier/go-toml (RESEARCH.md §Package Legitimacy Audit) |
</threat_model>

<verification>
1. `bash .planning/decisions/verify-0002.sh` exits 0 with "=== ALL CHECKS PASSED ===" (automated check in Task 1)
2. `grep '^\* Status: Accepted' .planning/decisions/0002-architecture-v2.md` exits 0
3. `grep 'Git SHA.*[0-9a-f]\{40\}' .planning/decisions/0002-architecture-v2.md` exits 0
4. `grep 'Phase 2.*Complete' .planning/STATE.md` exits 0
5. `grep 'ADR 0002' .planning/PROJECT.md` exits 0
6. `git status` shows clean working tree
</verification>

<success_criteria>
verify-0002.sh exits 0 (4 checks all pass).
ADR 0002 status is Accepted with real date, maintainer name, and git SHA in Signed block.
STATE.md Phase 2 status is Complete.
PROJECT.md Key Decisions has the ADR 0002 sign-off row.
All changes committed to rewrite/v2.
Phase 2 milestone is complete — Foundation Kernel (Phase 3) can begin.
</success_criteria>

<output>
Create `.planning/phases/02-architecture-v2-design/02-07-SUMMARY.md` when done.
</output>
