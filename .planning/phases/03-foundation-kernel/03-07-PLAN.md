---
phase: 03-foundation-kernel
plan: 07
type: execute
wave: 7
depends_on: ["03-01", "03-02", "03-03", "03-04", "03-05", "03-06"]
files_modified:
  - internal/core/testutil/mock_backend.go
  - internal/core/testutil/mock_backend_test.go
  - internal/core/testutil/mock_checkpoint.go
  - internal/core/testutil/mock_checkpoint_test.go
  - internal/core/testutil/mock_output_tree.go
  - internal/core/testutil/mock_output_tree_test.go
  - internal/core/testutil/fixtures/subfinder/example.com.txt
  - internal/core/testutil/fixtures/httpx/hosts.jsonl
  - internal/core/testutil/fixtures/README.md
  - internal/core/backend/tools.lock
  - internal/core/backend/registry_seed.go
  - internal/core/backend/registry_seed_test.go
  - cmd/reconftw/integration_smoke_test.go
  - cmd/reconftw/kernel_demo_test.go
  - .github/workflows/ci.yml
  - .planning/phases/03-foundation-kernel/03-ACCEPTANCE.md
  - interfaces_check/main.go
autonomous: true
requirements:
  - FOUND-15
  - FOUND-16
  - XCUT-02
  - XCUT-04

must_haves:
  truths:
    - "MockBackend, MockCheckpoint, MockOutputTree exist in `internal/core/testutil/` and satisfy the Backend / checkpoint.Interface / output.Interface contracts (interfaces introduced in Plan 03 per W15) — Phase 4-12 task tests import these to test without subprocess / disk."
    - "tools.lock seed file exists with at least 10 Phase 4 tools (subfinder, httpx, dnsx marked `critical = true`; crt, puredns, gotator, anew, asnmap, s3scanner, subzy marked required-but-not-critical) — Phase 4 plan-01 picks up a working registry; per-Blocker-5 the `Critical` field is consumed by health-check."
    - "An integration smoke test runs the full Scheduler + Backend + Tree + Checkpoint pipeline against the noop.demo Task and asserts: (a) manifest.json exists, (b) artefacts/demo.jsonl exists with one expected line, (c) checkpoint row exists with status=done, (d) re-running with same input_hash skips the task."
    - "Binary size verification runs in CI: stripped binary must be <50MB (XCUT-02). Activated now that Plan 06 has main.go."
    - "Phase 3 acceptance test certifies all 5 ROADMAP success criteria are met and emits `.planning/phases/03-foundation-kernel/03-ACCEPTANCE.md`."
    - "interfaces_check/main.go is updated to import the real `internal/core/{task,backend,appctx}` packages instead of placeholders — confirming the BINDING signatures hold; pre-sign gate Check 3 still passes."
    - "tools.lock seeding is safe per Blocker 7: all Plan 04 unit tests use fresh `*ToolRegistry{}` instances (no `backend.Default`); this plan can populate `backend.Default` via init() without duplicate-registration panics."
  artifacts:
    - path: internal/core/testutil/mock_backend.go
      provides: "MockBackend implementing backend.Backend interface; reads fixture files; never spawns subprocess"
      contains: "type MockBackend struct"
    - path: internal/core/testutil/mock_checkpoint.go
      provides: "MockCheckpoint: in-memory map satisfying checkpoint.Interface (Begin/Complete/Done)"
      contains: "type MockCheckpoint struct"
    - path: internal/core/testutil/mock_output_tree.go
      provides: "MockOutputTree: in-memory append; Lines(artefact) []string for assertions; satisfies output.Interface"
      contains: "type MockOutputTree struct"
    - path: internal/core/backend/tools.lock
      provides: "TOML manifest with 10 Phase 4 tools pinned (Go module@version + critical bool per FOUND-08 spec)"
      contains: "subfinder"
    - path: internal/core/backend/registry_seed.go
      provides: "init() that registers the 10 Phase 4 tools (including Critical field) with backend.Default ToolRegistry from tools.lock"
      contains: "func init"
    - path: cmd/reconftw/integration_smoke_test.go
      provides: "End-to-end integration test: kernel-demo subcommand → manifest + artefact + checkpoint"
      contains: "TestKernelDemoEndToEnd"
    - path: .planning/phases/03-foundation-kernel/03-ACCEPTANCE.md
      provides: "Phase 3 acceptance gate certifying all 5 ROADMAP success criteria"
      contains: "ROADMAP success criterion"
  key_links:
    - from: internal/core/testutil/mock_backend.go
      to: internal/core/testutil/fixtures/
      via: "Reads fixture files for deterministic Exec/Stream output"
      pattern: "fixtures/"
    - from: cmd/reconftw/integration_smoke_test.go
      to: internal/modules/demo/noop.go
      via: "Runs NoopTask through Scheduler + Backend + Tree + Checkpoint pipeline"
      pattern: "noop\\.demo"
    - from: internal/core/backend/tools.lock
      to: internal/core/backend/registry_seed.go
      via: "registry_seed reads tools.lock at init and Registers each tool (including Critical field per Blocker 5)"
      pattern: "tools\\.lock"
    - from: interfaces_check/main.go
      to: internal/core/task/task.go
      via: "Imports real package; compile-gate confirms BINDING signature parity"
      pattern: "internal/core/task"
---

<objective>
Close Phase 3. Ship the FOUND-15 test mocks, the FOUND-16 + XCUT-04 final CI gate lock-down (XCUT-02 binary size <50MB activated), the tools.lock seed for Phase 4 (with the `Critical` field populated per Blocker 5), the end-to-end integration smoke test using `noop.demo`, the Phase 3 acceptance report certifying all 5 ROADMAP success criteria, and the `interfaces_check/main.go` upgrade to import real packages (BINDING signature compatibility verified end-to-end).

Purpose: Phase 3 is "done" when the kernel proves it can run a task end-to-end (Scheduler + Backend + Tree + Checkpoint cooperate), CI enforces all gates (lint + format + race + coverage + binary size + sentinel + smoke), and Phase 4 can pick up a populated tools.lock + MockBackend test infrastructure without further setup.

Output: `internal/core/testutil/` (3 .go files + fixtures dir), `internal/core/backend/tools.lock` + `registry_seed.go`, `cmd/reconftw/integration_smoke_test.go` + `kernel_demo_test.go`, updated `.github/workflows/ci.yml` (binary-size gate activated), `.planning/phases/03-foundation-kernel/03-ACCEPTANCE.md`, updated `interfaces_check/main.go`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-foundation-kernel/03-01-SUMMARY.md
@.planning/phases/03-foundation-kernel/03-02-SUMMARY.md
@.planning/phases/03-foundation-kernel/03-03-SUMMARY.md
@.planning/phases/03-foundation-kernel/03-04-SUMMARY.md
@.planning/phases/03-foundation-kernel/03-05-SUMMARY.md
@.planning/phases/03-foundation-kernel/03-06-SUMMARY.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/decisions/0002-architecture-v2.md
@.planning/research/SUMMARY.md
@.planning/phases/03-foundation-kernel/03-CONTEXT.md
@.planning/phases/03-foundation-kernel/03-PATTERNS.md
@.planning/codebase/STACK.md
@interfaces_check/main.go
@spike/go/internal/proc/proc.go

<interfaces>
<!-- All Plans 01-06 SUMMARYs are inputs. Plan 07 lifts everything into a coherent kernel acceptance. -->

From `internal/core/backend/` (Plan 04):
- Backend interface (Exec/Stream/HealthCheck/Capacity)
- ToolRegistry (Register/Lookup/All/Discover/MissingRequired/MissingCritical)
- Tool struct (Name/Path/Version/DefaultArgs/Timeout/Critical) — Critical added per Blocker 5
- RateLimiter, Runner

From `internal/core/checkpoint/` (Plan 03):
- Store (NewStore/Begin/Complete/Done), InputHash
- `Interface` (introduced in Plan 03 per W15): Begin/Complete/Done — both *Store and MockCheckpoint satisfy

From `internal/core/output/` (Plan 03):
- OutputTree (NewTree/Append), ScopeFilter, Manifest, AtomicSymlink, WriteFile
- `Interface` (introduced in Plan 03 per W15): Append — both *OutputTree and MockOutputTree satisfy

From `internal/core/task/` + `internal/core/scheduler/` (Plan 05):
- Task interface (Name/Module/Description/Enabled/DependsOn/Run), Result, Status, Registry, LifecycleAware
- Scheduler (NewScheduler/runStage) — Scheduler.Checkpoint typed `checkpoint.Interface` per W15

From `internal/core/appctx/` (Plan 05):
- AppContext (9 fields), Target, Boot

From `internal/modules/demo/` (Plan 05):
- NoopTask (registered via init)

From `cmd/reconftw/` (Plan 06):
- main.go (ADR §10.3 init order), root.go (15 subcommands + v1 aliases), stub.go, version.go, healthcheck.go, modules.go (blank import), hidden kernel-demo subcommand (Plan 06 prepared per W16)

CONTEXT defaults applied:
- tools.lock seed (CONTEXT default (b)): 10 Phase 4 tools with Critical field per Blocker 5
- AxiomBackend (CONTEXT default (b)): compile-only stub — Plan 04 shipped; this plan does NOT extend
- Notifier (CONTEXT default (a)): LogSink + stubs — Plan 05 shipped
- CI matrix (CONTEXT default (a)): ubuntu-latest only in Phase 3 — Plan 01 yaml; this plan extends with binary-size activation
- spike code disposition (CONTEXT default): `spike/go/` stays until Phase 4 (this plan does NOT delete)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement FOUND-15 test mocks (MockBackend + MockCheckpoint + MockOutputTree) + fixtures directory</name>
  <files>
    internal/core/testutil/mock_backend.go
    internal/core/testutil/mock_backend_test.go
    internal/core/testutil/mock_checkpoint.go
    internal/core/testutil/mock_checkpoint_test.go
    internal/core/testutil/mock_output_tree.go
    internal/core/testutil/mock_output_tree_test.go
    internal/core/testutil/fixtures/subfinder/example.com.txt
    internal/core/testutil/fixtures/httpx/hosts.jsonl
    internal/core/testutil/fixtures/README.md
  </files>
  <read_first>
    - .planning/decisions/0002-architecture-v2.md §9.3 lines 2270-2326 (CANONICAL — Foundation Wave 0 Requirements for testutil package)
    - .planning/phases/03-foundation-kernel/03-PATTERNS.md §"Test mocks" (mock patterns + fixture directory layout)
    - FOUND-15 spec: "Test mocks shipped alongside real implementations: MockBackend (deterministic outputs), MockCheckpoint (in-memory), MockOutputTree (in-memory file system)"
    - All Plan 04 interfaces: Backend, Tool, Event, Result
    - Plan 03 SUMMARY: `checkpoint.Interface` and `output.Interface` introduced per W15 — testutil mocks target the interfaces
    - Plan 04 SUMMARY (Backend interface confirmation) + Plan 03 SUMMARY (checkpoint + output interfaces)
    - spike/go/internal/proc/proc.go (Backend behavioral semantics — kill-tree on ctx cancel; mock should match this even though it never spawns)
    - Plan 05 SUMMARY (Task interface final form; testutil should reference real `task.Task`, `appctx.AppContext`, not placeholders)
  </read_first>
  <behavior>
    MockBackend:
    - Test 1: `var _ backend.Backend = (*testutil.MockBackend)(nil)` compiles
    - Test 2: `MockBackend.Exec(ctx, &Tool{Name: "subfinder"}, args)` reads `fixtures/subfinder/<scenario>.txt` (scenario controlled via `MockBackend.Scenario = "..."`) and returns Result{Stdout: <content>, ExitCode: 0}
    - Test 3: `MockBackend.SetExitCode(2)` makes next Exec return `*errors.ToolError{ExitCode: 2}`
    - Test 4: `MockBackend.SetError(err)` makes next Exec return that error
    - Test 5: `MockBackend.Stream(ctx, tool, args)` opens the same fixture file, scans lines, yields each as Event on the channel; close on EOF
    - Test 6: `MockBackend.HealthCheck` returns nil by default; configurable via `MockBackend.SetHealthCheckError(err)`
    - Test 7: `MockBackend.Capacity()` returns the configured int (default 4)
    - Test 8: `MockBackend` is race-clean (`go test -race`)
    - Test 9: When ctx is cancelled mid-Stream, channel closes within 1ms (mock fast-paths cancellation — no need for real kill-tree timing)

    MockCheckpoint:
    - Test 10: `var _ checkpoint.Interface = (*testutil.MockCheckpoint)(nil)` compiles (interface introduced in Plan 03 per W15)
    - Test 11: `Begin/Complete/Done` API matches `*checkpoint.Store` semantics (Phase 3 plan-03 store)
    - Test 12: `Done` returns false until `Complete` called; then returns true for same key
    - Test 13: Different inputHash → different rows → Done returns false (verifies idempotency-by-hash)
    - Test 14: `MockCheckpoint.AllRecords()` returns sorted slice for test assertions
    - Test 15: Race-clean

    MockOutputTree:
    - Test 16: `var _ output.Interface = (*testutil.MockOutputTree)(nil)` compiles (interface introduced in Plan 03 per W15)
    - Test 17: `MockOutputTree.Append(artefact, lines)` stores in-memory; `Lines(artefact) []string` returns appended lines (sorted by append order)
    - Test 18: Reset() clears all artefacts (for test isolation)

    Fixtures:
    - Test 19: `internal/core/testutil/fixtures/subfinder/example.com.txt` exists with at least 5 subdomain-like lines (one per line, plain text — matches subfinder real output format)
    - Test 20: `internal/core/testutil/fixtures/httpx/hosts.jsonl` exists with at least 3 JSONL host entries matching the httpx schema
  </behavior>
  <action>
    **File 1 — `internal/core/testutil/mock_backend.go`**:

    Implement MockBackend satisfying `backend.Backend`. Fields: FixturesDir, Scenario, exitCode, runError, healthCheckErr, capacity (default 4), mu sync.RWMutex. Setter methods: SetScenario, SetExitCode, SetError, SetHealthCheckError, SetCapacity. Exec reads `<FixturesDir>/<tool.Name>/<Scenario>.txt` (with `.jsonl` fallback), returns `*backend.Result{Stdout: data, ExitCode: 0, Duration: time.Millisecond}` on success or `*coreerrors.ToolError` on configured exitCode/runError. Stream opens the same file, scans with `bufio.NewScanner` (1MiB initial / 10MiB max — matches LocalBackend behavior per RESEARCH.md §Pattern 3), yields `backend.Event{Line: ..., Source: t.Name}` on the channel, selects on `ctx.Done()` for fast cancellation, closes channel on EOF. HealthCheck returns the configured healthCheckErr (nil by default). Capacity returns configured int. Add a compile-time assertion `var _ backend.Backend = (*MockBackend)(nil)` in the test file.

    **File 2 — `internal/core/testutil/mock_checkpoint.go`**:

    Implement MockCheckpoint satisfying `checkpoint.Interface` (the interface introduced in Plan 03 per W15). In-memory map keyed by `(taskName + "\x00" + target + "\x00" + inputHash)`. Record fields: TaskName, Target, InputHash, Status, StartedAt, FinishedAt *time.Time, OutputPaths []string, ErrorClass. Methods: Begin (sets Status="in-progress", StartedAt=now), Done (returns true iff Status=="done"), Complete (sets FinishedAt, OutputPaths, Status="done" or "errored" based on runErr — classify err via errors.As on the 7 typed structs from Plan 01), AllRecords (returns sorted slice), Reset (clears map). All operations guarded by sync.RWMutex. Add compile-time assertion `var _ checkpoint.Interface = (*MockCheckpoint)(nil)` in the test file.

    NOTE: Per W15, Plan 03 introduced `checkpoint.Interface`. This task simply imports and satisfies it — no architectural follow-up needed.

    **File 3 — `internal/core/testutil/mock_output_tree.go`**:

    Implement MockOutputTree satisfying `output.Interface` (introduced in Plan 03 per W15). Field: `appends map[string][][]byte` guarded by sync.RWMutex. Methods: Append (deep-copies each line and stores by artefact name), Lines (returns appended lines as []string for assertions), Reset (clears map). Add compile-time assertion `var _ output.Interface = (*MockOutputTree)(nil)` in the test file.

    NOTE: Per W15, Plan 03 introduced `output.Interface`. This task simply imports and satisfies it.

    **File 4 — `internal/core/testutil/fixtures/subfinder/example.com.txt`** — 5+ realistic subdomain lines:
    ```
    api.example.com
    www.example.com
    mail.example.com
    admin.example.com
    dev.example.com
    ```

    **File 5 — `internal/core/testutil/fixtures/httpx/hosts.jsonl`** — 3+ JSONL entries with realistic httpx schema:
    ```json
    {"input":"https://api.example.com","host":"api.example.com","port":443,"status_code":200,"title":"API","tech":["nginx"]}
    {"input":"https://www.example.com","host":"www.example.com","port":443,"status_code":200,"title":"Example Domain","tech":["nginx"]}
    {"input":"https://mail.example.com","host":"mail.example.com","port":443,"status_code":403,"title":"Forbidden","tech":[]}
    ```

    **File 6 — `internal/core/testutil/fixtures/README.md`** — short doc explaining the fixture directory layout for Phase 4-7 contributors.

    **Tests (all 20 behaviors)** — in `mock_backend_test.go`, `mock_checkpoint_test.go`, `mock_output_tree_test.go`. Each test file MUST include the compile-time interface assertion line at package level.
  </action>
  <verify>
    <automated>cd /Users/six2dez/Tools/reconftw && go test -race ./internal/core/testutil/... && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'type MockBackend struct' internal/core/testutil/mock_backend.go` returns 1
    - Source: `grep -c 'type MockCheckpoint struct' internal/core/testutil/mock_checkpoint.go` returns 1
    - Source: `grep -c 'type MockOutputTree struct' internal/core/testutil/mock_output_tree.go` returns 1
    - Source: `grep -q 'var _ backend\.Backend = (\*MockBackend)(nil)' internal/core/testutil/mock_backend_test.go` (compile-time assertion)
    - Source: `grep -q 'var _ checkpoint\.Interface = (\*MockCheckpoint)(nil)' internal/core/testutil/mock_checkpoint_test.go` (compile-time assertion — interface lives in Plan 03 per W15)
    - Source: `grep -q 'var _ output\.Interface = (\*MockOutputTree)(nil)' internal/core/testutil/mock_output_tree_test.go` (compile-time assertion — interface lives in Plan 03 per W15)
    - Filesystem: `[ -d internal/core/testutil/fixtures/subfinder ] && [ -f internal/core/testutil/fixtures/subfinder/example.com.txt ]`
    - Filesystem: `[ -f internal/core/testutil/fixtures/httpx/hosts.jsonl ]` and `wc -l internal/core/testutil/fixtures/httpx/hosts.jsonl | awk '{print ($1 >= 3 ? 0 : 1)}'` exits 0
    - Build: `go build ./...` exits 0 (Scheduler uses checkpoint.Interface — confirm Plan 05 Scheduler wired via interface)
    - Test: `go test -race ./internal/core/testutil/...` exits 0
    - Test: `go test -race ./...` exits 0 (full module green after interface introductions)
    - `interfaces_check/main.go` still compiles
  </acceptance_criteria>
  <done>
    MockBackend, MockCheckpoint, MockOutputTree implemented and tested. Each satisfies the corresponding interface (`backend.Backend`, `checkpoint.Interface`, `output.Interface`) via compile-time assertions. Fixture directory seeded with subfinder + httpx test data. Phase 4 module tests can import `internal/core/testutil` and substitute mocks without further setup.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Seed tools.lock with 10 Phase 4 tools (including Critical field per Blocker 5) + auto-register on init</name>
  <files>
    internal/core/backend/tools.lock
    internal/core/backend/registry_seed.go
    internal/core/backend/registry_seed_test.go
  </files>
  <read_first>
    - .planning/phases/03-foundation-kernel/03-CONTEXT.md §"Tools.lock seeding in Phase 3" — default (b): seed with 5-10 tools needed by Phase 4 subdomains module: subfinder, httpx, crt, dnsx, puredns, gotator, anew, asnmap, s3scanner, subzy
    - .planning/codebase/STACK.md (full v1 tool inventory — Phase 11 ships the full 70+ manifest; Phase 3 seeds only what Phase 4 needs)
    - Plan 04 SUMMARY (ToolRegistry mechanism + Tool struct shape — including the `Critical bool` field added per Blocker 5)
    - FOUND-08 spec (REQUIREMENTS line 47): "ToolRegistry with self-registering tools via init()... discovers binaries at startup; warns on missing-but-required, fails on missing-and-critical"
    - .planning/decisions/0002-architecture-v2.md §1.2 (ToolRegistry placement) + §5.2 (Tool struct — Blocker 5 cites ADR §0 D-07 non-breaking field addition)
    - reconftw.cfg + reconftw.sh (semantic intent: v1 invokes these tools; v2 ToolRegistry catalogs them)
    - Plan 04 SUMMARY "Open items for Plan 07" — Critical field semantics confirmed in Plan 04
    - Per Blocker 7: Plan 04 unit tests use fresh `*ToolRegistry{}` instances, not `backend.Default` — so this seed is safe.
  </read_first>
  <behavior>
    - Test 1: `internal/core/backend/tools.lock` exists in TOML format with 10 tools (subfinder, httpx, crt, dnsx, puredns, gotator, anew, asnmap, s3scanner, subzy)
    - Test 2: Each tool entry has: name, kind, go_module (for Go tools), default_args (optional), timeout_seconds, **critical bool** (per Blocker 5)
    - Test 3: Three tools marked `critical = true`: subfinder, httpx, dnsx (the smallest passive+probe+resolve trio needed for any meaningful Phase 4 run). Other 7 marked `critical = false`.
    - Test 4: `registry_seed.go` init() reads tools.lock at process startup and calls `backend.Default.Register(...)` for each tool, populating the `Critical` field on the Tool struct
    - Test 5: After binary startup, `backend.Default.All()` returns 10 tools sorted alphabetically; `backend.Default.MissingCritical()` returns up to 3 entries when none of the critical tools are on PATH
    - Test 6: `reconftw health-check` (Plan 06) emits `[FAIL]` for any missing-critical tool and `[WARN]` for missing-but-not-critical (per FOUND-08 wording: "warns on missing-but-required, errors on missing-and-critical")
    - Test 7: tools.lock format is parseable by Go's `pelletier/go-toml/v2` (Plan 02 dep already in go.mod)
  </behavior>
  <action>
    **File 1 — `internal/core/backend/tools.lock`** — TOML manifest with 10 tools per CONTEXT default (b) plus `critical` field per Blocker 5:

    ```toml
    # reconFTW v2.0 — Tools Lock
    # Phase 3 seed: 10 tools needed by Phase 4 (Subdomains E2E + Axiom Integration).
    # Phase 11 (Installer + Cross-Platform + Docker) extends this to the full 70+ tool inventory
    # with SHA-256 verification per INST-02..04.
    # Format: per-tool table; required keys: name, kind (go|python|system|rust),
    #          go_module (Go tools only), default_args, timeout_seconds, critical (bool — FOUND-08).
    # Critical tools cause health-check to exit 1; non-critical missing tools warn only.

    schema_version = "0.1"
    generated_by   = "phase-3-plan-07"
    generated_at   = "2026-05-28"

    [[tools]]
    name        = "subfinder"
    kind        = "go"
    go_module   = "github.com/projectdiscovery/subfinder/v2/cmd/subfinder"
    description = "Passive multi-source subdomain enumeration"
    default_args = []
    timeout_seconds = 180
    critical = true

    [[tools]]
    name        = "httpx"
    kind        = "go"
    go_module   = "github.com/projectdiscovery/httpx/cmd/httpx"
    description = "HTTP probe with tech detection"
    default_args = []
    timeout_seconds = 0  # 0 = no hard timeout; long-running
    critical = true

    [[tools]]
    name        = "dnsx"
    kind        = "go"
    go_module   = "github.com/projectdiscovery/dnsx/cmd/dnsx"
    description = "DNS toolkit"
    default_args = []
    timeout_seconds = 14400  # 4h default per v1 DNS_RESOLVE_TIMEOUT
    critical = true

    [[tools]]
    name        = "crt"
    kind        = "go"
    go_module   = "github.com/cemulus/crt"
    description = "crt.sh search"
    default_args = []
    timeout_seconds = 60
    critical = false

    [[tools]]
    name        = "puredns"
    kind        = "go"
    go_module   = "github.com/d3mondev/puredns/v2"
    description = "Mass DNS resolution with wildcard filtering"
    default_args = []
    timeout_seconds = 0
    critical = false

    [[tools]]
    name        = "gotator"
    kind        = "go"
    go_module   = "github.com/Josue87/gotator"
    description = "Subdomain permutations"
    default_args = []
    timeout_seconds = 600
    critical = false

    [[tools]]
    name        = "anew"
    kind        = "go"
    go_module   = "github.com/tomnomnom/anew"
    description = "Append new lines only (dedup)"
    default_args = []
    timeout_seconds = 30
    critical = false

    [[tools]]
    name        = "asnmap"
    kind        = "go"
    go_module   = "github.com/projectdiscovery/asnmap/cmd/asnmap"
    description = "ASN-to-CIDR mapping"
    default_args = []
    timeout_seconds = 120
    critical = false

    [[tools]]
    name        = "s3scanner"
    kind        = "go"
    go_module   = "github.com/sa7mon/s3scanner"
    description = "S3/GCS/Azure Blob misconfiguration scanner"
    default_args = []
    timeout_seconds = 600
    critical = false

    [[tools]]
    name        = "subzy"
    kind        = "go"
    go_module   = "github.com/PentestPad/subzy"
    description = "Subdomain takeover detection"
    default_args = []
    timeout_seconds = 600
    critical = false
    ```

    **File 2 — `internal/core/backend/registry_seed.go`** — init() reads tools.lock and registers each tool with the `Critical` field populated:

    ```go
    package backend

    import (
        _ "embed"
        "log/slog"
        "time"

        tomlv2 "github.com/pelletier/go-toml/v2"
    )

    //go:embed tools.lock
    var toolsLockData []byte

    type toolsLockSchema struct {
        SchemaVersion string `toml:"schema_version"`
        Tools         []struct {
            Name           string   `toml:"name"`
            Kind           string   `toml:"kind"`
            GoModule       string   `toml:"go_module"`
            Description    string   `toml:"description"`
            DefaultArgs    []string `toml:"default_args"`
            TimeoutSeconds int      `toml:"timeout_seconds"`
            Critical       bool     `toml:"critical"`
        } `toml:"tools"`
    }

    func init() {
        var lock toolsLockSchema
        if err := tomlv2.Unmarshal(toolsLockData, &lock); err != nil {
            slog.Error("tools_lock_parse_failed", "err", err)
            return
        }
        for _, t := range lock.Tools {
            tool := &Tool{
                Name:        t.Name,
                DefaultArgs: append([]string(nil), t.DefaultArgs...),
                Timeout:     time.Duration(t.TimeoutSeconds) * time.Second,
                Critical:    t.Critical,  // Plan 04 added this field per Blocker 5
            }
            Default.Register(tool)
        }
        slog.Info("tools_lock_loaded", "tool_count", len(lock.Tools))
    }
    ```

    **File 3 — `internal/core/backend/registry_seed_test.go`**: Tests 5-7. Use `package backend_test` (external test package) so this test file is in a separate compilation unit and importing it triggers the init() exactly once via the embedded blank-import pattern in the test runner. Verify: TOML parses; verify `backend.Default.All()` returns 10 tools after package import (Go runtime runs init() at import time); verify each tool by name lookup; verify `backend.Default.MissingCritical()` correctly identifies the 3 critical entries when synthesizing a registry where subfinder/httpx/dnsx are not on PATH.

    Per Blocker 7: Plan 04's registry_test.go MUST use fresh `*ToolRegistry{tools: map[string]*Tool{}}` instances (NOT `backend.Default`). This task's acceptance criteria include verifying that this is the case — a grep gate is added below.
  </action>
  <verify>
    <automated>cd /Users/six2dez/Tools/reconftw && go test -race ./internal/core/backend/...</automated>
  </verify>
  <acceptance_criteria>
    - Filesystem: `[ -f internal/core/backend/tools.lock ]` (file exists)
    - Source: `grep -c '^\[\[tools\]\]' internal/core/backend/tools.lock` returns 10 (10 tool entries)
    - Source: `grep -E '^name = "(subfinder|httpx|crt|dnsx|puredns|gotator|anew|asnmap|s3scanner|subzy)"' internal/core/backend/tools.lock | wc -l | awk '{print ($1 == 10 ? 0 : 1)}'` exits 0 (all 10 expected tools present)
    - Source: `grep -c '^critical = true' internal/core/backend/tools.lock` returns 3 (subfinder + httpx + dnsx critical)
    - Source: `grep -c '^critical = false' internal/core/backend/tools.lock` returns 7 (rest non-critical)
    - Source: `grep -c '//go:embed tools.lock' internal/core/backend/registry_seed.go` returns 1 (uses go:embed — single binary preserved)
    - Source: `grep -c 'func init' internal/core/backend/registry_seed.go` returns 1
    - Source: `grep -q 'Critical:.*t\.Critical\|Critical: t\.Critical' internal/core/backend/registry_seed.go` (registers Critical field)
    - Source: `grep -r 'backend\.Default' internal/core/backend/*_test.go | grep -v 'registry_seed_test.go'` returns no matches (Blocker 7 — Plan 04 tests use fresh `*ToolRegistry` instances; only this plan's registry_seed_test.go references Default)
    - Build: `go build ./internal/core/backend/...` exits 0
    - Test: `go test -race ./internal/core/backend/...` exits 0 (Plan 04 tests must still pass after seed lands — they use fresh registry instances per Blocker 7)
    - Behavior: after `import "github.com/six2dez/reconftw/internal/core/backend"` at binary start, `backend.Default.All()` returns 10 tools sorted by name; `backend.Default.MissingCritical()` is populated according to PATH lookup state
    - CLI behavior: `reconftw health-check` now lists 10 tool checks; missing-critical → `[FAIL]`, missing-required-but-not-critical → `[WARN]`, present → `[OK  ]` (per FOUND-08 + Blocker 5)
  </acceptance_criteria>
  <done>
    tools.lock seed file contains 10 Phase 4 tools in TOML format with the `critical` field set (3 critical: subfinder, httpx, dnsx; 7 required-but-not-critical). registry_seed.go embeds the file via go:embed (preserves single-binary promise) and registers each tool — including the Critical field — with backend.Default at init time. Phase 4 plan-01 picks up a working registry without further setup. `reconftw health-check` exercises the registry end-to-end and correctly differentiates WARN vs FAIL per FOUND-08.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Phase 3 acceptance: kernel-demo integration smoke test + binary size CI gate activation + Phase 3 ACCEPTANCE.md</name>
  <files>
    cmd/reconftw/integration_smoke_test.go
    cmd/reconftw/kernel_demo_test.go
    .github/workflows/ci.yml
    .planning/phases/03-foundation-kernel/03-ACCEPTANCE.md
    interfaces_check/main.go
  </files>
  <read_first>
    - .planning/ROADMAP.md §"Phase 3: Foundation Kernel" success criteria 1-5 (CANONICAL acceptance gates)
    - .planning/phases/03-foundation-kernel/03-CONTEXT.md §"End-of-Phase-3 demo shape" — default (b): noop.demo Task proves Scheduler + Backend + Tree + Checkpoint
    - All Plan 01-06 SUMMARYs (each subsystem's contribution to the integration)
    - Plan 06 SUMMARY: kernel-demo hidden subcommand authorized per W16 (CONTEXT-amendment) and built; integration test invokes it
    - Plan 01 SUMMARY: CI yaml with binary-size gate scaffolded but gated on `hashFiles('cmd/reconftw/main.go') != ''`; Plan 06 created main.go, so this task ACTIVATES the gate
    - XCUT-02 spec: "Go binary <50MB stripped" — `52428800 bytes`
    - XCUT-04 spec: "CI policy: race detector mandatory; lint zero-tolerance; integration-full split"
    - XCUT-07 spec: sentinel test runs every commit (Plan 01 wired)
    - XCUT-09 spec: heartbeat tested (Plan 05 wired — Scheduler now lives in Plan 05)
    - FOUND-16 spec: "CI pipeline on every push: lint, gofmt/ruff format check, unit tests, integration smoke (with mock backend), go test -race; branch coverage gate at ≥75%"
    - interfaces_check/main.go (Phase 2 D-14 placeholders — this task UPGRADES to import real packages, verifying BINDING signature compatibility)
  </read_first>
  <behavior>
    Integration smoke test:
    - Test 1: `TestKernelDemoEndToEnd` invokes the built binary with args `["kernel-demo", "--target", "example.com"]`
    - Test 2: After successful run, `workspaces/example.com-*/manifest.json` exists and parses as valid JSON with required keys (workspace_version, target, started_at, finished_at, config_hash)
    - Test 3: `workspaces/example.com-*/artefacts/demo.jsonl` exists and contains exactly one line matching `{"demo":"phase-3-kernel","timestamp":"..."}`
    - Test 4: `workspaces/example.com-*/checkpoints.db` exists and contains one row with status="done", task_name="noop.demo", target="example.com"
    - Test 5: Re-running the same `kernel-demo --target example.com` invocation SKIPS the task (input_hash unchanged); manifest.finished_at updated but no new demo.jsonl line written
    - Test 6: With `--force` flag, the task re-runs regardless of checkpoint

    XCUT-02 binary size:
    - Test 7: CI yaml `binary-size` job no longer gated on `hashFiles('cmd/reconftw/main.go')` since main.go now exists; the gate is unconditional
    - Test 8: Stripped binary built with `-ldflags="-s -w" -trimpath` is <50MB (52428800 bytes); CI fails the binary-size job if exceeded

    Phase 3 ACCEPTANCE certification:
    - Test 9: `.planning/phases/03-foundation-kernel/03-ACCEPTANCE.md` exists with explicit certification of all 5 ROADMAP success criteria, each pointing at the SUMMARY/test that validates it
    - Test 10: ACCEPTANCE includes binary size measurement, sentinel test command, kill-tree timing, scheduler heartbeat cadence, mock test coverage, tools.lock content summary (including critical-tool breakdown)

    interfaces_check upgrade:
    - Test 11: `interfaces_check/main.go` imports `github.com/six2dez/reconftw/internal/core/task`, `.../internal/core/backend`, `.../internal/core/appctx` (NOT placeholders)
    - Test 12: `bash .planning/decisions/verify-0002.sh` Check 3 still passes — confirming the real packages match the interfaces_check shape; this is the END-OF-PHASE BINDING confirmation
  </behavior>
  <action>
    **File 1 — `cmd/reconftw/integration_smoke_test.go`**:

    Implement `TestKernelDemoEndToEnd` (package `main_test`). Approach: use `t.TempDir()` for the workspaces root; build the binary via `exec.Command("go", "build", "-ldflags=-s -w", "-trimpath", "-o", binPath, "./cmd/reconftw")` (working dir set to `mustFindRepoRoot(t)`); invoke `binPath kernel-demo --target example.com --output <tmp>` with a 30s timeout. After exit-0:
    - Find the workspace directory matching `example.com-*` under tmpRoot
    - Read and JSON-parse `manifest.json`; assert keys present: workspace_version, target, started_at, config_hash
    - Read `artefacts/demo.jsonl`; assert exactly 1 line containing `"demo":"phase-3-kernel"`
    - Stat `checkpoints.db`; open via modernc/sqlite + query the one expected row (status=done, task_name=noop.demo, target=example.com)
    - Re-run the same command; assert demo.jsonl still has exactly 1 line (checkpoint skipped re-execution)

    This test runs in CI's integration-smoke job and is NOT smoke-tagged because it exercises the binary end-to-end without external tools.

    **File 2 — `cmd/reconftw/kernel_demo_test.go`** — unit test for the hidden kernel-demo subcommand (added in Plan 06 per W16):
    - Test 6 from `<behavior>` — `--force` flag bypasses checkpoint
    - Tests the cobra wiring of kernel-demo (Hidden: true; flag set; RunE invokes Scheduler.runStage on noop.demo)

    **File 3 — `.github/workflows/ci.yml` UPDATE** — activate the binary-size gate (Plan 01 had it gated on `hashFiles('cmd/reconftw/main.go') != ''`; Plan 06 created main.go; this plan removes the gate):

    Find the `binary-size` job and remove the `if: hashFiles(...)` condition. Confirm the gate is always-on now.

    Also add an explicit job for the integration smoke test:
    ```yaml
      integration-smoke:
        name: Integration smoke (kernel-demo)
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4
          - uses: actions/setup-go@v5
            with: { go-version-file: go.mod }
          - run: go test -race -run TestKernelDemoEndToEnd ./cmd/reconftw/...
    ```

    **File 4 — `.planning/phases/03-foundation-kernel/03-ACCEPTANCE.md`** — Phase 3 acceptance gate certification:

    ```markdown
    # Phase 3 — Foundation Kernel — Acceptance Gate

    **Certified:** <DATE>
    **Phase status:** Complete
    **Plan count:** 7 plans (renumbered from initial 6 per revision-iter-1 split — Blockers 2/3)

    ## ROADMAP Success Criteria Verification

    ### Success Criterion 1: CLI wired to all subsystems
    > `reconftw --help` runs against the kernel binary; the CLI is wired to the configured subsystems (typed errors, structured logger, layered config loader with 8-source precedence, scheduler, tool registry, output tree, checkpoint store, notifier stubs, UI dot-fill); empty `reconftw run` against a mock target writes a `workspaces/<target>/manifest.json` and exits 0 (FOUND-01, FOUND-02, FOUND-03, FOUND-13, FOUND-14)

    **VERIFIED** via:
    - Plan 01 SUMMARY (errors + logger + Secret + Redactor)
    - Plan 02 SUMMARY (config loader 8-source)
    - Plan 03 SUMMARY (output tree + checkpoint + interfaces introduced per W15)
    - Plan 04 SUMMARY (Backend + LocalBackend + ToolRegistry + RateLimiter + FOUND-10 lint rule; Tool.Critical field added per Blocker 5)
    - Plan 05 SUMMARY (Task interface + AppContext + Scheduler + UI + Notifier + noop.demo)
    - Plan 06 SUMMARY (CLI binary — 15 subcommands + v1 aliases + version + health-check + hidden kernel-demo per W16)
    - Plan 07 Task 3 integration smoke test (`TestKernelDemoEndToEnd`) confirms manifest.json written + exit 0

    **CONTEXT default applied:** "reconftw run" interpreted as hidden `kernel-demo` subcommand — see Plan 06 SUMMARY for rationale and W16 (CONTEXT amendment) authorizing the hidden subcommand per ADR §0 D-07 non-breaking addition.

    ### Success Criterion 2: Atomic writes + checkpoint idempotency
    > Atomic writes verified... SQLite checkpoint store records (task_name, target, input_hash, status, timings, output_paths, error_class) and re-running with same input_hash skips the task (FOUND-04, FOUND-05)

    **VERIFIED** via:
    - Plan 03 Task 1: FOUND-04 atomicity test (`TestFOUND04Atomicity`) — hook simulation passes; Plan 01 CI yaml's `smoke` job also runs the subprocess SIGKILL variant (per W12)
    - Plan 03 Task 3: Checkpoint WAL mode + idempotency tests
    - Plan 07 Task 3: integration smoke test ASSERT 4 — re-run skips task

    ### Success Criterion 3: Subprocess safety enforced
    > Every subprocess uses `Setpgid`+`WaitDelay` + lint rule forbids raw `exec.Command` + SIGINT-kill test within 10s passes (FOUND-09, FOUND-10)

    **VERIFIED** via:
    - Plan 04 Task 1: LocalBackend.Exec/Stream use Setpgid + WaitDelay + Cancel + group-SIGKILL goroutine
    - Plan 04 Task 4: FOUND-10 test-based AST scan (`TestFOUND10_NoRawSubprocessOutsideBackend`) catches violations — implementation uses `go/ast` + `golang.org/x/tools/go/packages` to walk every package, fails with positional violations per Blocker 8
    - Plan 04 Task 1: smoke test (`TestKillTreeWithin10s`) — uses `syscall.Kill(pid, 0)` liveness checks per W17

    ### Success Criterion 4: Scheduler + RateLimiter + ToolRegistry + Notifier
    > Scheduler enforces bounded concurrency... rate limiter supports per-tool/per-target/global caps; tool registry self-registers tools via `func init()`; warns on missing-but-required; **structured errors on missing-and-critical**; notifier interface + log sink + Slack/Telegram/Discord stubs wired through redactor (FOUND-06, FOUND-07, FOUND-08, FOUND-11, FOUND-12, XCUT-09)

    **VERIFIED** via:
    - Plan 05 Task 1 (Scheduler section): errgroup.SetLimit + failure_policy fork + heartbeat
    - Plan 04 Task 2: RateLimiter using golang.org/x/time/rate.Limiter
    - Plan 04 Task 2: ToolRegistry.MissingRequired() + MissingCritical() methods (Blocker 5)
    - Plan 07 Task 2: tools.lock seed with 10 Phase 4 tools (3 critical: subfinder, httpx, dnsx; 7 required-but-not-critical) — health-check exits 1 when critical missing
    - Plan 05 Task 1 (Notifier section): Notifier interface + LogSink + Slack/Telegram/Discord stubs (Phase 10 wires real)

    ### Success Criterion 5: Test mocks + CI gate + binary size + sentinel
    > Test mocks ship; CI on every push runs lint + format-check + unit + integration smoke + `-race`; branch coverage gate ≥75% on lib code; Go binary stripped <50MB; XCUT-07 sentinel test asserts no secret patterns leak (FOUND-15, FOUND-16, XCUT-02, XCUT-04, XCUT-07)

    **VERIFIED** via:
    - Plan 07 Task 1: MockBackend + MockCheckpoint + MockOutputTree shipped in `internal/core/testutil/`; satisfy `backend.Backend`, `checkpoint.Interface`, `output.Interface` (interfaces introduced in Plan 03 per W15)
    - Plan 01 + Plan 07 Task 3: CI yaml with `lint`, `unit`, `integration`, `binary-size`, `smoke`, `integration-smoke` jobs; binary-size gate activated unconditionally; per-file coverage gates on `redactor.go`, `redacting_handler.go`, `checkpoint/store.go`, `output/atomic.go` ≥90% per W20
    - Plan 01 Task 3: XCUT-07 sentinel test (`TestXCUT07Sentinel`)
    - Plan 07 Task 3: binary size measurement (last CI run): <50MB

    ## Measurements

    | Metric | Value | Gate |
    |--------|-------|------|
    | Binary size (stripped, `-ldflags=-s -w -trimpath`) | <X MB> | <50MB (XCUT-02) — PASS |
    | Kill-tree timing (smoke test, syscall.Kill liveness probe) | <Y ms> | <10s (FOUND-09) — PASS |
    | Heartbeat cadence | every <Z s> | configurable (XCUT-09) — PASS |
    | Branch coverage on `internal/core/...` (combined) | <P%> | ≥75% (XCUT-04) — PASS |
    | Per-file coverage on critical paths (W20) | <P2%> | ≥90% (XCUT-03) — PASS |
    | XCUT-07 sentinel leak | 0 occurrences | 0 (XCUT-07) — PASS |
    | tools.lock entries | 10 (3 critical, 7 non-critical) | ≥5 (CONTEXT default (b)) — PASS |
    | interfaces_check compile (pre-sign gate Check 3) | exits 0 | required — PASS |

    ## Open Items Carried to Phase 4
    - Delete `internal/modules/demo/noop.go` (replaced by `internal/modules/subdomains/passive.go`)
    - Extend tools.lock with web/vulns/osint tools as those phases land
    - Promote FOUND-10 lint rule from test-based AST scan to real golangci-lint plugin if violations accumulate
    - Phase 11 cutover: AxiomBackend stub → real implementation
    ```

    Fill in `<DATE>`, `<X MB>`, `<Y ms>`, `<Z s>`, `<P%>`, `<P2%>` from actual measurement.

    **File 5 — `interfaces_check/main.go` UPDATE** — replace placeholder interfaces with real imports:

    Implement `interfaces_check/main.go` to import `github.com/six2dez/reconftw/internal/core/{task,backend,appctx,config}` and declare compile-time assertions:
    - `var _ task.Task = (*dummyTask)(nil)`
    - `var _ task.LifecycleAware = (*dummyLifecycleTask)(nil)`
    - `var _ backend.Backend = (*dummyBackend)(nil)`

    Each dummy type satisfies the interface with no-op methods returning zero values. dummyTask.Enabled MUST use the real signature `Enabled(cfg *config.Config) bool` (import config). main() is a no-op (`_ = task.StatusDone`).

    Verify `verify-0002.sh` Check 3 still passes — BINDING signature gate.

    **File 6 — final Makefile update**: ensure `make check` runs the full suite including the integration smoke:
    ```make
    check: fmt-check lint test coverage integration-smoke

    integration-smoke:
    	go test -race -run TestKernelDemoEndToEnd ./cmd/reconftw/...
    ```

    Run `make check` locally before commit; capture output for the SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/six2dez/Tools/reconftw && go test -race -run TestKernelDemoEndToEnd ./cmd/reconftw/... && go build -ldflags="-s -w" -trimpath -o /tmp/reconftw ./cmd/reconftw && SIZE=$(stat -f%z /tmp/reconftw 2>/dev/null || stat -c%s /tmp/reconftw) && echo "binary size: $SIZE" && [ "$SIZE" -lt 52428800 ] && rm -f /tmp/reconftw && bash .planning/decisions/verify-0002.sh && [ -f .planning/phases/03-foundation-kernel/03-ACCEPTANCE.md ]</automated>
  </verify>
  <acceptance_criteria>
    - Test: `go test -race -run TestKernelDemoEndToEnd ./cmd/reconftw/...` exits 0 (Tests 1-5)
    - Test: `go test -race -run TestKernelDemoForce ./cmd/reconftw/...` exits 0 (Test 6)
    - Build: stripped binary `-ldflags="-s -w" -trimpath` < 50MB (52428800 bytes) — XCUT-02 PASS
    - YAML: `binary-size` job no longer gated on `hashFiles(...)` — `grep -v '^#' .github/workflows/ci.yml | grep -c 'hashFiles..cmd/reconftw/main.go'` returns 0 (gate removed)
    - YAML: `integration-smoke` job added to CI
    - Filesystem: `.planning/phases/03-foundation-kernel/03-ACCEPTANCE.md` exists; `grep -c 'ROADMAP success criterion\|### Success Criterion' .planning/phases/03-foundation-kernel/03-ACCEPTANCE.md` returns at least 5
    - Pre-sign gate: `bash .planning/decisions/verify-0002.sh` exits 0 (Check 3 — interfaces_check compiles against REAL packages now)
    - Source: `grep -q 'internal/core/task' interfaces_check/main.go` (real imports)
    - Source: `grep -q 'internal/core/backend' interfaces_check/main.go`
    - Source: `grep -q 'internal/core/appctx' interfaces_check/main.go`
    - Source: `grep -q '_ task\.Task = (\*' interfaces_check/main.go` (compile-time assertion)
    - Source: `grep -q '_ backend\.Backend = (\*' interfaces_check/main.go` (compile-time assertion)
    - Make: `make check` runs successfully
    - All Plans 01-07 verify steps still pass: `go test -race ./...` exits 0
    - All Plans 01-07 lint clean: `golangci-lint run ./...` exits 0
    - All Plans 01-07 format clean: `gofumpt -d .` produces no output
  </acceptance_criteria>
  <done>
    Phase 3 acceptance test (`TestKernelDemoEndToEnd`) passes: noop.demo Task runs through Scheduler + LocalBackend + OutputTree + Checkpoint pipeline; manifest.json + artefacts/demo.jsonl + checkpoints.db all produced; re-run honours checkpoint idempotency. XCUT-02 binary size gate active and PASSING (<50MB stripped). XCUT-07 sentinel test still passing. FOUND-09 kill-tree smoke test still passing. `interfaces_check/main.go` upgraded to import real `internal/core/{task,backend,appctx,config}` packages with compile-time assertions — BINDING signatures verified end-to-end. `.planning/phases/03-foundation-kernel/03-ACCEPTANCE.md` certifies all 5 ROADMAP success criteria.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test mocks → production code | Tests must not be wired into the production binary; mocks live in `testutil` package only — production wiring uses `*checkpoint.Store` and `*output.OutputTree` |
| Embedded tools.lock → process startup | Go:embed makes tools.lock immutable at build time; tampering requires rebuilding the binary |
| Integration smoke test → real binary | Test builds and runs the real binary in a tempdir; no persistent side effects |
| ACCEPTANCE.md → release gate | Pre-merge review reads ACCEPTANCE.md to verify Phase 3 completion claims |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07-01 | Tampering | testutil package imported by production code | mitigate | Naming convention (`testutil` is non-production by convention) + lint rule check (could be added in a future iteration); CI can be extended to flag `import .*testutil` outside `_test.go` files |
| T-03-07-02 | Tampering | tools.lock manifest tampering | mitigate | Embedded via go:embed at build time; binary integrity depends on Go module supply chain (Phase 11 owns full SHA-256 chain) |
| T-03-07-03 | DoS | Integration smoke test runs go build inside a Go test | accept | Build time + test time ≈ 30s on CI; acceptable per ADR §9.1 integration ring budget |
| T-03-07-04 | Tampering | ACCEPTANCE.md claims not matching reality | mitigate | Each criterion cites a specific test command + SUMMARY reference; pre-merge review verifies; Plan 07's CI integration-smoke job re-runs the canonical test on every push |
| T-03-07-05 | Information Disclosure | tools.lock leaks tool inventory | accept | Tool list is public per ROADMAP / PROJECT.md (reconFTW openly catalogs its orchestrated tools); no secret data |
| T-03-07-SC | Tampering | npm/pip/cargo installs | accept | Plan 07 adds 0 new Go modules. Uses pelletier/go-toml/v2 (Plan 02), errgroup (Plan 04), modernc/sqlite (Plan 03) — all pre-vetted. |
| T-03-07-06 | Tampering | interfaces_check delta detector bypass | mitigate | verify-0002.sh Check 3 runs on every commit (via CI lint or integration job); BINDING signature drift fails the build before merge |
| T-03-07-07 | DoS | Critical tool false-positive in health-check | accept | Critical = {subfinder, httpx, dnsx} is a minimal trio; CI runners can install via `go install` in a setup step; health-check failure on missing critical is intended behavior per FOUND-08 |
</threat_model>

<verification>
- `go build ./...` exits 0
- `go vet ./...` exits 0
- `go test -race ./...` exits 0 (all packages green)
- `go test -race -run TestKernelDemoEndToEnd ./cmd/reconftw/...` exits 0 (Phase 3 acceptance integration smoke)
- `make check` exits 0
- Stripped binary <50MB (XCUT-02)
- XCUT-07 sentinel test passes
- FOUND-09 kill-tree smoke test passes (<10s) — uses syscall.Kill liveness probe per W17
- FOUND-10 raw subprocess lint rule passes — AST scan per Blocker 8
- `bash .planning/decisions/verify-0002.sh` exits 0 (BINDING signature gate end-to-end)
- `.planning/phases/03-foundation-kernel/03-ACCEPTANCE.md` exists and certifies all 5 ROADMAP success criteria with measured values
- Coverage on `internal/core/...` ≥75% combined (XCUT-04)
- Coverage on critical paths (redactor, redacting_handler, checkpoint/store, output/atomic) ≥90% (XCUT-03 + W20)
</verification>

<success_criteria>
- All 3 tasks pass automated verification
- ROADMAP §"Phase 3" success criterion 5 fully satisfied (test mocks ship; CI runs every gate; binary <50MB; XCUT-07 sentinel gate active)
- Phase 3 is DONE: kernel runs an end-to-end Task pipeline; CI proves it; ACCEPTANCE.md certifies it
- Phase 4 readiness:
  - tools.lock seeded with 10 Phase 4 tools (3 critical, 7 non-critical per FOUND-08)
  - MockBackend + MockCheckpoint + MockOutputTree available for Phase 4 unit tests (satisfy real interfaces per W15)
  - `internal/modules/demo/noop.go` ready to be deleted in Phase 4 plan-01 (replaced by `internal/modules/subdomains/passive.go`)
- interfaces_check/main.go imports real packages — BINDING signature compatibility VERIFIED
- D-01..D-04 all proven via the integration smoke + CLI behavior tests in Plan 06 + this plan
</success_criteria>

<output>
Create `.planning/phases/03-foundation-kernel/03-07-SUMMARY.md` when done.

Summary MUST capture:
- FOUND-15 mocks shipped: MockBackend, MockCheckpoint, MockOutputTree — each tested independently + via integration smoke; interface satisfaction verified via compile-time assertions
- Fixture directory layout: subfinder/example.com.txt + httpx/hosts.jsonl
- tools.lock seed: 10 tools enumerated with Go module paths + critical tier breakdown (3 critical: subfinder/httpx/dnsx; 7 non-critical)
- Integration smoke test result: TestKernelDemoEndToEnd PASS with measured timing
- Binary size: actual measurement under XCUT-02 50MB gate
- All 5 ROADMAP success criteria: cite specific test commands + SUMMARY references
- interfaces_check/main.go upgrade: real imports + compile-time assertions
- Final CI yaml configuration: 6 jobs (lint, unit, integration, integration-smoke, binary-size, smoke)
- Coverage report per package (including per-file critical-path 90% gates per W20)
- Open items for Phase 4 (CONTEXT default (a) for run-vs-ADR; demo deletion; tools.lock extension)
- Open items for Phase 11 (full 70+ tool inventory; SHA-256 verification)
- Open items for Phase 12 (CompatWriter full lifecycle; V1ToV2Mapping extension)
- Open items for future iteration: golangci-lint v2 custom plugin for FOUND-10
</output>
