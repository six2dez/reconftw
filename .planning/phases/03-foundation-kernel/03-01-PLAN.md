---
phase: 03-foundation-kernel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - internal/core/errors/errors.go
  - internal/core/errors/errors_test.go
  - internal/core/log/secret.go
  - internal/core/log/secret_test.go
  - internal/core/log/redactor.go
  - internal/core/log/redactor_test.go
  - internal/core/log/redacting_handler.go
  - internal/core/log/redacting_handler_test.go
  - internal/core/log/logger.go
  - internal/core/log/logger_test.go
  - .github/workflows/ci.yml
  - .golangci.yml
  - Makefile
  - spike/python/  # DELETE (loser cleanup)
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - XCUT-04
  - XCUT-07

must_haves:
  truths:
    - "Logger is the first subsystem available — every other subsystem can log from its init."
    - "Every secret in Config / AppContext / Tool structs is type-tagged with `log.Secret`; logging it auto-redacts to `\"***\"`."
    - "An XCUT-07 sentinel-value test fails CI if a known fake secret string ever appears in any captured log output."
    - "Every typed error type implements `Is()` so `errors.Is(err, ErrTool)` works across the 7-class hierarchy."
    - "CI runs `go vet`, `golangci-lint`, `gofumpt`, unit tests with `-race`, and the XCUT-07 sentinel test on every push."
    - "`spike/python/` is gone from the working tree (Phase 1 loser cleanup per CONTEXT default for spike disposition)."
  artifacts:
    - path: internal/core/errors/errors.go
      provides: "7-class error hierarchy (ToolError, ToolTimeout, OutOfScope, AxiomFailure, ConfigError, ScopeError, ChecksumMismatch) + 6 sentinel anchors (ErrTool/ErrTimeout/ErrScope/ErrAxiom/ErrConfig/ErrChecksum)"
      contains: "type ToolError struct"
    - path: internal/core/log/secret.go
      provides: "Secret type implementing slog.LogValuer (no String() method)"
      contains: "type Secret string"
    - path: internal/core/log/redactor.go
      provides: "Redactor.Register(string) + Redactor.Redact(string) string"
      contains: "type Redactor struct"
    - path: internal/core/log/redacting_handler.go
      provides: "RedactingHandler wrapping slog.Handler with full Enabled/Handle/WithAttrs/WithGroup chain"
      contains: "type RedactingHandler struct"
    - path: internal/core/log/logger.go
      provides: "log.New(cfg, redactor) factory returning *slog.Logger"
      contains: "func New"
    - path: .github/workflows/ci.yml
      provides: "CI pipeline running unit + integration + smoke rings per ADR §9.3"
      contains: "go test -race"
    - path: .golangci.yml
      provides: "golangci-lint v2.12.2 config (error-level gate)"
      contains: "golangci"
    - path: Makefile
      provides: "Targets: build, test, lint, fmt, check; build uses -ldflags=\"-s -w\" -trimpath"
      contains: "ldflags"
  key_links:
    - from: internal/core/log/redacting_handler.go
      to: internal/core/log/redactor.go
      via: "RedactingHandler.redactor field receives *Redactor at construction"
      pattern: "NewRedactingHandler.*Redactor"
    - from: internal/core/log/logger.go
      to: internal/core/log/redacting_handler.go
      via: "log.New chains JSON/text handler → RedactingHandler"
      pattern: "NewRedactingHandler"
    - from: .github/workflows/ci.yml
      to: internal/core/log/redactor_test.go
      via: "XCUT-07 sentinel-value test runs in `unit` job"
      pattern: "go test -race"
---

<objective>
Ship the kernel's foundational base layer — typed errors, the Secret type, the Redactor + RedactingHandler chain, the logger factory, the `internal/core/` directory tree, and the CI gate that enforces them on every push. This plan MUST land first because ADR §10.3 mandates the logger active BEFORE config load (config validation lines must be captured), and the redactor must be wired into the logger chain BEFORE the first secret can be registered. Subsequent plans (config / output / scheduler / backend / CLI) all consume the logger and error types this plan defines.

Purpose: This is the "no working logger = no observable kernel" layer. It also wires the XCUT-07 logging hygiene CI gate so every future commit is automatically tested for secret leak. The 7-class error hierarchy lets every subsystem return typed errors that downstream Phase 4-12 modules can `errors.Is`/`errors.As` against without ambiguity.

Output: `internal/core/errors/` (typed hierarchy), `internal/core/log/` (Secret + Redactor + RedactingHandler + logger factory), `.github/workflows/ci.yml` (Phase 3 baseline pipeline), `.golangci.yml` (custom-rule scaffold), `Makefile` (build/test/lint targets honouring XCUT-02 ldflags). Plus `spike/python/` deletion per CONTEXT decision-default for spike disposition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/decisions/0001-language.md
@.planning/decisions/0002-architecture-v2.md
@.planning/decisions/verify-0002.sh
@.planning/research/SUMMARY.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@.planning/phases/03-foundation-kernel/03-CONTEXT.md
@.planning/phases/03-foundation-kernel/03-PATTERNS.md
@.planning/codebase/CONCERNS.md
@interfaces_check/main.go
@go.mod
@modules/core.sh
@lib/common.sh

<interfaces>
<!-- Key external dependencies and stdlib shapes the executor MUST import correctly. -->
<!-- ADR §10.1 cites the official Go stdlib LogValuer example as the canonical Secret pattern. -->

From `log/slog` (Go stdlib):
```go
type LogValuer interface {
    LogValue() Value
}
func StringValue(s string) Value
// Handler chain interface:
type Handler interface {
    Enabled(context.Context, Level) bool
    Handle(context.Context, Record) error
    WithAttrs([]Attr) Handler
    WithGroup(string) Handler
}
```

From `errors` (Go stdlib):
```go
func Is(err, target error) bool      // walks Unwrap chain; honors x.Is(target) sentinel bridge
func As(err error, target any) bool  // walks Unwrap chain; assigns into target pointer
func Unwrap(err error) error
func New(text string) error
```

From `sync` (Go stdlib):
```go
type RWMutex struct{ /* ... */ }
// Used in Redactor for concurrent-safe Register/Redact
```

From `strings` (Go stdlib):
```go
func ReplaceAll(s, old, new string) string  // used by Redactor.Redact
```

Phase 2 placeholder Task/Backend/AppContext interfaces (already in `interfaces_check/main.go` at repo root): Status enum, Result struct, Task interface, Backend interface, FailurePolicy enum. Phase 3 implementation files will mirror these shapes verbatim per ADR §5 (BINDING).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold internal/core/ tree + bump Go toolchain + clean up spike/python/</name>
  <files>
    go.mod
    go.sum
    Makefile
    .gitignore
    spike/python/
  </files>
  <read_first>
    - go.mod (current: `module github.com/six2dez/reconftw`, `go 1.23`; ADR/STACK calls for 1.24+, target 1.26 — bump to 1.24 to gain stable `slog`, `WaitDelay`, `errgroup.SetLimit` per RESEARCH.md §Stack Snapshot)
    - .planning/research/SUMMARY.md §"Stack Snapshot" — locks library picks
    - .planning/research/STACK.md §"What NOT to Use" — library blacklist (no viper, no logrus, no mattn/go-sqlite3, no http.DefaultClient, no cmd.Process.Kill alone, no hand-rolled retry)
    - .planning/decisions/0002-architecture-v2.md §1.2 lines 199-234 (Recommended Project Structure — the directory tree this task creates)
    - spike/go/Makefile (build target reference with `-ldflags="-s -w" -trimpath`)
    - .planning/phases/03-foundation-kernel/03-CONTEXT.md "Deferred Ideas" — spike disposition default: delete `spike/python/` in Phase 3 plan-01 cleanup (loser per Phase 1 D-03 spirit); keep `spike/go/` until Phase 4
    - .planning/phases/01-language-adr-spike/01-CONTEXT.md D-03 (research-file collapse to Go-only happened in Phase 1)
  </read_first>
  <behavior>
    - Test 1: `go build ./...` (after touching only go.mod) exits 0
    - Test 2: `go test ./...` finds zero test files in newly created empty packages (no error)
    - Test 3: `[ ! -d spike/python ]` (directory gone)
    - Test 4: `[ -f Makefile ]` and `make -n build` (dry run) prints the `-ldflags="-s -w" -trimpath` invocation
  </behavior>
  <action>
    Bump `go.mod` from `go 1.23` to `go 1.24` (canonical version per RESEARCH.md §Stack Snapshot row "Runtime: Go 1.24+"; do not jump to 1.26 yet — 1.24 has all required stable features and is the broadest CI target).

    Create the empty `internal/core/` directory tree per ADR §1.2 (use `mkdir -p` + `.gitkeep` placeholders so empty packages can be committed even before Plan 02-06 populate them):
    - `internal/core/errors/` (this plan populates)
    - `internal/core/log/` (this plan populates)
    - `internal/core/config/` (Plan 02)
    - `internal/core/task/` (Plan 05)
    - `internal/core/scheduler/` (Plan 04)
    - `internal/core/backend/` (Plan 04)
    - `internal/core/output/` (Plan 03)
    - `internal/core/checkpoint/` (Plan 03)
    - `internal/core/appctx/` (Plan 05)
    - `internal/core/notifier/` (Plan 05)
    - `internal/core/ui/` (Plan 05)
    - `internal/core/testutil/` (Plan 06)
    - `internal/modules/demo/` (Plan 05)
    - `cmd/reconftw/` (Plan 05)

    Use `.gitkeep` placeholder files in each directory that this plan does not populate. Do NOT put `.gitkeep` in `errors/` or `log/` since this plan adds real files there.

    Create `Makefile` at repo root with these targets (port from `spike/go/Makefile` and extend):
    - `build`: `go build -ldflags="-s -w" -trimpath -o bin/reconftw ./cmd/reconftw` (XCUT-02 ldflags from RESEARCH.md §Stack Snapshot row "Packaging")
    - `test`: `go test -race -short ./...` (Ring 1 + Ring 4 per ADR §9.1)
    - `test-integration`: `go test -race ./...` (Ring 1 + Ring 2 + Ring 4 per ADR §9.1)
    - `test-smoke`: `go test -race -tags smoke ./...` (Ring 3 per ADR §9.1)
    - `lint`: `golangci-lint run ./...`
    - `fmt`: `gofumpt -w .`
    - `fmt-check`: `gofumpt -d . | (! grep .)`
    - `check`: `make fmt-check lint test` (composite gate)
    - `clean`: `rm -rf bin/`
    - Include a `.PHONY:` declaration for all targets.

    Delete `spike/python/` recursively (`rm -rf spike/python`). Rationale per CONTEXT default: Phase 1 ADR picked Go; Python is the loser; `spike/python/` is 37MB of dead reference; no Phase 3+ work depends on it. Keep `spike/go/` (live reference until Phase 4 ports first module — CONTEXT decision).

    Add `bin/` to `.gitignore` if not already present (build target writes there).

    Confirm `cmd/reconftw/` is created (with `.gitkeep`) so Plan 05 can populate it. The `Makefile` `build` target references `./cmd/reconftw` — this will fail-fast in CI until Plan 05 lands main.go (intentional — fail-fast is better than silent skip).
  </action>
  <verify>
    <automated>cd /Users/six2dez/Tools/reconftw && go build ./... 2>&1 && [ ! -d spike/python ] && grep -q 'go 1.24' go.mod && grep -q 'ldflags="-s -w" -trimpath' Makefile && [ -d internal/core/errors ] && [ -d internal/core/log ] && [ -d internal/core/config ] && [ -d cmd/reconftw ]</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -q 'go 1.24' go.mod` exits 0
    - Source: `grep -q 'module github.com/six2dez/reconftw' go.mod` exits 0
    - Source: `grep -q 'ldflags="-s -w" -trimpath' Makefile` exits 0
    - Build: `go build ./...` exits 0 (no compilation errors)
    - Filesystem: `[ -d internal/core/errors ] && [ -d internal/core/log ] && [ -d internal/core/config ] && [ -d internal/core/task ] && [ -d internal/core/scheduler ] && [ -d internal/core/backend ] && [ -d internal/core/output ] && [ -d internal/core/checkpoint ] && [ -d internal/core/appctx ] && [ -d internal/core/notifier ] && [ -d internal/core/ui ] && [ -d internal/core/testutil ] && [ -d internal/modules/demo ] && [ -d cmd/reconftw ]` (all created)
    - Filesystem: `[ ! -d spike/python ]` (Python spike gone)
    - Filesystem: `[ -d spike/go ]` (Go spike kept per CONTEXT decision)
    - Make: `make -n build 2>&1 | grep -q 'cmd/reconftw'` (Makefile build target wired)
    - Make: `make -n test 2>&1 | grep -q 'go test -race -short'` (test target wired)
    - `interfaces_check/main.go` still compiles: `go build -o /tmp/iface_check_p1 ./interfaces_check/... && rm -f /tmp/iface_check_p1` exits 0
  </acceptance_criteria>
  <done>
    `internal/core/` tree exists with placeholder `.gitkeep` files in packages not yet populated; `errors/` and `log/` are real directories (no `.gitkeep` since this plan adds real files). `go.mod` bumped to 1.24. `Makefile` ships with build/test/lint targets honouring XCUT-02 ldflags. `spike/python/` is deleted from working tree. `interfaces_check/main.go` still compiles (delta-detector intact for ADR §5 contract verification).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement 7-class typed error hierarchy with Is() sentinel bridge</name>
  <files>
    internal/core/errors/errors.go
    internal/core/errors/errors_test.go
  </files>
  <read_first>
    - .planning/decisions/0002-architecture-v2.md §6 lines 1748-1912 (CANONICAL — full ADR section: 6 sentinel anchors + 7 typed structs + Is() bridge + serialization note + PITFALL NOTES on secret leak via ConfigError and circular DependsOn detection requirements)
    - .planning/decisions/0002-architecture-v2.md lines 1762-1764 (PITFALL: ConfigError.Message MUST NOT include raw secret values)
    - .planning/decisions/0002-architecture-v2.md lines 1843-1844 (PITFALL: AxiomFailure.Inner MUST NOT contain raw credential values — run Redactor.Redact on error strings before construction)
    - .planning/decisions/0002-architecture-v2.md §9.2 lines 2234-2245 (Ring 1 test list — "Error type Is() / As() traversal across the 7-class hierarchy" is canonical Ring 1 example)
    - .planning/research/PITFALLS.md §2 (T-02-04-02: secret leak via ConfigError)
    - .planning/phases/03-foundation-kernel/03-PATTERNS.md §3 (file mapping + sentinel anchor pattern + typed struct + Is() bridge pattern)
    - interfaces_check/main.go (does NOT mirror §6 yet — this task is the first to materialize ADR §6 contract; interfaces_check stays focused on §5 Task/Backend/AppContext; do NOT update interfaces_check in this task)
    - modules/core.sh (v1 untyped error strings — semantic intent only; v2 replaces with typed structs)
  </read_first>
  <behavior>
    - Test 1: `errors.Is(&ToolError{Tool: "subfinder", ExitCode: 2}, errors.ErrTool)` returns true (sentinel bridge works)
    - Test 2: `errors.As(wrapped, &te)` where `wrapped` is `fmt.Errorf("subfinder: %w", &ToolError{...})` populates `te` with the original struct (Unwrap chain works)
    - Test 3: `(&ToolError{Tool: "x", ExitCode: 1, Inner: io.EOF}).Error()` returns `"tool x (exit 1): EOF"` (no secret leak; format stable)
    - Test 4: `(&ConfigError{File: "/tmp/c.toml", Line: 42, Key: "slack.webhook_url", Message: "invalid URL format"}).Error()` produces exactly `/tmp/c.toml:42 key "slack.webhook_url": invalid URL format` — no raw value
    - Test 5: `(&AxiomFailure{Operation: "exec", Inner: errors.New("ssh timeout")}).Is(ErrAxiom)` returns true
    - Test 6: All 7 typed structs implement the `error` interface; `var _ error = (*ToolError)(nil); var _ error = (*ToolTimeout)(nil); ...` compiles
    - Test 7: `ChecksumMismatch.Error()` truncates the SHA-256 hex to first 8 chars + `"..."` (regression test for "expected %s got %s" format per ADR §6 line 1893-1895)
    - Test 8: A registry of all sentinel anchors (ErrTool, ErrTimeout, ErrScope, ErrAxiom, ErrConfig, ErrChecksum) exists and each is non-nil
  </behavior>
  <action>
    Implement `internal/core/errors/errors.go` exactly per ADR §6 (lines 1771-1898). Package name MUST be `errors` (per ADR — shadows stdlib `errors`; callers import with named import or use `pkgerrors` alias internally). Use Go stdlib `errors` package with named import to avoid shadowing:

    ```go
    package errors

    import (
        stderrors "errors"
        "fmt"
        "time"
    )
    ```

    Materialize:
    1. **Six sentinel anchors** (var block, ADR lines 1786-1793): `ErrTool`, `ErrTimeout`, `ErrScope`, `ErrAxiom`, `ErrConfig`, `ErrChecksum` — each `stderrors.New("...")` with the exact message from ADR.
    2. **Seven typed structs**: `ToolError` (Tool, ExitCode, Stderr, Inner), `ToolTimeout` (Tool, Timeout), `OutOfScope` (Value, Reason), `AxiomFailure` (Operation, Inner), `ConfigError` (File, Line, Key, Message), `ScopeError` (Input, Reason), `ChecksumMismatch` (URL, Expected, Got).
    3. **Per struct**: `Error() string` with the exact format from ADR (already shown in `<behavior>` Test 3-7), `Unwrap() error` where Inner exists (ToolError, AxiomFailure), `Is(target error) bool` sentinel bridge for ALL seven.
    4. **PITFALL enforcement** — Add a code comment ON `ConfigError` and `AxiomFailure` referencing the security note in ADR §6 (SECURITY NOTE: Message/Inner MUST NOT include raw secret values).

    **Truncation rule for `ChecksumMismatch.Error()`:** Per ADR line 1894-1895, format MUST be `"checksum mismatch for %s: expected %s got %s"` with `Expected[:8]+"..."` and `Got[:8]+"..."`. Defensive bounds check: if `len(Expected) < 8`, use entire string (no panic).

    `internal/core/errors/errors_test.go` — table-driven tests covering all 8 behaviors above. Use `testing` only (testify can be added later when Plan 02+ need it). MUST include `import stderrors "errors"` to call `stderrors.Is` and `stderrors.As`. Test format:

    ```go
    func TestErrTool_IsSentinel(t *testing.T) {
        e := &ToolError{Tool: "subfinder", ExitCode: 2}
        if !stderrors.Is(e, ErrTool) {
            t.Errorf("Is(err, ErrTool) = false; want true")
        }
    }
    ```

    All 7 typed types get a similar "TestErrXxx_IsSentinel" test. Wrap-with-fmt test ensures `fmt.Errorf("wrapped: %w", e)` is still `stderrors.Is(_, ErrTool)`. The `errors_test` should NOT depend on the log package (decoupled), and the package name in the test file is `errors_test` (external test package — exercise public API only).
  </action>
  <verify>
    <automated>cd /Users/six2dez/Tools/reconftw && go test -race -run 'TestErr' ./internal/core/errors/... && go vet ./internal/core/errors/...</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'func (e \*ToolError) Is(' internal/core/errors/errors.go` returns 1 (exactly one Is method on ToolError)
    - Source: `grep -E 'ErrTool|ErrTimeout|ErrScope|ErrAxiom|ErrConfig|ErrChecksum' internal/core/errors/errors.go | grep -v '^//' | grep -c 'errors\.New(' | head -1` returns at least 6 (6 sentinel anchors created via errors.New)
    - Source: `grep -c '^type \(ToolError\|ToolTimeout\|OutOfScope\|AxiomFailure\|ConfigError\|ScopeError\|ChecksumMismatch\) struct' internal/core/errors/errors.go` returns 7 (all 7 typed structs)
    - Source: `grep -B1 'type ConfigError struct' internal/core/errors/errors.go | grep -qi 'SECURITY\|secret'` (security note comment present)
    - Source: `grep -B1 'type AxiomFailure struct' internal/core/errors/errors.go | grep -qi 'SECURITY\|credential'` (security note comment present)
    - Test: `go test -race ./internal/core/errors/...` exits 0
    - Test: `go vet ./internal/core/errors/...` exits 0
    - Test: All 8 behaviors in `<behavior>` block above are covered (one TestXxx per behavior)
    - Coverage: `go test -coverprofile=/tmp/cov.out ./internal/core/errors/... && go tool cover -func=/tmp/cov.out | grep '^total:' | awk '{print ($3+0 >= 90.0) ? 0 : 1}'` exits 0 (≥90% on critical paths per XCUT-03)
    - `interfaces_check/main.go` still compiles after this task: `go build -o /tmp/ic ./interfaces_check/... && rm -f /tmp/ic` exits 0
  </acceptance_criteria>
  <done>
    All 6 sentinel anchors and all 7 typed error structs exist in `internal/core/errors/errors.go` matching ADR §6 verbatim. `Is()` bridge works on every typed struct (`errors.Is(err, ErrXxx)` returns true). `Unwrap()` works on `ToolError` and `AxiomFailure`. Security notes are documented inline on `ConfigError` and `AxiomFailure`. Tests cover all 8 behaviors with ≥90% branch coverage. `go vet` clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Secret type + Redactor + RedactingHandler + logger factory (two-layer defense)</name>
  <files>
    internal/core/log/secret.go
    internal/core/log/secret_test.go
    internal/core/log/redactor.go
    internal/core/log/redactor_test.go
    internal/core/log/redacting_handler.go
    internal/core/log/redacting_handler_test.go
    internal/core/log/logger.go
    internal/core/log/logger_test.go
  </files>
  <read_first>
    - .planning/decisions/0002-architecture-v2.md §10 lines 2328-2594 (CANONICAL — full two-layer defense: §10.1 Secret type, §10.2 RedactingHandler full implementation, §10.3 build-order, §10.4 XCUT-07 sentinel CI gate)
    - .planning/decisions/0002-architecture-v2.md §10.1 lines 2356-2375 (Secret type — official Go stdlib LogValuer example pattern; CRITICAL: no String() method on Secret)
    - .planning/decisions/0002-architecture-v2.md §10.2 lines 2429-2518 (Redactor + RedactingHandler full source — lift verbatim)
    - .planning/decisions/0002-architecture-v2.md §10.4 lines 2576-2593 (XCUT-07 sentinel test: 5 steps; sentinel value `test_sentinel_value_not_a_real_key_abc123`)
    - .planning/phases/03-foundation-kernel/03-PATTERNS.md §4-§5 (Secret + Redactor + RedactingHandler patterns with bash semantic anchors)
    - modules/core.sh lines 24-89 (`REDACT_VARS` array + `register_secret` + `redact_secrets` — v1 bash semantic equivalent)
    - lib/common.sh — search for `register_secret` invocation pattern
    - .planning/research/PITFALLS.md §4 (secret leak in logs — top-5 pitfall)
    - spike/go/internal/ui/ui.go (minimum-viable stderr writer — production logger is the production successor)
    - .planning/research/SUMMARY.md §"Stack Snapshot" row "Logging: log/slog + LogValuer redaction"
  </read_first>
  <behavior>
    Secret type:
    - Test 1: `Secret("supersecret-token-abc-12345")` — calling `LogValue()` returns `slog.StringValue("***")` (not the raw value)
    - Test 2: A struct field typed `log.Secret` embedded in a slog attribute auto-redacts: `slog.Any("token", mySecret)` emits `token=***` in output
    - Test 3: `fmt.Sprintf("%s", Secret("raw"))` does NOT print "raw" via String() — Secret has no String() method, so `%s` falls back to default Go string semantics (this test verifies absence of String())
    - Test 4: Explicit `string(Secret("raw"))` returns `"raw"` (only sanctioned way to extract; for Redactor.Register)

    Redactor:
    - Test 5: `r.Register("abc")` — value ≤4 chars is silently ignored (no panic, no add)
    - Test 6: `r.Register("supersecret-token-abc-12345")` then `r.Redact("my supersecret-token-abc-12345 leaked")` returns `"my *** leaked"`
    - Test 7: Concurrent `Register` from 10 goroutines + concurrent `Redact` is race-clean (`go test -race`)
    - Test 8: `r.Register("dup"); r.Register("dup")` — dedup: only added once; `r.Redact("dup")` doesn't double-replace (it's a no-op in this case anyway since "dup" is ≤4 chars; pick a longer value: `r.Register("repeated-value"); r.Register("repeated-value")` → only one entry)

    RedactingHandler:
    - Test 9: A `slog.Logger` built with `RedactingHandler` wrapping a `slog.NewJSONHandler(buf, nil)` where Redactor has registered `"sentinel-12345"`, calling `logger.Info("got value: sentinel-12345")` produces JSON output containing `"msg":"got value: ***"` (Message redacted)
    - Test 10: Same handler, calling `logger.Info("event", slog.String("token", "sentinel-12345"))` produces JSON containing `"token":"***"` (string Attr redacted)
    - Test 11: Non-string Attr (e.g., `slog.Int("count", 42)`) passes through unchanged: `"count":42` (no Redact applied)
    - Test 12: `RedactingHandler.WithAttrs([]slog.Attr{slog.String("k", "sentinel-12345")})` returns a handler whose pre-applied attrs are already redacted (test by emitting any log line and asserting `k=***`)
    - Test 13: `RedactingHandler.WithGroup("g")` returns a handler that delegates to inner.WithGroup; logging into it produces `g.key=value` style (no functional change vs inner)

    Logger factory:
    - Test 14: `log.New(cfg, redactor)` returns a `*slog.Logger` whose handler chain is JSON→RedactingHandler (test by emitting a log line and asserting the output is valid JSON AND contains "***" for registered secrets)
    - Test 15: XCUT-07 sentinel-value gate: register `"test_sentinel_value_not_a_real_key_abc123"`, emit `slog.Info("loading config", slog.String("k", "test_sentinel_value_not_a_real_key_abc123"))`, capture the buffer, assert the raw sentinel string does NOT appear anywhere in the buffer
  </behavior>
  <action>
    **File 1 — `internal/core/log/secret.go`:** Lift verbatim from ADR §10.1 lines 2356-2375. Package name `log` (NOT `slog`). Type definition:

    ```go
    package log

    import "log/slog"

    type Secret string

    func (Secret) LogValue() slog.Value {
        return slog.StringValue("***")
    }
    ```

    Include the full ADR comment block above the type declaration (it documents the BINDING "no String() method" rule). Add this concrete cite: `// Source: ADR 0002 §10.1 — BINDING type-level secret tagging`.

    **File 2 — `internal/core/log/redactor.go`:** Lift verbatim from ADR §10.2 lines 2429-2464. Includes:
    ```go
    type Redactor struct {
        mu      sync.RWMutex
        secrets []string
    }
    func (r *Redactor) Register(value string)
    func (r *Redactor) Redact(s string) string
    ```
    Imports: `sync`, `strings`. Skip values with `len(value) <= 4` (ADR rule line 2442 — too short to be meaningful). Dedup on Register (ADR line 2446-2451). Use `sync.RWMutex` (write-lock for Register, read-lock for Redact).

    **File 3 — `internal/core/log/redacting_handler.go`:** Lift verbatim from ADR §10.2 lines 2466-2518. Types:
    ```go
    type RedactingHandler struct {
        inner    slog.Handler
        redactor *Redactor
    }
    func NewRedactingHandler(inner slog.Handler, r *Redactor) *RedactingHandler
    func (h *RedactingHandler) Enabled(ctx context.Context, l slog.Level) bool
    func (h *RedactingHandler) Handle(ctx context.Context, r slog.Record) error
    func (h *RedactingHandler) WithAttrs(attrs []slog.Attr) slog.Handler
    func (h *RedactingHandler) WithGroup(name string) slog.Handler
    func (h *RedactingHandler) redactAttr(a slog.Attr) slog.Attr
    ```
    The `redactAttr` method per ADR lines 2513-2517 only redacts attrs with `Kind() == slog.KindString`. Non-string attrs pass through unchanged.

    **CRITICAL Handle() pattern (ADR lines 2486-2494):** Build new record with redacted message via `slog.NewRecord(r.Time, r.Level, h.redactor.Redact(r.Message), r.PC)`; iterate original attrs via `r.Attrs(func(a slog.Attr) bool { r2.AddAttrs(h.redactAttr(a)); return true })`; delegate `h.inner.Handle(ctx, r2)`.

    **File 4 — `internal/core/log/logger.go`:** New file (no direct ADR snippet — ADR §10.3 specifies the call shape `log.New(cfg, redactor)` returning `*slog.Logger`). For Plan 01, `cfg` is a `*minimalLogConfig` defined inline in this package (Plan 02 will replace with the real `config.Config` once it lands — see migration note in code comment). Minimal config shape:

    ```go
    type Config struct {
        Level  slog.Level // default: slog.LevelInfo
        Format string     // "json" or "text"; default "json"
        Output io.Writer  // default: os.Stderr
    }
    ```

    `New(cfg *Config, redactor *Redactor) *slog.Logger`:
    1. Choose inner handler: `slog.NewJSONHandler` if `cfg.Format == "json"`, else `slog.NewTextHandler`. Pass `&slog.HandlerOptions{Level: cfg.Level}`.
    2. Wrap with `NewRedactingHandler(innerHandler, redactor)`.
    3. Return `slog.New(redactingHandler)`.

    Add doc comment: `// New is the logger factory called from cmd/reconftw/main.go per ADR §10.3 init order. Phase 2 of Plan 02 replaces *Config with *config.Config (no signature change beyond pointer type).`

    **File 5 — `internal/core/log/secret_test.go`:** Tests 1-4 from `<behavior>` block. Use external test package `package log_test`. The Test 3 ("no String() method") uses a compile-time assertion: `var _ fmt.Stringer = log.Secret("")` MUST NOT compile — but a Go test can verify runtime behavior by checking `reflect.TypeOf(log.Secret("")).MethodByName("String")` returns `_, ok := ...; !ok`.

    **File 6 — `internal/core/log/redactor_test.go`:** Tests 5-8 from `<behavior>` block. Test 7 (concurrent race) MUST run under `-race` and finish in < 1 second.

    **File 7 — `internal/core/log/redacting_handler_test.go`:** Tests 9-13 from `<behavior>` block. Use `bytes.Buffer` as the inner handler's writer. Assert JSON output by `json.Unmarshal` into `map[string]any` and checking `m["msg"]` and the named attr.

    **File 8 — `internal/core/log/logger_test.go`:** Test 14 (factory builds correct chain) + Test 15 (XCUT-07 sentinel gate — this is THE canonical gate test). Test 15 MUST use the exact sentinel value from ADR §10.4 line 2582: `test_sentinel_value_not_a_real_key_abc123`. Assert: `!bytes.Contains(buf.Bytes(), []byte("test_sentinel_value_not_a_real_key_abc123"))` and `bytes.Contains(buf.Bytes(), []byte("***"))`.

    **Naming note:** The package directory name `log` shadows Go stdlib `log` package. This is intentional per ADR §1.2 layout. Callers in subsequent plans import as `github.com/six2dez/reconftw/internal/core/log` and use it as `log.New`, `log.Secret`, etc. Callers needing stdlib `log` (none expected — slog replaces it) would use named import.
  </action>
  <verify>
    <automated>cd /Users/six2dez/Tools/reconftw && go test -race ./internal/core/log/... && go vet ./internal/core/log/... && go test -race -run TestXCUT07Sentinel ./internal/core/log/... 2>&1 | grep -q 'PASS'</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'type Secret string' internal/core/log/secret.go` returns 1
    - Source: `! grep -E 'func \(.*Secret\) String\(' internal/core/log/secret.go` (NO String method exists on Secret — XCUT-07 type-level invariant)
    - Source: `grep -c 'func (Secret) LogValue()' internal/core/log/secret.go` returns 1
    - Source: `grep -c 'type Redactor struct' internal/core/log/redactor.go` returns 1
    - Source: `grep -c 'len(value) <= 4' internal/core/log/redactor.go` returns 1 (ADR rule: skip short values)
    - Source: `grep -c 'type RedactingHandler struct' internal/core/log/redacting_handler.go` returns 1
    - Source: `grep -c 'func NewRedactingHandler' internal/core/log/redacting_handler.go` returns 1
    - Source: `grep -c 'KindString' internal/core/log/redacting_handler.go` returns at least 1 (string-only redaction per ADR)
    - Source: `grep -c 'func New(' internal/core/log/logger.go` returns 1
    - Test: `go test -race ./internal/core/log/...` exits 0
    - Test: XCUT-07 sentinel test passes — `go test -race -run TestXCUT07Sentinel ./internal/core/log/...` shows PASS
    - Test: `go vet ./internal/core/log/...` exits 0
    - Coverage: `go test -coverprofile=/tmp/log_cov.out ./internal/core/log/... && go tool cover -func=/tmp/log_cov.out | grep '^total:' | awk '{print ($3+0 >= 90.0) ? 0 : 1}'` exits 0 (XCUT-03 ≥90% on critical paths — secret redaction qualifies)
    - Behavior: Sentinel test verifies BOTH that the raw value never leaks AND that `***` appears (proves both layer 1 type-tagging and layer 2 substring replacement)
    - Behavior: Test 7 (concurrent Register + Redact) finishes under 1 second with `-race`
  </acceptance_criteria>
  <done>
    Secret type implements `slog.LogValuer` and has NO String() method (BINDING per ADR §10.1). Redactor + RedactingHandler match ADR §10.2 verbatim; thread-safe via RWMutex. Logger factory returns a chain `[JSON|Text]Handler → RedactingHandler → slog.Logger`. XCUT-07 sentinel test passes — confirms the two-layer defense actually prevents leak of a known sentinel value. Tests cover 15 distinct behaviors. ≥90% coverage on the secret-redaction critical path.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Wire CI pipeline + golangci-lint config + XCUT-07 sentinel CI gate</name>
  <files>
    .github/workflows/ci.yml
    .golangci.yml
    Makefile
  </files>
  <read_first>
    - .planning/decisions/0002-architecture-v2.md §9.3 lines 2270-2326 (CANONICAL CI yaml seed: unit/integration/smoke jobs)
    - .planning/decisions/0002-architecture-v2.md §10.1 lines 2396-2400 (golangci-lint custom rule scaffold note — flags exported `*Key/*Token/*Password/*Secret` not typed as `log.Secret`; Phase 3 ships scaffold, Phase 4-7 may extend with reusable rule packages)
    - .planning/decisions/0002-architecture-v2.md §10.4 lines 2576-2594 (XCUT-07 gate — runs in unit job on every commit)
    - .planning/research/SUMMARY.md §"Stack Snapshot" row "Lint: golangci-lint v2.12.2"
    - .planning/phases/03-foundation-kernel/03-CONTEXT.md Claude's Discretion §CI matrix scope (default a: single-platform ubuntu-latest only in Phase 3; XPLAT-05 cross-platform is Phase 11)
    - .planning/phases/03-foundation-kernel/03-PATTERNS.md §18 (CI yaml seed)
    - .github/workflows/ (existing v1 workflow files — leave bats v1 CI intact; this task ADDS a Go CI job, does NOT replace shellcheck/bats)
    - Makefile (from Task 1 — extend with `lint`, `fmt-check`, `coverage` targets if not already present)
  </read_first>
  <behavior>
    - Test 1: GitHub Actions YAML is valid syntax (`yamllint .github/workflows/ci.yml` or `python3 -c 'import yaml; yaml.safe_load(open(".github/workflows/ci.yml"))'`)
    - Test 2: The `unit` job runs `go test -race -short ./...` on `ubuntu-latest`
    - Test 3: The `integration` job runs `go test -race ./...` on `ubuntu-latest`
    - Test 4: The `lint` job runs `golangci-lint run` and `gofumpt -d .` (format-check)
    - Test 5: The `coverage` job (or integration job's last step) asserts ≥75% branch coverage on `internal/core/...` packages (XCUT-04 + XCUT-03 gate)
    - Test 6: The `binary-size` job builds with `-ldflags="-s -w" -trimpath` and asserts binary is <50MB (XCUT-02) — gated to `cmd/reconftw` existing (Plan 05 introduces this; until then the job is `if: false` or filtered with `hashFiles('cmd/reconftw/main.go') != ''`)
    - Test 7: A separate XCUT-07 sentinel test step in the unit job explicitly runs the sentinel-gate test (subset of `go test ./internal/core/log/...`)
    - Test 8: `.golangci.yml` enables `govet`, `staticcheck`, `gofumpt` (format), and an `enabledLinters` list including at minimum `errcheck`, `govet`, `staticcheck`, `unused`, `gofumpt`
  </behavior>
  <action>
    **File 1 — `.github/workflows/ci.yml`** (new file — Go CI pipeline; leaves existing bats v1 workflows untouched):

    Use ADR §9.3 yaml lines 2288-2313 as the seed. Extend with:
    - `name: ci` (top-level workflow name)
    - `on: { push: { branches: ["**"] }, pull_request: {} }` (every push + every PR)
    - `permissions: { contents: read }` (least privilege; supply-chain hygiene)
    - 4 jobs:
      1. `lint`: ubuntu-latest, setup-go@v5 (with `go-version-file: go.mod`), run `gofumpt -d . | tee /dev/stderr | (! grep .)` (format check; fails on any output) AND `golangci-lint run --timeout=5m ./...`
      2. `unit`: ubuntu-latest, setup-go@v5, run `go test -race -short ./...`. ALSO run an explicit step "XCUT-07 sentinel gate" that runs `go test -race -run 'TestXCUT07Sentinel|TestSentinel' ./internal/core/log/...` — assertion: PASS (Phase 3 plan-03's redactor sentinel test).
      3. `integration`: ubuntu-latest, setup-go@v5, run `go test -race ./...`. Last step: coverage gate. `go test -race -coverprofile=coverage.out -covermode=atomic ./internal/core/...` then `go tool cover -func=coverage.out | tail -1 | awk '{ if (substr($3,1,length($3)-1)+0 < 75.0) { print "Coverage below 75% gate"; exit 1 } else { print "Coverage:", $3 } }'` (XCUT-04 branch coverage gate). Until Plans 02-06 populate other packages, this gate runs against `errors/` + `log/` only — which exceeds 75% trivially.
      4. `binary-size`: ubuntu-latest, setup-go@v5. `if: hashFiles('cmd/reconftw/main.go') != ''` (skipped until Plan 05 creates main.go). Step: build with `go build -ldflags="-s -w" -trimpath -o bin/reconftw ./cmd/reconftw` then `SIZE=$(stat --printf=%s bin/reconftw 2>/dev/null || stat -f%z bin/reconftw); echo "binary size: $SIZE bytes"; [ "$SIZE" -lt 52428800 ] || { echo "binary >50MB"; exit 1; }`.

    Use Go version pinning via `go-version-file: go.mod` (so CI matches local toolchain — no drift).

    Add a `smoke` job (per ADR §9.3 lines 2305-2313): `if: github.event_name == 'pull_request' || github.event_name == 'schedule'` — runs `go test -race -tags smoke ./...`. **W12 reinforcement (revision iter 1):** the smoke job is MANDATORY (not a placeholder) — Plan 03 ships the FOUND-04 subprocess SIGKILL atomicity test under `//go:build smoke` (in addition to the hook-based unit ring simulation), and Plan 04 ships the FOUND-09 kill-tree test under `//go:build smoke`. Both gate atomicity proofs that the unit ring cannot deliver. The smoke job acceptance: `go test -race -tags smoke ./internal/core/output/... ./internal/core/backend/...` exits 0.

    **File 2 — `.golangci.yml`** (new file, version 2 config):
    ```yaml
    version: "2"
    linters:
      default: standard
      enable:
        - errcheck
        - govet
        - staticcheck
        - unused
        - gofumpt
      disable: []
    formatters:
      enable:
        - gofumpt
    run:
      timeout: 5m
      tests: true
    ```
    Add a top-of-file comment: `# golangci-lint v2.12.2 config — RESEARCH.md §Stack Snapshot.\n# Phase 3 scaffold; Phase 4-7 may extend with reusable custom rule packages flagging exported *Key/*Token/*Password/*Secret fields not typed as log.Secret (ADR §10.1 lines 2396-2400).\n# A custom golangci-lint plugin for that rule is out of scope for Phase 3 — for now, code review + grep audit is the manual enforcement, per CONTEXT discretion.`

    **NOTE on the FOUND-10 raw subprocess lint rule:** Plan 04 owns FOUND-10. This plan ships the scaffold (`.golangci.yml`) and `internal/core/backend/lint/` placeholder is created by Plan 04. Do NOT pre-create the lint rule here.

    **File 3 — `Makefile` extension (from Task 1):** If Task 1's Makefile is missing a `coverage` target, add it:
    ```make
    coverage:
    	go test -race -coverprofile=coverage.out -covermode=atomic ./internal/core/...
    	go tool cover -func=coverage.out | tail -1
    ```
    Also add a `ci` target: `make ci: fmt-check lint test coverage` (composite gate matching CI).

    **File 4 — Sanity check verify-0002.sh still passes:** After this task lands, `bash .planning/decisions/verify-0002.sh` MUST exit 0 (it builds `interfaces_check/main.go` as Check 3, which doesn't depend on `internal/core/log/` — still independent). Verify in acceptance criteria.

    Commit. Push to a branch and confirm CI runs green on the new commit. (Branch already `rewrite/v2`; the run will appear automatically when the planner-driven commit fires.)
  </action>
  <verify>
    <automated>cd /Users/six2dez/Tools/reconftw && python3 -c 'import yaml; yaml.safe_load(open(".github/workflows/ci.yml"))' && [ -f .golangci.yml ] && grep -q 'gofumpt' .golangci.yml && grep -q 'go test -race -short' .github/workflows/ci.yml && grep -q 'TestXCUT07Sentinel\|TestSentinel' .github/workflows/ci.yml && grep -q '52428800' .github/workflows/ci.yml && bash .planning/decisions/verify-0002.sh</antml-automated>
  </verify>
  <acceptance_criteria>
    - YAML valid: `python3 -c 'import yaml; yaml.safe_load(open(".github/workflows/ci.yml"))'` exits 0
    - Source: `grep -c '^  unit:' .github/workflows/ci.yml` returns 1 (unit job exists)
    - Source: `grep -c '^  integration:' .github/workflows/ci.yml` returns 1
    - Source: `grep -c '^  lint:' .github/workflows/ci.yml` returns 1
    - Source: `grep -c '^  binary-size:' .github/workflows/ci.yml` returns 1
    - Source: `grep -q 'go test -race -short' .github/workflows/ci.yml` exits 0 (unit job runs short mode)
    - Source: `grep -q 'go test -race -tags smoke' .github/workflows/ci.yml` exits 0 (smoke job exists)
    - Source: `grep -q '52428800' .github/workflows/ci.yml` exits 0 (binary-size gate set to 50MB = 52428800 bytes)
    - Source: `grep -q 'TestXCUT07Sentinel\|TestSentinel' .github/workflows/ci.yml` exits 0 (XCUT-07 explicit step)
    - Source: `grep -q 'hashFiles..cmd/reconftw/main.go..' .github/workflows/ci.yml` exits 0 (binary-size gated until Plan 05)
    - Source: `grep -q '75.0' .github/workflows/ci.yml || grep -q '< 75' .github/workflows/ci.yml` (XCUT-04 75% coverage gate)
    - Source: `grep -E 'gofumpt|errcheck|govet|staticcheck' .golangci.yml | wc -l | awk '{ print ($1 >= 4 ? 0 : 1) }'` exits 0 (at least 4 linters enabled)
    - Pre-sign gate: `bash .planning/decisions/verify-0002.sh` exits 0 (interfaces_check still compiles after this task)
    - Make: `make ci` (composite gate) is wired (or at minimum `make fmt-check`, `make lint`, `make test`, `make coverage` all exist)
    - Coverage on `internal/core/errors/` + `internal/core/log/` combined ≥ 75% (verify locally before push)
  </acceptance_criteria>
  <done>
    `.github/workflows/ci.yml` exists with 5 jobs (lint, unit, integration, smoke, binary-size). `.golangci.yml` exists with v2 config enabling at least errcheck/govet/staticcheck/unused/gofumpt. XCUT-07 sentinel test runs explicitly in the unit job. XCUT-04 ≥75% coverage gate runs in the integration job. Binary-size gate (XCUT-02 <50MB) is wired but skipped until Plan 05 lands main.go. `verify-0002.sh` still exits 0 (interfaces_check unaffected). Existing v1 bats workflows are untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stderr / stdout / log files / notifier sinks | Untrusted operator-facing surfaces; secret leak threat origin |
| Config file (user TOML / secrets.toml) | Source of secret values; Secret type-tagging applies at struct definition |
| Go module supply chain (go.mod / go.sum) | Library tampering surface; only stdlib + spf13/cobra (Plan 05) added in Phase 3 plan-01 (no third-party libs added here) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Information Disclosure | `internal/core/log` Redactor / Secret | mitigate | XCUT-07 sentinel test gate runs every commit; Secret type has no String() method (compile-time protection); Redactor.Register skips ≤4-char values to prevent over-redaction |
| T-03-01-02 | Information Disclosure | `internal/core/errors` ConfigError / AxiomFailure | mitigate | Inline SECURITY NOTE comments in `errors.go`; error message format only includes field name + violation type, never raw value (e.g., `"invalid URL format"` not `"invalid URL: http://..."`) |
| T-03-01-03 | Information Disclosure | Logger initialization order | mitigate | ADR §10.3 init order documented; Plan 05 (cmd/reconftw/main.go) enforces order; XCUT-07 test verifies sentinel value cannot leak through config-load error path |
| T-03-01-04 | Tampering | Go module supply chain | accept | Phase 3 plan-01 adds NO third-party deps (stdlib only); Plan 02 adds koanf (v2), Plan 05 adds cobra (v1.9.1); Phase 11 owns full tools.lock + go.sum review |
| T-03-01-SC | Tampering | npm/pip/cargo installs | accept | Phase 3 plan-01 has zero package-manager installs; the Package Legitimacy Audit gate does not apply to this plan |
| T-03-01-05 | Denial of Service | Redactor with thousands of registered secrets | accept | Phase 3 user has 0-10 secrets in config (Slack/Telegram/Discord webhooks); RWMutex contention is irrelevant at this scale; mitigation deferred to Phase 10 (real notifier work) if profiling shows hot path |
| T-03-01-06 | Repudiation | CI bypass via direct `-no-verify` push | mitigate | Branch protection rules MUST gate `main` on CI green (out-of-Phase-3 organizational control; documented in CLAUDE.md). Phase 3 plan-01's role is to make CI green a hard requirement by adding the failing-by-design gates. |
</threat_model>

<verification>
After all 4 tasks complete:
- `go build ./...` exits 0 (whole module compiles)
- `go vet ./...` exits 0
- `go test -race ./internal/core/...` exits 0 (errors + log packages green)
- `go test -race -run 'TestXCUT07Sentinel|TestSentinel' ./internal/core/log/...` exits 0 (XCUT-07 sentinel gate is enforced)
- `bash .planning/decisions/verify-0002.sh` exits 0 (interfaces_check delta detector still compiles; ADR contracts unchanged)
- `python3 -c 'import yaml; yaml.safe_load(open(".github/workflows/ci.yml"))'` exits 0
- `grep -c 'type Secret string' internal/core/log/secret.go` returns 1 and `! grep -E 'func \(.*Secret\) String\(' internal/core/log/secret.go` (no String method)
- `golangci-lint run ./internal/core/...` exits 0 (after installation per .golangci.yml; this may be deferred to CI verification — locally optional)
- `gofumpt -d ./internal/core/...` produces no output (format-clean)
- `[ ! -d spike/python ]` (Python spike gone)
- Coverage on `internal/core/errors/` + `internal/core/log/` ≥ 90% (XCUT-03 critical-path requirement for redaction code)
</verification>

<success_criteria>
- All 4 tasks pass their automated verification commands
- ROADMAP §"Phase 3" success criterion 5 partially satisfied (CI on every push runs lint + format-check + unit tests + `-race` + branch coverage gate ≥75% on lib code AND no secret patterns leak in test logs under varied input — XCUT-07 sentinel asserts this)
- The kernel's first observable promise holds: typed errors + structured logger + redaction work, and CI proves it on every push
- Downstream plans (02, 03, 04, 05, 06) can import `internal/core/errors` and `internal/core/log` without further changes
- D-01..D-04 are NOT addressed by this plan (CLI surface lives in Plan 05) but the scaffold (`cmd/reconftw/.gitkeep`) is in place
- Phase 4 readiness checkpoint (partial): typed errors + logger + redaction subsystems exist and are CI-gated — Phase 4 Subdomains module can return `*errors.ToolError` from `subfinder` failures starting day 1
</success_criteria>

<output>
Create `.planning/phases/03-foundation-kernel/03-01-SUMMARY.md` when done.

Summary MUST capture:
- Files created (all `internal/core/errors/*.go`, `internal/core/log/*.go`, CI yaml, `.golangci.yml`, Makefile; `spike/python/` deleted; `internal/core/` skeleton with `.gitkeep` files)
- Coverage percentage on `internal/core/errors/` and `internal/core/log/`
- XCUT-07 sentinel test command + last passing run
- CI workflow run URL / commit SHA where it first ran green (or local `make ci` output if pre-push)
- Open items for Plan 02: `log.New(*Config, *Redactor)` config type is a placeholder; Plan 02 swaps to `*config.Config` when koanf loader lands (no signature change beyond pointer type)
- Open items for Plan 05: `cmd/reconftw/main.go` not yet created; binary-size CI job is gated until then
</output>
</content>
</invoke>
