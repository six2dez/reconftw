---
phase: 01-language-adr-spike
plan: 05
type: execute
wave: 4
depends_on:
  - 01-04
files_modified:
  - .planning/decisions/0001-language.md
  - .planning/research/STACK.md
  - .planning/research/ARCHITECTURE.md
  - .planning/research/SUMMARY.md
  - .planning/research/PITFALLS.md
  - .planning/STATE.md
  - .planning/PROJECT.md
autonomous: false  # contains the ADR-commit task that requires user approval per CONTEXT.md "NO commits by planner or future executors without explicit user approval per turn"
requirements_addressed:
  - DEC-01
  - DEC-05
tags:
  - adr
  - sign-off
  - research-collapse
must_haves:
  truths:
    - "ADR file `.planning/decisions/0001-language.md` has Status: Accepted (NOT Proposed); Date is filled; Signed By section has six2dez identifier; spike git SHAs are committed values (not TBD placeholders)"
    - "The ADR commit is on branch `rewrite/v2` (the v2.0 working branch per STATE.md) — verifiable via `git log --oneline -- .planning/decisions/0001-language.md`"
    - "`.planning/research/STACK.md` contains ZERO mentions of the LOSING language (verified via grep against losing lang's keywords)"
    - "`.planning/research/ARCHITECTURE.md` contains ZERO dual-tracked sections; ONLY the chosen language's idiomatic patterns remain"
    - "`.planning/research/SUMMARY.md` §Stack Snapshot and §Architecture: Top Cross-Cutting Patterns reference ONLY the chosen language"
    - "`.planning/research/PITFALLS.md` contains the chosen language's pitfalls + cross-cutting ones; losing-language-specific pitfalls are deleted"
    - "Post-collapse grep validation passes: `grep -i \"<loser keywords>\" .planning/research/*.md` returns ZERO matches (except in explicit §Historical or §References sections, which the worksheet should also not have unless explicitly placed)"
    - "`.planning/STATE.md` shows Phase 1 complete; `.planning/PROJECT.md` Key Decisions table includes the language pick"
  artifacts:
    - path: ".planning/decisions/0001-language.md"
      provides: "Signed ADR (Status: Accepted) — DEC-01 satisfied; THE source of historical truth for the language choice per D-03"
      contains: "Status: Accepted"
    - path: ".planning/research/STACK.md"
      provides: "Collapsed to chosen language only — DEC-05 satisfied (loser language section removed)"
      contains: "Stack (locked in ADR 0001)"
    - path: ".planning/research/ARCHITECTURE.md"
      provides: "Collapsed to chosen language patterns only — DEC-05 partial satisfaction"
      contains: ""
    - path: ".planning/research/SUMMARY.md"
      provides: "Stack Snapshot rewritten to reference chosen language only"
      contains: ""
    - path: ".planning/research/PITFALLS.md"
      provides: "Losing-language-specific pitfalls removed; cross-cutting pitfalls retained"
      contains: ""
  key_links:
    - from: ".planning/decisions/0001-language.md"
      to: "spike/comparison.json (committed)"
      via: "ADR References section cites the committed spike artifacts"
      pattern: "spike/comparison.json"
    - from: ".planning/STATE.md"
      to: ".planning/decisions/0001-language.md"
      via: "STATE.md Decisions section records the language pick as v2.0 milestone decision"
      pattern: ".planning/decisions/0001-language.md"
---

<objective>
Execute the D-02 solo same-day sign-off ceremony: flip ADR Status to Accepted, fill the signed date + SHAs, commit the ADR (and spike artifacts the ADR references) to branch `rewrite/v2`. THEN apply the D-03 post-ADR collapse: edit STACK.md, ARCHITECTURE.md, SUMMARY.md, PITFALLS.md in place to remove the LOSING language's content. Verify the collapse via grep gates. Finally update STATE.md and PROJECT.md to reflect Phase 1 complete + language pick.

Purpose: Without this plan, the ADR is a draft in git history but the milestone hasn't formally accepted it; the dual-tracked research files would continue to suggest both languages are alive options (they're not — the ADR closed that branch). DEC-01 and DEC-05 are the two REQ-IDs this plan exclusively closes.

Output:
- `.planning/decisions/0001-language.md` — Status: Accepted, signed, committed
- `.planning/research/STACK.md` — collapsed to chosen lang
- `.planning/research/ARCHITECTURE.md` — collapsed to chosen lang
- `.planning/research/SUMMARY.md` — Stack Snapshot rewritten to chosen lang
- `.planning/research/PITFALLS.md` — loser-specific pitfalls removed
- `.planning/STATE.md` — Phase 1 marked complete
- `.planning/PROJECT.md` — Key Decisions table updated with language pick
- A commit on `rewrite/v2` containing the ADR + spike artifacts (created in Task 1)
- (Optionally) a second commit on `rewrite/v2` containing the research-file collapse + STATE/PROJECT updates (created in Task 4)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/phases/01-language-adr-spike/01-CONTEXT.md
@.planning/phases/01-language-adr-spike/01-RESEARCH.md
@.planning/phases/01-language-adr-spike/01-01-SUMMARY.md
@.planning/phases/01-language-adr-spike/01-02-SUMMARY.md
@.planning/phases/01-language-adr-spike/01-03-SUMMARY.md
@.planning/phases/01-language-adr-spike/01-04-SUMMARY.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/SUMMARY.md
@.planning/research/PITFALLS.md
@.planning/decisions/0001-language.md
@CLAUDE.md
@spike/measurement-worksheet.md

<interfaces>
<!-- Inputs from Plan 01-04: -->
<!-- - .planning/decisions/0001-language.md  (Status: Proposed → flipped to Accepted by this plan) -->
<!-- - spike/comparison.json  (referenced from ADR; staged but not committed by 01-04) -->
<!-- - spike/measurement-worksheet.md  (referenced from ADR; staged but not committed by 01-04) -->

<!-- D-03 collapse references (RESEARCH.md §4.3.1-§4.3.4): -->
<!-- STACK.md has §0 (Language Runtime), §1-§14 (parallel "### Go" / "### Python" subsections), §Summary, §What NOT to Use, §Risks, §Sources -->
<!-- ARCHITECTURE.md has §1 System Overview, §2-§15 parallel "### 2a Go" / "### 2b Python" / "### 2c Comparison" -->
<!-- SUMMARY.md has §Stack Snapshot (parallel tables), §Library blacklist, §Architecture: Top Cross-Cutting Patterns, §Build Order, §Open Risks -->
<!-- PITFALLS.md has cross-cutting pitfalls + lang-specific ones (e.g., §2.4 Python await in loop; §6.1 cgo) -->

<!-- D-03 validation greps (RESEARCH.md §4.3.6, refined per B5 cross-AI review fix): -->
<!-- Canonical regex used by Task 3 verify: -->
<!-- If Go won:    grep -riE "python|asyncio|pydantic|typer|uv pip|tomllib|structlog" .planning/research/ | grep -v "0001-language" | grep -v "## References|## Sources|## Historical"  → expect EXACTLY 0 matches -->
<!-- If Python won: grep -riE "golang|cobra|koanf|errgroup|setpgid|slog|goroutine" .planning/research/ | grep -v "0001-language" | grep -v "## References|## Sources|## Historical"  → expect EXACTLY 0 matches -->
<!-- Pre-fix tolerance of "<5" accepted real leakage; B5 tightens to "==0" because the grep -v already excludes legitimate citation sections. -->

<!-- CRITICAL: This plan is autonomous: false because Task 1 ("commit ADR") MUST request explicit user approval before running git commit. -->
<!-- Per CONTEXT.md/planning_context.specifics: "NO commits by the planner or future executors without explicit user approval per turn." -->
<!-- D-02 says "same-day sign-off" — the SIGNING + COMMIT happens on the same day. The COMMIT itself requires user approval. -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 0: Pre-commit checkpoint — confirm ADR draft + worksheet + spike artifacts are correct before signing</name>
  <what-built>
    Plans 01-01 through 01-04 produced:
    - Spike harness (Plan 01-01) — directory tree, compare.sh, mock_stubborn_tool.sh, README
    - Go spike implementation (Plan 01-02) — bin/spike binary + .killtree_result + .xplat_ordinal
    - Python spike implementation (Plan 01-03) — dist/spike binary + .killtree_result + .xplat_ordinal + .venv
    - Comparison + ADR draft (Plan 01-04) — comparison.json + measurement-worksheet.md + decisions/0001-language.md (Status: Proposed)

    This plan executes the **same-day sign-off ceremony per D-02**: flip ADR to Accepted, fill SHAs, commit on rewrite/v2. THEN apply D-03 research collapse.
  </what-built>
  <how-to-verify>
    Before this checkpoint resolves, the user verifies the ADR draft is right:

    1. `cat .planning/decisions/0001-language.md` — read the entire ADR
       - Status MUST be `Proposed` (Plan 01-04 left it there; this plan flips to Accepted)
       - Decision section names the chosen language (Go or Python — NOT a placeholder)
       - Measurement table has all 7 rows (M1-M6 + MCP) with real numbers
       - Verdict statement matches EXACTLY one of:
         a. "Metrics show clear winner (X by Y%): tie-breaker NOT invoked, choose X"
         b. "Metrics within 25% noise band: tie-breaker INVOKED per DEC-04, choose Go"
       - Killer-feature gates explicitly evaluated (both passed OR one disqualified)
       - Consequences section has bullets specific to chosen lang
       - References section cites all RESEARCH.md, REQUIREMENTS.md, ROADMAP.md, spike/*.json, spike/measurement-worksheet.md

    2. `cat spike/measurement-worksheet.md` — read the scoring trace
       - Per-metric ratios computed correctly (max/min)
       - 25% noise band rule applied per metric (ratio < 1.25 = TIED)
       - Killer-feature override evaluation visible
       - Aggregate count matches the ADR Verdict

    3. `jq . spike/comparison.json` — confirm valid JSON, all 12 metric fields populated

    4. **Decide:** Is the language choice the right one given the data?
       - If YES → "approved" → Plan 01-05 proceeds with Task 1 (flip Status to Accepted + commit)
       - If NO → "rejected" + reasoning → Plan 01-05 stops; revise ADR; re-run checkpoint
       - If "needs revision" → describe specific changes → Plan 01-04 revises ADR; re-run checkpoint

    Verify NO git commits have been made yet by Plans 01-01 through 01-04:
    `git log --oneline -- .planning/decisions/ spike/` (should show no commits in the spike/ tree from prior plans)
  </how-to-verify>
  <resume-signal>
    User types "approved" → Plan 01-05 Task 1 proceeds with sign-off.
    OR user types "rejected: <reason>" → halt; revise ADR; re-run this checkpoint.
    OR user types "needs revision: <details>" → halt; cycle back to Plan 01-04 with feedback.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 1: Sign + finalize ADR — flip Status to Accepted, fill SHAs (NO commit yet)</name>
  <read_first>
    - .planning/decisions/0001-language.md (current draft from Plan 01-04)
    - .planning/phases/01-language-adr-spike/01-CONTEXT.md §decisions D-02 (Solo same-day sign-off)
    - .planning/phases/01-language-adr-spike/01-RESEARCH.md §4.2 (What "Signed" means in this project)
    - CLAUDE.md (commit discipline: NO co-author from Claude; user commits)
    - planning_context.specifics: "NO commits by the planner or future executors without explicit user approval per turn"
  </read_first>
  <action>
**CRITICAL — Approval gate per CONTEXT.md "NO commits...without explicit user approval per turn":** Before running `git commit`, this task MUST request EXPLICIT user approval. The Task 0 checkpoint provides the data-correctness approval; this task's git operations need a SECOND approval before executing.

**Executor protocol:**

1. **Sign + finalize ADR file** (no git operations yet):
   - Edit `.planning/decisions/0001-language.md`:
     - Change `Status: Proposed` to `Status: Accepted`
     - Set the metadata `Date:` field to today's date (ISO format YYYY-MM-DD)
     - In `## Signed` section:
       - `Signed by: six2dez (single maintainer, project owner)` (already there from template)
       - `Date: YYYY-MM-DD` — today
       - `Git SHA (this ADR): <leave as <will be filled by git after commit> — git fills it post-commit; we cannot pre-determine without committing first>`
       - `Git SHA (spike final): <get from `git log -n1 --oneline -- spike/` before commit>` — record the SHA of the most recent commit affecting any spike/ file IF spike has commits (probably none, since Plans 01-01 through 01-04 made NO commits per CONTEXT.md rule). If no spike commits yet: write `Git SHA (spike final): <will be filled at commit-time> (spike artifacts committed alongside this ADR in same commit)`
   - Delete the footer note `*This ADR is currently in Status: Proposed...*` (it referenced a state that no longer applies)
   - Atomic-write to the same file (tempfile + rename habit; matters here because this is the canonical foundational decision file)

2. **Stage the files** (still no commit yet):
   ```bash
   git add .planning/decisions/0001-language.md
   git add spike/comparison.json
   git add spike/measurement-worksheet.md
   git add spike/README.md
   git add spike/corpus/
   git add spike/baselines/
   git add spike/compare.sh
   git add spike/.spike_sessions.log
   # Also stage the spike code from Plans 01-02 and 01-03:
   git add spike/go/go.mod spike/go/go.sum spike/go/Makefile
   git add spike/go/cmd spike/go/internal spike/go/tests
   git add spike/go/out/.killtree_result spike/go/out/.xplat_ordinal spike/go/out/.xplat_notes
   git add spike/python/pyproject.toml spike/python/uv.lock spike/python/Makefile
   git add spike/python/src spike/python/tests
   git add spike/python/out/.killtree_result spike/python/out/.xplat_ordinal spike/python/out/.xplat_notes
   # Do NOT add: spike/go/bin/, spike/python/.venv/, spike/python/dist/, spike/python/build/, spike/*/out/run.log, spike/*/out/run.err
   # These are gitignored per Plan 01-01 Task 1.
   git status
   ```
   `git status` shows what's about to be committed. **STOP and present this output to the user.**

3. **Request explicit approval before commit**:
   Print a message to the user:
   ```
   Ready to commit on branch rewrite/v2:
       docs(phase-01): sign ADR 0001 — language choice (<chosen-lang>) per Phase 1 spike

   Files staged:
       <output of git diff --cached --stat>

   Per D-02 (solo same-day sign-off) and CONTEXT.md "no commits without explicit user approval per turn",
   I'm requesting your approval to run:

       git commit -m "..."

   Please type "approved" to proceed, or describe any changes needed.
   ```

   If user is running this via `/gsd:execute-phase` in interactive mode, the orchestrator will pause here for input. If running in yolo/auto mode, this task is NOT auto-resumable (it's `autonomous: false`); the executor MUST halt and surface the diff for review.

4. **On approval — run commit**:
   ```bash
   git commit -m "$(cat <<'EOF'
   docs(phase-01): sign ADR 0001 — language choice (<chosen-lang>) per Phase 1 spike

   - .planning/decisions/0001-language.md: Status: Accepted, signed by six2dez
   - Measurement table M1-M6 + MCP row populated from spike/comparison.json
   - Verdict: <clear-winner | noise-band tie-breaker invoked> per RESEARCH.md §2.2
   - Includes spike/ artifacts (Go + Python PoCs, comparison.json, worksheet)
   - Per D-02: solo same-day sign-off (no community pre-review gate)

   Refs: DEC-01, DEC-02, DEC-03, DEC-04
   EOF
   )"
   ```
   (NO co-author per CLAUDE.md / user instructions. The user is committing under their own name.)

5. **Verify commit landed**:
   ```bash
   git log -n1 --oneline -- .planning/decisions/0001-language.md
   ```
   Should show the new commit SHA. Capture it; manually update the ADR's `Git SHA (this ADR):` field if the user wants the SHA recorded inside the file itself (this would require an amend or follow-up commit — RESEARCH.md §4.2 mentions this is OPTIONAL; `git log -- .planning/decisions/0001-language.md` is sufficient for audit since the SHA is in git history).

   If the executor opts NOT to amend (recommended — amends are awkward and the SHA is git-trackable anyway): leave the ADR's SHA field as `<see git log -- .planning/decisions/0001-language.md>` for posterity.

**If the user does NOT approve commit**: this task halts. Do NOT proceed to Task 2 (research collapse). The ADR remains in `Status: Accepted` LOCALLY (uncommitted); the user decides whether to revert to Proposed, edit further, or eventually approve. Phase 1 remains open.
  </action>
  <verify>
    <automated>git log -n1 --oneline -- .planning/decisions/0001-language.md 2>/dev/null | tee /tmp/spike_adr_commit.txt && test -s /tmp/spike_adr_commit.txt && grep -q "Status: Accepted" .planning/decisions/0001-language.md && (! grep -q "Status: Proposed" .planning/decisions/0001-language.md) && grep -qE "^\* Date: 20[0-9]{2}-[0-9]{2}-[0-9]{2}$" .planning/decisions/0001-language.md && (git log -n1 --pretty=%B -- .planning/decisions/0001-language.md 2>/dev/null | grep -qiE "sign|ADR|language") && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - User explicit approval received before `git commit` ran
    - `.planning/decisions/0001-language.md` Status field is `Accepted` (NOT Proposed)
    - `.planning/decisions/0001-language.md` Date is set to today's date in ISO format
    - `.planning/decisions/0001-language.md` is in git history (`git log -- .planning/decisions/0001-language.md` shows at least one commit)
    - Commit is on branch `rewrite/v2` (verifiable via `git rev-parse --abbrev-ref HEAD` at time of commit)
    - Commit message mentions ADR 0001 or language choice
    - Spike artifacts (comparison.json, measurement-worksheet.md, mock_stubborn_tool.sh, etc.) are also in the same commit OR in a prior commit (per CONTEXT.md rule, prior commits would NOT exist — so they're in the same commit as the ADR)
    - NO Claude co-author footer in the commit (per CLAUDE.md / user MEMORY)
  </acceptance_criteria>
  <done>ADR file finalized in working tree (Status: Accepted, signed date filled). NOT YET committed — Task 1.5 (W4 fix per cross-AI review) gates commit on per-commit user approval before Task 2 commits.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1.5: Per-commit approval checkpoint (W4 fix — explicit per-turn approval before ADR commit)</name>
  <what-built>
    Task 1 has finalized `.planning/decisions/0001-language.md` (Status: Accepted, today's date) and STAGED the following files:
    - .planning/decisions/0001-language.md
    - spike/comparison.json + spike/measurement-worksheet.md + spike/.rss_measurements.log
    - spike/README.md, spike/compare.sh, spike/corpus/*, spike/baselines/*
    - spike/.spike_sessions.log
    - spike/go/go.mod, go.sum, Makefile, cmd/, internal/, tests/, out/.killtree_result, out/.xplat_ordinal, out/.xplat_notes
    - spike/python/pyproject.toml, uv.lock, Makefile, src/, tests/, out/.killtree_result, out/.xplat_ordinal, out/.xplat_notes
    (No spike/go/bin/, no spike/python/.venv/, no spike/python/dist/, no spike/python/build/ — gitignored.)

    NO `git commit` has run yet. This checkpoint converts the plan-level `autonomous: false` signal into a discrete per-commit gate per MEMORY.md feedback: "Never run git commit without explicit user approval in the current turn."
  </what-built>
  <how-to-verify>
    The user reviews what's about to be committed BEFORE the commit fires:

    1. `git status` — shows staged files
    2. `git diff --cached --stat` — shows the changed-files summary
    3. `git diff --cached -- .planning/decisions/0001-language.md` — shows the exact ADR content that will land in git history

    Proposed commit message (will be passed to Task 2):
    ```
    docs(phase-01): sign ADR 0001 — language choice (<chosen-lang>) per Phase 1 spike

    - .planning/decisions/0001-language.md: Status: Accepted, signed by six2dez
    - Measurement table M1-M6 + MCP row populated from spike/comparison.json
    - Verdict: <clear-winner | noise-band tie-breaker invoked | killer-feature override> per RESEARCH.md §2.2
    - Includes spike/ artifacts (Go + Python PoCs, comparison.json, worksheet)
    - Per D-02: solo same-day sign-off (no community pre-review gate)

    Refs: DEC-01, DEC-02, DEC-03, DEC-04
    ```

    No Claude co-author footer (per CLAUDE.md / user MEMORY.md feedback_commits.md).

    Decide:
    - "approved" → Task 2 runs `git commit` immediately.
    - "rejected: <reason>" → halt; surface diff for user to amend manually.
    - "needs revision: <details>" → halt; user edits files; re-run this checkpoint.
  </how-to-verify>
  <resume-signal>
    User types "approved" → Task 2 (the actual `git commit`) proceeds.
    OR user types "rejected" / "needs revision" → halt; per CONTEXT.md "NO commits without explicit user approval per turn", no commit fires.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Execute the ADR commit (Task 1.5 must be approved first)</name>
  <read_first>
    - .planning/decisions/0001-language.md (Task 1 — final form, Status: Accepted)
    - Output of `git status` and `git diff --cached --stat` (Task 1.5 surfaced these)
    - CLAUDE.md (commit discipline: NO co-author from Claude; user commits)
    - .claude/projects/-Users-six2dez-Tools-reconftw/memory/feedback_commits.md (NO Claude co-author footers)
  </read_first>
  <action>
**Pre-condition:** Task 1.5 must have resolved as `approved`. If not, this task does not run.

Execute the commit:
```bash
git commit -m "$(cat <<'EOF'
docs(phase-01): sign ADR 0001 — language choice (<chosen-lang>) per Phase 1 spike

- .planning/decisions/0001-language.md: Status: Accepted, signed by six2dez
- Measurement table M1-M6 + MCP row populated from spike/comparison.json
- Verdict: <clear-winner | noise-band tie-breaker invoked | killer-feature override> per RESEARCH.md §2.2
- Includes spike/ artifacts (Go + Python PoCs, comparison.json, worksheet)
- Per D-02: solo same-day sign-off (no community pre-review gate)

Refs: DEC-01, DEC-02, DEC-03, DEC-04
EOF
)"
```

NO Claude co-author footer (per MEMORY.md feedback_commits.md). The user commits under their own name; the heredoc body contains the commit message body only.

Verify commit landed:
```bash
git log -n1 --oneline -- .planning/decisions/0001-language.md
```

If `git commit` exits non-zero due to a pre-commit hook (CLAUDE.md disallows `--no-verify`): fix the underlying issue, re-stage, and create a NEW commit (NOT `--amend`). Re-run Task 1.5 first to re-confirm user approval for the new commit.
  </action>
  <verify>
    <automated>git log -n1 --oneline -- .planning/decisions/0001-language.md 2>/dev/null | tee /tmp/spike_adr_commit.txt && test -s /tmp/spike_adr_commit.txt && grep -q "Status: Accepted" .planning/decisions/0001-language.md && (! grep -q "Status: Proposed" .planning/decisions/0001-language.md) && grep -qE "^\* Date: 20[0-9]{2}-[0-9]{2}-[0-9]{2}$" .planning/decisions/0001-language.md && (git log -n1 --pretty=%B -- .planning/decisions/0001-language.md 2>/dev/null | grep -qiE "sign|ADR|language") && (! git log -n1 --pretty=%B -- .planning/decisions/0001-language.md 2>/dev/null | grep -qi "claude") && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Task 1.5 resolved as `approved` BEFORE this task ran (W4 fix per cross-AI review)
    - `git commit` exited 0; commit landed on branch `rewrite/v2`
    - Commit message references ADR 0001 / language / sign
    - NO Claude co-author footer in commit message (per MEMORY.md)
    - If pre-commit hook failed: new commit created (NOT `--amend`); Task 1.5 re-run for re-approval
  </acceptance_criteria>
  <done>ADR signed AND committed via per-commit user-approval gate; DEC-01 satisfied; ready for Task 3 (research collapse).</done>
</task>

<task type="auto">
  <name>Task 3: D-03 research-file collapse — edit STACK.md, ARCHITECTURE.md, SUMMARY.md, PITFALLS.md to remove losing language</name>
  <read_first>
    - .planning/decisions/0001-language.md (Task 1 — to know which language won)
    - .planning/research/STACK.md (full file — collapse target)
    - .planning/research/ARCHITECTURE.md (full file — collapse target)
    - .planning/research/SUMMARY.md (full file — collapse target)
    - .planning/research/PITFALLS.md (full file — collapse target)
    - .planning/phases/01-language-adr-spike/01-RESEARCH.md §4.3 (Post-ADR Collapse Checklist — concrete file-by-file edits)
    - .planning/phases/01-language-adr-spike/01-CONTEXT.md §decisions D-03 (rationale: "Clean cut prevents future confusion")
  </read_first>
  <action>
Apply RESEARCH.md §4.3 collapse procedure file-by-file. **Determine chosen language from `Status: Accepted` ADR file's Decision section** (`grep -E "^We will use \*\*(Go|Python)\*\*" .planning/decisions/0001-language.md` → extract Go or Python). This determines which language's sections stay vs delete.

### 2.1 — `.planning/research/STACK.md` (per RESEARCH.md §4.3.1)

For each section §0 through §14:
1. STACK.md has a parallel "### <N>a Go" / "### <N>b Python" structure for many sections (and §<N>c for comparison tables). DELETE the losing language's subsection entirely.
2. RENAME winner's subsection: e.g., if Go won, "### 1a Go" → "### 1 Go" (or just "### 1" — drop the suffix). Apply consistently.
3. Comparison subsections (§<N>c) — keep, but rewrite as single-lang where they were 3-way "Bash vs Go vs Python" tables; reduce to 2-way "Bash vs <chosen>".
4. §Summary Recommendation Matrix — delete loser's column; rename file-level "Stack Snapshot" to "Stack (locked by ADR 0001)".
5. §What NOT to Use (Both Languages) — keep "Both:" entries; delete entries prefixed with loser's name (e.g., if Python won, delete "Go: cmd.Run() — buffers GB...").
6. §Risks & Open Questions — delete risks specific to losing language.
7. §Sources — keep all (sources for chosen lang remain relevant). Optionally annotate loser-specific sources with "(historical — language not chosen per ADR 0001)" but DELETE-and-move-on is cleaner per D-03 "clean cut".

### 2.2 — `.planning/research/ARCHITECTURE.md` (per RESEARCH.md §4.3.2)

1. For sections §2 through §15: delete the loser's subsection (typically "### Xa Go" or "### Xb Python"). Rename winner's "### Xa <Lang>" to just "### X".
2. Comparison subsections (e.g., "### 2c Component boundaries") — rewrite to drop the 3-column "Bash vs Go vs Python" tables to 2-column "Bash vs <chosen>" tables.
3. §1 System Overview — keep the conceptual diagram (language-agnostic). Annotations under it: keep only winner's; delete loser's.

### 2.3 — `.planning/research/SUMMARY.md` (per RESEARCH.md §4.3.3)

1. §Stack Snapshot — has "### If Go wins the spike" and "### If Python wins the spike" sections. Delete loser's. Rename winner's to "### Stack (locked in ADR 0001)".
2. §Library blacklist — keep "Both:" entries; delete loser-prefixed entries.
3. §Architecture: Top Cross-Cutting Patterns — each pattern has "Go: ..." and "Python: ..." rows. Delete loser's rows.
4. §Build Order — Phase 0 / Phase 1 description references "Go OR Python". Change to chosen language.
5. §Open Risks — delete risks specific to loser.

### 2.4 — `.planning/research/PITFALLS.md` (per RESEARCH.md §4.3.4)

1. Walk every pitfall. Each has "Why it happens", "Prevention", "Bash analog" subsections that may reference both langs.
2. Lang-specific pitfalls (e.g., "Pitfall 2.4 Python `await` in a Loop") — if the LOSER's lang, DELETE entirely. If the WINNER's lang, KEEP. (Pitfall 6.1 cgo is Go-specific; Pitfall 2.4 is Python-specific.)
3. Cross-cutting pitfalls (most of them — kill-tree, atomic write, secret leak, etc.) — keep, edit to remove loser lang mentions.

### 2.5 — Implementation guidance

Use the Edit tool for surgical changes. Pattern for each file:
1. Read the full file (use `Read`)
2. Identify all sections to delete (grep for the loser lang's section markers)
3. Apply Edit calls one section at a time

**Atomic-write discipline:** For each file, write to a temp file first (e.g., `STACK.md.tmp`), then `mv` into place. Prevents half-written files if executor is interrupted mid-edit.

Order of edits within a file (recommended for predictability):
1. Top-of-file metadata (date, scope notes)
2. §0 / §1 (canonical introductions)
3. §2 through §N (parallel sections — biggest delete blocks)
4. §Summary / §Recommendation
5. §What NOT to Use
6. §Risks
7. §Sources

After all 4 files edited, the next task (2.5) runs grep validation.
  </action>
  <verify>
    <automated>test -f .planning/research/STACK.md && test -f .planning/research/ARCHITECTURE.md && test -f .planning/research/SUMMARY.md && test -f .planning/research/PITFALLS.md && CHOSEN_LANG=$(grep -oE "We will use \*\*(Go|Python)\*\*" .planning/decisions/0001-language.md | grep -oE "(Go|Python)" | head -1) && [ -n "$CHOSEN_LANG" ] && echo "Chosen: $CHOSEN_LANG"; if [ "$CHOSEN_LANG" = "Go" ]; then LOSER_HITS=$(grep -riE "asyncio\.TaskGroup|pydantic-settings|typer\." .planning/research/ 2>/dev/null | grep -v "0001-language" | grep -v "# Historical\|## Historical" | wc -l | awk '{print $1}'); else LOSER_HITS=$(grep -riE "errgroup\.SetLimit|spf13/cobra|knadh/koanf" .planning/research/ 2>/dev/null | grep -v "0001-language" | grep -v "# Historical\|## Historical" | wc -l | awk '{print $1}'); fi && echo "Loser keyword hits: $LOSER_HITS"; test "$LOSER_HITS" -lt 5 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All 4 research files edited in place: STACK.md, ARCHITECTURE.md, SUMMARY.md, PITFALLS.md
    - Each file's parallel "### Go / ### Python" subsections collapsed to single chosen-lang sections
    - Loser-specific pitfalls in PITFALLS.md deleted
    - Loser keyword count in `.planning/research/*.md` (excluding 0001-language.md ADR + explicit Historical sections) drops dramatically (target: <5 incidental mentions in cross-references)
    - SUMMARY.md §Stack Snapshot renamed to "Stack (locked in ADR 0001)"
    - SUMMARY.md §Build Order Phase 0 description names chosen lang explicitly
    - No file mid-edit / half-collapsed — each edit completed atomically before moving to next file
  </acceptance_criteria>
  <done>Research files collapsed; DEC-05 satisfied modulo final grep gate in next task.</done>
</task>

<task type="auto">
  <name>Task 4: Run final D-03 validation greps (RESEARCH.md §4.3.6); fix any unexpected matches</name>
  <read_first>
    - .planning/decisions/0001-language.md (chosen lang)
    - .planning/research/{STACK,ARCHITECTURE,SUMMARY,PITFALLS}.md (post-collapse state from Task 2)
    - .planning/phases/01-language-adr-spike/01-RESEARCH.md §4.3.6 (validation greps)
  </read_first>
  <action>
Run RESEARCH.md §4.3.6 validation greps. These are the canonical post-collapse assertions (B5 fix per cross-AI review: tolerance is ZERO matches, not "<5", because the grep -v already excludes legitimate `## References` / `## Sources` / `## Historical` lines).

Determine chosen lang from ADR: `CHOSEN_LANG=$(grep -oE "We will use \*\*(Go|Python)\*\*" .planning/decisions/0001-language.md | grep -oE "(Go|Python)" | head -1)`.

**Canonical grep regex per RESEARCH.md §4.3.6 + ADR ID exclusion** (the ADR file IS allowed to keep cross-references):

```bash
# If Go won (chose Go, removing Python references):
if [ "$CHOSEN_LANG" = "Go" ]; then
    LOSER_HITS=$(grep -riE "python|asyncio|pydantic|typer|uv pip|tomllib|structlog" .planning/research/ 2>/dev/null \
        | grep -v "0001-language" \
        | grep -v "## References\|## Sources\|## Historical" \
        | wc -l | tr -d ' ')
else
    # Python won (removing Go references):
    LOSER_HITS=$(grep -riE "golang|cobra|koanf|errgroup|setpgid|slog|goroutine" .planning/research/ 2>/dev/null \
        | grep -v "0001-language" \
        | grep -v "## References\|## Sources\|## Historical" \
        | wc -l | tr -d ' ')
fi
test "$LOSER_HITS" -eq 0  # ZERO matches expected (B5 canonical RESEARCH.md §4.3.6 standard)
```

The tolerance is **strictly zero**. Per RESEARCH.md §4.3.6 "Expected: zero matches except in `## References` / `## Historical` sections" — the `grep -v` above already filters those, so the residual must be exactly zero. Anything more = real leak from Task 2's collapse = D-03 NOT done.

**If `LOSER_HITS > 0`**:
1. Run the grep WITHOUT the `wc -l` to list each match (file:line:content)
2. For each: DELETE the line/section if it's a leaked dual-tracked reference, OR move the citation to a `## References` / `## Sources` section (then the filter excludes it).
3. Re-run the canonical grep until `LOSER_HITS -eq 0`.

**Document the grep results** in a file `spike/.collapse_validation.log` (gitignored — not committed; just a working note):
```
D-03 Collapse Validation — YYYY-MM-DD
Chosen lang: <Go|Python>
Canonical grep regex (RESEARCH.md §4.3.6 per B5 fix):
  - filter: -riE "<loser-keywords>" .planning/research/
  - exclude: 0001-language file + ## References/Sources/Historical sections
  - tolerance: exactly 0
Result: <LOSER_HITS>

If LOSER_HITS > 0: list each below and resolve before this task completes.
<leaks>

Final status: <PASS|FAIL>
```

This log file is for THIS executor session — it's not a committed artifact.
  </action>
  <verify>
    <automated>CHOSEN_LANG=$(grep -oE "We will use \*\*(Go|Python)\*\*" .planning/decisions/0001-language.md | grep -oE "(Go|Python)" | head -1); [ -n "$CHOSEN_LANG" ] || (echo "FAIL: could not determine chosen lang from ADR" && exit 1); if [ "$CHOSEN_LANG" = "Go" ]; then LOSER_HITS=$(grep -riE "python|asyncio|pydantic|typer|uv pip|tomllib|structlog" .planning/research/ 2>/dev/null | grep -v "0001-language" | grep -v "## References\|## Sources\|## Historical" | wc -l | tr -d ' '); else LOSER_HITS=$(grep -riE "golang|cobra|koanf|errgroup|setpgid|slog|goroutine" .planning/research/ 2>/dev/null | grep -v "0001-language" | grep -v "## References\|## Sources\|## Historical" | wc -l | tr -d ' '); fi && echo "Chosen: $CHOSEN_LANG; Canonical loser-keyword matches (B5 fix): $LOSER_HITS" && test "$LOSER_HITS" -eq 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Chosen lang determined unambiguously from ADR's `Decision` section
    - **B5 fix:** Canonical loser-keyword grep against `.planning/research/*.md` (with `grep -v "0001-language"` + `grep -v "## References\|## Sources\|## Historical"` filters) returns **exactly zero** matches. The filter already excludes legitimate citation sections, so the residual MUST be 0. The pre-fix tolerance of <5 accepted dual-tracking leakage; the canonical standard is strictly zero per RESEARCH.md §4.3.6.
    - If Go won: regex is `python|asyncio|pydantic|typer|uv pip|tomllib|structlog`. If Python won: regex is `golang|cobra|koanf|errgroup|setpgid|slog|goroutine`.
    - `spike/.collapse_validation.log` documents the grep results (working note, not committed)
    - If matches found in Task 2 output: they were resolved (deleted or moved to ## References/Sources/Historical sections) by additional Edit calls before this task's verify passed
  </acceptance_criteria>
  <done>D-03 collapse VALIDATED — losing-language content is permanently out of scope. DEC-05 fully satisfied.</done>
</task>

<task type="auto">
  <name>Task 5: Update STATE.md and PROJECT.md (no commit yet)</name>
  <read_first>
    - .planning/STATE.md (current state)
    - .planning/PROJECT.md (key decisions table)
    - .planning/decisions/0001-language.md (chosen lang)
    - .planning/phases/01-language-adr-spike/01-RESEARCH.md §Recommendations Plan 01-05
    - CLAUDE.md (commit discipline reminder)
  </read_first>
  <action>
Edit STATE.md and PROJECT.md per the housekeeping spec below. NO `git commit` runs in this task — the optional second commit is gated by Task 5.5 (W4 fix per cross-AI review).

1. **Update `.planning/STATE.md`**:

   Edit the YAML frontmatter:
   - `last_activity` → today's date with phrase: "Phase 1 ADR signed (language: <Chosen>)"
   - `progress.completed_phases` → 1 (was 0)
   - `progress.total_plans` → 5 (was 0 — Phase 1 had 5 plans)
   - `progress.completed_plans` → 5 (all of Phase 1's plans complete)
   - `progress.percent` → `1 / 12 * 100 = ~8` (rounded)
   - `stopped_at` → "Phase 1 complete — ADR signed and research collapsed"

   Edit the Current Position section:
   - Phase: "Phase 1 complete; Phase 2 (Architecture v2 Design) is next"
   - Plan: —
   - Status: Phase 1 closed; awaiting `/gsd:plan-phase 2`
   - Last activity: today — Phase 1 ADR signed, language = <Chosen>

   Edit the Performance Metrics section:
   - Update the v2.0 phase status table:
     ```
     | 1 | Language ADR & Spike | 5/5 | Complete | YYYY-MM-DD |
     ```

   Edit the Accumulated Context §Decisions section:
   - Append: "Phase 1 ADR (2026-MM-DD): Chose <Chosen> per ADR 0001 — measurements at spike/measurement-worksheet.md; tie-breaker <invoked | not invoked>"

   Edit Pending Todos:
   - Replace "run `/gsd:plan-phase 1`" with "run `/gsd:plan-phase 2` (Architecture v2 Design)"

   Edit Blockers/Concerns:
   - Remove the "Decisión de lenguaje pendiente" bullet (ADR signed — no longer blocking)
   - Append: "Spike code disposition pending Phase 2 plan: delete `spike/<loser>/` tree, archive in a tarball, or leave in git history only — Phase 2 planner decides."

   Edit Session Continuity:
   - `Last session: 2026-MM-DDTHH:MM:SS`
   - `Stopped at: Phase 1 complete — ADR signed`
   - `Resume file: .planning/decisions/0001-language.md`

2. **Update `.planning/PROJECT.md`**:

   Find the "Key Decisions" table (the rolling decision log). Append a new row:
   ```
   | YYYY-MM-DD | v2.0 Language | Chose **<Chosen>** for v2.0 rewrite | ADR 0001 (signed); measured 6-metric comparison vs other lang; tie-breaker <invoked / not invoked> |
   ```

   If PROJECT.md has a "Current Milestone v2.0" section with a sub-bullet about language: update it from "TBD pending Phase 1 ADR" to "Decided: <Chosen> (ADR 0001)".

3. **ROADMAP.md update** is OPTIONAL — Phase 1 status sync:
   Find Phase 1 section. Mark complete:
   - `- [ ] **Phase 1...** → `- [x] **Phase 1: Language ADR & Spike** (complete YYYY-MM-DD)`
   The same status table at top of ROADMAP.md gets the same `Complete` marking. If a plan completion ceremony script does this automatically post-commit (per `/gsd:complete-phase` or similar), DEFER to that.

4. **Optional: D-02 GitHub Release note**: EXPLICITLY DEFERRED to Phase 12 per CONTEXT.md "Deferred Ideas". This task does NOT publish a release note.

5. **Optional: spike/<loser>/ disposition**: EXPLICITLY DEFERRED to Phase 2 per CONTEXT.md `<specifics>`. STATE.md Blockers note (step 1 above) already captures this. This task does NOT delete spike/<loser>/.
  </action>
  <verify>
    <automated>grep -q "Phase 1 ADR signed" .planning/STATE.md && grep -qE "completed_phases: 1" .planning/STATE.md && grep -qE "completed_plans: 5" .planning/STATE.md && (grep -qE "Phase 1 .*Complete" .planning/STATE.md || grep -qE "1 \| Language ADR.*5/5 \| Complete" .planning/STATE.md) && CHOSEN_LANG=$(grep -oE "We will use \*\*(Go|Python)\*\*" .planning/decisions/0001-language.md | grep -oE "(Go|Python)" | head -1) && grep -q "$CHOSEN_LANG" .planning/PROJECT.md && grep -qE "ADR 0001|0001-language" .planning/PROJECT.md && (! grep -q "Decisión de lenguaje pendiente" .planning/STATE.md) && grep -q "Spike code disposition" .planning/STATE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/STATE.md` updated per spec; language pending bullet removed; spike-loser disposition note added
    - `.planning/PROJECT.md` Key Decisions table has new row recording the language pick + ADR 0001
    - NO `git commit` ran in this task (gated by Task 5.5)
    - NO GitHub Release note published (deferred to Phase 12 per CONTEXT.md)
    - NO `spike/<loser>/` deletion (deferred to Phase 2 per CONTEXT.md)
  </acceptance_criteria>
  <done>STATE.md + PROJECT.md edited in working tree; ready for Task 5.5 (per-commit approval gate) → Task 6 (optional second commit).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5.5: Per-commit approval checkpoint for the OPTIONAL second commit (W4 fix)</name>
  <what-built>
    Task 5 has edited `.planning/STATE.md` and `.planning/PROJECT.md` to reflect Phase 1 completion. Task 3 collapsed `.planning/research/STACK.md`, ARCHITECTURE.md, SUMMARY.md, PITFALLS.md to chosen-lang-only.

    The maintainer may choose to:
    (a) Commit these as a SEPARATE commit on `rewrite/v2` (clean per-concern history) — RECOMMENDED
    (b) Hold them locally as uncommitted working-tree changes (Phase 1 is still considered closed; Task 2's commit was the ADR sign-off)
    (c) Squash them into the Task 2 commit via amend — NOT recommended (CLAUDE.md commit discipline prefers new commits over amends)

    Per CONTEXT.md "NO commits without explicit user approval per turn" and MEMORY.md feedback_commits.md.
  </what-built>
  <how-to-verify>
    1. `git status` — shows the modified files (research/*.md, STATE.md, PROJECT.md)
    2. `git diff --stat` — shows changed-line counts
    3. `git diff -- .planning/research/STACK.md | head -60` — sample of collapse changes

    Proposed commit message (for option (a)):
    ```
    docs(phase-01): collapse research to <Chosen> per D-03; mark Phase 1 complete

    - .planning/research/STACK.md, ARCHITECTURE.md, SUMMARY.md, PITFALLS.md: removed losing-language sections per ADR 0001 D-03 cleanup
    - .planning/STATE.md: Phase 1 status = Complete (5/5 plans); milestone progress 1/12 phases
    - .planning/PROJECT.md: Key Decisions table updated with language pick
    - Validation grep (RESEARCH.md §4.3.6, B5 fix): zero unexpected loser-language matches

    Refs: DEC-05
    ```

    Decide:
    - "approved (commit)" → Task 6 runs the `git commit` immediately.
    - "approved (hold)" → no commit; user holds changes in working tree; Phase 1 still considered closed (Task 2's commit was the milestone-closing ADR sign-off).
    - "rejected" / "needs revision" → halt; user resolves; re-run this checkpoint.
  </how-to-verify>
  <resume-signal>
    User types "approved (commit)" → Task 6 runs `git commit`.
    OR "approved (hold)" → Task 6 skipped; Phase 1 still closes.
    OR "rejected" / "needs revision" → halt.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 6: Execute the OPTIONAL second commit (Task 5.5 must approve)</name>
  <read_first>
    - .planning/STATE.md, .planning/PROJECT.md (Task 5 — edited in working tree)
    - .planning/research/STACK.md, ARCHITECTURE.md, SUMMARY.md, PITFALLS.md (Task 3 — collapsed in working tree)
    - .planning/decisions/0001-language.md (Task 2 — already committed; this task does NOT touch it again)
    - CLAUDE.md (commit discipline reminder)
    - .claude/projects/-Users-six2dez-Tools-reconftw/memory/feedback_commits.md (NO Claude co-author footer)
  </read_first>
  <action>
**Pre-condition:** Task 5.5 must have resolved as `approved (commit)`. If it resolved as `approved (hold)`, this task is skipped (return success without committing — Phase 1 still closes). If it resolved as `rejected` / `needs revision`, this task does not run.

If approved-and-commit:

```bash
git add .planning/research/STACK.md .planning/research/ARCHITECTURE.md .planning/research/SUMMARY.md .planning/research/PITFALLS.md
git add .planning/STATE.md .planning/PROJECT.md
git status  # surface to user one more time

CHOSEN_LANG=$(grep -oE "We will use \*\*(Go|Python)\*\*" .planning/decisions/0001-language.md | grep -oE "(Go|Python)" | head -1)

git commit -m "$(cat <<EOF
docs(phase-01): collapse research to $CHOSEN_LANG per D-03; mark Phase 1 complete

- .planning/research/STACK.md, ARCHITECTURE.md, SUMMARY.md, PITFALLS.md: removed losing-language sections per ADR 0001 D-03 cleanup
- .planning/STATE.md: Phase 1 status = Complete (5/5 plans); milestone progress 1/12 phases
- .planning/PROJECT.md: Key Decisions table updated with language pick
- Validation grep (RESEARCH.md §4.3.6, B5 fix): zero unexpected loser-language matches

Refs: DEC-05
EOF
)"

git log -n1 --oneline
```

NO Claude co-author footer (per MEMORY.md feedback_commits.md). If pre-commit hook fails: fix the underlying issue, re-stage, create a NEW commit (NOT --amend); re-run Task 5.5 first.
  </action>
  <verify>
    <automated>if [ -f /tmp/spike_skip_t6 ]; then echo "Task 6 skipped per Task 5.5 'approved (hold)' resolution" && rm -f /tmp/spike_skip_t6 && echo OK; else git log -n1 --pretty=%B 2>/dev/null | grep -qE "collapse research|Phase 1 complete" && (! git log -n1 --pretty=%B 2>/dev/null | grep -qi "claude") && echo OK; fi</automated>
  </verify>
  <acceptance_criteria>
    - Task 5.5 approval received with explicit "commit" or "hold" disposition
    - If "approved (commit)": git commit landed on `rewrite/v2`; commit message references D-03 / Phase 1 complete / research collapse; NO Claude co-author footer
    - If "approved (hold)": NO commit ran; STATE.md / PROJECT.md / research/*.md remain as working-tree changes; Phase 1 still considered closed (Task 2's ADR commit was the milestone-closing event)
    - If pre-commit hook failed: new commit created (NOT --amend); Task 5.5 re-run for re-approval
  </acceptance_criteria>
  <done>Phase 1 closed: ADR signed (Task 2), research collapsed (Task 3), validation passed (Task 4), bookkeeping updated (Task 5), housekeeping commit gated by Task 5.5 (Task 6). v2.0 Phase 2 (Architecture v2 Design) can begin.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (Plan 01-05 scope)

| Boundary | Description |
|----------|-------------|
| Plan 01-05 → git commit operations | User-approval-gated; cannot bypass per CONTEXT.md rule |
| Plan 01-05 → .planning/research/*.md | Surgical edits removing losing-lang content; atomic-write discipline applied |
| Plan 01-05 → .planning/STATE.md, PROJECT.md | Milestone bookkeeping; failure modes are misreporting progress, not data loss |
| Plan 01-05 → .planning/decisions/0001-language.md | Foundational governance doc; integrity matters across milestone lifetime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05-SI-01 | Tampering | git commit happening without user approval | mitigate | Task 0 (checkpoint:human-verify, blocking) gates Task 1. Task 1 also explicitly halts before `git commit` and re-requests approval. Task 4's second commit ALSO requires explicit approval. Three approval gates total. |
| T-01-05-SI-02 | Tampering | ADR Status field flipped to Accepted but unsigned | mitigate | Task 1 verify gate asserts Date is filled (regex `^\* Date: 20[0-9]{2}-[0-9]{2}-[0-9]{2}$`) and Status is Accepted (NOT Proposed). Combined with the commit message gate, can't have flipped-without-signed state. |
| T-01-05-SI-03 | Tampering | research-collapse leaves losing-lang content (incomplete D-03) | mitigate | Task 3 explicit grep validation gate. Pre-defined maximum unexpected matches (5). Validation log file documents results. |
| T-01-05-SI-04 | Repudiation | future contributors disputing the ADR's measurements | mitigate | Task 1 commits the ADR alongside spike/comparison.json and spike/measurement-worksheet.md — full audit trail in git. The ADR's References section enumerates these explicitly. |
| T-01-05-SI-05 | Denial of Service | git commit pre-commit hook failures | mitigate | If hook fails, Task 1 creates a NEW commit fixing the issue (per CLAUDE.md commit discipline — never amend a hooks-failed commit). |
| T-01-05-SI-06 | Information Disclosure | secrets in committed spike artifacts | mitigate | spike/.gitignore excludes spike/corpus/.tokens.* (locked by Plan 01-01). git status pre-commit (Task 1 step 2) surfaces all staged files for user review BEFORE commit. Throwaway tokens (if any) live outside the repo by design. |
| T-01-05-SC | Tampering | n/a | n/a | No package installs in Plan 01-05 — pure document/state edits + git. |
</threat_model>

<verification>
**Plan-level verification (updated for W4 + B5 cross-AI review fixes):**
1. Task 0 checkpoint resolved as "approved" before Task 1 ran (data correctness)
2. Task 1.5 checkpoint resolved as "approved" before Task 2 ran (per-commit ADR approval — W4 fix)
3. ADR Status: Accepted; Date filled; committed in git history (Task 2)
4. `.planning/research/STACK.md`, ARCHITECTURE.md, SUMMARY.md, PITFALLS.md collapsed to chosen lang only (Task 3)
5. **B5 fix:** Canonical loser-keyword grep (with `grep -v "0001-language"` + `grep -v "## References|## Sources|## Historical"`) returns **exactly 0** matches (Task 4)
6. STATE.md shows Phase 1 complete (5/5 plans, 1/12 phases) (Task 5)
7. PROJECT.md Key Decisions table has new row with language pick (Task 5)
8. Task 5.5 checkpoint resolved with explicit "commit" or "hold" disposition before Task 6 ran (W4 fix)
9. NO Claude co-author footer in any commit (per CLAUDE.md / MEMORY.md feedback_commits.md)
10. Every `git commit` was gated by a discrete `checkpoint:human-verify` task (Task 1.5 → Task 2; Task 5.5 → Task 6)
</verification>

<success_criteria>
Plan 01-05 succeeds when (revised for W4 + B5 fixes):
- [ ] Task 0 checkpoint resolved (user approved data correctness)
- [ ] Task 1 signed the ADR in the working tree (Status: Accepted, Date filled) — NO commit yet
- [ ] **Task 1.5 (W4 fix) checkpoint resolved** with "approved" before Task 2 ran
- [ ] Task 2 committed the ADR to `rewrite/v2` with no Claude co-author footer
- [ ] Task 3 collapsed `.planning/research/{STACK,ARCHITECTURE,SUMMARY,PITFALLS}.md` to chosen lang only
- [ ] **Task 4 (B5 fix):** Canonical loser-keyword grep returns **exactly 0** matches (with `grep -v "0001-language"` + `grep -v "## References|## Sources|## Historical"` filters); per RESEARCH.md §4.3.6 standard
- [ ] Task 5 edited STATE.md (Phase 1 complete) and PROJECT.md (Key Decisions row) in working tree — NO commit yet
- [ ] **Task 5.5 (W4 fix) checkpoint resolved** with explicit "commit" or "hold" disposition before Task 6 ran
- [ ] Task 6: either committed the housekeeping changes (no Claude co-author footer) OR cleanly skipped per "hold" disposition
- [ ] Spike-loser disposition deferred to Phase 2 (documented in STATE.md Blockers)
- [ ] GitHub Release note deferred to Phase 12 (NOT done here)
- [ ] No `spike/<loser>/` deletion (deferred to Phase 2)
- [ ] NO Claude co-author footers in any commit (per MEMORY.md feedback_commits.md)
- [ ] Every `git commit` was preceded by a discrete `checkpoint:human-verify` task that the user approved in the current turn (W4)
- [ ] requirements_addressed (DEC-01 ADR signed and committed, DEC-05 research collapsed) satisfied
</success_criteria>

<output>
Create `.planning/phases/01-language-adr-spike/01-05-SUMMARY.md` when done. Summary must include:
- Chosen language (Go or Python) per the signed ADR
- ADR commit SHA on `rewrite/v2`
- Grep validation result (number of unexpected loser-keyword matches)
- STATE.md / PROJECT.md update confirmation
- Phase 1 milestone status: COMPLETE
- Open items deferred to later phases:
  - spike/<loser>/ disposition → Phase 2
  - GitHub Release note → Phase 12 (post-cutover)
- Whether the optional second commit (research-collapse housekeeping) was approved + landed
- Total Phase 1 calendar duration (from Plan 01-01 start → Plan 01-05 close)
</output>
