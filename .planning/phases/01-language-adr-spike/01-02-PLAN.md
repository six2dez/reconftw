---
phase: 01-language-adr-spike
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - spike/go/go.mod
  - spike/go/go.sum
  - spike/go/Makefile
  - spike/go/cmd/spike/main.go
  - spike/go/internal/passive/passive.go
  - spike/go/internal/passive/subfinder.go
  - spike/go/internal/passive/crt.go
  - spike/go/internal/passive/github.go
  - spike/go/internal/passive/gitlab.go
  - spike/go/internal/httpxprobe/httpx.go
  - spike/go/internal/output/atomic.go
  - spike/go/internal/proc/proc.go
  - spike/go/internal/ui/ui.go
  - spike/go/tests/atomic_test.go
  - spike/go/tests/killtree_test.go
  - spike/go/tests/passive_test.go
  - spike/go/tests/httpx_test.go
  - spike/go/tests/integration_test.go
  - spike/go/out/.killtree_result
  - spike/go/out/.xplat_ordinal
  - spike/.spike_sessions.log
autonomous: true
requirements_addressed:
  - DEC-02
  - DEC-03
tags:
  - spike
  - go
  - language-adr
must_haves:
  truths:
    - "Go spike compiles to a single binary at `spike/go/bin/spike` via `make -C spike/go build`"
    - "Go spike implements identical recon slice to Python spike (Plan 01-03): 4 passive sources fan-out + httpx probe + atomic JSONL writes"
    - "Go spike's kill-tree implementation: SIGINT to spike parent → ALL spawned subprocesses dead within 10s, asserted via TestKillTree_SyntheticMock against `spike/corpus/mock_stubborn_tool.sh`"
    - "Go spike's atomic write: SIGKILL between fsync and rename leaves original `subs.jsonl` intact, asserted via TestAtomicWrite_CrashSafe (fault-injection child process pattern)"
    - "All 5 tests pass: TestAtomicWrite_CrashSafe, TestKillTree_SyntheticMock, TestKillTree_RealTools (integration), TestPassive_FourSources, TestHTTPxProbe_Streaming"
    - "DEC-03 measurements recorded for Go: M1 (tokei LoC), M2 (`.spike_sessions.log` hours sum), M3 (stripped binary bytes), M4 (PASS|FAIL written to `spike/go/out/.killtree_result`), M5 (max RSS via /usr/bin/time -l), M6 (cross-platform ordinal 1|2|3 written to `spike/go/out/.xplat_ordinal`)"
    - "1-week calendar timebox honored per D-01: if spike incomplete at 1 week, executor STOPS and emits a SUMMARY noting what was built; remaining work is NOT carried over"
  artifacts:
    - path: "spike/go/cmd/spike/main.go"
      provides: "Entry point — cobra v1.9.1 CLI with --target flag (per OQ2 locked INCLUDE)"
      contains: "cobra"
    - path: "spike/go/internal/proc/proc.go"
      provides: "Subprocess wrapper with Setpgid + WaitDelay + Cancel handler"
      contains: "Setpgid"
    - path: "spike/go/internal/output/atomic.go"
      provides: "AtomicWriter — tempfile + fsync + rename + parent-dir-fsync"
      contains: "parentFD.Sync"
    - path: "spike/go/internal/passive/passive.go"
      provides: "Fan-out across 4 passive sources with errgroup.SetLimit"
      contains: "errgroup"
    - path: "spike/go/internal/httpxprobe/httpx.go"
      provides: "httpx subprocess streaming + bufio.Scanner NDJSON parser"
      contains: "bufio.Scanner"
    - path: "spike/go/tests/killtree_test.go"
      provides: "Synthetic mock kill-tree test (unit) + real-tool kill-tree (integration build tag)"
      contains: "TestKillTree_SyntheticMock"
    - path: "spike/go/tests/atomic_test.go"
      provides: "Atomic write fault-injection test"
      contains: "TestAtomicWrite_CrashSafe"
    - path: "spike/go/out/.killtree_result"
      provides: "M4 kill-tree result (PASS|FAIL) — read by compare.sh"
      contains: "PASS"
    - path: "spike/go/out/.xplat_ordinal"
      provides: "M6 cross-platform pain ordinal (1|2|3) — written by maintainer, read by compare.sh"
      contains: "1"
    - path: "spike/go/Makefile"
      provides: "build, test, integration-test, lint, clean targets — symmetric with Python Makefile in 01-03"
      contains: "build:"
  key_links:
    - from: "spike/go/cmd/spike/main.go"
      to: "spike/go/internal/passive"
      via: "import"
      pattern: "internal/passive"
    - from: "spike/go/internal/passive/*.go"
      to: "spike/go/internal/proc"
      via: "exec.CommandContext wrapped with Setpgid"
      pattern: "proc\\.Run|exec\\.CommandContext"
    - from: "spike/go/internal/passive/passive.go"
      to: "spike/go/internal/output/atomic.go"
      via: "writes merged + scope-filtered subdomains via AtomicWriter"
      pattern: "atomic\\.Write|AtomicWriter"
    - from: "spike/go/internal/httpxprobe/httpx.go"
      to: "spike/go/internal/output/atomic.go"
      via: "writes hosts.jsonl via AtomicWriter line-by-line buffer"
      pattern: "atomic\\.Write|AtomicWriter"
---

<objective>
Implement the Go spike PoC: identical recon slice to the Python spike (Plan 01-03), implementing the 4 passive sources (subfinder, crt, github-subdomains, gitlab-subdomains) + httpx probe + atomic JSONL writes + SIGINT kill-tree test. The spike measures DEC-03 metrics M1-M6 for Go; the apples-to-apples comparison happens in Plan 01-04.

Purpose: Without Go and Python both implementing the SAME slice with the SAME code-shape (per RESEARCH.md §3.1), DEC-03 measurements are meaningless. This plan delivers the Go half of the head-to-head; Plan 01-03 delivers Python.

Output:
- `spike/go/go.mod` pinning Go 1.24+ + cobra v1.9.1 + testify v1.10.x
- `spike/go/cmd/spike/main.go` — entry point
- `spike/go/internal/{passive,httpxprobe,output,proc,ui}/*.go` — feature packages
- `spike/go/tests/*.go` — 5 tests
- `spike/go/Makefile` — symmetric with Python's
- `spike/go/out/.killtree_result` — `PASS` or `FAIL` (M4)
- `spike/go/out/.xplat_ordinal` — `1`, `2`, or `3` (M6)
- `spike/.spike_sessions.log` — appended per-session timer entries
- Each file has a top-of-file comment: `// Spike PoC — DO NOT EVOLVE INTO PRODUCTION` (RESEARCH.md Pitfall 1 guardrail)

HARD STOP: 1 calendar week per D-01. If incomplete at the timebox, emit SUMMARY noting what built/tested and STOP.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-language-adr-spike/01-CONTEXT.md
@.planning/phases/01-language-adr-spike/01-RESEARCH.md
@.planning/phases/01-language-adr-spike/01-01-SUMMARY.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@CLAUDE.md
@modules/subdomains.sh
@lib/parallel.sh
@spike/README.md
@spike/compare.sh
@spike/corpus/mock_stubborn_tool.sh

<interfaces>
<!-- Bash v1 sub_passive() canonical pattern that this Go spike ports (lines 507-553 of modules/subdomains.sh). -->
<!-- And the bash kill-tree pattern (lib/parallel.sh:55) that this Go spike replaces via process-group kill. -->

From modules/subdomains.sh (sub_passive, lines 507-553):
- subfinder: `subfinder -all -d "$domain" -max-time "$SUBFINDER_ENUM_TIMEOUT" -silent -o .tmp/subfinder_psub.txt` → text/NDJSON capable (-json flag)
- crt (HTTP-bound to crt.sh): `crt -s -json -l "${CTR_LIMIT}" "$domain"` → JSON array
- github-subdomains: `github-subdomains -d "$domain" -t "$GITHUB_TOKENS" -o .tmp/github_subdomains_psub.txt` → text, requires token file
- gitlab-subdomains: `gitlab-subdomains -d "$domain" -t "$GITLAB_TOKENS"` → text, requires token file
- Skip pattern: `if [[ -s $GITHUB_TOKENS ]]; then ... else ...skip`
- Merge: `find .tmp -type f -iname "*_psub.txt" -exec cat {} + | sed "s/^\*\.//" | anew .tmp/passive_subs.txt | sed '/^$/d' | wc -l`

From lib/parallel.sh (_kill_tree, lines 44-66):
- Bash uses `pgrep -P` recursive walk because `set +m` disables job control (no pgid)
- Go spike inverts: uses Setpgid + `syscall.Kill(-pid, SIGTERM)` (process-group kill) — no job control constraint
- See RESEARCH.md §Pattern 1 for verbatim canonical Go pattern

Locked from spike/README.md (Plan 01-01):
- OQ1: NO config loader — `--target` only via cobra flag
- OQ2: Use cobra v1.9.1 (parity with Phase 3 lock)
- OQ4: Tokens at spike/corpus/.tokens.github / .tokens.gitlab (gitignored) — read env var GITHUB_TOKENS / GITLAB_TOKENS; if absent → skip source gracefully (log `[SKIP]` + continue)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Go module + cmd + internal packages + Makefile</name>
  <read_first>
    - spike/README.md (locked scope and Open Questions)
    - .planning/phases/01-language-adr-spike/01-RESEARCH.md §3.1 (directory structure for spike/go/)
    - .planning/phases/01-language-adr-spike/01-RESEARCH.md §Standard Stack §Go Spike (library versions)
    - .planning/research/STACK.md (Go column — locked libraries for production parity)
    - CLAUDE.md §Conventions (Go conventions — though spike code is throwaway, structure follows production layout)
  </read_first>
  <action>
Create the Go module and all package skeletons. Concrete steps:

1. **`spike/go/go.mod`** — initialize module with `go mod init github.com/six2dez/reconftw/spike/go` (the path is local-only; the spike does NOT need to publish). Pin Go version to `go 1.24` minimum (per RESEARCH.md §Standard Stack). Add dependencies via `go get` (executor runs the actual install in this task — these become entries in go.mod/go.sum):
   - `github.com/spf13/cobra@v1.9.1` (per RESEARCH.md OQ2 INCLUDE)
   - `github.com/stretchr/testify@latest` (for assertions; RESEARCH.md §Standard Stack)
   - `golang.org/x/sync/errgroup` (latest; for bounded concurrency)

2. **`spike/go/Makefile`** with targets symmetric to Python's (Plan 01-03). Required targets:
   - `build`: `go build -ldflags="-s -w" -trimpath -o bin/spike ./cmd/spike` (stripped binary for M3)
   - `test`: `go test ./tests/... -v -timeout 60s` (unit tests only)
   - `integration-test`: `go test ./tests/... -v -tags=integration -timeout 300s` (real-tool kill-tree)
   - `lint`: `go vet ./... && (command -v golangci-lint && golangci-lint run || echo "golangci-lint not installed, skipping")` — non-fatal lint per RESEARCH.md (spike is throwaway)
   - `clean`: `rm -rf bin/ out/`
   - `loc`: `(command -v tokei && tokei . --output json) || (command -v cloc && cloc .) || echo "tokei or cloc required"` (for M1 measurement)
   - `session-start`: `date +%s > .spike_session_start && echo "Session started at $$(date)"`
   - `session-end`: `[[ -f .spike_session_start ]] && echo "go $$(( ( $$(date +%s) - $$(cat .spike_session_start) ) / 60 ))" >> ../.spike_sessions.log && rm .spike_session_start && echo "Session logged"` (for M2 measurement)

3. Create package directories (empty `.gitkeep` placeholders if needed before code is written):
   - `spike/go/cmd/spike/`
   - `spike/go/internal/passive/`
   - `spike/go/internal/httpxprobe/`
   - `spike/go/internal/output/`
   - `spike/go/internal/proc/`
   - `spike/go/internal/ui/`
   - `spike/go/tests/`
   - `spike/go/bin/` (gitignored — populated by `make build`)
   - `spike/go/out/` (gitignored — populated by `./bin/spike` runs)

4. **Create stub Run() implementations in each `internal/<pkg>/<pkg>.go` file (B3 fix per cross-AI review).** Each stub matches the EXACT signature the package will eventually need — locking these signatures now means Task 1's `go build ./cmd/spike` verify gate passes BEFORE Tasks 2-3 implement the real logic. Without these stubs, main.go's imports break and Task 1 cannot complete.

   - **`spike/go/internal/passive/passive.go`** (stub):
     ```go
     // SPDX-License-Identifier: MIT
     // Spike PoC — DO NOT EVOLVE INTO PRODUCTION
     // Stub Run() for Task 1; real implementation in Task 3 (Plan 01-02 Task 3).
     package passive

     import (
         "context"
         "io"
     )

     func Run(ctx context.Context, domain string, out io.Writer) error { return nil }
     ```

   - **`spike/go/internal/httpxprobe/httpx.go`** (stub):
     ```go
     package httpxprobe

     import (
         "context"
         "io"
     )

     func Run(ctx context.Context, hosts []string, out io.Writer) error { return nil }
     ```

   - **`spike/go/internal/output/output.go`** (stub):
     ```go
     package output

     import "io"

     func AtomicWriter(path string) (io.WriteCloser, error) { return nil, nil }
     ```

   - **`spike/go/internal/proc/proc.go`** (stub — **B4 fix per cross-AI review**: `lineFn` returns `error` so callers can abort early):
     ```go
     package proc

     import "context"

     // lineFn signature is locked as `func([]byte) error` at the stub stage.
     // - For streaming-friendly sources (NDJSON like subfinder/httpx), the callback
     //   parses each line and may return non-nil to abort early.
     // - For buffered sources (JSON-array like crt), the callback simply appends to
     //   a buffer and always returns nil; source-specific parsing happens AFTER
     //   proc.Run returns. This dual-mode usage MUST be documented in Task 2 proc.go.
     func Run(ctx context.Context, name string, args []string, lineFn func([]byte) error) error {
         return nil
     }
     ```

   - **`spike/go/internal/ui/ui.go`** (stub — 4 exported funcs called by main.go):
     ```go
     package ui

     func Info(msg string)                {}
     func Skip(component, reason string)  {}
     func Warn(msg string)                {}
     func Err(msg string)                 {}
     ```

   These are throwaway stubs. Tasks 2-3 replace them with real implementations. The point is to satisfy `go build ./cmd/spike` at the Task 1 verify gate. **DO NOT skip this step** — without these stubs, the Task 1 verify gate fails because main.go references the packages, and Phase 1 cannot proceed.

5. **`spike/go/cmd/spike/main.go`** — minimum entrypoint:
   - Top comment: `// Spike PoC — DO NOT EVOLVE INTO PRODUCTION`
   - Uses `github.com/spf13/cobra` (locked)
   - Single command with `--target <domain>` required flag
   - On invocation: calls `passive.Run(ctx, target, "out/subs.jsonl")` then `httpxprobe.Run(ctx, "out/subs.jsonl", "out/hosts.jsonl")`
   - SIGINT/SIGTERM handler via `signal.NotifyContext(ctx, os.Interrupt, syscall.SIGTERM)` so ctx cancels the whole pipeline
   - Exit 0 on success, exit 1 on any error

Use named exports — package-level functions are `passive.Run`, `httpxprobe.Run`, `proc.Run`, `output.WriteJSONL`. This is the contract Tasks 2-4 implement against; locking it here prevents downstream "scavenger hunt" exploration.

Each created `.go` file MUST have this comment block as the first 5 lines:
```go
// SPDX-License-Identifier: MIT
// Spike PoC — DO NOT EVOLVE INTO PRODUCTION
// Source: .planning/phases/01-language-adr-spike/01-02-PLAN.md
// Locked scope: spike/README.md §Scope
// Pattern reference: .planning/phases/01-language-adr-spike/01-RESEARCH.md §<section>
```

Start the session timer at the top of executing Task 1: `cd spike/go && make session-start`. End it via `make session-end` ONLY at end of Task 4 (one timer entry per task is too granular; one per Task 1+2+3+4 sweep is the right size).
  </action>
  <verify>
    <automated>test -f spike/go/go.mod && grep -q "module github.com/six2dez/reconftw/spike/go" spike/go/go.mod && grep -q "github.com/spf13/cobra v1.9" spike/go/go.mod && grep -q "github.com/stretchr/testify" spike/go/go.mod && grep -q "golang.org/x/sync" spike/go/go.mod && test -f spike/go/Makefile && grep -q "^build:" spike/go/Makefile && grep -q "^test:" spike/go/Makefile && grep -q "^integration-test:" spike/go/Makefile && grep -q "^lint:" spike/go/Makefile && grep -q "^clean:" spike/go/Makefile && grep -q "^loc:" spike/go/Makefile && grep -q "^session-start:" spike/go/Makefile && grep -q "^session-end:" spike/go/Makefile && test -d spike/go/cmd/spike && test -d spike/go/internal/passive && test -d spike/go/internal/httpxprobe && test -d spike/go/internal/output && test -d spike/go/internal/proc && test -d spike/go/internal/ui && test -d spike/go/tests && test -f spike/go/cmd/spike/main.go && grep -q "Spike PoC — DO NOT EVOLVE INTO PRODUCTION" spike/go/cmd/spike/main.go && grep -q "cobra" spike/go/cmd/spike/main.go && grep -q "signal.NotifyContext" spike/go/cmd/spike/main.go && test -f spike/go/internal/passive/passive.go && grep -q "func Run" spike/go/internal/passive/passive.go && test -f spike/go/internal/httpxprobe/httpx.go && grep -q "func Run" spike/go/internal/httpxprobe/httpx.go && test -f spike/go/internal/output/output.go && grep -q "AtomicWriter" spike/go/internal/output/output.go && test -f spike/go/internal/proc/proc.go && grep -qE "lineFn func\(\[\]byte\) error" spike/go/internal/proc/proc.go && test -f spike/go/internal/ui/ui.go && (cd spike/go && go build ./cmd/spike) && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `spike/go/go.mod` exists, pins Go 1.24+, includes cobra v1.9.x + testify + errgroup
    - `spike/go/Makefile` has all 8 targets (build, test, integration-test, lint, clean, loc, session-start, session-end)
    - All `internal/` subdirectories exist
    - `cmd/spike/main.go` exists with throwaway comment + cobra setup + signal.NotifyContext handler
    - **B3 fix:** Stub `Run()` functions exist in `internal/passive/passive.go`, `internal/httpxprobe/httpx.go`, `internal/proc/proc.go`; stub `AtomicWriter()` in `internal/output/output.go`; stub `Info/Skip/Warn/Err` in `internal/ui/ui.go`. These satisfy main.go's imports and make `go build ./cmd/spike` pass at Task 1 verify gate.
    - **B4 fix:** `proc.Run` stub signature is `func Run(ctx context.Context, name string, args []string, lineFn func([]byte) error) error` — `lineFn` returns `error` to support both streaming and buffered subprocess patterns (locked at Task 1; Task 2 implements behind this contract).
    - `cd spike/go && go build ./cmd/spike` succeeds (stubs make the package symbols resolvable; Tasks 2-3 replace the stubs with real implementations behind the same exported signatures)
    - Each `.go` file has the 5-line top comment block
  </acceptance_criteria>
  <done>Go module scaffolded; `go build` succeeds against stub Run() functions; ready for Task 2 to implement subprocess wrapper + atomic writer.</done>
</task>

<task type="auto">
  <name>Task 2: Implement subprocess wrapper (proc) + AtomicWriter (output) + tests for both</name>
  <read_first>
    - spike/go/cmd/spike/main.go (Task 1 — see how `proc.Run` and `output.WriteJSONL` are imported and called)
    - .planning/phases/01-language-adr-spike/01-RESEARCH.md §Pattern 1 (Process-Group Subprocess Wrapper — verbatim Go example)
    - .planning/phases/01-language-adr-spike/01-RESEARCH.md §Pattern 2 (Atomic JSONL Write — verbatim Go example)
    - .planning/phases/01-language-adr-spike/01-RESEARCH.md §1.3.1 (TestKillTree_SyntheticMock shape)
    - .planning/research/PITFALLS.md §1.2 (process-group escape — top-impact pitfall) and §3.1 (atomic write — top-5)
    - lib/parallel.sh:44-66 (bash _kill_tree — historical context for what we're improving on)
    - spike/corpus/mock_stubborn_tool.sh (the synthetic mock that TestKillTree_SyntheticMock invokes)
  </read_first>
  <action>
Implement the two foundational primitives (subprocess + atomic write) AND their tests. These are the two patterns from PITFALLS §1.2 and §3.1 (top-5 pitfalls) — they are the entire reason the spike exists.

1. **`spike/go/internal/proc/proc.go`** — subprocess wrapper. Exact API (named export):
   ```go
   package proc

   // Run executes name+args under process-group isolation (Setpgid: true). On ctx cancel,
   // the entire process group receives SIGTERM, then SIGKILL after 5s WaitDelay grace.
   // stdout is streamed line-by-line through lineFn; nil lineFn means stdout is discarded.
   //
   // lineFn signature is `func([]byte) error` (B4 fix per cross-AI review):
   //   - For streaming-friendly sources (NDJSON like subfinder, httpx), callers parse
   //     each line and may return a non-nil error to abort the subprocess early.
   //   - For buffered sources (JSON-array like crt.sh), callers simply append the line
   //     to a `bytes.Buffer` and return nil; the source-specific package then parses
   //     the accumulated buffer AFTER proc.Run returns. This dual-mode usage is the
   //     reason lineFn returns error: it removes the ambiguity Task 3 crt.go would
   //     otherwise face (mid-implementation workaround for missing abort semantics).
   //
   // Returns the cmd's wait error (nil = clean exit).
   //
   // Source pattern: .planning/phases/01-language-adr-spike/01-RESEARCH.md §Pattern 1
   // Pitfall reference: .planning/research/PITFALLS.md §1.2 (process-group escape, top-impact)
   func Run(ctx context.Context, name string, args []string, lineFn func([]byte) error) error
   ```

   Implementation MUST follow RESEARCH.md §Pattern 1 verbatim:
   - `exec.CommandContext(ctx, name, args...)`
   - `cmd.SysProcAttr = &syscall.SysProcAttr{Setpgid: true}`
   - `cmd.WaitDelay = 5 * time.Second`
   - `cmd.Cancel = func() error { return syscall.Kill(-cmd.Process.Pid, syscall.SIGTERM) }` ← negative PID = process group
   - If `lineFn` provided: `cmd.StdoutPipe()` → `bufio.Scanner` with 1MiB initial / 10MiB max buffer (RESEARCH.md §Pattern 3, verbatim) → iterate `Scanner.Scan()` calling `if err := lineFn(scanner.Bytes()); err != nil { return err }` (B4: a non-nil error from lineFn aborts the stream)
   - On stderr: discard (out of scope for spike; spike's own ui package handles user-facing status)
   - Return `cmd.Wait()` error

2. **`spike/go/internal/output/atomic.go`** — atomic writer. Exact API:
   ```go
   package output

   // WriteJSONL writes lines (each already serialized JSON) to target atomically.
   // 4-step pattern: tempfile (same dir as target) + fsync + rename + parent-dir fsync.
   // The parent-dir fsync is the often-forgotten step (most implementations stop at 3).
   //
   // Source: .planning/phases/01-language-adr-spike/01-RESEARCH.md §Pattern 2
   // Pitfall reference: .planning/research/PITFALLS.md §3.1 (atomic write, top-5)
   func WriteJSONL(target string, lines [][]byte) error
   ```

   Implementation MUST follow RESEARCH.md §Pattern 2 verbatim:
   - `dir, base := filepath.Split(target)`
   - `tmp, err := os.CreateTemp(dir, base+".tmp.*")` (same-filesystem requirement)
   - Write each `line + "\n"` to tmp
   - `tmp.Sync()` then `tmp.Close()` (handles fsync of file)
   - `os.Rename(tmp.Name(), target)` (atomic rename)
   - **CRITICAL** — fsync parent dir: `parentFD, err := os.Open(dir); parentFD.Sync(); parentFD.Close()`
   - Error path: `defer os.Remove(tmp.Name())` to clean up on early return

3. **`spike/go/tests/proc_test.go`** (NEW file, not in must_haves list — created as part of this task):
   - `TestProc_RunCaptures` — invokes `echo "hello"` via `proc.Run` with a `lineFn` collecting lines; asserts collected lines == `["hello"]`.
   - `TestProc_RunTimeout` — invokes `sleep 10` with a 1s context; asserts function returns within 6s (1s context + 5s WaitDelay grace) and the sleep PID is dead.

4. **`spike/go/tests/atomic_test.go`** — atomic write tests:
   - `TestAtomicWrite_HappyPath` — writes 100 lines via `output.WriteJSONL` to a temp file; asserts file exists, line count is 100, lines match input.
   - `TestAtomicWrite_CrashSafe` — fault-injection test:
     1. Pre-populate `target.jsonl` with known content (e.g., line "previous").
     2. Spawn a child Go test binary that ATTEMPTS `output.WriteJSONL(target, newLines)` but is built with a fault-injection flag (use `os.Exit(1)` after fsync but before rename — gated by env var `SPIKE_FAULT_INJECT=after_fsync`).
     3. Send SIGKILL to child within 100ms of start (race condition: child may complete first; test retries up to 10 times — if all 10 ATTEMPTS see clean write, test passes "no torn write observed" which IS the assertion).
     4. After child dies: read `target.jsonl` → MUST be either "previous" (rename never happened) OR the new content (rename was atomic) — NEVER partial. Assert file size matches one of those two known sizes.
     - Implementation detail: use `os/exec` with `cmd.Env = append(os.Environ(), "SPIKE_FAULT_INJECT=after_fsync")` and the test's own binary as the child (via `os.Executable()` trick). Add a `TestMain` to the test file that branches on the env var.

5. **`spike/go/tests/killtree_test.go`** — kill-tree synthetic mock test:
   - `TestKillTree_SyntheticMock` — exactly per RESEARCH.md §1.3.1 verbatim:
     1. `ctx, cancel := context.WithCancel(context.Background())`
     2. Use `proc.Run` to spawn `../../corpus/mock_stubborn_tool.sh` (resolve relative path to absolute from test file's `runtime.Caller` location)
     3. Wait 2s for the mock to fork its children
     4. Capture descendant PIDs via `pgrep -P <spike_pid>` (use exec.Command, not the wrapper — this is the assertion mechanism, not the device-under-test)
     5. Assert ≥3 descendants alive (parent + 2 children)
     6. `cancel()` to trigger ctx-driven process-group SIGTERM via proc.Run's Cancel handler
     7. Wait up to 10s for SIGKILL escalation (WaitDelay = 5s grace; SIGKILL fires after that)
     8. Assert ALL captured PIDs are now dead (use `syscall.Kill(pid, 0)` — returns ESRCH if dead)

   On test PASS: write `PASS\n` to `spike/go/out/.killtree_result`. On test FAIL: write `FAIL\n`. (Compare.sh reads this file.)

6. Run tests: `cd spike/go && go test ./tests/proc_test.go ./tests/atomic_test.go ./tests/killtree_test.go -v -race -timeout 60s`. ALL 5 sub-tests MUST pass (TestProc_RunCaptures, TestProc_RunTimeout, TestAtomicWrite_HappyPath, TestAtomicWrite_CrashSafe, TestKillTree_SyntheticMock).

`-race` flag is part of CI policy locked at Phase 3 (FOUND-16) but RESEARCH.md does not require it for the spike. Including it here as a habit costs ~0 LoC and surfaces concurrency bugs early.
  </action>
  <verify>
    <automated>test -f spike/go/internal/proc/proc.go && grep -q "Setpgid: true" spike/go/internal/proc/proc.go && grep -q "WaitDelay" spike/go/internal/proc/proc.go && grep -q "syscall.Kill(-" spike/go/internal/proc/proc.go && test -f spike/go/internal/output/atomic.go && grep -q "parentFD.Sync\|parentDir.Sync" spike/go/internal/output/atomic.go && grep -q "os.CreateTemp\|tempfile" spike/go/internal/output/atomic.go && grep -q "os.Rename" spike/go/internal/output/atomic.go && test -f spike/go/tests/proc_test.go && test -f spike/go/tests/atomic_test.go && grep -q "TestAtomicWrite_CrashSafe" spike/go/tests/atomic_test.go && test -f spike/go/tests/killtree_test.go && grep -q "TestKillTree_SyntheticMock" spike/go/tests/killtree_test.go && grep -q "mock_stubborn_tool.sh" spike/go/tests/killtree_test.go && (cd spike/go && go test ./tests/... -v -race -timeout 60s -run 'TestProc_RunCaptures|TestProc_RunTimeout|TestAtomicWrite_HappyPath|TestAtomicWrite_CrashSafe|TestKillTree_SyntheticMock') && test -f spike/go/out/.killtree_result && grep -qx "PASS" spike/go/out/.killtree_result && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `spike/go/internal/proc/proc.go` exists with `Run(ctx, name, args, lineFn) error` exported
    - **B4 fix:** `lineFn` parameter signature is `func([]byte) error` (NOT `func([]byte)`). proc.go's stream loop honors lineFn's error return — non-nil aborts early. Comment block documents the dual-mode usage (streaming vs buffered) explicitly.
    - proc.go uses `Setpgid: true`, `WaitDelay = 5*time.Second`, `syscall.Kill(-cmd.Process.Pid, ...)` (process-group kill)
    - `spike/go/internal/output/atomic.go` exists with `WriteJSONL(target, lines) error` exported
    - atomic.go implements ALL 4 steps: tempfile, fsync file, rename, fsync parent dir
    - All 5 tests pass under `go test -race -timeout 60s`: TestProc_RunCaptures, TestProc_RunTimeout, TestAtomicWrite_HappyPath, TestAtomicWrite_CrashSafe, TestKillTree_SyntheticMock
    - `spike/go/out/.killtree_result` contains `PASS` (single line) after TestKillTree_SyntheticMock passes
    - PITFALL §1.2 (kill-tree) is verified by TestKillTree_SyntheticMock with REAL subprocess (mock_stubborn_tool.sh) — not mocked OS calls
    - PITFALL §3.1 (atomic write) is verified by TestAtomicWrite_CrashSafe with REAL fault injection (forked child with SPIKE_FAULT_INJECT env var)
  </acceptance_criteria>
  <done>Two foundational primitives + their tests live. Subprocess hygiene + atomic write hygiene proven via REAL process / REAL file operations, not mocks.</done>
</task>

<task type="auto">
  <name>Task 3: Implement passive fan-out (4 sources) + httpx probe streaming + their tests</name>
  <read_first>
    - spike/go/internal/proc/proc.go (Task 2 — see proc.Run signature)
    - spike/go/internal/output/atomic.go (Task 2 — see output.WriteJSONL signature)
    - .planning/phases/01-language-adr-spike/01-RESEARCH.md §1.1 (4 passive sources locked) and §1.2 (httpx minimal flag set)
    - .planning/phases/01-language-adr-spike/01-RESEARCH.md §Pattern 3 (Streaming Subprocess Output — Go example)
    - .planning/phases/01-language-adr-spike/01-RESEARCH.md §3.4 (5 minimum tests)
    - .planning/research/PITFALLS.md §1.4 (stdin race), §1.5 (subprocess output assumptions)
    - modules/subdomains.sh:507-553 (sub_passive — canonical bash pattern this Go code ports)
    - spike/README.md (locked OQ4 credential pattern — env var → file path → skip if missing)
  </read_first>
  <action>
Implement the feature packages — the actual recon slice.

1. **`spike/go/internal/passive/passive.go`** — fan-out coordinator. Exact API:
   ```go
   package passive

   // Run fans out to 4 passive sources (subfinder, crt, github-subdomains, gitlab-subdomains)
   // concurrently using errgroup.SetLimit(4). Each source's stdout is parsed into subdomain strings,
   // merged into a deduplicated set, then written atomically to outFile as JSONL: {"subdomain": "x.example.com", "source": "subfinder"}.
   //
   // Source pattern: bash modules/subdomains.sh:sub_passive() (lines 507-553)
   // Slice locked: spike/README.md §Slice Locked
   func Run(ctx context.Context, target string, outFile string) error
   ```

   Implementation:
   - `g, gctx := errgroup.WithContext(ctx); g.SetLimit(4)`
   - For each source in `["subfinder", "crt", "github", "gitlab"]`: `g.Go(func() error { return sourceFn(gctx, target, &collectMu, collected) })`
   - `g.Wait()` then dedupe + atomic write
   - Use a `sync.Mutex` over a `map[string]string` (subdomain → source) for the merge — small enough that a channel is overkill

2. **Individual source files** — one per `.planning/phases/01-language-adr-spike/01-RESEARCH.md §1.1` source:

   **`spike/go/internal/passive/subfinder.go`**:
   - Function: `subfinderRun(ctx context.Context, target string, collect func(subdomain, source string)) error`
   - Invokes via `proc.Run`: `subfinder -d <target> -silent -json -all -max-time 180`
   - Parses NDJSON from `lineFn`: `{"host": "..."}` → call `collect(host, "subfinder")` for each line

   **`spike/go/internal/passive/crt.go`**:
   - Function: `crtRun(ctx, target, collect) error`
   - Invokes via `proc.Run`: `crt -s -json -l 200 <target>`
   - Parses JSON ARRAY (not NDJSON — different from subfinder): buffer all stdout into a byte slice, then `json.Unmarshal` into `[]struct{ Subdomain string `json:"subdomain"` }` and call `collect(item.Subdomain, "crt")` for each
   - Use a custom `lineFn` that appends to a `bytes.Buffer` AND parse at end (per RESEARCH.md §1.1 — crt is a "JSON-array buffered" source, the OPPOSITE of subfinder's streaming NDJSON)

   **`spike/go/internal/passive/github.go`**:
   - Function: `githubRun(ctx, target, collect) error`
   - Reads env var `GITHUB_TOKENS` (per RESEARCH.md OQ4)
   - If unset OR file empty/unreadable: log via `ui.Skip("github-subdomains", "GITHUB_TOKENS unset or empty")` and return nil
   - Else: invokes via `proc.Run`: `github-subdomains -d <target> -t <GITHUB_TOKENS> -k -q`
   - Parses text output (one subdomain per line); `collect(line, "github")`

   **`spike/go/internal/passive/gitlab.go`**:
   - Same as github but for `GITLAB_TOKENS` env var, command `gitlab-subdomains -d <target> -t <GITLAB_TOKENS>`

3. **`spike/go/internal/httpxprobe/httpx.go`** — httpx subprocess + NDJSON parser. Exact API:
   ```go
   package httpxprobe

   // Run invokes httpx with the locked minimal flag set and writes NDJSON-shaped host records
   // to outFile via atomic write at end. The locked flags are:
   //   -l <subsFile> -silent -json -status-code -title -tech-detect -no-color -threads 50 -timeout 10
   //
   // Source pattern: .planning/phases/01-language-adr-spike/01-RESEARCH.md §1.2
   // Locked: spike/README.md §Slice Locked
   func Run(ctx context.Context, subsFile string, outFile string) error
   ```

   Implementation:
   - Verify `subsFile` exists and is non-empty before invoking httpx (RESEARCH.md PITFALL 1.4 — never pipe via stdin; always file mode)
   - `proc.Run(ctx, "httpx", []string{"-l", subsFile, "-silent", "-json", "-status-code", "-title", "-tech-detect", "-no-color", "-threads", "50", "-timeout", "10"}, lineFn)`
   - In `lineFn`: append raw line to an in-memory `[][]byte` slice (each line is already valid JSON from httpx; don't re-parse)
   - After `proc.Run` returns nil: call `output.WriteJSONL(outFile, lines)` to atomically write the host records

   **Streaming detail** — for memory safety at scale (M5 RSS metric measures this):
   - If `len(lines) > 1000`: flush to outFile via atomic write, then drop the in-memory slice. (Production would do this differently with a persistent writer; spike just demonstrates the principle. For 1K hosts the slice will be ~500KB — not an issue. Document this as a known spike-vs-prod difference at the top of the file.)
   - Use `bufio.Scanner.Buffer(make([]byte, 1024*1024), 10*1024*1024)` to handle long lines (httpx can emit big JSON objects with tech-detect arrays)

4. **`spike/go/internal/ui/ui.go`** — minimal status output (per RESEARCH.md §3.1 layout, spike/go has a `ui` package). Exact API:
   ```go
   package ui

   func Info(msg string)
   func Skip(component, reason string)  // emits "[SKIP] <component>: <reason>"
   func Warn(msg string)
   func Err(msg string)
   ```

   Implementation: each writes to stderr with a prefix like the bash UI does (`_print_status` in lib/ui.sh — but we don't need dot-fill for a spike). 4 functions, ~30 LoC total. Output format:
   ```
   [INFO] passive: fan-out starting (4 sources, target=hackerone.com)
   [SKIP] github-subdomains: GITHUB_TOKENS unset or empty
   [WARN] crt: HTTP 502 from crt.sh — retry skipped (spike scope)
   [ERR ] httpx: subprocess exited with code 2
   ```

5. **Tests**:

   **`spike/go/tests/passive_test.go`** — `TestPassive_FourSources`:
   - Set up `GITHUB_TOKENS` and `GITLAB_TOKENS` env vars to point at temp files containing dummy tokens (or omit if real tokens not available — test will assert the skip path took)
   - Call `passive.Run(ctx, "example.com", tmpOut)` with 30s timeout context
   - Assert `tmpOut` exists, contains ≥1 JSONL line, each line parses as `{subdomain, source}` JSON
   - Assert all 4 sources contributed at least once (if their token was provided) OR the SKIP was logged
   - **If tools unavailable on dev machine**: test SKIPS with `t.Skip("subfinder/crt not in PATH")` — does not fail. RESEARCH.md §Environment Availability says these are "likely ✓"; on the maintainer's machine they should be present.

   **`spike/go/tests/httpx_test.go`** — `TestHTTPxProbe_Streaming`:
   - Create a fake `subs.jsonl` with 10 subdomains (use small known-resolvable hosts like `www.example.com`, `www.iana.org`, `dns.google`)
   - Call `httpxprobe.Run(ctx, subsJSONL, tmpHosts)`
   - Assert `tmpHosts` exists, contains ≥1 line per resolvable host, each line parses as JSON with at least `url` and `status_code` fields
   - Allow `httpx not in PATH` skip path

   **`spike/go/tests/integration_test.go`** — `TestKillTree_RealTools` (build tag `integration`):
   ```go
   //go:build integration
   ```
   - Launches `./bin/spike --target hackerone.com` as a child process (must be pre-built; test calls `make build` if not)
   - Waits until at least 1 descendant (subfinder OR httpx) is spawned (poll `pgrep -P` for up to 30s)
   - Captures all descendant PIDs
   - Sends `syscall.SIGINT` to the spike child
   - Waits 12s for SIGKILL escalation
   - Asserts ALL captured PIDs are dead
   - Per RESEARCH.md §1.3.2: this test runs nightly, NOT per-push. Gated by `-tags=integration`.

6. Update `spike/go/cmd/spike/main.go` to wire it up:
   - After cobra parses `--target`: `ui.Info("passive: starting"); err := passive.Run(ctx, target, "out/subs.jsonl"); if err != nil { ui.Err(...); os.Exit(1) }`
   - Then: `ui.Info("httpx: probing"); err := httpxprobe.Run(ctx, "out/subs.jsonl", "out/hosts.jsonl"); if err != nil { ui.Err(...); os.Exit(1) }`
   - Then: `ui.Info("spike: done"); os.Exit(0)`

7. Run unit tests: `cd spike/go && make test` (runs ./tests/... WITHOUT integration tag). All ~5 test functions must pass (or skip cleanly on missing tools). Then `make build`.

If tools (subfinder, crt, github-subdomains, gitlab-subdomains, httpx) are NOT installed on the dev machine: install via `go install` per RESEARCH.md §Environment Availability fallbacks. Each install costs ~30s. Verify presence with `command -v <tool>` before running tests.
  </action>
  <verify>
    <automated>test -f spike/go/internal/passive/passive.go && grep -q "errgroup.WithContext\|errgroup\.SetLimit" spike/go/internal/passive/passive.go && test -f spike/go/internal/passive/subfinder.go && test -f spike/go/internal/passive/crt.go && test -f spike/go/internal/passive/github.go && test -f spike/go/internal/passive/gitlab.go && grep -q "GITHUB_TOKENS" spike/go/internal/passive/github.go && grep -q "GITLAB_TOKENS" spike/go/internal/passive/gitlab.go && test -f spike/go/internal/httpxprobe/httpx.go && grep -q "tech-detect" spike/go/internal/httpxprobe/httpx.go && grep -q "threads.*50\|threads\", \"50" spike/go/internal/httpxprobe/httpx.go && test -f spike/go/internal/ui/ui.go && test -f spike/go/tests/passive_test.go && grep -q "TestPassive_FourSources" spike/go/tests/passive_test.go && test -f spike/go/tests/httpx_test.go && grep -q "TestHTTPxProbe_Streaming" spike/go/tests/httpx_test.go && test -f spike/go/tests/integration_test.go && grep -q "go:build integration" spike/go/tests/integration_test.go && grep -q "TestKillTree_RealTools" spike/go/tests/integration_test.go && (cd spike/go && go test ./tests/... -v -race -timeout 60s) && (cd spike/go && go build -ldflags="-s -w" -trimpath -o bin/spike ./cmd/spike) && test -x spike/go/bin/spike && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All 5 passive package files exist: `passive.go`, `subfinder.go`, `crt.go`, `github.go`, `gitlab.go`
    - `passive.go` uses `errgroup.WithContext` + `SetLimit(4)` for bounded fan-out
    - `subfinder.go` parses NDJSON via `lineFn` (streaming) — `crt.go` buffers + parses JSON array (NOT streaming) — the 4 sources exercise all 4 axes per RESEARCH.md §1.1
    - `github.go` and `gitlab.go` read env vars `GITHUB_TOKENS` / `GITLAB_TOKENS`, skip gracefully if absent
    - `httpx.go` uses EXACTLY the locked 8 flags from RESEARCH.md §1.2
    - `ui.go` has 4 exported funcs: `Info`, `Skip`, `Warn`, `Err`
    - `TestPassive_FourSources` passes OR skips with reason (tools not installed)
    - `TestHTTPxProbe_Streaming` passes OR skips
    - `TestKillTree_RealTools` exists with `//go:build integration` (gated; not run by `make test`)
    - `cd spike/go && go test ./tests/... -v -race -timeout 60s` exits 0
    - `cd spike/go && go build -ldflags="-s -w" -trimpath -o bin/spike ./cmd/spike` succeeds
    - `spike/go/bin/spike` exists and is executable
  </acceptance_criteria>
  <done>Go spike feature-complete: 4-source passive fan-out + httpx probe + atomic writes + 5 tests. Binary built. M3 (packaging) is now measurable.</done>
</task>

<task type="auto">
  <name>Task 4: Run integration test, capture M4-M6 metrics, write session log, run compare.sh dry-run</name>
  <read_first>
    - spike/go/Makefile (Task 1 — see session-end target)
    - .planning/phases/01-language-adr-spike/01-RESEARCH.md §2.1 (six metrics measurement protocol)
    - spike/compare.sh (Task 3 of 01-01 — see what metric files it reads)
    - spike/go/tests/integration_test.go (Task 3 — TestKillTree_RealTools)
  </read_first>
  <action>
Finalize Go spike measurements per DEC-03 (M3, M4, M5, M6 for Go).

1. **Run integration test** (real-tool kill-tree from RESEARCH.md §1.3.2):
   - `cd spike/go && make integration-test`
   - If the test passes: `spike/go/out/.killtree_result` already exists from Task 2 (synthetic mock) with `PASS`. The integration test additionally validates against REAL tools — if it fails, OVERWRITE `.killtree_result` with `FAIL` (the synthetic test is the unit baseline; the integration test is the production-shape validator; failure of the integration test means PITFALL 1.2 risk is present in real-tool tree → that lang loses per §2.3 killer-feature override).
   - If `subfinder` / `httpx` not on PATH and test SKIPS: keep `.killtree_result` = `PASS` from the synthetic test (the synthetic exercises the worst case — children ignoring SIGTERM — which is strictly harder than real tools that handle SIGTERM correctly).

2. **Measure M5 (RSS) against hackerone.com**:
   - `cd spike/go && ./bin/spike --target hackerone.com > out/run.log 2> out/run.err &` (run via `/usr/bin/time -l ./bin/spike ...` actually — proper measurement)
   - Run via: `/usr/bin/time -l ./bin/spike --target hackerone.com >out/run.log 2>out/run.err`
   - This populates `out/run.err` with the `maximum resident set size` line for compare.sh to extract.
   - If `hackerone.com` doesn't resolve enough subdomains (depends on internet + tool installs), run against `example.com` as fallback. The metric is "RSS under load" — both targets exercise the orchestrator; hackerone.com is preferred for higher subdomain count.
   - Run 3 times; compare.sh takes the median. Just running once for the spike is acceptable — the variance over 3 runs is documented but not gated.

3. **Set M6 (cross-platform pain ordinal)**:
   - The executor self-rates (per RESEARCH.md §2.1 M6 ordinal definitions):
     - `1` = `go build` + `make test` passed on first try with no platform-specific tweaks
     - `2` = had to install 1 system dep (e.g., `brew install go`) or set 1 env var
     - `3` = hit a real build/runtime issue requiring code change or platform branch
   - Write the chosen ordinal to `spike/go/out/.xplat_ordinal`. Also write a 1-3 sentence justification to `spike/go/out/.xplat_notes` (NOT in must_haves but captured for the ADR's M6 §"specifics" per RESEARCH.md §2.1).

4. **End the session timer** (per RESEARCH.md Pitfall 2 — Measuring "felt easier" instead of LoC + hours):
   - `cd spike/go && make session-end`
   - This appends `go <minutes>` to `spike/.spike_sessions.log`
   - The ENTIRE Plan 01-02 execution counts as ONE session; one entry. (If the executor spent multiple sessions across the 1-week budget, they should run `session-end` at the end of each, but the simpler model is one Plan = one session.)

5. **Dry-run compare.sh** to validate the metrics integrate:
   - `./spike/compare.sh hackerone.com`
   - Verify `spike/comparison.json` contains real Go data (not null) for: `loc`, `hours`, `binary_bytes`, `killtree` (= "PASS"), `rss_kb`, `xplat_ordinal`
   - Python data remains null (Plan 01-03 not yet run); compare.sh exits 1 — that's expected.

6. **Append a top-of-package comment** to `spike/go/cmd/spike/main.go` summarizing the spike's STATUS at end-of-Plan-01-02 (visible to anyone reading the spike code months from now):
   ```go
   // STATUS at Plan 01-02 close:
   //   - Slice complete: 4 passive sources + httpx probe + atomic JSONL writes
   //   - Tests: 5/5 unit, integration gated by -tags=integration
   //   - Measurements captured: spike/go/out/.killtree_result, .xplat_ordinal; spike/.spike_sessions.log
   //   - Awaiting Plan 01-03 (Python spike) for head-to-head comparison
   ```
  </action>
  <verify>
    <automated>test -f spike/go/out/.killtree_result && grep -qE "^(PASS|FAIL)$" spike/go/out/.killtree_result && test -f spike/go/out/.xplat_ordinal && grep -qE "^[123]$" spike/go/out/.xplat_ordinal && test -f spike/.spike_sessions.log && grep -q "^go " spike/.spike_sessions.log && test -x spike/go/bin/spike && (./spike/compare.sh hackerone.com 2>&1 | head -5; true) && test -f spike/comparison.json && jq -e '.go | .binary_bytes != null and .killtree == "PASS" and .xplat_ordinal != null' spike/comparison.json && grep -q "STATUS at Plan 01-02 close" spike/go/cmd/spike/main.go && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `spike/go/out/.killtree_result` contains `PASS` (or `FAIL` if integration test failed — both are valid completions of Task 4, but FAIL means Go is disqualified per killer-feature override)
    - `spike/go/out/.xplat_ordinal` contains `1`, `2`, or `3`
    - `spike/.spike_sessions.log` contains at least one line starting with `go ` followed by minutes
    - `spike/go/bin/spike` exists, executable, stripped binary
    - `spike/compare.sh hackerone.com` produces a valid `spike/comparison.json` with non-null Go fields (loc, binary_bytes, killtree, xplat_ordinal)
    - `cmd/spike/main.go` has a STATUS comment block referring to Plan 01-02 close
  </acceptance_criteria>
  <done>Go spike DEC-03 measurements (M3, M4, M5, M6) captured; compare.sh produces partial JSON; ready for Plan 01-03 (Python spike) to populate the other half.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (Plan 01-02 scope)

| Boundary | Description |
|----------|-------------|
| spike/go binary → external tools (subfinder, crt, etc.) | proc.Run wraps with Setpgid; external tool stdout untrusted |
| spike/go binary → GITHUB_TOKENS / GITLAB_TOKENS files | Tokens read from file path env var; file lives outside repo (spike/corpus/.tokens.* gitignored) |
| Test suite → mock_stubborn_tool.sh | Trusted (created by Plan 01-01 with locked exact contents) |
| spike binary → user (--target arg) | --target is a CLI arg; could be attacker-controlled but spike is maintainer-run; no privilege escalation surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-SI-01 | Tampering | passive sources (subprocess command injection via --target) | mitigate | Validate target with a domain regex BEFORE passing to subprocess (Task 3 passive.go): `^[a-zA-Z0-9.-]+$` — rejects shell metacharacters. Mirrors `validate_domain()` in lib/validation.sh. Reject on invalid → exit 1 with clear message. |
| T-01-02-SI-02 | Information Disclosure | GITHUB_TOKENS / GITLAB_TOKENS leakage | mitigate | Read from env var (not CLI arg → never in `ps`); pass token FILE path to subprocess (not token contents → file stays on disk); .gitignore covers `spike/corpus/.tokens.*` (locked Plan 01-01); ui.Skip on missing tokens explicitly does NOT echo env var contents |
| T-01-02-SI-03 | Denial of Service | orphaned subprocesses after Ctrl-C | mitigate | Setpgid + WaitDelay + Cancel handler (Task 2 proc.go) — process-group SIGTERM on ctx cancel, SIGKILL escalation at 5s. TestKillTree_SyntheticMock (Task 2) and TestKillTree_RealTools (Task 3, gated) prove it. PITFALL §1.2 |
| T-01-02-SI-04 | Tampering | atomic write torn output | mitigate | 4-step atomic write pattern (tempfile + fsync + rename + parent-dir fsync) in output/atomic.go (Task 2). TestAtomicWrite_CrashSafe (Task 2) proves it via fault-injection child process. PITFALL §3.1 |
| T-01-02-SI-05 | Tampering | httpx subprocess shell injection | mitigate | proc.Run uses `exec.CommandContext(ctx, name, args...)` — NEVER `sh -c "..."` style. Args passed as separate strings, not interpolated. Go's exec package does NOT spawn a shell unless explicitly using `sh -c` (which we don't). |
| T-01-02-SI-06 | Information Disclosure | spike outputs contain target subdomain data | accept | Subdomain data is the spike's product; user invoked --target intentionally; outputs go to spike/go/out/ which is gitignored. No further mitigation needed. |
| T-01-02-SC | Tampering | Go module installs (cobra, testify, errgroup) | mitigate | RESEARCH.md §Package Legitimacy Audit verified all approved via slopcheck. `go.sum` checksums pin exact module versions. `go mod verify` post-install asserts no tampering. No `checkpoint:human-verify` needed (all `OK` verdicts per RESEARCH.md). |
</threat_model>

<verification>
**Plan-level verification:**
1. `cd spike/go && make build` produces `bin/spike` (stripped via -s -w)
2. `cd spike/go && make test` exits 0 — all 5+ tests pass or skip cleanly
3. `cd spike/go && make integration-test` is gated by `-tags=integration` (RUNS the real-tool kill-tree; updates `.killtree_result`)
4. `spike/go/out/.killtree_result` contains `PASS`
5. `spike/go/out/.xplat_ordinal` contains `1`, `2`, or `3`
6. `spike/.spike_sessions.log` has at least one `go <N>` entry
7. `./spike/compare.sh hackerone.com` produces a `spike/comparison.json` with non-null Go fields
8. Every `.go` file contains the throwaway comment block
9. 1-week timebox: if the executor spent ≥7 calendar days on this plan, STOP and emit SUMMARY noting what built vs. didn't. Do NOT extend timebox.
</verification>

<success_criteria>
Plan 01-02 succeeds when:
- [ ] Go module initialized with cobra v1.9.x, testify, errgroup pinned in go.mod
- [ ] All packages exist: cmd/spike, internal/{passive,httpxprobe,output,proc,ui}, tests/
- [ ] All 4 passive sources implemented (subfinder, crt, github, gitlab) with correct parse shape per RESEARCH.md §1.1
- [ ] httpx invoked with EXACTLY the 8 locked flags per RESEARCH.md §1.2
- [ ] AtomicWriter implements 4-step pattern (including parent-dir fsync — the often-forgotten step)
- [ ] proc.Run uses Setpgid + WaitDelay + Cancel with process-group kill
- [ ] All 5 tests present: TestAtomicWrite_HappyPath, TestAtomicWrite_CrashSafe, TestKillTree_SyntheticMock, TestPassive_FourSources, TestHTTPxProbe_Streaming
- [ ] `go test -race` passes (including atomic-crash-safe fault-injection test)
- [ ] `TestKillTree_RealTools` gated by `-tags=integration` (nightly, not per-push)
- [ ] DEC-03 measurements captured: `.killtree_result` (PASS|FAIL), `.xplat_ordinal` (1|2|3), `.spike_sessions.log` (`go <minutes>` entry), bin/spike stripped binary
- [ ] `compare.sh hackerone.com` produces valid JSON with non-null Go fields
- [ ] Every .go file has the throwaway comment block
- [ ] 1-week calendar timebox honored (D-01)
- [ ] requirements_addressed (DEC-02 Go half, DEC-03 Go measurements) satisfied
</success_criteria>

<output>
Create `.planning/phases/01-language-adr-spike/01-02-SUMMARY.md` when done. Summary must include:
- Final M1-M6 numbers measured for Go (LoC, hours, binary bytes, kill-tree result, RSS kB, xplat ordinal)
- Test counts: how many passed / skipped / failed
- Calendar days actually spent (D-01 timebox compliance)
- **Per W6 cross-AI review fix:** an explicit `timebox_complete: true | false` line. `true` = full slice (4 sources + httpx + atomic + kill-tree tests + measurements) completed within the 1-week D-01 budget. `false` = STOPPED at the timebox with incomplete work. Plan 01-04 Task 2 reads this line into the §Killer-Feature Gate "Timebox compliance" row. A `false` here disqualifies Go via killer-feature override.
- Any features cut due to timebox (and what they were)
- Confirmation that the spike code is throwaway (no production evolution)
- A note: "Apples-to-apples comparison with Python spike (Plan 01-03) happens in Plan 01-04"
</output>