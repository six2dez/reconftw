---
phase: 09-composite-modes
plan: 02
type: execute
wave: 3
depends_on:
  - 09-01
  - 09-03
files_modified:
  - internal/mcp/handlers/composite.go
  - cmd/reconftw/composite_subcommands.go
  - cmd/reconftw/composite_test.go
  - cmd/reconftw/stub_subcommands.go
  - cmd/reconftw/root.go
autonomous: true
requirements:
  - MODE-01
  - MODE-02
  - MODE-03
  - MODE-04
  - MODE-05
  - MODE-12

must_haves:
  truths:
    - "reconftw recon --target X runs passive subs → web → osint stages (skips vulns) under a single Boot / single scheduler / single checkpoint (D-01)"
    - "reconftw all --target X adds vulns stages after osint (D-01)"
    - "reconftw passive --target X backend hard-guard blocks active tools via ErrPassiveViolation (D-09)"
    - "reconftw zen --target X applies ApplyZenProfile before Boot (D-02/D-03)"
    - "reconftw deep --target X applies ApplyDeepProfile before Boot (D-02/D-03)"
    - "reconftw recon --dry-run prints all pipeline stage tasks without executing tools (MODE-12)"
    - "composite handler boots ONCE: no per-pipeline BootReconApp calls; no per-pipeline axiom Launch/Shutdown; checkpoint closed once"
    - "phasePointers entries for recon/all/passive/zen/deep removed; TestEveryStubReturnsExit64 updated"
  artifacts:
    - path: "internal/mcp/handlers/composite.go"
      provides: "RunCompositeAsync function and CompositeMode type"
      exports: ["RunCompositeAsync", "CompositeMode", "ModeRecon", "ModeAll", "ModePassive"]
    - path: "cmd/reconftw/composite_subcommands.go"
      provides: "newReconCmd/newAllCmd/newPassiveCmd/newZenCmd/newDeepCmd + commonAfterBoot helper"
      contains: "commonAfterBoot"
    - path: "cmd/reconftw/composite_test.go"
      provides: "TestReconPipelineOrder, TestAllPipelineIncludesVulns, TestPassiveModeBlocksActiveTool, TestCompositeDryRun, TestDryRunRedactsSecrets (D-10 scoped here for composites)"
      contains: "TestPassiveModeBlocksActiveTool"
  key_links:
    - from: "cmd/reconftw/composite_subcommands.go newReconCmd RunE"
      to: "internal/mcp/handlers/composite.go RunCompositeAsync"
      via: "handlers.RunCompositeAsync(ctx, opts, handlers.ModeRecon)"
      pattern: "RunCompositeAsync.*ModeRecon"
    - from: "internal/mcp/handlers/composite.go RunCompositeAsync"
      to: "internal/mcp/handlers/common.go BootReconApp"
      via: "single BootReconApp call; CompositeMode.ConfigTransform wired before call"
      pattern: "BootReconApp.*opts"
    - from: "cmd/reconftw/root.go"
      to: "cmd/reconftw/composite_subcommands.go newReconCmd"
      via: "rootCmd.AddCommand(newReconCmd(), newAllCmd(), ...)"
      pattern: "AddCommand.*newReconCmd"
---

<objective>
Ship the five composite subcommands (recon/all/passive/zen/deep) as real implementations replacing the stub exit-64 bodies. Extract the commonAfterBoot helper, implement RunCompositeAsync in the handlers layer, wire passive hard-guard (D-09), and clean up stub artifacts.

Purpose: MODE-01/02/03/04/05 require functional composite orchestration. D-01 mandates single-Boot composite model so all stages share one AppContext/scheduler/workspace. MODE-12 requires dry-run across composites. D-09 requires backend-level passive guard provable by network-free unit test.

Output: composite_subcommands.go + composite.go (handlers) + composite_test.go + stub cleanup.

Depends on 09-03 (Wave 2): batch.go defines runBatch which composite_subcommands.go calls. Both files are package main and compiled as a unit; 09-03 must complete before this plan so the package compiles.
</objective>

<execution_context>
@/Users/six2dez/.claude/get-shit-done/workflows/execute-plan.md
@/Users/six2dez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-composite-modes/09-CONTEXT.md
@.planning/phases/09-composite-modes/09-RESEARCH.md
@.planning/phases/09-composite-modes/09-PATTERNS.md
@.planning/phases/09-composite-modes/09-01-SUMMARY.md
@.planning/phases/09-composite-modes/09-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RunCompositeAsync handler + commonAfterBoot extraction + stub cleanup (D-01/D-09/MODE-01/02/03)</name>
  <files>internal/mcp/handlers/composite.go, cmd/reconftw/composite_subcommands.go, cmd/reconftw/stub_subcommands.go, cmd/reconftw/root.go</files>
  <read_first>
    - cmd/reconftw/stub_subcommands.go — read in FULL. Lines 1-77: phasePointers map + newStubCmd shape. Lines 109-258: runSubsCmd (full afterBoot block at 159-237 and RunOptions wiring at 239-248). Lines 379-487: runWebCmd afterBoot (identical structure). Lines 641-944: runOSINTCmd afterBoot. Lines 862-924: runVulnsCmd afterBoot. Identify all four afterBoot bodies and confirm they are structurally identical except for the log-warn prefix string.
    - internal/mcp/handlers/subs.go — read in full. RunSubsAsync structure: scheduler nil-check, BootReconApp call, defer checkpoint.Close, opts.AfterBoot call, opts.DryRun early return, sched.Checkpoint assignment, task.Default.Build, stage loop using RunStage. This is the exact pattern RunCompositeAsync replicates.
    - internal/mcp/handlers/osint.go — read lines 14-57 (osintStageSpec + osintHandlerStages exported function). The composite stage loop iterates these specs; must replicate this pattern for subs/web/vulns stage groups.
    - internal/mcp/handlers/web.go — read in full for WebHandlerStages / stage spec pattern.
    - internal/mcp/handlers/vulns.go — read in full for VulnsHandlerStages / stage spec pattern.
    - internal/core/appctx/appctx.go — read to confirm BootOptions struct fields available for PassiveMode (D-09) and how appctx.Boot signature looks.
    - internal/core/backend/local.go — read to see Exec/Stream signatures for adding PassiveMode guard.
    - .planning/phases/09-composite-modes/09-PATTERNS.md — "internal/mcp/handlers/composite.go" section for RunCompositeAsync shape; "cmd/reconftw/composite_subcommands.go" section for commonAfterBoot extracted body + newReconCmd constructor + runBatch integration; "Key Structural Constraints" list.
    - .planning/phases/09-composite-modes/09-RESEARCH.md — Pattern 1 (stage seam extraction), Pattern 4 (passive hard-guard), Q4 (passive guard implementation), Pitfall 1 (axiom lifecycle), Pitfall 2 (checkpoint lifecycle).
    - cmd/reconftw/root_test.go — read lines 72-92 (TestEveryStubReturnsExit64) and lines 99-151 (TestDeprecationWarning) to understand what must be updated.
    - cmd/reconftw/appctx_init.go — read in full for filterByModuleAndEnabled, badgeForStatus, registerSecrets, and the imports those functions use.
    - cmd/reconftw/batch.go — read in full (created by 09-03); understand runBatch signature and how composite RunE calls it for --list.
  </read_first>
  <behavior>
    - Test (ordering): TestReconPipelineOrder — build full task DAG, run through composite recon stage groups (subs-passive+resolve+discovery → web → osint), assert subs tasks precede web tasks precede osint tasks by stage index
    - Test (all includes vulns): TestAllPipelineIncludesVulns — assert the ModeAll stage group list includes vulns prefixes absent from ModeRecon
    - Test (passive hard-guard): TestPassiveModeBlocksActiveTool — boot with PassiveMode=true via appctx.BootOptions, run passive composite, assert MockBackend.ExecCalls contains no entry whose tool name is in the active-tool set (puredns, naabu, nmap, dalfox, sqlmap, ffuf, httpx-active, commix)
    - Test (dry-run prints stages): TestCompositeDryRun — run recon --dry-run, capture cmd.OutOrStdout(), assert output contains "[dry-run]", "SUBS", "WEB", "OSINT" headers and exits 0 without executing any tool
    - Test (stub cleanup): TestEveryStubReturnsExit64 must be updated to REMOVE recon/all/passive/zen/deep from its stubs list; those five must NOT return exit 64 after this plan
  </behavior>
  <action>
    STEP A — Create internal/mcp/handlers/composite.go (package handlers):

    Define CompositeMode type (int const) with: ModeRecon (subs-passive+resolve+discovery → web → osint), ModeAll (subs-all → web → osint → vulns), ModePassive (subs-passive only), ModeZen (same pipeline as ModeRecon, ConfigTransform=ApplyZenProfile applied by caller), ModeDeep (same pipeline as ModeAll, ConfigTransform=ApplyDeepProfile applied by caller).

    Implement RunCompositeAsync(ctx context.Context, opts RunOptions, mode CompositeMode) error following the RunSubsAsync pattern from subs.go:
    1. Nil scheduler check → error.
    2. Single BootReconApp(ctx, opts) call — opts.ConfigTransform already set by caller for zen/deep.
    3. Single defer app.Checkpoint.Close().
    4. opts.AfterBoot(boot) call.
    5. opts.DryRun early return (dry-run printing done in cmd layer).
    6. sched.Checkpoint = app.Checkpoint.
    7. task.Default.Build() → allTasks.
    8. Axiom lifecycle (composite scope only): extract axiomBE from boot.ChosenBackend FailoverBackend once; call axiomBE.Launch(ctx) ONCE here in RunCompositeAsync; defer axiomBE.Shutdown(context.Background()) ONCE here in RunCompositeAsync. RunCompositeAsync is the SOLE owner of Launch + defer Shutdown for composite modes. The commonAfterBoot helper (STEP B) must NOT contain axiomBE.Launch() or defer axiomBE.Shutdown() — those calls are stripped from the composite afterBoot body so they cannot execute again. Acceptance criteria: the body of commonAfterBoot (as generated) contains neither a call to axiomBE.Launch nor a defer axiomBE.Shutdown; RunCompositeAsync contains exactly one axiomBE.Launch call and exactly one defer axiomBE.Shutdown call. The single-pipeline handlers (RunSubsAsync, RunWebAsync, RunVulnsAsync, RunOSINTAsync) are UNCHANGED: they continue to own their own Launch + defer Shutdown inside their respective afterBoot closures for non-composite invocations.
    9. Stage loop: iterate stageGroups for the mode (defined as slices of stageSpec from the per-pipeline exported stage functions). For ModeRecon: run subs-passive+resolve+discovery stages → web stages → osint stages. For ModeAll: run subs-all stages → web stages → osint stages → vulns stages. For ModePassive: run subs-passive stage only. Use sched.RunStage for each stage; on error apply best_effort (warn and continue) for web/osint/vulns; fail-fast for subs.
    10. After all stages complete, call each pipeline's MergeAll function sequentially (subs merge, then web merge, then osint merge, then vulns merge for ModeAll).

    Passive hard-guard (D-09): Add PassiveMode bool to appctx.BootOptions (if not already present — check appctx.go). In LocalBackend.Exec and LocalBackend.Stream, if PassiveMode is true and the tool name is in the hard-blocked set (puredns, massdns, dnsx-brute, naabu, nmap, dalfox, sqlmap, commix, ffuf, nuclei-active), return coreerrors.ErrPassiveViolation (add this sentinel to the errors package if missing). The composition guard (ModePassive only schedules passive tasks) is primary; the backend guard is defense-in-depth.

    STEP B — Create cmd/reconftw/composite_subcommands.go (package main):

    Extract commonAfterBoot from the four near-identical afterBoot closures in stub_subcommands.go. Signature: commonAfterBoot(ctx context.Context, boot handlers.AppBoot, sched *scheduler.Scheduler, dryRun bool, module string, rdct *log.Redactor, capture *dryRunCapture). Body: exact copy of the afterBoot body from runSubsCmd lines 159-237, with these changes: (a) remove both axiomBE.Launch() and defer axiomBE.Shutdown() from the extracted body — for composite callers, RunCompositeAsync owns Launch + Shutdown at composite scope; for single-pipeline callers (runSubsCmd etc.), the axiom calls remain in their own afterBoot closures directly, not via commonAfterBoot; (b) the rdct is passed in so dry-run callers can apply Redact() to tool arg strings (D-10). The existing single-pipeline runSubsCmd/runWebCmd/runVulnsCmd/runOSINTCmd are refactored to call commonAfterBoot internally for the shared non-axiom logic only (removes ~320 lines of duplication); their own axiom Launch + defer Shutdown are preserved in their own afterBoot closures after the commonAfterBoot call returns.

    Implement newReconCmd, newAllCmd, newPassiveCmd, newZenCmd, newDeepCmd following the newSubsCmd constructor pattern (stub_subcommands.go lines 109-127). Each has Use/Short/Long and a RunE that:
    1. Resolves target + dryRun + axiomEnabled flags (exact pattern from runSubsCmd lines 132-158).
    2. Parses efs := parseEarlyFlags(os.Args[1:]).
    3. Creates sched := scheduler.NewScheduler(0, 0, nil, nil) (per-invocation, never shared).
    4. Handles --list batch: if listFlag is set, calls runBatch (from batch.go, created by 09-03). If --target is set, runs single target.
    5. Calls handlers.RunCompositeAsync with opts.ConfigTransform = config.ApplyZenProfile (zen) or config.ApplyDeepProfile (deep) or nil (others).
    6. For --dry-run: after RunCompositeAsync returns early, calls composite dry-run printers (printDryRun, printWebDryRun, printOSINTDryRun in pipeline order, with rdct.Redact applied — D-10).
    7. printCompositeSummary at end.

    Composite dry-run redaction (D-10 scoped to composites): wrap each printXDryRun output line through rdct.Redact(line) before printing to cmd.OutOrStdout(). Build rdct := &log.Redactor{} and registerSecrets(cfg, rdct) before calling dry-run printers.

    STEP C — Update cmd/reconftw/stub_subcommands.go:
    Remove the five entries from phasePointers: "recon", "all", "passive", "zen", "deep". The three entries for "subs"/"web"/"vulns"/"osint" remain for docblock only (they are already real implementations; their phasePointers entries may be left or removed — leave them as comments).

    STEP D — Update cmd/reconftw/root.go:
    Replace the five newStubCmd usages for recon/all/passive/zen/deep with newReconCmd(), newAllCmd(), newPassiveCmd(), newZenCmd(), newDeepCmd() in the rootCmd.AddCommand(...) call.

    STEP E — Remove the five entries from TestEveryStubReturnsExit64's stubs slice in root_test.go. These subcommands now have real RunE bodies that do NOT return exit 64.
  </action>
  <verify>
    <automated>go test ./cmd/reconftw/... ./internal/mcp/handlers/... -run "TestReconPipelineOrder|TestAllPipelineIncludesVulns|TestPassiveModeBlocksActiveTool|TestCompositeDryRun|TestEveryStub" -count=1 -v</automated>
  </verify>
  <done>RunCompositeAsync exists in internal/mcp/handlers/composite.go; five composite subcommands are registered via real constructors; TestEveryStubReturnsExit64 no longer includes recon/all/passive/zen/deep; TestPassiveModeBlocksActiveTool passes asserting no active tool exec; TestCompositeDryRun passes asserting stage headers in output; grep confirms commonAfterBoot body contains neither axiomBE.Launch nor defer axiomBE.Shutdown; grep confirms RunCompositeAsync contains exactly one axiomBE.Launch and one defer axiomBE.Shutdown; go test ./... -race -count=1 green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Composite stage-order test + dry-run redaction test (MODE-12/D-10/composite acceptance)</name>
  <files>cmd/reconftw/composite_test.go</files>
  <read_first>
    - cmd/reconftw/osint_stage_order_test.go — read in full. This is the primary analog. Copy the package declaration, blank module imports, osintStageIndex pattern, and DependsOn cross-pipeline assertion approach.
    - cmd/reconftw/dag_build_test.go — read in full for TestRegisteredTaskDAGBuilds pattern and how MockBackend is used in tests.
    - cmd/reconftw/root_test.go — read lines 99-151 (TestDeprecationWarning) for how cobra Execute + output capture works; read lines 72-92 (TestEveryStubReturnsExit64) for the stub test update reference.
    - internal/mcp/handlers/composite.go — read after Task 1 creates it; use the stage group definitions to source the stage-order assertions.
    - .planning/phases/09-composite-modes/09-PATTERNS.md — "cmd/reconftw/composite_test.go" section for all four test patterns with exact function signatures.
    - .planning/phases/09-composite-modes/09-RESEARCH.md — Q4 passive guard test approach; Q7 D-10 dry-run redaction test approach.
  </read_first>
  <behavior>
    - TestReconPipelineOrder: assert no web task's DependsOn includes a subs task name that appears AFTER it in the subs stage group; assert osint tasks follow web tasks in the composite ordering (mirrors osint_stage_order_test.go DAG guard pattern)
    - TestAllPipelineIncludesVulns: assert ModeAll stage groups contain at least one task with module prefix "vulns." absent from ModeRecon stage groups
    - TestPassiveModeBlocksActiveTool: create MockBackend recording exec calls; boot with PassiveMode=true (via appctx.BootOptions); run passive composite; assert ExecCalls is empty or contains no tool in {puredns, naabu, nmap, dalfox, sqlmap, ffuf, commix}
    - TestCompositeDryRun: run "reconftw recon --target example.com --dry-run"; capture stdout; assert contains "[dry-run]" string, "SUBS" or "subs", "WEB" or "web", "OSINT" or "osint"; assert exit 0
    - TestDryRunRedactsSecrets: register a fake secret ("supersecret-api-key-12345") via registerSecrets on a cfg; run composite --dry-run with an arg that would include the secret; capture output; assert "supersecret-api-key-12345" is NOT present in output (D-10)
  </behavior>
  <action>
    Create cmd/reconftw/composite_test.go in package main.

    Blank imports: import all four module packages to trigger task registration (same pattern as osint_stage_order_test.go lines 18-29):
      _ "github.com/six2dez/reconftw/internal/modules/subdomains"
      _ "github.com/six2dez/reconftw/internal/modules/web"
      _ "github.com/six2dez/reconftw/internal/modules/osint"
      _ "github.com/six2dez/reconftw/internal/modules/vulns"

    TestReconPipelineOrder: build stageGroupIndex from composite stage specs for ModeRecon. For each task T in a later pipeline group (web, osint), assert that all of T.DependsOn() are either (a) in the same or earlier pipeline group, or (b) empty. This is the DAG guard ensuring no cross-pipeline dependency inversion. Use the same stageIndex approach from osint_stage_order_test.go.

    TestAllPipelineIncludesVulns: compare ModeRecon stage prefixes vs ModeAll stage prefixes. Assert ModeAll includes at least one prefix starting with "vulns." not present in ModeRecon. Obtain the prefix list by calling the composite stage-spec accessor exposed from composite.go.

    TestPassiveModeBlocksActiveTool: use handlers.RunCompositeAsync with a test-double — set a custom backend in BootOptions that records Exec calls. Assert that after running ModePassive, no recorded Exec call targeted puredns, naabu, nmap, dalfox, sqlmap, ffuf, or commix. If the test infrastructure requires a mock backend, use the existing MockBackend from internal/core/backend (or the test mock package from Phase 3).

    TestCompositeDryRun: construct rootCmd via newRootCmd(nil, &config.Config{}); set args to ["recon", "--target", "example.com", "--dry-run"]; capture OutOrStdout via bytes.Buffer; execute; assert output contains "[dry-run]" and substrings indicating subs+web+osint stages; assert no error.

    TestDryRunRedactsSecrets: create a *config.Config with a known fake secret configured in a secrets field (e.g., PDCP_API_KEY or similar); build rdct := &log.Redactor{}; call registerSecrets(cfg, rdct); simulate the composite dry-run output path with that rdct; assert rdct.Redact("run --api-key supersecret-api-key-12345") returns "run --api-key ***" (or the redacted form).
  </action>
  <verify>
    <automated>go test ./cmd/reconftw/... -run "TestReconPipelineOrder|TestAllPipeline|TestPassiveModeBlocks|TestCompositeDryRun|TestDryRunRedactsSecrets" -count=1 -v</automated>
  </verify>
  <done>composite_test.go exists with all five tests; all pass; go test ./cmd/reconftw/... -race -count=1 green; no test references stubs for recon/all/passive/zen/deep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI --target flag → composite pipeline | Target domain must be validated before workspace creation |
| passive mode guarantee → backend execution | Active tool invocations must be hard-blocked at backend level |
| --dry-run output → user terminal | Tool arg strings may contain registered secrets; must be redacted before printing |
| axiom fleet lifecycle → composite scope | Axiom Launch/Shutdown must be scoped to composite RunCompositeAsync, not per-pipeline or per-stage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Spoofing | passive mode bypass | mitigate | Backend-level ErrPassiveViolation hard-guard in LocalBackend.Exec for active-tool set; composition guard (only passive task prefixes scheduled) is primary layer (D-09); unit test TestPassiveModeBlocksActiveTool verifies both layers |
| T-09-02-02 | Information Disclosure | composite --dry-run printing tool args | mitigate | D-10: rdct.Redact(line) applied to every tool invocation string before fmt.Fprintf in composite dry-run printers; TestDryRunRedactsSecrets verifies; uses the registered-secret path from commonAfterBoot |
| T-09-02-03 | Tampering | axiom fleet multi-launch in composite | mitigate | RunCompositeAsync is the SOLE owner of axiomBE.Launch and defer axiomBE.Shutdown for composite modes; commonAfterBoot body contains NEITHER axiomBE.Launch NOR defer axiomBE.Shutdown (both are stripped during extraction); single-pipeline handlers (subs/web/vulns/osint) are unchanged and keep their own Launch + Shutdown for non-composite calls; acceptance verified by grep gate in <done> |
| T-09-02-04 | Denial of Service | checkpoint double-close in composite | mitigate | RunCompositeAsync owns single defer app.Checkpoint.Close(); extracted stage runners do NOT call Close (Pitfall 2); PATTERNS.md constraint 2 |
| T-09-02-05 | Elevation of Privilege | zen/deep profile bypass via file config | mitigate | ConfigTransform applied inside BootReconApp after config.Load; Boot uses post-transform cfg; file-config values for rate limits / vulns.enabled cannot override mode transforms (D-03) |
| T-09-02-SC | Tampering | npm/pip/cargo installs | accept | No new external dependencies; pure internal Go packages |
</threat_model>

<verification>
1. `go build ./...` passes
2. `go test ./cmd/reconftw/... ./internal/mcp/handlers/... -run "TestReconPipelineOrder|TestAllPipeline|TestPassiveMode|TestCompositeDryRun|TestDryRunRedactsSecrets|TestEveryStub" -count=1 -v` — all pass
3. `reconftw recon --help` shows real subcommand (not "not implemented — Phase N" message)
4. `grep -c "phasePointers\[\"recon\"\]" cmd/reconftw/stub_subcommands.go` == 0 (entry removed)
5. `go test ./... -race -count=1` green
6. `grep -r "RunCompositeAsync" internal/mcp/handlers/composite.go` shows the function exists
7. `grep -v '^//' cmd/reconftw/composite_subcommands.go | grep -c "axiomBE\.Launch\|defer axiomBE\.Shutdown"` == 0 (commonAfterBoot body is clean of axiom lifecycle calls)
8. `grep -v '^//' internal/mcp/handlers/composite.go | grep -c "axiomBE\.Launch"` == 1 and `grep -v '^//' internal/mcp/handlers/composite.go | grep -c "defer axiomBE\.Shutdown"` == 1 (RunCompositeAsync owns exactly one of each)
</verification>

<success_criteria>
- internal/mcp/handlers/composite.go contains RunCompositeAsync function with CompositeMode parameter; contains exactly one axiomBE.Launch and one defer axiomBE.Shutdown
- cmd/reconftw/composite_subcommands.go contains commonAfterBoot helper (no axiomBE.Launch, no defer axiomBE.Shutdown inside it) and five new subcommand constructors
- cmd/reconftw/composite_test.go contains TestReconPipelineOrder, TestAllPipelineIncludesVulns, TestPassiveModeBlocksActiveTool, TestCompositeDryRun, TestDryRunRedactsSecrets — all pass
- phasePointers in stub_subcommands.go no longer has entries for recon/all/passive/zen/deep
- TestEveryStubReturnsExit64 no longer lists recon/all/passive/zen/deep
- go test ./... -race -count=1 green
</success_criteria>

<output>
Create `.planning/phases/09-composite-modes/09-02-SUMMARY.md` when done
</output>
