---
phase: 09-composite-modes
plan: 04
type: execute
wave: 4
depends_on:
  - 09-02
  - 09-03
files_modified:
  - internal/core/resolvers/gen.go
  - internal/core/resolvers/gen_test.go
  - cmd/reconftw/stateful_subcommands.go
  - cmd/reconftw/stateful_test.go
  - cmd/reconftw/root.go
autonomous: true
requirements:
  - MODE-06
  - MODE-07
  - MODE-08

must_haves:
  truths:
    - "reconftw gen-resolvers calls dnsvalidator (two source URLs) and falls back to HTTP download if dnsvalidator is absent or produces zero output (D-04)"
    - "reconftw refresh-cache --target X sets cfg.Cache.Refresh=true + cfg.Advanced.Diff=true and invalidates per-target cache staging files (D-05)"
    - "reconftw quick-rescan --target X sets cfg.Advanced.Diff=true and re-runs the recon pipeline with checkpoint bypass (D-06)"
    - "gen-resolvers requires no --target (standalone per D-04)"
    - "quick-rescan records a new scan row in the store.db scans table so Phase 10 can diff against it"
    - "all three stateful subcommands are registered in root.go"
  artifacts:
    - path: "internal/core/resolvers/gen.go"
      provides: "RunGenResolvers(ctx, cfg) invoking dnsvalidator with fallback HTTP download"
      exports: ["RunGenResolvers"]
      contains: "RunGenResolvers"
    - path: "internal/core/resolvers/gen_test.go"
      provides: "TestGenResolversCallsDnsvalidator, TestGenResolversFallsBackToHTTP"
      contains: "TestGenResolvers"
    - path: "cmd/reconftw/stateful_subcommands.go"
      provides: "newGenResolversCmd, newRefreshCacheCmd, newQuickRescanCmd constructors + RunE bodies"
      contains: "newGenResolversCmd"
    - path: "cmd/reconftw/stateful_test.go"
      provides: "TestQuickRescanForcesDiff, TestRefreshCacheSetsForceRefreshFlag, TestGenResolversDryRun"
      contains: "TestQuickRescanForcesDiff"
  key_links:
    - from: "cmd/reconftw/stateful_subcommands.go newGenResolversCmd RunE"
      to: "internal/core/resolvers/gen.go RunGenResolvers"
      via: "resolvers.RunGenResolvers(ctx, cfg)"
      pattern: "RunGenResolvers"
    - from: "cmd/reconftw/stateful_subcommands.go newQuickRescanCmd RunE"
      to: "internal/mcp/handlers/composite.go RunCompositeAsync"
      via: "ConfigTransform sets Diff=true; calls RunCompositeAsync with ModeRecon"
      pattern: "RunCompositeAsync.*ModeRecon"
    - from: "cmd/reconftw/root.go addSubcommands"
      to: "cmd/reconftw/stateful_subcommands.go newGenResolversCmd"
      via: "rootCmd.AddCommand(newGenResolversCmd(), newRefreshCacheCmd(), newQuickRescanCmd())"
      pattern: "AddCommand.*newGenResolversCmd"
---

<objective>
Ship the three stateful utility subcommands: gen-resolvers (dnsvalidator + HTTP fallback per D-04), refresh-cache (cache invalidation per D-05), and quick-rescan (checkpoint bypass + store record per D-06). Register them in root.go.

Purpose: MODE-06/07/08 complete the stateful mode surface. gen-resolvers mirrors v1 modules/axiom.sh:resolvers_update (dnsvalidator invocation + fallback download). refresh-cache invalidates per-target cache staging files so the next run fetches fresh DNS/ASN/geo data. quick-rescan sets checkpoint bypass so all tasks re-run and records a new scan baseline in store.db for Phase 10 diff consumption.

Output: internal/core/resolvers/gen.go + stateful_subcommands.go + stateful_test.go + root.go registration.
</objective>

<execution_context>
@/Users/six2dez/.claude/get-shit-done/workflows/execute-plan.md
@/Users/six2dez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-composite-modes/09-CONTEXT.md
@.planning/phases/09-composite-modes/09-RESEARCH.md
@.planning/phases/09-composite-modes/09-PATTERNS.md
@.planning/phases/09-composite-modes/09-01-SUMMARY.md
@.planning/phases/09-composite-modes/09-02-SUMMARY.md
@.planning/phases/09-composite-modes/09-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RunGenResolvers + gen_test.go (D-04/MODE-08)</name>
  <files>internal/core/resolvers/gen.go, internal/core/resolvers/gen_test.go</files>
  <read_first>
    - modules/axiom.sh — read lines 16-65 (resolvers_update function). This is the v1 parity reference: two dnsvalidator invocations (public-dns.info + massdns resolver list), merge via anew, fallback download from trickest/resolvers + six2dez/trusted_resolvers. Copy the source URLs and the fallback logic.
    - internal/core/config/config.go — find PathsConfig struct fields: Resolvers, ResolversTrusted, and ResolversDownload sub-struct containing URL and TrustedURL fields. Also find Advanced.Tools.DNSValidator.Threads (or equivalent field) for thread count.
    - internal/core/backend/local.go — read in full to understand LocalBackend.Exec and LocalBackend.ExecEnv signatures and how to construct a Tool for dnsvalidator. Also understand how ExecEnv(ctx, tool, args, envVars) returns (*Result, error).
    - .planning/phases/09-composite-modes/09-PATTERNS.md — "internal/core/resolvers/gen.go" section for RunGenResolvers function skeleton.
    - .planning/phases/09-composite-modes/09-RESEARCH.md — Q5 "gen-resolvers" section for the exact implementation description: LocalBackend.Exec call, HTTP fallback, output paths, thread count field, dnsvalidator source URLs.
    - cmd/reconftw/stub_test.go — read lines 1-85 for the test package structure pattern to replicate in gen_test.go.
  </read_first>
  <behavior>
    - Test (TestGenResolversCallsDnsvalidator): when dnsvalidator is on PATH, RunGenResolvers calls LocalBackend.Exec with a Tool named "dnsvalidator" and args containing the two source URLs and the output path; output file at cfg.Paths.Resolvers is non-empty after execution (use a test fixture dnsvalidator stub or mock backend)
    - Test (TestGenResolversFallsBackToHTTP): when dnsvalidator is NOT on PATH (tool not found error), RunGenResolvers falls back to HTTP GET of cfg.Paths.ResolversDownload.URL and writes the response to cfg.Paths.Resolvers; no error returned
    - Test (TestGenResolversEmptyOutputFallsBack): when dnsvalidator runs but produces zero output lines, RunGenResolvers falls back to HTTP download
    - Test (TestGenResolversOutputPathDefaultFallback): when cfg.Paths.Resolvers is empty string, RunGenResolvers falls back to a default path (e.g., ~/.config/reconftw/resolvers.txt or workdir-relative)
  </behavior>
  <action>
    Create internal/core/resolvers/ directory if it does not exist. Create internal/core/resolvers/gen.go in package resolvers.

    Imports: context, fmt, net/http, os, path/filepath, strings; internal/core/backend, internal/core/config.

    Implement RunGenResolvers(ctx context.Context, cfg *config.Config) error:
    1. Determine output path: if cfg.Paths.Resolvers is non-empty, use it. Otherwise fall back to filepath.Join(os.UserHomeDir(), ".config/reconftw/resolvers.txt") or a sensible default. Create parent directories with os.MkdirAll.
    2. Determine thread count: use cfg.Advanced.Tools.DNSValidator.Threads if > 0, else default to 10 (v1 default DNSVALIDATOR_THREADS=10).
    3. Build dnsvalidator Tool: Name="dnsvalidator", Args include the two source URLs from v1 axiom.sh:16-65: "https://public-dns.info/nameservers.txt" and "https://raw.githubusercontent.com/blechschmidt/massdns/master/lists/resolvers.txt". Run each as a separate invocation (per v1 pattern) with flags -tL {url} -threads {n} -o {tmpfile}. Use LocalBackend.Exec.
    4. If dnsvalidator execution fails (ToolError with "not found" or produces a zero-size output file), fall back to HTTP download: GET cfg.Paths.ResolversDownload.URL (or the hardcoded trickest URL if that field is empty) with a 60-second timeout; write the response body to cfg.Paths.Resolvers.
    5. Also download cfg.Paths.ResolversTrusted URL if non-empty (the six2dez trusted_resolvers.txt fallback).
    6. Log a slog.Info("gen-resolvers: complete", "path", outputPath, "fallback", didFallback) at end.
    7. Return nil on success.

    Create internal/core/resolvers/gen_test.go in package resolvers_test. Use an httptest.NewServer for the HTTP fallback test. For the dnsvalidator invocation test, use a mock approach: if the full binary path is not available in the test environment, skip the dnsvalidator test with t.Skip("dnsvalidator not on PATH"). Provide a TestGenResolversFallsBackToHTTP that replaces the URL with the httptest server URL.
  </action>
  <verify>
    <automated>go test ./internal/core/resolvers/... -count=1 -v</automated>
  </verify>
  <done>internal/core/resolvers/gen.go exists with RunGenResolvers; gen_test.go passes (HTTP fallback test passes unconditionally; dnsvalidator test skips gracefully if tool absent); go build ./internal/core/resolvers/... succeeds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: stateful_subcommands.go + stateful_test.go + root.go registration (D-05/D-06/MODE-06/07/08)</name>
  <files>cmd/reconftw/stateful_subcommands.go, cmd/reconftw/stateful_test.go, cmd/reconftw/root.go</files>
  <read_first>
    - cmd/reconftw/stub_subcommands.go — read lines 109-158 (newSubsCmd + runSubsCmd target resolution). These are the constructor + RunE patterns to replicate for newGenResolversCmd/newRefreshCacheCmd/newQuickRescanCmd.
    - cmd/reconftw/composite_subcommands.go — read after 09-02 creates it; use the same RunCompositeAsync invocation pattern for quick-rescan (it re-runs the recon pipeline via RunCompositeAsync with ModeRecon + ConfigTransform).
    - internal/store/sqlc/scans.sql.go — read to understand CreateScan(ctx, CreateScanParams) and the Mode field in CreateScanParams. quick-rescan calls CreateScan with mode="quick-rescan" before running the pipeline.
    - internal/core/config/config.go — find CacheConfig struct (Refresh bool, MaxAgeDaysResolvers, MaxAgeDays fields) and AdvancedConfig (Diff bool field — confirm field name and type).
    - cmd/reconftw/root.go — read lines 80-200 (addPersistentGlobalFlags + addSubcommands / rootCmd.AddCommand calls) to see where to register the three new subcommands.
    - cmd/reconftw/stub_test.go — read lines 1-85 for the test package structure pattern.
    - .planning/phases/09-composite-modes/09-PATTERNS.md — "cmd/reconftw/stateful_subcommands.go" section for all three subcommand shapes.
    - .planning/phases/09-composite-modes/09-RESEARCH.md — Q5 "refresh-cache" and "quick-rescan" sections for exact implementation details.
  </read_first>
  <behavior>
    - Test (TestQuickRescanForcesDiff): call the ConfigTransform closure that newQuickRescanCmd builds; assert cfg.Advanced.Diff == true (D-06 THIN: quick-rescan is cfg.Advanced.Diff=true + scan row mode="quick-rescan" + checkpoint bypass; no separate QuickRescan field needed)
    - Test (TestRefreshCacheSetsForceRefreshFlag): call the ConfigTransform closure that newRefreshCacheCmd builds; assert cfg.Cache.Refresh == true and cfg.Advanced.Diff == true
    - Test (TestGenResolversDryRun): newGenResolversCmd() executes without --target and returns no error (gen-resolvers is standalone, no target required per D-04)
    - Test (TestStatefulCmdsRegisteredInRoot): newRootCmd() returns a command that has "gen-resolvers", "refresh-cache", "quick-rescan" as subcommand names accessible via Find
  </behavior>
  <action>
    Create cmd/reconftw/stateful_subcommands.go (package main):

    newGenResolversCmd(): cobra.Command with Use="gen-resolvers", Short="Regenerate the DNS resolver list via dnsvalidator (D-04)". RunE:
    1. Parse efs := parseEarlyFlags(os.Args[1:]).
    2. Load config: cfg, err := config.Load(config.LoadOptions{ExplicitConfigPath: efs.configPath, SecretsPath: efs.secretsPath}).
    3. Call resolvers.RunGenResolvers(cmd.Context(), cfg).
    4. Print result path to cmd.OutOrStdout(). Return nil.
    No --target flag required.

    newRefreshCacheCmd(): cobra.Command with Use="refresh-cache", Short="Rebuild DNS/ASN/geo cached data for a target (D-05)". Flags: --target (required). RunE:
    1. Resolve targetFlag from --target or inherited global.
    2. Build ConfigTransform for refresh: func(cfg *config.Config) { cfg.Cache.Refresh = true; cfg.Advanced.Diff = true }.
    3. Call handlers.RunCompositeAsync(ctx, opts with ConfigTransform + ModeRecon) — the Diff=true forces all checkpoint-based skips to be bypassed, causing full re-fetch of cache-producing tasks (geo, ASN, DNS). The workspace is the existing workspace for the target.
    4. Separately: delete per-target cache staging files in workdir/inputs/ matching patterns: "geo.*.txt", "asn*.jsonl", "resolvers*.txt" — do this BEFORE running RunCompositeAsync so the re-fetch starts clean.
    Note: does NOT wipe findings.jsonl, subdomains.jsonl, or any artefact files (those are results, not caches per RESEARCH.md Q5).

    newQuickRescanCmd(): cobra.Command with Use="quick-rescan", Short="Re-run recon with checkpoint bypass against the last workspace (D-06)". Flags: --target (required). RunE:
    1. Resolve targetFlag.
    2. Build ConfigTransform: func(cfg *config.Config) { cfg.Advanced.Diff = true }. Per D-06 (THIN), quick-rescan sets Advanced.Diff=true to bypass checkpoints and force-refresh; it does NOT require a separate QuickRescan config field. Do not conditionally set any QuickRescan-named field — rely solely on Advanced.Diff.
    3. Record new scan row in store: open the store.db at cfg.Paths.DataDir + workdir, call store.CreateScan with Mode="quick-rescan" and target. This baseline allows Phase 10's DiffBetweenScans to compute findings delta. If the store is not available, log a warning and continue.
    4. Call handlers.RunCompositeAsync(ctx, opts with ConfigTransform + ModeRecon). The Diff=true bypasses checkpoints so all tasks re-run.

    Update cmd/reconftw/root.go: in the rootCmd.AddCommand(...) call (find where newSubsCmd / newWebCmd etc. are registered), add newGenResolversCmd(), newRefreshCacheCmd(), newQuickRescanCmd() to the AddCommand list.

    Create cmd/reconftw/stateful_test.go (package main):
    - TestQuickRescanForcesDiff: create a *config.Config; invoke the ConfigTransform closure built by the quick-rescan RunE; assert cfg.Advanced.Diff == true. (No assertion on QuickRescan field — that field does not exist per D-06 THIN.)
    - TestRefreshCacheSetsForceRefreshFlag: same pattern for refresh-cache transform; assert cfg.Cache.Refresh == true and cfg.Advanced.Diff == true.
    - TestGenResolversDryRun: construct newGenResolversCmd(); set args to []; execute; assert no error and no panic (gen-resolvers requires no target).
    - TestStatefulCmdsRegisteredInRoot: call newRootCmd(nil, &config.Config{}); assert cmd.Find("gen-resolvers") != nil; cmd.Find("refresh-cache") != nil; cmd.Find("quick-rescan") != nil.
  </action>
  <verify>
    <automated>go test ./cmd/reconftw/... ./internal/core/resolvers/... -run "TestQuickRescan|TestRefreshCache|TestGenResolvers|TestStateful" -count=1 -v</automated>
  </verify>
  <done>stateful_subcommands.go contains newGenResolversCmd, newRefreshCacheCmd, newQuickRescanCmd; root.go registers all three; stateful_test.go passes all four tests; newQuickRescanCmd ConfigTransform sets only cfg.Advanced.Diff=true (no QuickRescan field mutation); go test ./... -race -count=1 green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gen-resolvers HTTP download URL → network | External URL fetched; content written to a system file path |
| refresh-cache file deletion → workspace | glob pattern deletion of inputs/ files must be scoped to workspace dir only |
| quick-rescan store.db write → SQLite | Scan record insertion must be well-formed; no SQL injection via target string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04-01 | Tampering | gen-resolvers HTTP resolver download | mitigate | Download URL comes from cfg.Paths.ResolversDownload.URL (config, not user CLI arg); HTTPS enforced by hardcoded https:// scheme; response written to validated cfg.Paths.Resolvers path; no shell execution of downloaded content |
| T-09-04-02 | Tampering | refresh-cache file deletion glob | mitigate | File deletion is scoped to workdir/inputs/ only (workdir is derived from output.WorkspaceInit which creates a per-target isolated directory); pattern-based deletion uses filepath.Glob within the workdir — no absolute path deletion possible |
| T-09-04-03 | Information Disclosure | quick-rescan store record target field | mitigate | target string passed through appctx.NewTarget validation before workspace creation; same validation as all other subcommands; validated domain stored in scans table |
| T-09-04-04 | Denial of Service | gen-resolvers HTTP timeout | mitigate | net/http client created with 60-second timeout; no indefinite hang possible |
| T-09-04-05 | Information Disclosure | dnsvalidator output contains resolver IPs | accept | Resolver list is non-sensitive operational data; output written to cfg.Paths.Resolvers (config-controlled path); no secrets involved |
| T-09-04-SC | Tampering | npm/pip/cargo installs | accept | No new external dependencies; stdlib net/http + internal packages only; dnsvalidator is an existing tool, not a new install |
</threat_model>

<verification>
1. `go build ./...` passes
2. `go test ./cmd/reconftw/... ./internal/core/resolvers/... -run "TestQuickRescan|TestRefreshCache|TestGenResolvers|TestStateful" -count=1 -v` — all pass
3. `go test ./... -race -count=1` green
4. `reconftw gen-resolvers --help` shows the subcommand (registered in root)
5. `reconftw quick-rescan --help` shows the subcommand with --target flag
6. `reconftw refresh-cache --help` shows the subcommand with --target flag
7. `grep -c "newGenResolversCmd" cmd/reconftw/root.go` >= 1 (registered)
8. `grep -c "QuickRescan" cmd/reconftw/stateful_subcommands.go` == 0 (no QuickRescan config field mutation per D-06 THIN)
</verification>

<success_criteria>
- internal/core/resolvers/gen.go contains RunGenResolvers with dnsvalidator invocation + HTTP fallback
- cmd/reconftw/stateful_subcommands.go contains three constructor functions; newQuickRescanCmd ConfigTransform sets only cfg.Advanced.Diff=true (no cfg.Advanced.QuickRescan field)
- cmd/reconftw/root.go AddCommand list includes gen-resolvers, refresh-cache, quick-rescan
- stateful_test.go passes: TestQuickRescanForcesDiff, TestRefreshCacheSetsForceRefreshFlag, TestGenResolversDryRun, TestStatefulCmdsRegisteredInRoot
- go test ./... -race -count=1 green
- All MODE-06, MODE-07, MODE-08 acceptance criteria satisfied
</success_criteria>

<output>
Create `.planning/phases/09-composite-modes/09-04-SUMMARY.md` when done
</output>
