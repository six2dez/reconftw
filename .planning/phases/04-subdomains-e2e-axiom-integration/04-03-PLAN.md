---
phase: 04-subdomains-e2e-axiom-integration
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - internal/extract/favicon/favicon.go
  - internal/extract/js/js.go
  - internal/extract/analytics/analytics.go
  - internal/modules/subdomains/scraping.go
autonomous: true
requirements:
  - SUBD-01

must_haves:
  truths:
    - "internal/extract/favicon, internal/extract/js, internal/extract/analytics packages exist as pure-transform packages with no Backend dependency"
    - "SubScrapingTask (name: subdomains.scraping) registers subdomain-discovery from favicon+JS; Phase 5 can reuse the extractor packages for web-analysis outputs"
    - "SubAnalyticsTask (name: subdomains.analytics) registers analytics tracking-ID pivot"
    - "SubNSDelegationTask (name: subdomains.ns_delegation) registers NS delegation discovery"
    - "extract package API is the D-02 shared interface: Extract(rawOutput []byte, domain string) ([]Result, error)"
  artifacts:
    - path: "internal/extract/favicon/favicon.go"
      provides: "Pure favicon hash extractor returning subdomain candidates + hash for Phase 5"
      exports: ["Result", "Extract"]
    - path: "internal/extract/js/js.go"
      provides: "Pure JS link extractor returning subdomain candidates from JS output"
      exports: ["Result", "Extract"]
    - path: "internal/extract/analytics/analytics.go"
      provides: "Analytics tracking-ID pivot returning related domains"
      exports: ["Result", "Extract"]
    - path: "internal/modules/subdomains/scraping.go"
      provides: "SubScrapingTask, SubAnalyticsTask, SubNSDelegationTask"
      contains: "subdomains.scraping, subdomains.analytics, subdomains.ns_delegation"
  key_links:
    - from: "internal/modules/subdomains/scraping.go"
      to: "internal/extract/favicon/favicon.go"
      via: "favicon.Extract(rawOutput, domain)"
      pattern: "favicon\\.Extract"
    - from: "internal/modules/subdomains/scraping.go"
      to: "internal/core/backend/runner.go"
      via: "app.Tools.Run(ctx, \"favirecon\", args)"
      pattern: "Tools\\.Run"
---

<objective>
Build the shared extractor packages (favicon, JS, analytics — D-02) as pure-transform packages with no Backend dependency, and implement the subdomain-discovery Tasks that use them. Phase 5 will reuse these packages for web-analysis outputs without modification.

Purpose: Satisfies D-01 full parity (sub_scraping + sub_analytics + sub_ns_delegation) and D-02 (Phase 4 owns the package API; Phase 5 extends). The pure-transform design means the packages are independently testable without a running backend.

Output: 3 extractor packages + 3 scraping/analytics/delegation Tasks; Phase 5 can import internal/extract/* and extend for web outputs.
</objective>

<execution_context>
@/Users/six2dez/Tools/reconftw/.claude/get-shit-done/workflows/execute-plan.md
@/Users/six2dez/Tools/reconftw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/six2dez/Tools/reconftw/.planning/ROADMAP.md
@/Users/six2dez/Tools/reconftw/.planning/phases/04-subdomains-e2e-axiom-integration/04-CONTEXT.md
@/Users/six2dez/Tools/reconftw/.planning/phases/04-subdomains-e2e-axiom-integration/04-PATTERNS.md

<interfaces>
<!-- Extractor package API (D-02 shared contract — Phase 5 reuses these) -->

The Extract function signature across all three packages:
  func Extract(rawOutput []byte, domain string) ([]Result, error)
  type Result struct { Subdomains []string; ... package-specific fields }

Phase 4 subdomain-discovery slice consumes Subdomains []string from Result.
Phase 5 web analysis extends by reading the package-specific fields (Hash for favicon tech fingerprinting, etc.).

From internal/core/output/scope.go (analog — pure transform, no I/O):
  Pattern: pure function, no global state, no Backend, inputs are raw bytes

From internal/modules/subdomains/passive.go (plan-01 output):
  SubdomainRecord{Subdomain, Source, FirstSeen} — used to write to app.Tree.Append("subdomains", ...)

From internal/core/backend/runner.go:
  Runner.Run(ctx, toolName, args) (*Result, error)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared extractor packages (favicon, JS, analytics)</name>
  <read_first>
    - .planning/phases/04-subdomains-e2e-axiom-integration/04-PATTERNS.md §Group 7 (extractor package shapes and v1 source)
    - modules/subdomains.sh lines 1149-1245 (sub_scraping — favirecon, subjs, jsluice invocations)
    - modules/subdomains.sh lines 1326-1380 (sub_analytics — analyticsrelationships invocation)
    - internal/core/output/scope.go (analog: pure transform package pattern)
  </read_first>
  <behavior>
    - Test 1 (favicon): favicon.Extract(favireconOutputBytes, "example.com") returns Result.Subdomains containing hostnames parsed from favirecon output lines
    - Test 2 (favicon): favicon.Extract(favireconOutputBytes, "example.com") returns Result.Hash containing the mmh3 hash string parsed from output
    - Test 3 (js): js.Extract(subjsOutputBytes, "example.com") returns Result.Subdomains containing hostnames parsed from subjs/jsluice output
    - Test 4 (analytics): analytics.Extract(analyticsRelOutputBytes, "example.com") returns Result.Subdomains containing related domains from tracking-ID pivot output
    - Test 5 (nil safety): all three Extract functions return empty Result (not error or panic) on empty input
  </behavior>
  <action>
    Create three pure-transform packages under `internal/extract/`:

    `internal/extract/favicon/favicon.go` — package favicon:
      type Result struct {
        Subdomains []string // hostnames discovered via favirecon/shodan pivot
        Hash       string   // mmh3 hash (for Phase 5 tech fingerprinting)
        URL        string   // source URL
      }
      func Extract(rawFaviconOutput []byte, domain string) ([]Result, error)
        Parse favirecon output lines (one host per line, or JSON output with hash field).
        For each line that is a valid hostname (domainRe or import from output.domainRe pattern), add to Subdomains.
        Extract mmh3 hash from favirecon output if present.
        Pure function: no I/O, no global state, no Backend import.

    `internal/extract/js/js.go` — package js:
      type Result struct {
        Subdomains []string // hostnames found in JS files
        URLs       []string // all URLs extracted (for Phase 5 full JS analysis)
      }
      func Extract(rawJSOutput []byte, domain string) ([]Result, error)
        Parse subjs output (one URL per line) or jsluice JSON output.
        Filter URLs to extract hostname; include only those matching the domain (scope check via strings.HasSuffix — not full DefaultScopeFilter, since this is a pure transform).
        Return discovered subdomains + all URLs (Phase 5 uses URLs for full analysis).

    `internal/extract/analytics/analytics.go` — package analytics:
      type Result struct {
        Subdomains []string // related domains sharing analytics tracking IDs
        TrackerIDs []string // e.g., "UA-12345", "G-ABCDEF" (for Phase 7 OSINT)
      }
      func Extract(rawAnalyticsOutput []byte, domain string) ([]Result, error)
        Parse analyticsrelationships output (one domain per line, or structured output with tracker IDs).
        Return discovered related domains as Subdomains.

    All three packages: no imports from internal/core/* (pure transform — no Backend, no AppContext, no OutputTree).
    Test files colocated: `favicon_test.go`, `js_test.go`, `analytics_test.go` — table-driven tests with fixture byte slices.
  </action>
  <verify>
    <automated>go test -race ./internal/extract/... -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race ./internal/extract/...` exits 0
    - `grep -r "internal/core" internal/extract/` returns no matches (pure transform — no kernel imports)
    - `internal/extract/favicon/favicon.go` exports `type Result struct` with Subdomains and Hash fields
    - `internal/extract/js/js.go` exports `type Result struct` with Subdomains and URLs fields
    - `internal/extract/analytics/analytics.go` exports `type Result struct` with Subdomains and TrackerIDs fields
    - All three `Extract` functions handle empty input without panic
  </acceptance_criteria>
  <done>3 extractor packages with stable Phase 5-reusable API; all tests pass; no kernel imports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SubScrapingTask + SubAnalyticsTask + SubNSDelegationTask</name>
  <read_first>
    - modules/subdomains.sh lines 1149-1245 (sub_scraping)
    - modules/subdomains.sh lines 1326-1380 (sub_analytics)
    - modules/subdomains.sh lines 1247-1324 (sub_ns_delegation — NS zone delegation discovery)
    - .planning/phases/04-subdomains-e2e-axiom-integration/04-PATTERNS.md §Group 1 (Task scaffold pattern)
    - internal/extract/favicon/favicon.go (Task 1 output)
    - internal/extract/js/js.go (Task 1 output)
    - internal/extract/analytics/analytics.go (Task 1 output)
    - internal/modules/subdomains/passive.go (SubdomainRecord type)
  </read_first>
  <behavior>
    - Test 1: SubScrapingTask.Name() == "subdomains.scraping"
    - Test 2: SubScrapingTask.DependsOn() contains "subdomains.barrier.passive"
    - Test 3: SubAnalyticsTask.Name() == "subdomains.analytics"; DependsOn contains "subdomains.barrier.passive"
    - Test 4: SubNSDelegationTask.Name() == "subdomains.ns_delegation"; DependsOn contains "subdomains.barrier.passive"
    - Test 5: SubScrapingTask.Enabled returns false when cfg.Subdomains.Scraping.Enabled == false
  </behavior>
  <action>
    Create `internal/modules/subdomains/scraping.go` implementing 3 Tasks:

    SubScrapingTask (name: "subdomains.scraping"):
      - Enabled: cfg.Subdomains.Scraping.Enabled
      - DependsOn: ["subdomains.barrier.passive"]
      - Run:
        1. Invoke favirecon: res, err := app.Tools.Run(ctx, "favirecon", ["-s", "-f", faviconListPath, "-d", domain])
           Parse output via favicon.Extract(res.Stdout, domain).
           Append discovered subdomains as SubdomainRecord{Source: "favirecon"} to app.Tree.Append("subdomains", ...).
        2. Invoke subjs for JS URL extraction: res, err = app.Tools.Run(ctx, "subjs", ["-i", subsFile]) where subsFile is the current subdomains file.
           Pipe subjs output through jsluice: res2, err = app.Tools.Run(ctx, "jsluice", ["urls", "-"]) feeding subjs stdout as stdin (write to temp file; pass as jsluice arg).
           Parse via js.Extract(res.Stdout, domain) or js.Extract(res2.Stdout, domain).
           Append discovered subdomains as SubdomainRecord{Source: "subjs-jsluice"}.
        faviconListPath from cfg.Subdomains.Scraping.FaviconList (default: wordlists/favicon.lst per reconftw.cfg).

    SubAnalyticsTask (name: "subdomains.analytics"):
      - Enabled: cfg.Subdomains.Analytics.Enabled
      - DependsOn: ["subdomains.barrier.passive"]
      - Run: app.Tools.Run(ctx, "analyticsrelationships", ["-cn", domain])
        Parse via analytics.Extract(res.Stdout, domain).
        Append discovered domains as SubdomainRecord{Source: "analyticsrelationships"}.

    SubNSDelegationTask (name: "subdomains.ns_delegation"):
      - Enabled: cfg.Subdomains.Active.Enabled (reuses active gate — v1 behavior)
      - DependsOn: ["subdomains.barrier.passive"]
      - Run: enumerate NS records for subdomains (dnsx -ns query on known subdomains);
        for each delegated NS record that points to a different zone, perform recursive enum (passive sources on the delegate).
        V1 source: sub_ns_delegation uses dnsx to query NS records and discovers subdomain takeover candidates via zone delegation.
        Write discovered subdomains as SubdomainRecord{Source: "ns-delegation"}.

    All 3 Tasks call `func init() { task.Register(...) }` at end of scraping.go.
  </action>
  <verify>
    <automated>go test -race ./internal/modules/subdomains/... -run "TestScraping|TestAnalytics|TestNSDelegation" -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `internal/modules/subdomains/scraping.go` contains all three Task Name() implementations
    - `grep "favicon\.Extract\|js\.Extract\|analytics\.Extract" internal/modules/subdomains/scraping.go` shows extractor calls
    - `grep -c "task\.Register" internal/modules/subdomains/scraping.go` outputs 3
    - Unit tests for Name/DependsOn/Enabled pass for all 3 Tasks
    - `go build -o /dev/null ./...` exits 0
  </acceptance_criteria>
  <done>3 scraping/analytics/delegation Tasks registered using D-02 shared extractor packages; D-01 full parity scraping functions ported; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| favirecon/subjs/jsluice/analyticsrelationships stdout → extract.Extract | Tool output (raw bytes) parsed by pure-transform functions before entering scope filter |
| Extract output → app.Tree.Append | Parsed subdomains cross scope filter at OutputTree.Append boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Injection | analytics.Extract output → app.Tree.Append | mitigate | Extract returns []string hostnames; each hostname passes domainRe validation inside DefaultScopeFilter.IsInScope at Append boundary; metacharacters rejected |
| T-04-09 | Information Disclosure | jsluice extracting JS secrets | mitigate | Phase 4 extracts only subdomain/URL candidates from JS output; full secret extraction is Phase 5 WEB-04; Phase 4 Task does NOT write secrets to artefacts — only subdomain lines |
| T-04-10 | Spoofing | Malformed favirecon output exploiting Extract parser | mitigate | Extract functions use line-by-line parsing with domainRe gate; malformed lines rejected silently; no eval/exec in pure-transform code |
| T-04-SC | Tampering | npm/pip installs in this plan | accept | This plan has no package manager installs — pure Go code only; tools are already registered in tools.lock (plan-01) |
</threat_model>

<verification>
- `go test -race ./internal/extract/...` exits 0
- `go test -race ./internal/modules/subdomains/...` exits 0
- `grep -r "internal/core" internal/extract/` returns no matches
- `grep -c "task\.Register" internal/modules/subdomains/scraping.go` outputs 3
- `go build -o /dev/null ./...` exits 0
</verification>

<success_criteria>
- 3 pure-transform extractor packages with stable API (Phase 5 can import and extend)
- D-02 honored: shared package API set by Phase 4, reusable in Phase 5 without duplication
- 3 Tasks registered covering sub_scraping + sub_analytics + sub_ns_delegation (D-01 parity)
- No kernel imports in internal/extract/* (independently testable)
</success_criteria>

<output>
Create `.planning/phases/04-subdomains-e2e-axiom-integration/04-03-SUMMARY.md` when done.
</output>
