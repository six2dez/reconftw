---
phase: 04-subdomains-e2e-axiom-integration
plan: 06
type: execute
wave: 5
depends_on:
  - 04-04
  - 04-05
files_modified:
  - internal/core/backend/axiom.go
  - internal/core/backend/failover.go
  - cmd/reconftw/stub_subcommands.go
  - cmd/reconftw/appctx_init.go
  - internal/core/appctx/boot.go
autonomous: true
requirements:
  - AXIOM-01
  - AXIOM-02
  - AXIOM-03
  - AXIOM-04
  - AXIOM-05
  - AXIOM-06
  - AXIOM-07
  - AXIOM-08
  - SUBD-11

must_haves:
  truths:
    - "AxiomBackend.Exec uses Tool.InputFlag (NOT heuristic arg scraping) to identify the input file to split for axiom-scan fleet dispatch"
    - "NewAxiomBackend takes (cfg *config.Config, reg *ToolRegistry, log *slog.Logger) — 3 args; boot.go updated to pass these args"
    - "FailoverBackend.Exec retries local on *AxiomFailure; after FailoverThreshold consecutive failures sets kill-switch; subsequent calls skip Primary"
    - "FailoverBackend.Stream: on primary stream failure, partial-drain goroutine is ctx/timeout-bounded (select on primaryCh and ctx.Done()); then calls Fallback.Stream (no partial double-emit, no goroutine leak)"
    - "newSubsCmd RunE calls RunStage SEQUENTIALLY (5 calls): passive → resolve/brute/scraping → permut/recursive → enrichment → final-merge; each stage slice is filtered by filterByModuleAndEnabled BEFORE passing to RunStage"
    - "sched.Checkpoint = app.Checkpoint set AFTER Boot returns (B3 fix — checkpoint non-nil so per-tool resume works)"
    - "filterByModuleAndEnabled(tasks, module, cfg) calls t.Enabled(cfg) to filter disabled tasks BEFORE RunStage — addresses REVIEWS finding #4"
    - "MergeStage called after each sequential RunStage call before the next stage begins"
    - "After enrichment RunStage, command layer reads inputs/takeover.subzy.jsonl AND inputs/takeover.dnstake.jsonl, merges them, and calls app.Tree.Append('findings', merged) ONCE (B2 fix — single writer for findings artefact)"
    - "reconftw subs --target example.com --dry-run exits 0 listing the 5 stage slices"
    - "reconftw subs --target example.com (with real tools) executes all stages and produces workspaces/<target>/artefacts/subdomains.jsonl"
  artifacts:
    - path: "internal/core/backend/axiom.go"
      provides: "Real AxiomBackend (replaces Phase 3 stub): Exec with Tool.InputFlag-based split + fleet lifecycle"
      exports: ["AxiomBackend", "NewAxiomBackend"]
    - path: "internal/core/backend/failover.go"
      provides: "FailoverBackend decorator: retry + kill-switch + safe ctx-bounded Stream failover"
      exports: ["FailoverBackend"]
    - path: "cmd/reconftw/stub_subcommands.go"
      provides: "newSubsCmd with sequential RunStage loop (5 stages) + filterByModuleAndEnabled + sched.Checkpoint wiring + takeover staging merge"
      contains: "RunStage called 5 times sequentially, sched.Checkpoint = app.Checkpoint"
    - path: "cmd/reconftw/appctx_init.go"
      provides: "filterByModuleAndEnabled helper calling t.Enabled(cfg)"
      contains: "filterByModuleAndEnabled"
    - path: "internal/core/appctx/boot.go"
      provides: "boot.go updated: NewAxiomBackend called with (cfg, reg, log) 3-arg signature"
      contains: "NewAxiomBackend(cfg, reg, log)"
  key_links:
    - from: "internal/core/backend/axiom.go"
      to: "internal/core/backend/backend.go"
      via: "Tool.InputFlag used to identify split-target arg (NOT heuristic last-non-flag-arg)"
      pattern: "InputFlag"
    - from: "internal/core/backend/failover.go"
      to: "internal/core/errors/errors.go"
      via: "errors.As(err, &axErr) — *AxiomFailure triggers retry"
      pattern: "AxiomFailure"
    - from: "cmd/reconftw/stub_subcommands.go"
      to: "internal/core/scheduler/scheduler.go"
      via: "sched.RunStage(ctx, 'subdomains', stageSlice) called 5 times sequentially"
      pattern: "RunStage"
    - from: "cmd/reconftw/stub_subcommands.go"
      to: "internal/core/scheduler/scheduler.go"
      via: "sched.Checkpoint = app.Checkpoint set after Boot returns (B3 fix)"
      pattern: "sched\\.Checkpoint"
    - from: "cmd/reconftw/appctx_init.go"
      to: "internal/core/task/task.go"
      via: "t.Enabled(cfg) called in filterByModuleAndEnabled — addresses Enabled() not-called-by-Scheduler finding"
      pattern: "Enabled.*cfg"
---

<objective>
Replace Phase 3 AxiomBackend stub with real implementation using Tool.InputFlag-based input splitting, add FailoverBackend decorator with ctx-bounded stream drain, wire newSubsCmd with sequential RunStage calls per stage-slice + filterByModuleAndEnabled + sched.Checkpoint post-Boot wiring + takeover staging file merge for single findings.jsonl write.

Purpose: This plan resolves six REVIEWS/checker verified findings simultaneously:
  1. Scheduler does NOT honor DependsOn ordering → fixed by sequential RunStage per stage (D-07 honored: single Scheduler semaphore still owns all concurrency within each call)
  2. Axiom extractInputFile heuristic is brittle → fixed by Tool.InputFlag per-tool declaration (plan-00 added the field)
  3. checkpoint never wired (B3) → sched.Checkpoint = app.Checkpoint set after Boot returns
  4. Enabled() never invoked → fixed by filterByModuleAndEnabled calling t.Enabled(cfg) before RunStage
  5. findings.jsonl concurrent write race (B2) → command layer reads both takeover staging files and calls app.Tree.Append("findings") once after enrichment RunStage
  6. FailoverBackend.Stream goroutine leak → partial-drain goroutine is ctx/timeout-bounded (W4)
  (NewAxiomBackend signature mismatch → fixed here; boot.go updated)
</objective>

<execution_context>
@/Users/six2dez/Tools/reconftw/.claude/get-shit-done/workflows/execute-plan.md
@/Users/six2dez/Tools/reconftw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/six2dez/Tools/reconftw/.planning/ROADMAP.md
@/Users/six2dez/Tools/reconftw/.planning/phases/04-subdomains-e2e-axiom-integration/04-CONTEXT.md

<interfaces>
From internal/core/backend/backend.go (EXACT — after plan-00 adding InputFlag):
  type Tool struct { Name string; Path string; Version string; DefaultArgs []string; Timeout time.Duration; Critical bool; InputFlag string }
  Backend interface: Exec / Stream / HealthCheck / Capacity

From internal/core/backend/axiom.go (CURRENT Phase 3 stub):
  type AxiomBackend struct{} — REPLACE entirely
  func NewAxiomBackend() *AxiomBackend — REPLACE with 3-arg version

From internal/core/appctx/boot.go (CONFIRMED from source — READ BEFORE EDITING):
  func Boot(ctx, logger, cfg, target, sched, opt BootOptions) (*AppContext, error) — 6 args; DO NOT CHANGE
  pickBackend(cfg) currently calls backend.NewAxiomBackend() (zero-arg) — UPDATE to NewAxiomBackend(cfg, backend.Default, logger)
  BootOptions.Backend: used to inject FailoverBackend when --axiom flag is set in newSubsCmd

From internal/core/scheduler/scheduler.go (VERIFIED from source — READ CAREFULLY):
  type Scheduler struct { ...; Checkpoint checkpoint.Interface; ... }
  RunStage(ctx, module string, tasks []task.Task) error
  — fires ALL tasks in the slice concurrently under SetLimit(MaxConcurrent) errgroup
  — does NOT wait for DependsOn to complete before starting a dependent task
  — CONSEQUENCE: call RunStage once per stage-slice in sequence, NOT once for all tasks
  — D-07 is still satisfied: the SAME Scheduler (single semaphore) is used for all 5 calls
  NewScheduler(maxJobs, heartbeatSec int, cp checkpoint.Interface, log *slog.Logger) *Scheduler
  — cp (checkpoint) is passed as nil at construction time in newSubsCmd (cycle-break)
  — sched.Checkpoint MUST be set to app.Checkpoint AFTER Boot returns (B3 fix)

From internal/core/config/config.go (EXACT fields after plan-00):
  cfg.Axiom.Enabled bool
  cfg.Axiom.FleetName string
  cfg.Axiom.FleetCount int
  cfg.Axiom.ShutdownOnEnd bool
  cfg.Axiom.AutoFixHostkey bool
  cfg.Axiom.FailoverThreshold int  (added in plan-00)
  cfg.Advanced.Tools.Axiom.ResolversPath string
  cfg.Advanced.Tools.Axiom.ResolversTrustedPath string

From internal/core/task/task.go:
  Task.Enabled(cfg *config.Config) bool — defined but NOT called by Scheduler.runOne
  MUST be called in filterByModuleAndEnabled BEFORE passing tasks to RunStage

From internal/modules/subdomains/takeover.go (plan-05 output — B2 staging contract):
  TakeoverSubzyTask writes inputs/takeover.subzy.jsonl (NOT app.Tree.Append)
  TakeoverDNSTakeTask writes inputs/takeover.dnstake.jsonl (NOT app.Tree.Append)
  Command layer (here in Task 2) reads both files and calls app.Tree.Append("findings", merged) ONCE

Stage slices for newSubsCmd sequential RunStage (5 calls):
  Stage 1 "passive":      SubfinderTask, CrtTask, GithubSubdomainsTask, GitlabSubdomainsTask, UrlfinderTask, HackertargetTask
  Stage 2 "resolve":      SubResolversHealthTask, SubActiveTask, SubTLSTask, SubNoerrorTask, SubDNSTask, SubSRVTask, SubPTRTask, SubBruteTask, SubScrapingTask, SubAnalyticsTask, SubNSDelegationTask
  Stage 3 "permut":       SubPermutTask, SubRegexPermutTask, SubDNSCewlTask, SubIAPermutTask, SubRecursivePassiveTask, SubRecursiveBruteTask
  Stage 4 "enrichment":   TakeoverSubzyTask, TakeoverDNSTakeTask, SubBucketsTask, SubASNTask, SubGeoTask, ZoneTransferTask
  After enrichment stage: read inputs/takeover.subzy.jsonl + inputs/takeover.dnstake.jsonl → merge → app.Tree.Append("findings", merged) once
  After each stage: call subdomains.MergeStage(ctx, app, stageName) for subdomain artefact
  Stage 5 (implied): final MergeStage after enrichment produces the definitive subdomains.jsonl
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1a: AxiomBackend (Exec with Tool.InputFlag / Stream / HealthCheck / repairKnownHosts)</name>
  <read_first>
    - internal/core/backend/axiom.go (CURRENT Phase 3 stub — REPLACE entirely)
    - internal/core/backend/backend.go (Tool.InputFlag field added in plan-00)
    - internal/core/backend/local.go (LocalBackend — used for axiom-scan CLI subprocess dispatch)
    - modules/axiom.sh lines 199-270 (axiom_selected — connectivity probe + AXIOM_AUTO_FIX_HOSTKEY repair)
    - internal/core/errors/errors.go (AxiomFailure type)
    - internal/core/config/config.go (AxiomConfig + AdvToolAxiom fields — EXACT names)
  </read_first>
  <behavior>
    - Test 1: AxiomBackend.Exec for tool with InputFlag="-l" correctly passes that flag arg value as input file
    - Test 2: AxiomBackend.Exec for tool with InputFlag="" uses last positional arg as input file
    - Test 3: AxiomBackend.Exec for unmapped tool (e.g. "subfinder" — no axiom module) delegates to local.Exec transparently
    - Test 4: AxiomBackend.Exec for "puredns" (mapped to "puredns-resolve") rewrites invocation to axiom-scan -m puredns-resolve <inputFile>
    - Test 5: AxiomBackend.HealthCheck detects "REMOTE HOST IDENTIFICATION HAS CHANGED" and calls repairKnownHosts when AutoFixHostkey=true
    - Test 6: AxiomBackend.Capacity() returns cfg.Axiom.FleetCount
  </behavior>
  <action>
    REPLACE `internal/core/backend/axiom.go` entirely (Phase 3 stub → real implementation).
    Delete ErrAxiomNotImplemented sentinel.

    New AxiomBackend struct fields:
      cfg *config.Config
      reg *ToolRegistry
      log *slog.Logger
      moduleMap map[string]string   — Tool.Name → axiom-scan module name
      local *LocalBackend           — used to run axiom-scan CLI itself locally
      axiomScanTool *Tool           — looked up from reg at init time

    NewAxiomBackend(cfg *config.Config, reg *ToolRegistry, log *slog.Logger) *AxiomBackend:
      Initialize from cfg.Axiom.* and cfg.Advanced.Tools.Axiom.*; populate moduleMap from defaultAxiomModuleMap(); create local = NewLocalBackend(killGrace).

    defaultAxiomModuleMap() map[string]string:
      "puredns"  → "puredns-resolve"
      "tlsx"     → "tlsx"
      "dnsx"     → "dnsx"
      "s3scanner"→ "s3scanner"
      "nuclei"   → "nuclei"
      "subfinder"→ ""  (API-bound — local fallback)
      All others → "" (local fallback)

    Exec(ctx, t *Tool, args []string) (*Result, error):
      module = moduleMap[t.Name]; if "" → a.local.Exec(ctx, t, args) [transparent fallback]
      Otherwise:
        inputFile = extractInputFile(t, args)  — uses Tool.InputFlag (NOT heuristic)
        Run: a.local.Exec(ctx, axiomScanTool, [inputFile, "-m", module, "-o", outFile, a.cfg.Axiom.ExtraArgs...])
        Read outFile; return Result.

    extractInputFile(t *Tool, args []string) string:
      if t.InputFlag != "": find the arg immediately AFTER t.InputFlag in args slice; return it.
      if t.InputFlag == "": return last element of args (positional convention).
      If neither found: return "" (caller logs warning and falls back to local).

    Stream: if tool maps to axiom → run axiom-scan sync (no true streaming); buffer result and send over channel; close when done. If local fallback → a.local.Stream.

    HealthCheck(ctx): run axiom-exec "echo reconftw-axiom-probe"; check for "REMOTE HOST IDENTIFICATION HAS CHANGED" in output; if AutoFixHostkey → repairKnownHosts; else return *AxiomFailure.

    repairKnownHosts(ctx, errOutput): parse offending host; run ssh-keygen -R <host>; re-run axiom-select.

    Capacity(): return cfg.Axiom.FleetCount.

    Stubs for Task 1b (compile-safe, filled by Task 1b):
      func (a *AxiomBackend) Launch(ctx context.Context) error { return nil }
      func (a *AxiomBackend) Shutdown(ctx context.Context) error { return nil }
      func (a *AxiomBackend) resolversPropagation(ctx context.Context) error { return nil }
  </action>
  <verify>
    <automated>go test -race ./internal/core/backend/... -run "TestAxiom" -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep "ErrAxiomNotImplemented" internal/core/backend/axiom.go` returns no matches (sentinel deleted)
    - `grep "InputFlag" internal/core/backend/axiom.go` returns match in extractInputFile (Tool.InputFlag used)
    - `grep "last.*non-flag\|last.*positional.*heuristic" internal/core/backend/axiom.go` returns no matches (no heuristic arg scraping)
    - `grep "repairKnownHosts\|AutoFixHostkey" internal/core/backend/axiom.go` returns matches (AXIOM-07)
    - `go test -race ./internal/core/backend/... -run "TestAxiom"` exits 0
    - `go build -o /dev/null ./...` exits 0
  </acceptance_criteria>
  <done>AxiomBackend Exec/Stream/HealthCheck/repairKnownHosts with Tool.InputFlag-based input splitting; ErrAxiomNotImplemented deleted; build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1b: AxiomBackend fleet lifecycle + FailoverBackend (ctx-bounded stream drain) + boot.go signature fix</name>
  <read_first>
    - internal/core/backend/axiom.go (Task 1a output — fill Launch/Shutdown/resolversPropagation stubs)
    - internal/core/backend/failover.go (create new)
    - internal/core/appctx/boot.go (pickBackend — update to 3-arg NewAxiomBackend call; read FULL file first)
    - modules/axiom.sh lines 122-180 (axiom_launch — fleet launch v1 source)
    - modules/axiom.sh lines 183-197 (axiom_shutdown — fleet teardown v1 source)
    - modules/axiom.sh lines 55-120 (resolvers_update — resolver propagation v1 source)
    - internal/core/errors/errors.go (AxiomFailure type for FailoverBackend key)
  </read_first>
  <behavior>
    - Test 1: FailoverBackend.Exec calls Primary.Exec; on *AxiomFailure increments failures; calls Fallback.Exec; returns Fallback result
    - Test 2: After FailoverThreshold consecutive *AxiomFailure from Primary, killSwitch flips; subsequent Exec calls skip Primary entirely
    - Test 3: FailoverBackend.Stream on Primary *AxiomFailure: partial-drain goroutine uses select on primaryCh and ctx.Done() so a hung channel does not leak (W4 fix); falls back to Fallback.Stream; no double-emit
    - Test 4: FailoverBackend.Capacity returns Primary.Capacity() when killSwitch=false; Fallback.Capacity() when true
    - Test 5: AxiomBackend.Launch calls resolversPropagation and axiom-select
    - Test 6: FailoverBackend.Stream partial-drain goroutine exits when ctx is cancelled (no goroutine leak — verified by goleak or timeout test)
  </behavior>
  <action>
    Fill Launch/Shutdown/resolversPropagation stubs in `internal/core/backend/axiom.go`:

    Launch(ctx): list existing fleet (axiom-ls); provision missing nodes (axiom-fleet2); axiom-select FleetName; call resolversPropagation(ctx).
    Shutdown(ctx): run axiom-rm FleetName; log completion.
    resolversPropagation(ctx): upload cfg.Advanced.Tools.Axiom.ResolversPath + ResolversTrustedPath to fleet nodes via axiom-exec (mirrors v1 resolvers_update from modules/axiom.sh:55-120).

    Create `internal/core/backend/failover.go`:
      FailoverBackend{Primary Backend; Fallback Backend; Threshold int; mu sync.Mutex; failures int; killSwitch bool}

      Exec: if killSwitch → Fallback.Exec directly.
        Else: try Primary.Exec; on *AxiomFailure → mu.Lock(); failures++; if failures >= Threshold { killSwitch=true }; mu.Unlock(); return Fallback.Exec.
        Non-AxiomFailure errors: propagate without fallback.
        On Primary success: mu.Lock(); failures=0; mu.Unlock().

      Stream: if killSwitch → Fallback.Stream directly.
        Else: primaryCh, err := Primary.Stream(ctx, t, args)
          if error is *AxiomFailure:
            Spawn partial-drain goroutine with ctx-bounded select (W4 fix):
              go func() {
                for {
                  select {
                  case _, ok := <-primaryCh:
                    if !ok { return }  // channel closed — goroutine exits cleanly
                  case <-ctx.Done():
                    return  // context cancelled — goroutine exits, no leak
                  }
                }
              }()
            return Fallback.Stream(ctx, t, args)
          else: return primaryCh, nil (primary succeeded — use its channel)
        NOTE: The ctx-bounded select ensures a hung primary channel (e.g. fleet timeout) cannot
        leak a goroutine. The ctx passed here is the task-level context; it will be cancelled by
        the Scheduler when the task completes or times out.

      HealthCheck: Primary.HealthCheck; on *AxiomFailure → Fallback.HealthCheck.
      Capacity: if killSwitch → Fallback.Capacity(); else Primary.Capacity().

    Update `internal/core/appctx/boot.go` pickBackend function:
      Change: `return backend.NewAxiomBackend()`
      To: `return backend.NewAxiomBackend(cfg, backend.Default, logger)`
      This requires adding logger *slog.Logger parameter to pickBackend — update its call site in Boot() accordingly.
      VERIFY: Boot() signature itself does NOT change (still 6 args).
  </action>
  <verify>
    <automated>go test -race ./internal/core/backend/... -run "TestFailover|TestAxiomLaunch" -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race ./internal/core/backend/... -run "TestFailover"` exits 0; all failover tests pass
    - `grep "killSwitch" internal/core/backend/failover.go` returns matches
    - `grep "ctx\.Done()" internal/core/backend/failover.go` returns match in Stream partial-drain goroutine (W4 fix — ctx-bounded)
    - `grep "select" internal/core/backend/failover.go` returns match in partial-drain goroutine (W4 fix — select not plain range)
    - `grep "FailoverThreshold" internal/core/backend/axiom.go` returns no matches (threshold comes from FailoverBackend.Threshold, set in newSubsCmd from cfg.Axiom.FailoverThreshold)
    - `grep "resolversPropagation\|resolvers_update" internal/core/backend/axiom.go` returns matches (AXIOM-05)
    - `grep "NewAxiomBackend.*cfg.*reg.*log\|NewAxiomBackend(cfg, " internal/core/appctx/boot.go` returns match (3-arg call)
    - `go build -o /dev/null ./...` exits 0
  </acceptance_criteria>
  <done>Fleet lifecycle complete; FailoverBackend with ctx-bounded stream drain (W4 fix — no goroutine leak); boot.go pickBackend updated to 3-arg NewAxiomBackend; build green.</done>
</task>

<task type="auto">
  <name>Task 2: newSubsCmd RunE — sequential 5-stage RunStage + filterByModuleAndEnabled + sched.Checkpoint wiring + takeover staging merge</name>
  <read_first>
    - cmd/reconftw/stub_subcommands.go (current stub — replace newSubsCmd RunE only)
    - cmd/reconftw/appctx_init.go (create or extend with filterByModuleAndEnabled)
    - internal/core/appctx/boot.go (CONFIRMED 6-arg Boot signature — read before writing)
    - internal/core/scheduler/scheduler.go (RunStage — FIRES ALL TASKS CONCURRENTLY within one call; stage ordering requires sequential calls; Checkpoint field exists)
    - internal/core/task/task.go (Task.Enabled(cfg) — called by filterByModuleAndEnabled)
    - internal/modules/subdomains/merge.go (MergeStage function — called after each stage)
    - modules/axiom.sh lines 45-54 (axiom CLI detection — AXIOM flag condition)
  </read_first>
  <action>
    In `cmd/reconftw/stub_subcommands.go`, replace `newSubsCmd()` RunE body:

    1. Load config: cfg, err := config.Load(cliOverrides); if err { return err }
    2. Build target: target, err := appctx.NewTarget(cfg, domainFlag); if err { return err }
    3. Construct logger: logger := log.New(cfg.AsLoggerConfig(), redactor)
    4. Construct Scheduler BEFORE Boot (cycle-break):
         sched := scheduler.NewScheduler(cfg.Concurrency.MaxJobs, cfg.Concurrency.HeartbeatSeconds, nil, logger)
         NOTE: checkpoint passed as nil here — cycle-break; set AFTER Boot in step 6b.
    5. Choose backend:
         var chosenBackend backend.Backend
         if cfg.Axiom.Enabled {
           axiomBE := backend.NewAxiomBackend(cfg, backend.Default, logger)
           localBE := backend.NewLocalBackend(time.Duration(cfg.Concurrency.KillGraceSeconds)*time.Second)
           chosenBackend = &backend.FailoverBackend{Primary: axiomBE, Fallback: localBE, Threshold: cfg.Axiom.FailoverThreshold}
         }
         // nil → Boot.pickBackend selects LocalBackend
    6. Call Boot:
         app, err := appctx.Boot(cmd.Context(), logger, cfg, target, sched, appctx.BootOptions{Backend: chosenBackend})
         if err { return err }
    6b. Wire checkpoint AFTER Boot returns (B3 fix — sched.Checkpoint was nil at construction):
         sched.Checkpoint = app.Checkpoint
         NOTE: This is analogous to the RunTask cycle-break in step 7. Without this line,
         sched.Checkpoint remains nil and runOne skips all checkpoint integration silently,
         violating D-07 per-tool resume semantics.
    7. Set RunTask closure (cycle-break — MUST happen AFTER Boot returns):
         sched.RunTask = func(ctx context.Context, t task.Task) (task.Result, error) {
             return t.Run(ctx, app)
         }
    8. If cfg.Axiom.Enabled:
         axiomBE := chosenBackend.(*backend.FailoverBackend).Primary.(*backend.AxiomBackend)
         if err := axiomBE.Launch(cmd.Context()); err != nil { return err }
         defer axiomBE.Shutdown(context.Background())
    9. allTasks, err := task.Default.Build(); if err { return err }
   10. SEQUENTIAL STAGE EXECUTION (5 RunStage calls — fixes REVIEWS Scheduler-ordering finding):
         stageNames := []string{"passive", "resolve", "permut", "enrichment"}
         stagePrefixes := map[string][]string{
           "passive":     {"subdomains.passive."},
           "resolve":     {"subdomains.active", "subdomains.tls", "subdomains.noerror", "subdomains.dns", "subdomains.srv", "subdomains.ptr", "subdomains.brute", "subdomains.resolvers.", "subdomains.scraping", "subdomains.analytics", "subdomains.ns_delegation"},
           "permut":      {"subdomains.permut", "subdomains.recursive.", "subdomains.permut.dnscewl"},
           "enrichment":  {"subdomains.takeover.", "subdomains.buckets", "subdomains.asn", "subdomains.geo", "subdomains.zonetransfer"},
         }
         for _, stage := range stageNames {
           prefixes := stagePrefixes[stage]
           stageSlice := filterByModuleAndEnabled(allTasks, "subdomains", cfg, prefixes)
           if err := sched.RunStage(cmd.Context(), "subdomains", stageSlice); err != nil { return err }
           if stage == "enrichment" {
             // B2 fix: read both takeover staging files and call app.Tree.Append("findings") once
             if err := mergeTakeoverFindings(cmd.Context(), app); err != nil {
               app.Log.Warn("takeover_merge_failed", "err", err)
             }
           }
           if err := subdomains.MergeStage(cmd.Context(), app, stage); err != nil {
             app.Log.Warn("merge_stage_failed", "stage", stage, "err", err)
             // non-fatal: continue with best-effort merged set
           }
         }
   11. Return nil

    Add helper function mergeTakeoverFindings(ctx, app) error to appctx_init.go or stub_subcommands.go:
      Read filepath.Join(app.Target.WorkDir, "inputs", "takeover.subzy.jsonl") — empty/missing is OK
      Read filepath.Join(app.Target.WorkDir, "inputs", "takeover.dnstake.jsonl") — empty/missing is OK
      Concatenate both files' lines (deduplication optional — TakeoverRecord has enough fields to dedup by host+service)
      Call app.Tree.Append("findings", mergedLines) ONCE
      Return any Append error (out-of-scope findings are logged at Debug, not returned as error)

    In `cmd/reconftw/appctx_init.go`:
      func filterByModuleAndEnabled(tasks []task.Task, module string, cfg *config.Config, prefixes []string) []task.Task:
        Result: tasks where t.Module() == module AND t.Enabled(cfg) == true AND any prefix in prefixes is a prefix of t.Name()
        NOTE: calling t.Enabled(cfg) here is the REVIEWS finding #4 fix — Scheduler never calls it; we must.

    Ensure all other stub subcommands (web, vulns, recon...) still exit 64.
  </action>
  <verify>
    <automated>go build -o /tmp/reconftw-p4 ./cmd/reconftw && /tmp/reconftw-p4 subs --help 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `go build -o /tmp/reconftw-p4 ./cmd/reconftw` exits 0
    - `/tmp/reconftw-p4 subs --help` outputs help text (not stub error)
    - `grep -c "RunStage" cmd/reconftw/stub_subcommands.go` outputs at least 4 (called once per stage)
    - `grep "MergeStage" cmd/reconftw/stub_subcommands.go` returns matches (called after each stage)
    - `grep "Enabled(cfg)" cmd/reconftw/appctx_init.go` returns match (filterByModuleAndEnabled calls t.Enabled)
    - `grep "BootOptions{" cmd/reconftw/stub_subcommands.go` returns match
    - `grep "sched.RunTask" cmd/reconftw/stub_subcommands.go` returns match (RunTask closure wired post-Boot)
    - `grep "sched.Checkpoint = app.Checkpoint" cmd/reconftw/stub_subcommands.go` returns match (B3 fix)
    - `grep "filterByModuleAndEnabled" cmd/reconftw/appctx_init.go` returns match
    - `grep "mergeTakeoverFindings\|takeover\.subzy\.jsonl\|takeover\.dnstake\.jsonl" cmd/reconftw/stub_subcommands.go` returns matches (B2 fix)
    - `/tmp/reconftw-p4 web --target x` exits 64 (other stubs unaffected)
  </acceptance_criteria>
  <done>newSubsCmd RunE: sequential 5-stage RunStage + filterByModuleAndEnabled calling t.Enabled(cfg) + sched.Checkpoint = app.Checkpoint post-Boot (B3 fix) + mergeTakeoverFindings single Append call (B2 fix); FailoverBackend injection via BootOptions; all AXIOM-01 through AXIOM-09 DAG in place; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSH known_hosts | AxiomBackend.repairKnownHosts reads/modifies ~/.ssh/known_hosts |
| Fleet node → axiom-scan output | Remote fleet returns untrusted data; flows through scope filter at MergeStage/Append |
| AxiomBackend SSH stderr → log | SSH error output may contain hostnames/IPs from fleet; must be redacted |
| cfg.Axiom.* → AxiomBackend init | Fleet config derived from TOML; validated at config load time |
| FailoverBackend kill-switch | Threshold=1 would make axiom fail-permanently on first error; must be > 0 |
| Takeover staging files → app.Tree.Append | mergeTakeoverFindings reads attacker-influenced staging content; scope filter at Append boundary rejects out-of-scope entries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-01 | Tampering | AxiomBackend.repairKnownHosts modifies ~/.ssh/known_hosts | mitigate | Only removes exact offending host key via ssh-keygen -R; only when AutoFixHostkey=true (explicit opt-in); operation logged at Info |
| T-04-06-02 | Spoofing | Malicious fleet node returning injected subdomains | mitigate | All axiom-scan output flows through MergeStage → OutputTree.Append scope filter; injected out-of-scope lines rejected with *OutOfScope |
| T-04-06-03 | Information Disclosure | Fleet SSH stderr containing internal IPs in log | mitigate | Register fleet node IPs/hostnames as redacted secrets in app.Log before logging SSH stderr |
| T-04-06-04 | Denial of Service | FailoverThreshold=1 immediate kill-switch | accept | validate:"min=1" enforced; threshold=1 is valid emergency mode; default=3 (cfg.Axiom.FailoverThreshold) |
| T-04-06-05 | Injection | Tool.InputFlag="injected" in tools.lock | mitigate | InputFlag values are build-time constants in tools.lock (version-controlled); executor validates InputFlag matches `^(-[a-zA-Z-]+)?$` before use |
| T-04-06-06 | Denial of Service | FailoverBackend.Stream goroutine leak on hung primary | mitigate | Partial-drain goroutine uses select on primaryCh and ctx.Done() — goroutine exits when context cancelled or channel closed (W4 fix) |
</threat_model>

<verification>
- `go test -race ./internal/core/backend/... -run "TestFailover|TestAxiom"` exits 0
- `go build -o /tmp/reconftw-p4 ./cmd/reconftw` exits 0
- `/tmp/reconftw-p4 subs --help` shows real help
- `grep "ErrAxiomNotImplemented" internal/core/backend/axiom.go` returns no matches
- `grep "InputFlag" internal/core/backend/axiom.go` returns match in Exec (Tool.InputFlag used)
- `grep -c "RunStage" cmd/reconftw/stub_subcommands.go` >= 4 (sequential stages)
- `grep "Enabled(cfg)" cmd/reconftw/appctx_init.go` returns match
- `grep "NewAxiomBackend.*cfg" internal/core/appctx/boot.go` returns match (3-arg call)
- `grep "sched.Checkpoint = app.Checkpoint" cmd/reconftw/stub_subcommands.go` returns match (B3 fix)
- `grep "ctx\.Done()" internal/core/backend/failover.go` returns match in Stream (W4 fix)
- `grep "mergeTakeoverFindings" cmd/reconftw/stub_subcommands.go` returns match (B2 fix)
</verification>

<success_criteria>
- Real AxiomBackend with Tool.InputFlag-based split (brittle heuristic replaced) — REVIEWS finding #5 fixed
- FailoverBackend with ctx-bounded stream drain (no goroutine leak on hung primary) — W4 fix
- Sequential 5-stage RunStage pattern fixes REVIEWS finding #1 (Scheduler DependsOn ordering)
- filterByModuleAndEnabled calls t.Enabled(cfg) — REVIEWS finding #4 fixed
- sched.Checkpoint = app.Checkpoint set after Boot (B3 fix — per-tool resume operational)
- mergeTakeoverFindings single Append call after enrichment stage (B2 fix — single-writer invariant)
- boot.go pickBackend updated to 3-arg NewAxiomBackend — REVIEWS MEDIUM finding fixed
- `go test -race ./internal/core/backend/...` exits 0
</success_criteria>

<output>
Create `.planning/phases/04-subdomains-e2e-axiom-integration/04-06-SUMMARY.md` when done.
</output>
