---
phase: 04-subdomains-e2e-axiom-integration
plan: 04
type: execute
wave: 3
depends_on:
  - 04-02
  - 04-03
files_modified:
  - internal/modules/subdomains/permut.go
  - internal/modules/subdomains/recursive.go
  - internal/modules/subdomains/sysinfo/sysinfo.go
autonomous: true
requirements:
  - SUBD-03

must_haves:
  truths:
    - "SubPermutTask (subdomains.permut), SubRegexPermutTask (subdomains.permut.regex), SubIAPermutTask (subdomains.permut.ia) register with real OS memory back-pressure gate via sysinfo.MemAvailable()"
    - "SubDNSCewlTask (subdomains.permut.dnscewl) registers as the third SUBD-03 permutation tool named in D-01 (gotator + regulator + dnscewl)"
    - "Memory gate uses gopsutil/mem.VirtualMemory().Available (NOT runtime.ReadMemStats m.Sys-m.Alloc) for real OS free memory"
    - "Memory back-pressure behavior: chunk/wait until memory is available (NOT StatusSkipped/drop) unless cfg.Subdomains.Permut.MinFreeMemGB == 0 (disabled) OR context is cancelled"
    - "SubRecursivePassiveTask (subdomains.recursive.passive) and SubRecursiveBruteTask (subdomains.recursive.brute) register"
    - "SubIAPermutTask reads Internet Archive (IA) wordlist for permutations per v1 sub_ia_permut"
    - "task.Default.Build() exits 0 after all 6 Tasks registered (gotator + regex + dnscewl + ia + recursive-passive + recursive-brute; no forward DependsOn on unregistered names)"
    - "No barrier Task registered (sequential RunStage in command layer replaces barriers)"
    - "All permut/recursive Tasks write to staging files (inputs/permut.*.txt, inputs/recursive.*.txt)"
  artifacts:
    - path: "internal/modules/subdomains/permut.go"
      provides: "SubPermutTask, SubRegexPermutTask, SubDNSCewlTask, SubIAPermutTask"
      contains: "subdomains.permut, subdomains.permut.regex, subdomains.permut.dnscewl, subdomains.permut.ia"
    - path: "internal/modules/subdomains/recursive.go"
      provides: "SubRecursivePassiveTask, SubRecursiveBruteTask"
      contains: "subdomains.recursive.passive, subdomains.recursive.brute"
    - path: "internal/modules/subdomains/sysinfo/sysinfo.go"
      provides: "Injectable system memory provider (MemAvailable() uint64)"
      exports: ["MemProvider", "OSMemProvider", "MemAvailable"]
  key_links:
    - from: "internal/modules/subdomains/permut.go"
      to: "internal/modules/subdomains/sysinfo/sysinfo.go"
      via: "sysinfo.MemAvailable() for back-pressure threshold check"
      pattern: "sysinfo\\.MemAvailable"
    - from: "internal/modules/subdomains/permut.go"
      to: "inputs/resolved.merged.txt"
      via: "reads MergeStage-produced merged file as gotator/regulator/dnscewl input"
      pattern: "resolved\\.merged"
---

<objective>
Port the permutation stage (sub_permut + sub_regex_permut + sub_ia_permut) with CORRECT OS memory back-pressure (gopsutil/mem, not runtime.ReadMemStats), add SubDNSCewlTask for the third D-01/SUBD-03 permutation tool, and recursive enumeration (sub_recursive_passive + sub_recursive_brute). Use staging files. No barrier Tasks.

Purpose: Permutations are the heaviest stage (largest wordlist growth) and require SUBD-03 memory back-pressure. The REVIEWS finding confirmed runtime.ReadMemStats is wrong (Go heap metrics ≠ OS free RAM). The fix uses gopsutil/mem.VirtualMemory().Available via an injectable MemProvider interface. Chunk/wait behavior prevents silent permutation drops that would violate D-01 parity. dnscewl (DNS permutation wordlist generator) is the third permutation tool named in CONTEXT.md D-01 alongside gotator and regulator — it generates DNS-based permutations from the resolved subdomain list.
</objective>

<execution_context>
@/Users/six2dez/Tools/reconftw/.claude/get-shit-done/workflows/execute-plan.md
@/Users/six2dez/Tools/reconftw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/six2dez/Tools/reconftw/.planning/ROADMAP.md
@/Users/six2dez/Tools/reconftw/.planning/phases/04-subdomains-e2e-axiom-integration/04-CONTEXT.md

<interfaces>
From internal/core/config/config.go (EXACT fields after plan-00):
  cfg.Subdomains.Permut.Enabled bool
  cfg.Subdomains.Permut.LimitBytes int64      — max permutation output size (cfg.Subdomains.Permut.LimitBytes)
  cfg.Subdomains.Permut.IAEnabled bool
  cfg.Subdomains.Permut.RegexEnabled bool
  cfg.Subdomains.Permut.MinFreeMemGB int      (added in plan-00) — 0 = disable gate
  cfg.Subdomains.Recursive.PassiveEnabled bool
  cfg.Subdomains.Recursive.PassiveDepth int
  cfg.Subdomains.Recursive.BruteEnabled bool

From internal/modules/subdomains/merge.go (plan-02 output):
  MergeStage writes inputs/resolved.merged.txt — permutation Tasks read this file as their domain list input.
  MergeStage writes inputs/passive.merged.txt — recursive Tasks read this for passive recursive input.

From internal/core/task/topo.go (confirmed from source):
  Sort returns *coreerrors.ConfigError for any DependsOn target not registered at Build() time.
  Consequence: permut.go and recursive.go must NOT declare DependsOn on each other's tasks
  since they're in the same wave (Build() may see only one of them depending on import order).
  All tasks in this plan have DependsOn: nil — sequential RunStage ordering handles sequencing.

NOTE on memory back-pressure:
  runtime.ReadMemStats m.Sys-m.Alloc = Go runtime heap reservation delta — NOT OS free memory.
  Correct approach: github.com/shirou/gopsutil/v3/mem VirtualMemory().Available = OS MemAvailable.
  Use an injectable MemProvider interface for testability.

NOTE on dnscewl (B1 fix):
  dnscewl is a DNS permutation wordlist generator. It takes a list of subdomains (-f flag per
  tools.lock input_flag="-f") and generates permutation candidates. The go_module path
  "github.com/codingo/dnscewl" is noted with install-ambiguity in tools.lock — executor must
  verify before running go install. The tool call pattern is:
    app.Tools.Run(ctx, "dnscewl", ["-f", resolvedMergedFile, "-r", cfg.Paths.Resolvers])
  Output is one subdomain per line written to inputs/permut.dnscewl.txt staging file.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: sysinfo.MemProvider interface + gopsutil OS memory provider</name>
  <read_first>
    - internal/modules/subdomains/sysinfo/ (create new package)
    - go.mod (check if gopsutil is already a dependency; if not, add github.com/shirou/gopsutil/v3)
  </read_first>
  <behavior>
    - Test 1: OSMemProvider.Available() returns a value > 0 on the test host (integration smoke)
    - Test 2: a stub MemProvider returning 0 correctly simulates "no memory available"
    - Test 3: MemProvider interface is satisfied by both OSMemProvider and stub implementations
  </behavior>
  <action>
    Create `internal/modules/subdomains/sysinfo/sysinfo.go`:

    MemProvider interface { Available() uint64 } — returns available OS memory in bytes.

    OSMemProvider struct{} implements MemProvider:
      Available() uint64: call mem.VirtualMemory() from github.com/shirou/gopsutil/v3/mem; return vmStat.Available.
      On error: log warning, return 0 (conservative — back-pressure kicks in on error).

    Exported function MemAvailable() uint64 — calls OSMemProvider{}.Available() as the default production path.

    If gopsutil is not yet in go.mod, run `go get github.com/shirou/gopsutil/v3/mem` to add it.
    On Linux: uses /proc/meminfo MemAvailable. On macOS: uses vm_stat. Both are correct OS metrics.

    Write unit test sysinfo_test.go covering behavior cases above.
  </action>
  <verify>
    <automated>go test -race ./internal/modules/subdomains/sysinfo/... -v 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race ./internal/modules/subdomains/sysinfo/...` exits 0
    - `grep "VirtualMemory\|gopsutil" internal/modules/subdomains/sysinfo/sysinfo.go` returns match
    - `grep "ReadMemStats\|m\.Sys\|m\.Alloc" internal/modules/subdomains/sysinfo/sysinfo.go` returns no matches (NOT using runtime heap metrics)
    - `go build -o /dev/null ./...` exits 0
  </acceptance_criteria>
  <done>MemProvider interface + OSMemProvider using gopsutil created; runtime.ReadMemStats approach NOT used; tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SubPermutTask + SubRegexPermutTask + SubDNSCewlTask + SubIAPermutTask + SubRecursivePassiveTask + SubRecursiveBruteTask</name>
  <read_first>
    - internal/modules/subdomains/permut.go (create new)
    - internal/modules/subdomains/recursive.go (create new)
    - internal/modules/subdomains/sysinfo/sysinfo.go (Task 1 output)
    - modules/subdomains.sh lines 550-680 (sub_permut, sub_regex_permut, sub_ia_permut — v1 source)
    - modules/subdomains.sh lines 690-760 (sub_recursive_passive, sub_recursive_brute — v1 source)
    - internal/core/config/config.go (SubPermut, SubRecursive fields — EXACT names)
    - internal/core/appctx/appctx.go (Target.WorkDir)
  </read_first>
  <behavior>
    - Test 1: SubPermutTask.Run with MemProvider returning 0 bytes waits (polls every 5s up to cfg timeout) and then returns StatusSkipped if context cancelled — does NOT silently drop
    - Test 2: SubPermutTask.Run with MemProvider returning (MinFreeMemGB+1)*GB proceeds to run gotator
    - Test 3: SubPermutTask writes inputs/permut.gotator.txt staging file (does NOT call app.Tree.Append)
    - Test 4: SubDNSCewlTask.Run invokes dnscewl with -f resolvedMergedFile and writes inputs/permut.dnscewl.txt (does NOT call app.Tree.Append)
    - Test 5: SubDNSCewlTask.Enabled returns false when cfg.Subdomains.Permut.Enabled == false
    - Test 6: SubIAPermutTask.Enabled returns false when cfg.Subdomains.Permut.IAEnabled == false
    - Test 7: SubRecursivePassiveTask.Run makes subfinder calls for each subdomain up to PassiveDepth levels
    - Test 8: task.Default.Build() exits 0 after all 6 Tasks registered (no unregistered DependsOn)
  </behavior>
  <action>
    Create `internal/modules/subdomains/permut.go`:

    SubPermutTask (name "subdomains.permut"):
      Enabled: cfg.Subdomains.Permut.Enabled
      DependsOn: nil (sequential RunStage; command layer calls permut stage after resolved MergeStage)
      Run:
        memCheck(ctx, app, sysinfo.OSMemProvider{}, cfg.Subdomains.Permut.MinFreeMemGB) — wait loop:
          for mem < threshold: if ctx.Done() → return StatusSkipped; sleep 5s; recheck
        inputFile = inputs/resolved.merged.txt (written by MergeStage("resolved"))
        app.Tools.Stream(ctx, "gotator", ["-sub", inputFile, "-depth", "1", "-numbers", "3", ...])
        Write output to inputs/permut.gotator.txt
        Return Result{Status: StatusDone, Outputs: [stagingFilePath]}

    SubRegexPermutTask (name "subdomains.permut.regex"):
      Enabled: cfg.Subdomains.Permut.Enabled AND cfg.Subdomains.Permut.RegexEnabled
      DependsOn: nil
      Run: same memCheck; app.Tools.Run(ctx, "regulator", [inputFile, domain]); write inputs/permut.regex.txt

    SubDNSCewlTask (name "subdomains.permut.dnscewl"):
      Enabled: cfg.Subdomains.Permut.Enabled
      DependsOn: nil
      Run:
        same memCheck (cfg.Subdomains.Permut.MinFreeMemGB)
        inputFile = inputs/resolved.merged.txt (written by MergeStage("resolved"))
        app.Tools.Run(ctx, "dnscewl", ["-f", inputFile, "-r", cfg.Paths.Resolvers])
        Write stdout (one permuted subdomain per line) to inputs/permut.dnscewl.txt
        Return Result{Status: StatusDone, Outputs: [stagingFilePath], Stats: {"dnscewl_candidates": len(lines)}}
      NOTE: dnscewl generates DNS-based permutations from the resolved list. Input flag is "-f" per
      tools.lock entry (plan-00). If dnscewl binary not found (non-critical per tools.lock), return
      Result{Status: StatusSkipped} and log at Info level — do NOT error.

    SubIAPermutTask (name "subdomains.permut.ia"):
      Enabled: cfg.Subdomains.Permut.Enabled AND cfg.Subdomains.Permut.IAEnabled
      DependsOn: nil
      Run: same memCheck; fetch IA wordlist (or use cached); run gotator/puredns with IA wordlist; write inputs/permut.ia.txt

    memCheck helper function: takes ctx, app, MemProvider, minGB int; if minGB == 0 returns immediately (gate disabled); otherwise polls until available > minGB*GB or ctx cancelled.

    func init() { task.Register(SubPermutTask{}); task.Register(SubRegexPermutTask{}); task.Register(SubDNSCewlTask{}); task.Register(SubIAPermutTask{}) }

    Create `internal/modules/subdomains/recursive.go`:

    SubRecursivePassiveTask (name "subdomains.recursive.passive"):
      Enabled: cfg.Subdomains.Recursive.PassiveEnabled
      DependsOn: nil (command layer sequences recursive after permut MergeStage)
      Run: for each subdomain in inputs/resolved.merged.txt (up to PassiveDepth levels): run subfinder on sub; accumulate results; write inputs/recursive.passive.txt

    SubRecursiveBruteTask (name "subdomains.recursive.brute"):
      Enabled: cfg.Subdomains.Recursive.BruteEnabled
      DependsOn: nil
      Run: for each resolved subdomain, run puredns bruteforce (short wordlist); write inputs/recursive.brute.txt

    func init() { task.Register(SubRecursivePassiveTask{}); task.Register(SubRecursiveBruteTask{}) }

    All Tasks: no app.Tree.Append calls; no barrier Tasks; no runtime.ReadMemStats.

    Also update plan-06's stage-4 prefix list (stagePrefixes["permut"]) to include "subdomains.permut.dnscewl" — the command layer must include SubDNSCewlTask in the permut RunStage slice.
  </action>
  <verify>
    <automated>go test -race ./internal/modules/subdomains/... -run "TestPermut|TestRecursive" -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "ReadMemStats\|m\.Sys\|m\.Alloc" internal/modules/subdomains/permut.go` returns no matches (gopsutil fix)
    - `grep "sysinfo\." internal/modules/subdomains/permut.go` returns match (correct memory provider used)
    - `grep "StatusSkipped" internal/modules/subdomains/permut.go` returns match only for context-cancelled case (not for normal low-memory — that waits/chunks)
    - `grep "SubDNSCewlTask\|dnscewl" internal/modules/subdomains/permut.go` returns match (B1 fix — third SUBD-03 permut tool)
    - `grep "task\.Register.*SubDNSCewlTask\|task\.Register.*DNSCewl" internal/modules/subdomains/permut.go` returns match (registered in init())
    - `grep "inputs/permut\.dnscewl\.txt" internal/modules/subdomains/permut.go` returns match (staging file written)
    - `grep "Tree\.Append" internal/modules/subdomains/permut.go internal/modules/subdomains/recursive.go` returns no matches (staging contract)
    - `grep "barrier" internal/modules/subdomains/permut.go internal/modules/subdomains/recursive.go` returns no matches
    - `go test -race ./internal/modules/subdomains/... -run "TestPermut"` exits 0
    - `go build -o /dev/null ./...` exits 0
  </acceptance_criteria>
  <done>6 permut/recursive Tasks (gotator + regex + dnscewl + ia + recursive-passive + recursive-brute) with correct OS memory back-pressure; staging-file writes; no barrier Tasks; no runtime.ReadMemStats; SUBD-03 fully covered (all three named tools: gotator + regulator + dnscewl); build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inputs/resolved.merged.txt → gotator/regulator/dnscewl input | Merged resolved hostnames fed as wordlist seed to permutation tools |
| gopsutil/mem syscall → memory gate decision | OS memory query used for back-pressure; wrong value could over-suppress permutations |
| IA wordlist fetch → permutation input | Internet Archive wordlist is fetched from external source; should be cached and validated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Denial of Service | Memory gate with MinFreeMemGB too high | accept | User-configured; default 1GB is conservative; setting to 0 disables gate; documented behavior |
| T-04-04-02 | Injection | gotator/dnscewl -f/-sub flag with merged.txt content | mitigate | merged.txt is written by MergeStage from scope-filtered OutputTree.Append output; hostnames validated by scope filter before reaching merged.txt |
| T-04-04-03 | Information Disclosure | IA wordlist fetch exposes target domain | mitigate | IA query uses target domain label — accepted reconnaissance activity; operator opt-in via cfg.Subdomains.Permut.IAEnabled |
| T-04-04-04 | Tampering | gopsutil dependency supply chain | mitigate | gopsutil is well-established (github.com/shirou/gopsutil, widely used in Go ecosystem); Phase 11 adds pin to tools.lock; executor must verify module path before running go get |
| T-04-04-05 | Tampering | dnscewl go_module path install-ambiguity | mitigate | tools.lock entry annotated with install-ambiguity warning; executor must verify github.com/codingo/dnscewl on pkg.go.dev before go install; non-critical=false so missing binary returns StatusSkipped gracefully |
</threat_model>

<verification>
- `go test -race ./internal/modules/subdomains/sysinfo/...` exits 0
- `go test -race ./internal/modules/subdomains/... -run "TestPermut|TestRecursive"` exits 0
- `grep "ReadMemStats" internal/modules/subdomains/permut.go` returns no matches
- `grep "gopsutil\|sysinfo" internal/modules/subdomains/permut.go` returns matches
- `grep "SubDNSCewlTask\|dnscewl" internal/modules/subdomains/permut.go` returns matches (B1 fix)
- `grep "Tree\.Append" internal/modules/subdomains/permut.go internal/modules/subdomains/recursive.go` returns no matches
- `go build -o /dev/null ./...` exits 0
</verification>

<success_criteria>
- gopsutil-based OS memory provider (not runtime.ReadMemStats) — REVIEWS finding #memory fixed
- Chunk/wait behavior (not silent StatusSkipped drop) — parity preservation for SUBD-11
- 6 permutation + recursive Tasks using staging files, including SubDNSCewlTask (B1 fix — SUBD-03 all three named tools covered)
- No barrier Tasks; no DependsOn forward references
- `go test -race ./internal/modules/subdomains/...` exits 0
</success_criteria>

<output>
Create `.planning/phases/04-subdomains-e2e-axiom-integration/04-04-SUMMARY.md` when done.
</output>
