---
phase: 04-subdomains-e2e-axiom-integration
plan: 04
type: execute
wave: 3
depends_on:
  - 04-02
  - 04-03
files_modified:
  - internal/modules/subdomains/permut.go
  - internal/modules/subdomains/recursive.go
autonomous: true
requirements:
  - SUBD-03

must_haves:
  truths:
    - "SubPermutTask (subdomains.permut), SubRegexPermutTask (subdomains.permut.regex), SubIAPermutTask (subdomains.permut.ia) register with memory back-pressure gate"
    - "SubRecursivePassiveTask (subdomains.recursive.passive) and SubRecursiveBruteTask (subdomains.recursive.brute) register with DependsOn on active+passive barriers"
    - "resolvedBarrierTask (subdomains.barrier.resolved) registers as anchor for plan-05 Tasks"
    - "Memory back-pressure: gotator/regulator/dnscewl skip with StatusSkipped + structured log when free RAM < cfg.Subdomains.Permut.MinFreeMemGB"
    - "SubIAPermutTask reads IA (Internet Archive) wordlist for permutations per v1 sub_ia_permut"
    - "task.Default.Build() exits 0 (no ConfigError) after Task 1 completes, before Task 2 is run"
  artifacts:
    - path: "internal/modules/subdomains/permut.go"
      provides: "SubPermutTask, SubRegexPermutTask, SubIAPermutTask + resolvedBarrierTask"
      contains: "subdomains.permut, subdomains.permut.regex, subdomains.permut.ia, subdomains.barrier.resolved"
    - path: "internal/modules/subdomains/recursive.go"
      provides: "SubRecursivePassiveTask, SubRecursiveBruteTask"
      contains: "subdomains.recursive.passive, subdomains.recursive.brute"
  key_links:
    - from: "internal/modules/subdomains/permut.go"
      to: "internal/modules/subdomains/resolve.go"
      via: "DependsOn: [\"subdomains.barrier.active\"]"
      pattern: "barrier\\.active"
    - from: "internal/modules/subdomains/permut.go"
      to: "runtime.ReadMemStats"
      via: "memory back-pressure gate per CONTEXT.md SUBD-03 discretion"
      pattern: "ReadMemStats"
---

<objective>
Port the permutation stage (sub_permut + sub_regex_permut + sub_ia_permut with memory back-pressure) and recursive enumeration stage (sub_recursive_passive + sub_recursive_brute). Create the resolvedBarrierTask that anchors takeover/buckets/ASN/geo/zone-transfer Tasks in plan-05.

Purpose: Permutations are the heaviest stage (largest wordlist growth) and require the SUBD-03 memory back-pressure gate. Recursive passive/brute are the count drivers for SUBD-11 parity (D-01 "sub_recursive_passive/sub_recursive_brute are material"). The resolvedBarrierTask completes the DAG structure.

Output: 5 permutation/recursive Tasks + resolvedBarrierTask registered; memory back-pressure in gotator/regulator/dnscewl; IA permut working; recursive stages declared.

IMPORTANT — task ordering and topo.Sort safety: resolvedBarrierTask (in permut.go) declares DependsOn on "subdomains.recursive.passive" and "subdomains.recursive.brute" which are not registered until Task 2 creates recursive.go. topo.Sort (internal/core/task/topo.go) returns *ConfigError for any unregistered DependsOn target. Therefore Task 1 MUST register stub (no-op Run) implementations of SubRecursivePassiveTask and SubRecursiveBruteTask in permut.go so that task.Default.Build() succeeds after Task 1. Task 2 then creates recursive.go which replaces those stubs with real implementations. The stubs in permut.go are deleted when recursive.go is written.
</objective>

<execution_context>
@/Users/six2dez/Tools/reconftw/.claude/get-shit-done/workflows/execute-plan.md
@/Users/six2dez/Tools/reconftw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/six2dez/Tools/reconftw/.planning/ROADMAP.md
@/Users/six2dez/Tools/reconftw/.planning/phases/04-subdomains-e2e-axiom-integration/04-CONTEXT.md
@/Users/six2dez/Tools/reconftw/.planning/phases/04-subdomains-e2e-axiom-integration/04-PATTERNS.md

<interfaces>
<!-- Phase 3 + Phase 4 Plans 01-03 output contracts -->

From internal/modules/subdomains/resolve.go (plan-02):
  activeBarrierTask.Name() == "subdomains.barrier.active"  — use in DependsOn

From internal/modules/subdomains/passive.go (plan-01):
  SubdomainRecord — use for output
  passiveBarrierTask.Name() == "subdomains.barrier.passive"

From internal/core/task/topo.go (confirmed from source):
  Sort returns *coreerrors.ConfigError with Key="task.depends_on" if a DependsOn target name
  is not registered in the task map. This check fires at Build() time before any Task.Run.
  Consequence: resolvedBarrierTask's DependsOn on recursive task names requires those names
  to be registered (even as stubs) before Build() is called.

From .planning/phases/04-subdomains-e2e-axiom-integration/04-PATTERNS.md §Group 2:
  Memory back-pressure pattern:
    var m runtime.MemStats
    runtime.ReadMemStats(&m)
    freeGB := float64(m.Sys-m.Alloc) / (1 << 30)
    if freeGB < cfg.Subdomains.Permut.MinFreeMemGB {
        app.Log.Warn("permut_skipped_memory_pressure", ...)
        return task.Result{Status: task.StatusSkipped}, nil
    }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Permutation Tasks (SubPermutTask, SubRegexPermutTask, SubIAPermutTask) + resolvedBarrierTask + recursive stubs</name>
  <read_first>
    - modules/subdomains.sh lines 963-1062 (sub_permut — gotator invocation)
    - modules/subdomains.sh lines 1065-1147 (sub_regex_permut — regulator invocation)
    - modules/subdomains.sh lines 1383-1447 (sub_ia_permut — massdns IA wordlist permutations)
    - .planning/phases/04-subdomains-e2e-axiom-integration/04-PATTERNS.md §Group 2 (memory back-pressure gate pattern)
    - reconftw.cfg: PERMUTATIONS_LIMIT (2GB), GOTATOR_FLAGS, REGULATOR_FLAGS (config references)
    - internal/core/task/topo.go (Sort errors on unregistered DependsOn targets — stubs required)
  </read_first>
  <behavior>
    - Test 1: SubPermutTask.Name() == "subdomains.permut"
    - Test 2: SubPermutTask.DependsOn() contains "subdomains.barrier.active" and "subdomains.brute"
    - Test 3: SubPermutTask.Run returns StatusSkipped when runtime.ReadMemStats reports freeGB < MinFreeMemGB (mock MemStats or use cfg.Subdomains.Permut.MinFreeMemGB = 999.0 to force skip)
    - Test 4: SubRegexPermutTask.Name() == "subdomains.permut.regex"; DependsOn contains "subdomains.barrier.active"
    - Test 5: SubIAPermutTask.Name() == "subdomains.permut.ia"; uses massdns for IA permutation wordlist
    - Test 6: resolvedBarrierTask.Name() == "subdomains.barrier.resolved"; DependsOn contains all permut + active + recursive task names
    - Test 7: task.Default.Build() returns nil error after Task 1 completes (all DependsOn targets registered, including recursive stubs)
  </behavior>
  <action>
    Create `internal/modules/subdomains/permut.go` implementing 4 Tasks plus 2 stub Tasks:

    SubPermutTask (name: "subdomains.permut"):
      - Enabled: cfg.Subdomains.Permut.Enabled
      - DependsOn: ["subdomains.barrier.active", "subdomains.brute"]
      - Run: Memory back-pressure gate FIRST (per CONTEXT.md SUBD-03 discretion):
          var m runtime.MemStats
          runtime.ReadMemStats(&m)
          freeGB := float64(m.Sys-m.Alloc) / (1 << 30)
          if freeGB < cfg.Subdomains.Permut.MinFreeMemGB {
              app.Log.Warn("permut_skipped_memory_pressure", slog.Float64("free_gb", freeGB), slog.Float64("threshold_gb", cfg.Subdomains.Permut.MinFreeMemGB))
              return task.Result{Status: task.StatusSkipped}, nil
          }
        Then invoke gotator: app.Tools.Run(ctx, "gotator", ["-sub", subsFile, "-perm", permWordsFile, "-depth", strconv.Itoa(depth), "-numbers", strconv.Itoa(numbers), "-mindup", "-silent"])
        Where permWordsFile = cfg.Subdomains.Permut.PermutWords, depth = cfg.Subdomains.Permut.GotatorDepth (default 1), numbers = cfg.Subdomains.Permut.GotatorNumbers (default 3)
        subsFile = temp file written from current artefacts/subdomains.jsonl host list (extract Subdomain field from each JSONL line).
        Parse output lines as SubdomainRecord{Source: "gotator"} and append to app.Tree.Append("subdomains", ...).
        Stream for heartbeat if gotator output is large (use Stream if tool supports streaming, otherwise Exec).

    SubRegexPermutTask (name: "subdomains.permut.regex"):
      - Enabled: cfg.Subdomains.Permut.RegexEnabled
      - DependsOn: ["subdomains.barrier.active"]
      - Run: Memory back-pressure gate (same logic). Then invoke regulator (Python tool):
          app.Tools.Run(ctx, "regulator", ["-i", subsFile, "-o", outFile, "-d", domain])
          Parse outFile lines as SubdomainRecord{Source: "regulator"}.
        Note: regulator is a Python tool installed via uv; Runner.Run dispatches via ToolRegistry which has its exec path.

    SubIAPermutTask (name: "subdomains.permut.ia"):
      - Enabled: cfg.Subdomains.Permut.IAEnabled
      - DependsOn: ["subdomains.barrier.active"]
      - Run: Memory back-pressure gate. Then:
          Fetch IA wordlist: use massdns (or wget equivalent via system tool) to generate a permutation wordlist from Internet Archive URLs for the target domain.
          V1 logic (sub_ia_permut lines 1383-1447): calls `curl` or `waymore` to get IA URLs, extracts word components, builds a wordlist, then runs massdns to resolve.
          In v2: use app.Tools.Run(ctx, "massdns", args) for the resolution step.
          Parse resolved lines as SubdomainRecord{Source: "ia-permut"}.

    resolvedBarrierTask (name: "subdomains.barrier.resolved"):
      - DependsOn: ["subdomains.barrier.active", "subdomains.brute", "subdomains.permut", "subdomains.permut.regex", "subdomains.permut.ia", "subdomains.recursive.passive", "subdomains.recursive.brute"]
      - Run: no-op (barrier)

    STUB TASKS — register these in permut.go so task.Default.Build() succeeds after Task 1 (before recursive.go exists). They satisfy the topo.Sort requirement that all DependsOn targets be registered. Task 2 creates recursive.go which provides the real implementations and REMOVES these stubs from permut.go:

    subRecursivePassiveStub (name: "subdomains.recursive.passive"):
      - Enabled: func(cfg) bool { return false } // stub — real impl in Task 2
      - DependsOn: nil
      - Run: return task.Result{Status: task.StatusSkipped}, nil
      - Module: "subdomains"
      // NOTE: This stub is deleted from permut.go when Task 2 creates recursive.go

    subRecursiveBruteStub (name: "subdomains.recursive.brute"):
      - Enabled: func(cfg) bool { return false } // stub — real impl in Task 2
      - DependsOn: nil
      - Run: return task.Result{Status: task.StatusSkipped}, nil
      - Module: "subdomains"
      // NOTE: This stub is deleted from permut.go when Task 2 creates recursive.go

    Register all 6 types (4 real + 2 stubs) via `func init() { task.Register(...) }` at end of permut.go.

    Config keys consumed:
      - cfg.Subdomains.Permut.Enabled
      - cfg.Subdomains.Permut.RegexEnabled
      - cfg.Subdomains.Permut.IAEnabled
      - cfg.Subdomains.Permut.MinFreeMemGB (float64, default 2.0 per old SCALE-01 / CONTEXT.md discretion)
      - cfg.Subdomains.Permut.PermutWords (string, path to permutation wordlist)
      - cfg.Subdomains.Permut.GotatorDepth (int, default 1)
      - cfg.Subdomains.Permut.GotatorNumbers (int, default 3)
  </action>
  <verify>
    <automated>go test -race ./internal/modules/subdomains/... -run "TestPermut|TestResolvedBarrier|TestBuildAfterTask1" -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `internal/modules/subdomains/permut.go` contains `func (SubPermutTask) Name() string { return "subdomains.permut" }`
    - `grep "ReadMemStats" internal/modules/subdomains/permut.go` shows memory gate present
    - `grep -c "StatusSkipped" internal/modules/subdomains/permut.go` outputs at least 3 (one per permut task + stubs)
    - resolvedBarrierTask registered with DependsOn including "subdomains.recursive.passive" and "subdomains.recursive.brute"
    - `grep "recursive.passive\|recursive.brute" internal/modules/subdomains/permut.go` returns matches (stubs registered)
    - Unit test `TestBuildAfterTask1`: import subdomains package, call task.Default.Build(), verify nil error (topo.Sort succeeds with stubs in place)
    - Unit tests for memory gate (force skip) pass
    - `go build -o /dev/null ./...` exits 0
  </acceptance_criteria>
  <done>3 permutation Tasks with memory back-pressure + resolvedBarrierTask + 2 recursive stubs registered; task.Default.Build() succeeds; SUBD-03 gate implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SubRecursivePassiveTask + SubRecursiveBruteTask (replace stubs from Task 1)</name>
  <read_first>
    - modules/subdomains.sh lines 1449-1560 (sub_recursive_passive — the count driver per D-01)
    - modules/subdomains.sh lines 1563-1680 (sub_recursive_brute — puredns recursive brute)
    - .planning/phases/04-subdomains-e2e-axiom-integration/04-PATTERNS.md §Group 2 (DependsOn/Stream patterns)
    - internal/modules/subdomains/permut.go (Task 1 output — remove stubs subRecursivePassiveStub and subRecursiveBruteStub after creating recursive.go)
    - reconftw.cfg: DEEP flag, recursive depth controls (v1 uses PARALLEL_RECURSIVE_LIMIT)
  </read_first>
  <behavior>
    - Test 1: SubRecursivePassiveTask.Name() == "subdomains.recursive.passive"
    - Test 2: SubRecursivePassiveTask.DependsOn() contains "subdomains.barrier.active"
    - Test 3: SubRecursiveBruteTask.Name() == "subdomains.recursive.brute"
    - Test 4: SubRecursiveBruteTask.DependsOn() contains "subdomains.barrier.active" and "subdomains.resolvers.health"
    - Test 5: Both tasks disabled when cfg.Subdomains.Recursive.Enabled == false
    - Test 6: task.Default.Build() still returns nil error after Task 2 (stubs removed, real tasks present)
  </behavior>
  <action>
    Create `internal/modules/subdomains/recursive.go` implementing 2 Tasks:

    SubRecursivePassiveTask (name: "subdomains.recursive.passive"):
      - Enabled: cfg.Subdomains.Recursive.Enabled
      - DependsOn: ["subdomains.barrier.active"]
      - Run: Port v1 sub_recursive_passive logic — for each newly-discovered subdomain apex that has sub-subdomains (e.g., if "mail.api.example.com" is discovered, run passive enum on "api.example.com"):
          1. Extract unique second-level apex domains from artefacts/subdomains.jsonl that are subdomains of the target domain.
          2. For each apex (up to cfg.Subdomains.Recursive.MaxDepth levels), run passive enum (subfinder + crt at minimum) against that apex.
          3. Append discovered subdomains as SubdomainRecord{Source: "recursive-passive"}.
          This is the "material count driver" per D-01 — it must replicate v1 fully for SUBD-11 ±5% parity.
          Use app.Tools.Run(ctx, "subfinder", ["-all", "-d", apexDomain, "-max-time", strconv.Itoa(timeout), "-silent"]) per apex.
          Rate-limit by cfg.Subdomains.Recursive.ParallelLimit (default from v1 PARALLEL_RECURSIVE_LIMIT).

    SubRecursiveBruteTask (name: "subdomains.recursive.brute"):
      - Enabled: cfg.Subdomains.Recursive.Enabled && cfg.Subdomains.Brute.Enabled
      - DependsOn: ["subdomains.barrier.active", "subdomains.resolvers.health"]
      - Run: For each discovered subdomain apex (same apex extraction as recursive passive), run puredns bruteforce with the configured wordlist.
          Use app.Tools.Stream for puredns (XCUT-09 heartbeat).
          Append resolved subdomains as SubdomainRecord{Source: "recursive-brute"}.

    Both Tasks call `func init() { task.Register(...) }` at end of recursive.go.

    AFTER creating recursive.go: edit `internal/modules/subdomains/permut.go` to remove the two stub types (subRecursivePassiveStub and subRecursiveBruteStub) and their init() registration lines. The real registrations in recursive.go replace them. The resolvedBarrierTask DependsOn list in permut.go remains unchanged (still references "subdomains.recursive.passive" and "subdomains.recursive.brute" — now satisfied by recursive.go).

    Config keys consumed:
      - cfg.Subdomains.Recursive.Enabled
      - cfg.Subdomains.Recursive.MaxDepth (int, default 2)
      - cfg.Subdomains.Recursive.ParallelLimit (int, default 3 per v1 PARALLEL_RECURSIVE_LIMIT)
  </action>
  <verify>
    <automated>go test -race ./internal/modules/subdomains/... -run "TestRecursive" -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `internal/modules/subdomains/recursive.go` contains both Task Name() implementations
    - `grep "recursive.passive\|recursive.brute" internal/modules/subdomains/permut.go` shows both in resolvedBarrierTask DependsOn (but NOT as stub type definitions — stubs removed)
    - `grep "subRecursivePassiveStub\|subRecursiveBruteStub" internal/modules/subdomains/permut.go` returns no matches (stubs deleted)
    - Unit tests for Name/DependsOn/Enabled pass
    - `go test -race ./internal/modules/subdomains/...` exits 0
    - `go build -o /dev/null ./...` exits 0
  </acceptance_criteria>
  <done>SubRecursivePassiveTask + SubRecursiveBruteTask registered in recursive.go; stubs removed from permut.go; resolvedBarrierTask DependsOn complete; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Permutation wordlist path → disk read | cfg.Subdomains.Permut.PermutWords is config-validated path |
| gotator/regulator/massdns args → subprocess | Wordlist path, domain, integer flags from config |
| Recursive apex extraction → passive tool args | Apex domain derived from subdomain output — must be re-validated before passing to subfinder |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | Injection | Recursive apex domain → subfinder args | mitigate | Apex domain extracted from artefacts/subdomains.jsonl Subdomain field; already passed through DefaultScopeFilter.IsInScope at Append boundary; must additionally pass through NewTarget.validate_domain before use as subfinder -d arg |
| T-04-12 | Denial of Service | Unbounded recursive depth | mitigate | cfg.Subdomains.Recursive.MaxDepth cap (default 2); ParallelLimit gates concurrent apex runs; each apex run has SubfinderTimeout context timeout |
| T-04-13 | Denial of Service | Memory exhaustion in permutations | mitigate | SUBD-03 memory back-pressure gate: ReadMemStats freeGB < MinFreeMemGB → StatusSkipped; PERMUTATIONS_LIMIT (2GB) legacy cap also respected |
| T-04-SC | Tampering | regulator Python tool exec | mitigate | regulator is in tools.lock (plan-01); Runner.Run uses Registry.Lookup which verifies the binary exists at Path resolved at startup; no dynamic path construction |
</threat_model>

<verification>
- `go test -race ./internal/modules/subdomains/...` exits 0
- `grep "ReadMemStats" internal/modules/subdomains/permut.go` returns matches (memory gate present)
- `grep "subdomains.recursive.passive\|subdomains.recursive.brute" internal/modules/subdomains/permut.go` returns matches in resolvedBarrierTask DependsOn
- `grep "subRecursivePassiveStub\|subRecursiveBruteStub" internal/modules/subdomains/permut.go` returns no matches (stubs cleaned up after Task 2)
- `go build -o /dev/null ./...` exits 0
</verification>

<success_criteria>
- SubPermutTask/SubRegexPermutTask/SubIAPermutTask registered with memory back-pressure gate (SUBD-03)
- SubRecursivePassiveTask + SubRecursiveBruteTask registered (D-01 "material count drivers")
- resolvedBarrierTask DependsOn covers all resolution + permutation + recursive tasks
- task.Default.Build() succeeds after BOTH tasks (no ConfigError from topo.Sort at any point)
- D-07 respected: no nested errgroups; all concurrency via Scheduler semaphore
</success_criteria>

<output>
Create `.planning/phases/04-subdomains-e2e-axiom-integration/04-04-SUMMARY.md` when done.
</output>
