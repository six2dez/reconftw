---
phase: 04-subdomains-e2e-axiom-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - internal/modules/subdomains/resolve.go
  - internal/modules/subdomains/brute.go
  - internal/modules/subdomains/merge.go
autonomous: true
requirements:
  - SUBD-02
  - SUBD-04

must_haves:
  truths:
    - "6 active/DNS resolution Tasks register: subdomains.active (puredns resolve), subdomains.tls (tlsx), subdomains.noerror (dnsx NOERROR), subdomains.dns (dnsx multi-record), subdomains.srv (dnsx SRV), subdomains.ptr (puredns PTR)"
    - "SubBruteTask (subdomains.brute) aborts when live resolver file line-count is < cfg.Subdomains.Brute.MinResolvers, logging a structured error (SUBD-02 resolver-health gate)"
    - "SubResolversHealthTask (subdomains.resolvers.health) is a lightweight pre-check Task that verifies resolver file exists and has >= MinResolvers lines before brute runs"
    - "Stream (not Exec) used for puredns and dnsx so XCUT-09 heartbeat fires during long runs"
    - "No Task branches on axiom config (backend-agnostic per D-05)"
    - "All resolution Tasks write to inputs/resolved.{tool}.txt staging files (NOT app.Tree.Append)"
    - "MergeStage extended: MergeStage(ctx, app, 'resolved') merges inputs/resolved.*.txt and calls app.Tree.Append('subdomains', ...) once"
    - "Enabled() filtering is the correct place for SubBruteTask — BUT Enabled() is not called by Scheduler; the abort logic MUST also live in Run() to be effective"
  artifacts:
    - path: "internal/modules/subdomains/resolve.go"
      provides: "6 active DNS resolution Tasks"
      contains: "SubActiveTask, SubTLSTask, SubNoerrorTask, SubDNSTask, SubSRVTask, SubPTRTask"
    - path: "internal/modules/subdomains/brute.go"
      provides: "SubBruteTask + SubResolversHealthTask with resolver health gate in Run()"
      contains: "SubBruteTask, SubResolversHealthTask"
    - path: "internal/modules/subdomains/merge.go"
      provides: "MergeStage extended to handle 'resolved' stage"
      contains: "MergeStage handles passive and resolved stage names"
  key_links:
    - from: "internal/modules/subdomains/resolve.go"
      to: "internal/core/backend/runner.go"
      via: "app.Tools.Stream(ctx, 'puredns', args)"
      pattern: "Tools\\.Stream"
    - from: "internal/modules/subdomains/brute.go"
      to: "internal/core/config/config.go"
      via: "cfg.Subdomains.Brute.MinResolvers (added in plan-00)"
      pattern: "MinResolvers"
---

<objective>
Port v1's active DNS resolution pipeline and brute-force using per-source staging files and the sequential RunStage pattern. Extend MergeStage to handle the resolved stage.

Purpose: Provides the resolved subdomain set that the permutation stage (plan-04) and takeover/ASN/geo stages (plan-05) consume via inputs/resolved.*.txt staging files (read by their RunStage's pre-pass in the command layer). The puredns/dnsx Stream pattern satisfies XCUT-09 heartbeat. The resolver health gate satisfies SUBD-02.

Important: Enabled() is defined but NOT called by Scheduler (REVIEWS verified finding #4). The resolver-health abort logic MUST live in Task.Run(), not only in Enabled(). Enabled() should still return false when brute is config-disabled (for filterByModuleAndEnabled in newSubsCmd), but Run() must independently check resolver health and abort with StatusSkipped + structured log if the gate fails.
</objective>

<execution_context>
@/Users/six2dez/Tools/reconftw/.claude/get-shit-done/workflows/execute-plan.md
@/Users/six2dez/Tools/reconftw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/six2dez/Tools/reconftw/.planning/ROADMAP.md
@/Users/six2dez/Tools/reconftw/.planning/phases/04-subdomains-e2e-axiom-integration/04-CONTEXT.md

<interfaces>
From internal/core/backend/runner.go:
  Runner.Run(ctx, toolName, args) (*Result, error)       — bounded tools (dnsx non-streaming)
  Runner.Stream(ctx, toolName, args) (<-chan Event, error) — streaming (puredns, long dnsx runs) for XCUT-09
  Event{Line []byte, Source string, IsErr bool}

From internal/core/task/task.go:
  Task interface: 6 methods; func Register(t Task)
  NOTE: Scheduler.runOne does NOT call Enabled() before Run(). filterByModuleAndEnabled in
  newSubsCmd calls Enabled() BEFORE RunStage to skip disabled tasks. Therefore:
    - Enabled() gates config-disabled tasks (called by command layer before RunStage)
    - Run() must implement its own in-function guards for runtime conditions (resolver health, etc.)

From internal/modules/subdomains/passive.go (plan-01 output):
  SubdomainRecord{Subdomain, Source, FirstSeen} — write to staging file as JSON lines

From internal/core/config/config.go (EXACT fields after plan-00):
  cfg.Subdomains.Brute.Enabled bool
  cfg.Subdomains.Brute.MinResolvers int       (added in plan-00)
  cfg.Subdomains.DNSResolve.Resolver string   (oneof=auto puredns dnsx)
  cfg.Subdomains.DNSResolve.TimeoutMinutes int
  cfg.Subdomains.DNSResolve.BruteTimeout string
  cfg.Subdomains.DNSResolve.PurednsPublicLimit int
  cfg.Subdomains.DNSResolve.PurednsTrustedLimit int
  cfg.Subdomains.DNSResolve.PurednsWildcardtestLimit int
  cfg.Subdomains.DNSResolve.PurednsWilcardBatchLimit int
  cfg.Subdomains.DNSResolve.DNSXThreads int
  cfg.Subdomains.DNSResolve.DNSXRateLimit int
  cfg.Paths.Resolvers string       — path to public resolvers file
  cfg.Paths.ResolversTrusted string — path to trusted resolvers file

From internal/modules/subdomains/doc.go (plan-00 staging contract):
  Tasks write to: filepath.Join(app.Target.WorkDir, "inputs", "resolved."+toolName+".txt")
  MergeStage(ctx, app, "resolved") reads inputs/resolved.*.txt, deduplicates, calls Append once.
  IMPORTANT: resolved stage feeds BACK into "subdomains" artefact (merged resolved + passive set).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 6 resolution Tasks (SubActive, SubTLS, SubNoerror, SubDNS, SubSRV, SubPTR) with staging files</name>
  <read_first>
    - internal/modules/subdomains/resolve.go (create new)
    - modules/subdomains.sh lines 220-380 (sub_active, sub_tls, sub_noerror, sub_dns, sub_srv, sub_ptr_cidrs — v1 source)
    - internal/core/config/config.go (SubDNSResolve fields — read EXACT field names at lines 197-208)
    - internal/core/backend/runner.go (Stream method — for puredns, dnsx)
    - internal/core/appctx/appctx.go (Target.WorkDir for staging path)
  </read_first>
  <behavior>
    - Test 1: SubActiveTask.Name() == "subdomains.active"
    - Test 2: SubActiveTask.Run with MockBackend writes inputs/resolved.active.txt in WorkDir with subdomain lines
    - Test 3: SubActiveTask.Run uses Stream (not Run) — channel must be drained and lines collected
    - Test 4: SubNoerrorTask.DependsOn() does NOT reference a barrier task (barriers removed; sequential RunStage handles ordering)
    - Test 5: All 6 Tasks have Module() == "subdomains"
    - Test 6: task.Default.Build() after importing subdomains package exits 0 (no ConfigError — no forward DependsOn on unregistered names)
  </behavior>
  <action>
    Create `internal/modules/subdomains/resolve.go` with 6 Tasks implementing the staging-file pattern:

    SubActiveTask (name "subdomains.active"):
      Enabled: cfg.Subdomains.Passive.Enabled (active resolution is part of the passive+active pass)
      DependsOn: nil — sequential RunStage ordering (command layer calls this stage AFTER passive MergeStage)
      Run: app.Tools.Stream(ctx, "puredns", ["resolve", stagingFileFrom("passive"), "-r", cfg.Paths.Resolvers, "--wildcard-tests", str(cfg.Subdomains.DNSResolve.PurednsWildcardtestLimit), ...])
        — reads inputs/passive.merged.txt (output of MergeStage("passive")) as the domain list
        — streams stdout lines; writes to inputs/resolved.active.txt
        IMPORTANT: MergeStage writes merged output to inputs/passive.merged.txt so Tasks in the next stage have a stable read path. Update MergeStage in plan-01 to also write this file.

    SubTLSTask (name "subdomains.tls"):
      Run: app.Tools.Stream(ctx, "tlsx", ["-l", stagingFileFrom("passive"), "-silent", "-san", "-cn", ...])
      Writes inputs/resolved.tls.txt

    SubNoerrorTask (name "subdomains.noerror"):
      DependsOn: nil (feeds from same passive merged set; command layer sequencing handles this)
      Run: app.Tools.Stream(ctx, "dnsx", ["-l", stagingFileFrom("passive"), "-silent", "-rcode", "noerror"])
      Writes inputs/resolved.noerror.txt

    SubDNSTask (name "subdomains.dns"):
      Run: app.Tools.Run(ctx, "dnsx", ["-l", stagingFileFrom("passive"), "-a", "-aaaa", "-cname", "-ns", "-mx", "-silent"])
      Writes inputs/resolved.dns.txt

    SubSRVTask (name "subdomains.srv"):
      Run: app.Tools.Run(ctx, "dnsx", ["-l", stagingFileFrom("passive"), "-srv", "-silent"])
      Writes inputs/resolved.srv.txt

    SubPTRTask (name "subdomains.ptr"):
      Run: app.Tools.Stream(ctx, "puredns", ["reverse", "-r", cfg.Paths.Resolvers, "-silent"])
        — reads IPs from asnmap output (inputs/asn.ips.txt); use DependsOn: nil (command layer orders ptr after enrichment via separate RunStage call)
      Writes inputs/resolved.ptr.txt

    All Tasks: call func init() { task.Register(XTask{}) } at bottom of resolve.go.

    Helper function in resolve.go: stagingFileFrom(stage string) string returns the merged file path: filepath.Join("<workdir>", "inputs", stage+".merged.txt"). NOTE: WorkDir is not available at registration time. Tasks must use app.Target.WorkDir at Run() time, not at init() time. stagingFileFrom is a helper that takes app *appctx.AppContext and stage string.

    IMPORTANT: All Tasks write output to inputs/resolved.{tool}.txt — do NOT call app.Tree.Append.
  </action>
  <verify>
    <automated>go test -race ./internal/modules/subdomains/... -run "TestResolveTasks" -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "task\.Register" internal/modules/subdomains/resolve.go` outputs 6
    - `grep "Tree\.Append" internal/modules/subdomains/resolve.go` returns no matches (staging contract)
    - `grep "barrier" internal/modules/subdomains/resolve.go` returns no matches (no barrier task)
    - `grep "Tools\.Stream" internal/modules/subdomains/resolve.go` returns matches (Stream used for puredns/dnsx)
    - `go build -o /dev/null ./...` exits 0
  </acceptance_criteria>
  <done>6 resolution Tasks registered; staging-file writes; Stream used for long-running tools; no barriers; build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SubBruteTask + SubResolversHealthTask + extend MergeStage for resolved stage</name>
  <read_first>
    - internal/modules/subdomains/brute.go (create new)
    - internal/modules/subdomains/merge.go (plan-01 output — extend to handle resolved stage)
    - modules/subdomains.sh lines 185-220 (sub_brute — v1 brute force source)
    - modules/utils.sh lines for _ensure_dns_resolver_files and _bruteforce_domains (resolver file handling)
    - internal/core/config/config.go (SubBrute.MinResolvers added in plan-00; SubDNSResolve.BruteTimeout)
    - internal/core/appctx/appctx.go (Target.WorkDir; Cfg)
  </read_first>
  <behavior>
    - Test 1: SubResolversHealthTask.Run returns StatusSkipped when resolver file line-count < cfg.Subdomains.Brute.MinResolvers
    - Test 2: SubBruteTask.Run returns StatusSkipped with structured log entry {event: "brute_skipped_resolver_gate"} when resolver file has < MinResolvers lines
    - Test 3: SubBruteTask.Run calls puredns bruteforce with correct args when resolver check passes
    - Test 4: SubBruteTask.Enabled returns false when cfg.Subdomains.Brute.Enabled == false
    - Test 5: SubBruteTask writes inputs/resolved.brute.txt staging file (does NOT call app.Tree.Append)
  </behavior>
  <action>
    Create `internal/modules/subdomains/brute.go`:

    SubResolversHealthTask (name "subdomains.resolvers.health"):
      Enabled: cfg.Subdomains.Brute.Enabled (skip health check if brute disabled)
      DependsOn: nil
      Run:
        Read cfg.Paths.Resolvers; count non-empty lines.
        If count < cfg.Subdomains.Brute.MinResolvers:
          log.Warn("resolver_health_check_failed", "count", count, "min", cfg.Subdomains.Brute.MinResolvers)
          return Result{Status: StatusSkipped}, nil
        return Result{Status: StatusDone}

    SubBruteTask (name "subdomains.brute"):
      Enabled: cfg.Subdomains.Brute.Enabled
      DependsOn: nil (command layer ensures brute RunStage runs after passive MergeStage + resolve RunStage)
      Run:
        FIRST: check resolver file independently (Enabled() is NOT called by Scheduler — see REVIEWS finding #4):
          count lines in cfg.Paths.Resolvers; if < cfg.Subdomains.Brute.MinResolvers → log structured warning + return Result{Status: StatusSkipped}
        THEN: app.Tools.Stream(ctx, "puredns", ["bruteforce", wordlistPath, "-d", domain, "-r", cfg.Paths.Resolvers, "--wildcard-tests", limit, ...])
        Parse stream lines; write to inputs/resolved.brute.txt
        Return Result{Status: StatusDone, Outputs: [stagingFilePath], Stats: {"brute_candidates": count}}
      NOTE: wordlistPath = cfg.Paths.SubsWordlist (or SubsWordlistBig when cfg.Advanced.Deep)

    func init() { task.Register(SubResolversHealthTask{}); task.Register(SubBruteTask{}) }

    Extend `internal/modules/subdomains/merge.go`:
      MergeStage already handles "passive" stage. Add "resolved" case: reads all inputs/resolved.*.txt, deduplicates, merges into SubdomainRecord, calls app.Tree.Append("subdomains", records).
      Also: after MergeStage writes the artefact, write a merged.txt file at inputs/{stage}.merged.txt for downstream Tasks to use as their input list (e.g. inputs/passive.merged.txt for resolution Tasks, inputs/resolved.merged.txt for permutation Tasks).
  </action>
  <verify>
    <automated>go test -race ./internal/modules/subdomains/... -run "TestBrute|TestResolvers" -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race ./internal/modules/subdomains/... -run "TestBrute"` exits 0; resolver gate test passes
    - `grep "MinResolvers" internal/modules/subdomains/brute.go` returns match in Run() body (NOT only in Enabled)
    - `grep "StatusSkipped" internal/modules/subdomains/brute.go` returns match (gate skips, not errors)
    - `grep "Tree\.Append" internal/modules/subdomains/brute.go` returns no matches (staging contract)
    - `grep "merged\.txt" internal/modules/subdomains/merge.go` returns match (merged.txt written for downstream)
    - `go build -o /dev/null ./...` exits 0
  </acceptance_criteria>
  <done>SubBruteTask + SubResolversHealthTask with in-Run() resolver gate; MergeStage extended for resolved; SUBD-02 and SUBD-04 addressed; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Task.Run → staging file write | puredns/dnsx stdout (raw resolved hostnames) written to workspace inputs/ before scope filtering |
| Resolver file path → puredns args | cfg.Paths.Resolvers is user-configured; nopath_traversal validator in config protects against traversal |
| SubResolversHealthTask health gate | Resolver file line count read from filesystem; adversarial resolver file content cannot cause code injection (count-only read) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Injection | puredns/dnsx args (resolver path, domain) | mitigate | Resolver path validated by nopath_traversal at config load; domain validated by NewTarget regex; Go slice args not shell-expanded |
| T-04-02-02 | Denial of Service | Wildcard DNS response floods resolved set | mitigate | puredns wildcard-tests limit (cfg.Subdomains.DNSResolve.PurednsWildcardtestLimit) caps wildcard detection rounds; deep_wildcard_filter in plan-04 further filters |
| T-04-02-03 | Spoofing | DNS resolver file tampered with malicious resolvers | mitigate | Resolver file path is local; AXIOM-05 (resolver propagation) uses the same validated path; users are responsible for their resolver list integrity |
| T-04-02-04 | Information Disclosure | Brute wordlist path in logs | accept | Wordlist path is non-secret (local file path); logged at Debug verbosity only; no PII |
</threat_model>

<verification>
- `go test -race ./internal/modules/subdomains/... -run "TestBrute|TestResolveTasks"` exits 0
- `grep "MinResolvers" internal/modules/subdomains/brute.go` returns match in Run() (not just Enabled)
- `grep "Tree\.Append" internal/modules/subdomains/resolve.go` returns no matches
- `grep "Tree\.Append" internal/modules/subdomains/brute.go` returns no matches
- `grep "merged\.txt" internal/modules/subdomains/merge.go` returns match
- `go build -o /dev/null ./...` exits 0
</verification>

<success_criteria>
- 6 resolution Tasks + SubBruteTask + SubResolversHealthTask registered via init()
- All Tasks use staging-file writes (no direct Append) — satisfying the REVIEWS finding #2 fix
- In-Run() resolver gate in SubBruteTask (NOT only Enabled() — REVIEWS finding #4 fix)
- MergeStage extended to handle resolved stage with merged.txt output
- Stream used for puredns/dnsx (XCUT-09 heartbeat satisfied)
- `go test -race ./internal/modules/subdomains/...` exits 0
</success_criteria>

<output>
Create `.planning/phases/04-subdomains-e2e-axiom-integration/04-02-SUMMARY.md` when done.
</output>
