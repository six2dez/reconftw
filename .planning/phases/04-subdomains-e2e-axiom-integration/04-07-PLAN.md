---
phase: 04-subdomains-e2e-axiom-integration
plan: 07
type: execute
wave: 5
depends_on:
  - 04-05
  - 04-06
files_modified:
  - internal/modules/subdomains/parity_test.go
  - internal/modules/subdomains/testdata/fixtures/passive/hackerone.com.subfinder.txt
  - internal/modules/subdomains/testdata/fixtures/passive/hackerone.com.crt.txt
  - internal/modules/subdomains/testdata/fixtures/resolved/hackerone.com.puredns.txt
  - internal/modules/subdomains/testdata/fixtures/takeover/hackerone.com.dnstake.jsonl
  - internal/modules/subdomains/testdata/fixtures/passive/tesla.com.subfinder.txt
  - internal/modules/subdomains/testdata/fixtures/passive/tesla.com.crt.txt
  - internal/modules/subdomains/testdata/fixtures/resolved/tesla.com.puredns.txt
  - internal/modules/subdomains/testdata/fixtures/passive/maintainer.subfinder.txt
  - internal/modules/subdomains/testdata/fixtures/resolved/maintainer.puredns.txt
  - internal/core/backend/axiom_test.go
autonomous: false
requirements:
  - SUBD-11
  - AXIOM-09

must_haves:
  truths:
    - "Frozen-replay parity harness (TestPassiveParity, TestResolveParity) passes at 0% tolerance using MockBackend + REAL v1-captured fixture files for hackerone.com and tesla.com"
    - "Fixtures were captured from ACTUAL bash v1 runs (instructions below) — NOT hand-authored synthetic data"
    - "Per-category parity checked: passive set (subfinder+crt counts), resolved set (puredns), takeover findings"
    - "v1 reference reducer function reads v1 raw output and produces expected category counts for comparison"
    - "Axiom output-equivalence test (TestAxiomEquivalence) asserts axiom vs local run produce same scope-filtered set from same fixtures"
    - "Third canonical target (maintainer-controlled) fixtures are placeholders with a TODO comment pointing to fixture-capture instructions — CI test asserts their presence but skips them if empty"
    - "Live sign-off checkpoint: human runs reconftw subs --target hackerone.com AND tesla.com and compares v1 vs v2 per-category counts within ±5%"
  artifacts:
    - path: "internal/modules/subdomains/parity_test.go"
      provides: "Frozen-replay parity test harness using MockBackend + real v1-captured fixtures"
      contains: "TestPassiveParity, TestResolveParity, TestTakeoverParity, v1ReferenceReducer"
    - path: "internal/modules/subdomains/testdata/fixtures/"
      provides: "Real v1-captured tool output fixtures for deterministic CI testing"
      contains: "passive/hackerone.com.subfinder.txt (v1-captured), resolved/hackerone.com.puredns.txt (v1-captured)"
    - path: "internal/core/backend/axiom_test.go"
      provides: "Axiom output-equivalence test (AXIOM-09)"
      contains: "TestAxiomEquivalence"
  key_links:
    - from: "internal/modules/subdomains/parity_test.go"
      to: "internal/core/testutil/mock_backend.go"
      via: "testutil.NewMockBackend reads fixture files as simulated tool output"
      pattern: "NewMockBackend"
    - from: "internal/modules/subdomains/parity_test.go"
      to: "internal/modules/subdomains/testdata/fixtures/"
      via: "v1ReferenceReducer reads fixture files and counts per-category records"
      pattern: "v1ReferenceReducer"
---

<objective>
Build the frozen-replay parity harness (D-03) with REAL v1-captured fixtures (fixing the tautological hand-authored fixtures finding from REVIEWS), implement per-category parity assertions (D-04), add the Axiom output-equivalence test (AXIOM-09), and gate phase acceptance on a live back-to-back run.

Purpose: The parity harness is the CI guard detecting porting bugs. The REVIEWS finding confirmed hand-authored fixtures are tautological — they test that v2 parses data v2 invented. Real v1-captured fixtures make the harness a genuine guard. The v1ReferenceReducer function reads raw v1 tool output and extracts expected category counts, making the CI assertion "v2 output from real v1 inputs matches what v1 would produce from those same inputs."
</objective>

<execution_context>
@/Users/six2dez/Tools/reconftw/.claude/get-shit-done/workflows/execute-plan.md
@/Users/six2dez/Tools/reconftw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/six2dez/Tools/reconftw/.planning/ROADMAP.md
@/Users/six2dez/Tools/reconftw/.planning/phases/04-subdomains-e2e-axiom-integration/04-CONTEXT.md

<interfaces>
From internal/core/testutil/mock_backend.go:
  type MockBackendConfig struct { FixturesDir string; Scenario string; Capacity int }
  func NewMockBackend(cfg MockBackendConfig) *MockBackend
  MockBackend.Exec(ctx, t *Tool, args): reads FixturesDir/<t.Name()>/<Scenario>.txt and returns contents as stdout
  MockBackend.Stream(ctx, t *Tool, args): same as Exec but returns line-by-line channel

From internal/core/testutil/mock_output_tree.go:
  func NewMockOutputTree() *MockOutputTree
  MockOutputTree.Lines(artefact string) [][]byte — all lines appended to this artefact

From internal/core/testutil/mock_checkpoint.go:
  func NewMockCheckpoint() *MockCheckpoint — in-memory; Done=false always (never skip in tests)

From .planning/phases/04-subdomains-e2e-axiom-integration/04-CONTEXT.md D-03:
  CI (deterministic, hard gate): fixture files + MockBackend → both v1 reference reducer AND v2 produce same per-category counts.
  Phase sign-off (manual): live back-to-back run ±5%.

FIXTURE CAPTURE INSTRUCTIONS (executor must run these before Task 1):
  To capture real v1 fixtures for hackerone.com:
    bash reconftw.sh -d hackerone.com -s 2>&1 | tee /tmp/reconftw-v1-hackerone.log
    cp Recon/hackerone.com/subdomains.txt testdata/fixtures/passive/hackerone.com.subfinder.txt
    (repeat per tool: crt.sh output → hackerone.com.crt.txt, puredns resolved → hackerone.com.puredns.txt)
  Alternatively: use the bash v1 source directly to produce fixtures via:
    subfinder -all -d hackerone.com -silent > testdata/fixtures/passive/hackerone.com.subfinder.txt
    crt -d hackerone.com > testdata/fixtures/passive/hackerone.com.crt.txt
    # etc.
  If running full v1 is not feasible during plan execution, use real public data:
    The subfinder output for hackerone.com can be approximated from Certificate Transparency logs.
    The fixture must contain at minimum 50+ REAL hackerone.com subdomains (not invented).
    Mark the fixture file with a comment header: "# Captured from: subfinder -d hackerone.com -silent; date: YYYY-MM-DD"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capture real v1 fixtures + frozen-replay parity harness</name>
  <read_first>
    - internal/core/testutil/mock_backend.go (MockBackend fixture loading — read EXACTLY how it resolves fixture paths)
    - internal/core/testutil/mock_output_tree.go (MockOutputTree.Lines — how to assert artefact content)
    - internal/core/testutil/mock_checkpoint.go (in-memory checkpoint)
    - modules/subdomains.sh (v1 sub_passive source — to understand what format each tool outputs)
    - internal/modules/subdomains/passive.go (plan-01 output — SubdomainRecord schema)
    - internal/modules/subdomains/merge.go (plan-02 output — MergeStage function under test)
  </read_first>
  <behavior>
    - Test 1 (TestPassiveParity/hackerone): MockBackend returns hackerone.com.subfinder.txt fixture; v2 passive stage + MergeStage produces subdomains.jsonl; v1ReferenceReducer(hackerone.com.subfinder.txt) returns count N; assert |v2_count - N| == 0 (CI deterministic)
    - Test 2 (TestPassiveParity/tesla): same pattern for tesla.com
    - Test 3 (TestResolveParity/hackerone): MockBackend returns puredns fixture; v2 resolve stage produces subdomains.jsonl; assert v2 resolved count == v1 resolved count from fixture
    - Test 4 (TestPassiveParity/maintainer): fixture file exists but may be empty; if empty → t.Skip("maintainer target fixture not captured yet — see FIXTURE CAPTURE INSTRUCTIONS in doc.go")
    - Test 5: v1ReferenceReducer(fixtureBytes, "passive") returns the count of unique non-empty lines (v1 output is one subdomain per line, no JSON)
  </behavior>
  <action>
    Step A — Capture fixtures (MANDATORY before writing test code):
      Run bash v1 for hackerone.com and tesla.com (or use real public data as described in interface section).
      Create the testdata/fixtures directory structure:
        internal/modules/subdomains/testdata/fixtures/passive/
        internal/modules/subdomains/testdata/fixtures/resolved/
        internal/modules/subdomains/testdata/fixtures/takeover/

      For each target and tool, write the REAL captured output to the fixture file.
      Each fixture file MUST have a header comment line: "# source: <command> | date: <date>"
      Minimum fixture sizes to be a useful guard:
        hackerone.com subfinder: >= 50 unique subdomains
        hackerone.com puredns: >= 30 resolved subdomains
        tesla.com subfinder: >= 100 unique subdomains
      Maintainer target fixtures: create empty files with TODO header comment.

    Step B — Create `internal/modules/subdomains/parity_test.go`:

    v1ReferenceReducer(fixtureBytes []byte, category string) int:
      For "passive" / "resolved": count unique non-empty non-comment lines in fixture bytes.
      For "takeover": count lines matching JSONL takeover indicator pattern.
      Returns expected count from v1 output.

    TestPassiveParity subtests (one per target): uses table-driven test with target names and fixture paths.
      For each target:
        - Create MockBackend pointing to fixtures dir
        - Create MockOutputTree
        - Create AppContext with MockBackend + MockOutputTree + MockCheckpoint
        - Run SubfinderTask.Run(ctx, app) → writes staging file
        - Run CrtTask.Run(ctx, app) → writes staging file
        - Call MergeStage(ctx, app, "passive")
        - Assert MockOutputTree.Lines("subdomains") has len == v1ReferenceReducer(subfinderFixture+crtFixture, "passive")
        - Assert tolerance == 0 (deterministic fixtures — exact match required in CI)

    TestResolveParity subtests: same pattern for resolve stage.

    TestTakeoverParity subtests: same for dnstake fixture.

    Axiom-equivalence test skeleton (expanded in axiom_test.go by Task 2).
  </action>
  <verify>
    <automated>go test -race ./internal/modules/subdomains/... -run "TestPassiveParity|TestResolveParity" -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep "# source:" internal/modules/subdomains/testdata/fixtures/passive/hackerone.com.subfinder.txt` returns match (real capture, not invented)
    - `wc -l internal/modules/subdomains/testdata/fixtures/passive/hackerone.com.subfinder.txt` shows >= 50 lines (real data)
    - `grep "v1ReferenceReducer" internal/modules/subdomains/parity_test.go` returns match (v1 reference used for expected values)
    - `grep "hand-authored\|invented\|realistic" internal/modules/subdomains/parity_test.go` returns no matches (no synthetic fixtures)
    - `go test -race ./internal/modules/subdomains/... -run "TestPassiveParity"` exits 0
    - `go build -o /dev/null ./...` exits 0
  </acceptance_criteria>
  <done>Real v1-captured fixtures committed; parity harness using v1ReferenceReducer for expected counts; TestPassiveParity + TestResolveParity pass; REVIEWS tautological-fixture finding resolved.</done>
</task>

<task type="auto">
  <name>Task 2: AXIOM-09 equivalence test + phase acceptance checkpoint</name>
  <read_first>
    - internal/core/backend/axiom_test.go (create new)
    - internal/core/backend/failover.go (plan-06 output — FailoverBackend for test)
    - internal/core/testutil/mock_backend.go (MockBackend as both Primary and Fallback)
    - internal/modules/subdomains/testdata/fixtures/ (Task 1 output — fixtures for equivalence test)
  </read_first>
  <behavior>
    - Test 1 (TestAxiomEquivalence): AxiomBackend-path and LocalBackend-path both produce same scope-filtered subdomain set from same fixtures
    - Test 2: FailoverBackend that wraps MockBackend (as Primary failing) falls back to MockBackend (as Fallback) and produces same output as direct local run
  </behavior>
  <action>
    Create `internal/core/backend/axiom_test.go`:

    TestAxiomEquivalence:
      Two runs from the same fixture files:
        Run 1 (local): MockBackend returns fixture data → v2 passive stage → MockOutputTree
        Run 2 (axiom-simulated): AxiomBackend wrapping MockLocalBackend (axiom-scan is mocked to call MockBackend with same fixtures) → v2 passive stage → MockOutputTree
        Assert: both runs produce identical subdomains.jsonl content (same set, same dedup).
        This satisfies AXIOM-09: "axiom run produces same scope-filtered set as local run."

    NOTE: Since real axiom requires a fleet, TestAxiomEquivalence uses a MockAxiomBackend that:
      1. Accepts the same args as AxiomBackend.Exec
      2. Splits the input file (using Tool.InputFlag)
      3. Passes each shard to MockBackend.Exec
      4. Merges the shard outputs (simulating what axiom-scan does on the fleet)
      Assert the merged result == local MockBackend result on the full input.

    Create the phase acceptance checkpoint task for the human live run:

    CHECKPOINT NOTE in parity_test.go:
      Add a var skipLiveRun = os.Getenv("PARITY_LIVE") == ""
      TestLiveSignoff (always skipped in CI — requires PARITY_LIVE=1 env var):
        Instructions: run `PARITY_LIVE=1 go test -run TestLiveSignoff` manually
        Steps: bash v1 run on hackerone.com; reconftw subs --target hackerone.com; compare counts ±5%
        (This is not automated in CI; it is the D-03 manual sign-off gate.)
  </action>
  <verify>
    <automated>go test -race ./internal/core/backend/... -run "TestAxiomEquivalence" -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race ./internal/core/backend/... -run "TestAxiomEquivalence"` exits 0
    - `grep "AXIOM-09\|equivalence" internal/core/backend/axiom_test.go` returns match (AXIOM-09 coverage documented)
    - `grep "PARITY_LIVE\|TestLiveSignoff" internal/modules/subdomains/parity_test.go` returns match (manual gate present)
    - `go build -o /dev/null ./...` exits 0
  </acceptance_criteria>
  <done>AXIOM-09 equivalence test implemented; TestLiveSignoff manual gate in place; build green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 4 subdomain pipeline: 8 plans implemented (Wave 0-5). All Task implementations, AxiomBackend, FailoverBackend, sequential RunStage wiring, and frozen-replay parity harness are complete.
  </what-built>
  <how-to-verify>
    Step 1: Verify frozen-replay tests pass in CI:
      go test -race ./... 2>&1 | tail -30
      (All tests should pass with 0 failures. PARITY_LIVE tests skipped in CI — that is expected.)

    Step 2: Live parity sign-off (requires network + tools installed):
      Bash v1 run: ./reconftw.sh -d hackerone.com -s 2>&1 | grep "Subdomains found" | tail -1
      v2 run: go run ./cmd/reconftw subs --target hackerone.com 2>&1 | grep "subdomains" | tail -5
      Compare passive count, resolved count ±5% per ROADMAP Phase 4 success criteria.

    Step 3: Repeat Step 2 for tesla.com (second canonical target).

    Step 4: Spot-check artefact content:
      cat workspaces/hackerone.com/artefacts/subdomains.jsonl | head -5
      (Should show SubdomainRecord JSON objects with subdomain, source, first_seen fields)

    Step 5: Verify axiom stub (optional — skips if no fleet):
      go run ./cmd/reconftw subs --target hackerone.com --axiom 2>&1
      (Should fall back to local gracefully if axiom fleet not available)
  </how-to-verify>
  <resume-signal>Type "approved" confirming passive and resolved counts within ±5% for both hackerone.com and tesla.com, OR describe discrepancies for investigation.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| testdata/fixtures/ in repo | Committed fixture files contain real subdomain data from public bug-bounty targets (acceptable — public info) |
| TestLiveSignoff manual gate | Live run requires network access; scan targets are public bug-bounty programs with explicit permission |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07-01 | Information Disclosure | Real subdomain data in fixture files | accept | hackerone.com and tesla.com are public bug-bounty targets; their subdomain sets are public knowledge; fixtures contain no credentials or sensitive data |
| T-04-07-02 | Tampering | Fixture file modification breaks parity gate | mitigate | Fixtures committed to git; any modification requires a commit (auditable); CI computes hash of fixture files and fails if hash changes without explicit version bump |
| T-04-07-SC | Tampering | No new package installs in this plan | accept | No new go/pip/cargo installs; fixtures are plain text files |
</threat_model>

<verification>
- `go test -race ./internal/modules/subdomains/... -run "TestPassiveParity|TestResolveParity"` exits 0
- `go test -race ./internal/core/backend/... -run "TestAxiomEquivalence"` exits 0
- `grep "# source:" testdata/fixtures/passive/hackerone.com.subfinder.txt` returns match (real capture)
- `grep "v1ReferenceReducer" internal/modules/subdomains/parity_test.go` returns match
- `go build -o /dev/null ./...` exits 0
- Human: live parity ±5% confirmed on hackerone.com + tesla.com
</verification>

<success_criteria>
- Frozen-replay parity harness with REAL v1-captured fixtures (not synthetic data) — REVIEWS tautological-fixture finding fixed
- Per-category assertions: passive set, resolved set, takeover findings each checked independently (D-04)
- v1ReferenceReducer used for expected values (not test-generated expectations)
- AXIOM-09 output-equivalence test: axiom path == local path from same fixtures
- Phase acceptance: human confirms ±5% live parity on 2 canonical targets
</success_criteria>

<output>
Create `.planning/phases/04-subdomains-e2e-axiom-integration/04-07-SUMMARY.md` when done.
</output>
