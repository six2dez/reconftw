---
phase: 04-subdomains-e2e-axiom-integration
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - internal/core/config/config.go
  - internal/core/backend/backend.go
  - internal/core/backend/tools.lock
  - internal/modules/subdomains/doc.go
autonomous: true
requirements:
  - SUBD-02
  - SUBD-03
  - AXIOM-05
  - AXIOM-06
  - AXIOM-07
  - AXIOM-08

must_haves:
  truths:
    - "config.go compiles with new fields: SubBrute.MinResolvers, SubPermut.MinFreeMemGB, AxiomConfig.FailoverThreshold, all with koanf tags and validate constraints"
    - "backend.Tool struct has InputFlag string field for per-tool Axiom input-file declaration"
    - "tools.lock has an input_flag entry for puredns, dnsx, tlsx, s3scanner entries"
    - "tools.lock has a dnscewl entry (critical=false) covering the third SUBD-03 permutation tool named in D-01"
    - "internal/modules/subdomains/doc.go declares the per-source staging contract: Tasks write private inputs/passive.{tool}.txt files; command layer merges after each RunStage call"
    - "internal/modules/subdomains/doc.go documents that SubGeoTask.Enabled returns cfg.Subdomains.Enabled (parent flag) and that executors MUST NOT add a cfg.Subdomains.Geo struct"
    - "go build -o /dev/null ./... exits 0"
    - "go vet ./... exits 0"
  artifacts:
    - path: "internal/core/config/config.go"
      provides: "New config fields for Phase 4 use"
      contains: "MinResolvers, MinFreeMemGB, FailoverThreshold"
    - path: "internal/core/backend/backend.go"
      provides: "InputFlag field on Tool struct for AxiomExecSpec"
      contains: "InputFlag string"
    - path: "internal/modules/subdomains/doc.go"
      provides: "Package declaration + staging contract documentation"
      contains: "package subdomains"
  key_links:
    - from: "internal/core/config/config.go"
      to: "internal/modules/subdomains/brute.go"
      via: "cfg.Subdomains.Brute.MinResolvers consumed in SubBruteTask"
      pattern: "MinResolvers"
    - from: "internal/core/backend/backend.go"
      to: "internal/core/backend/axiom.go"
      via: "Tool.InputFlag consumed by AxiomBackend.Exec for input-file split"
      pattern: "InputFlag"
---

<objective>
Kernel-contract foundation: add the missing config fields all Phase 4 Tasks reference, add the Tool.InputFlag field for Axiom input-file splitting, expand tools.lock to 26 entries (including dnscewl per D-01/SUBD-03), and create the subdomains package doc with the per-source staging contract and SubGeoTask.Enabled invariant.

Purpose: Prevents compile breaks in every downstream plan. The config fields (MinResolvers, MinFreeMemGB, FailoverThreshold) are referenced by plans 02, 04, and 06 respectively — they MUST exist before any Task code is written. The Tool.InputFlag is the fix for the verified "brittle extractInputFile heuristic" finding. The staging contract doc makes the per-source-file → command-layer-merge pattern explicit for executor agents. The SubGeoTask.Enabled note prevents executors from inventing a non-existent cfg.Subdomains.Geo struct.

Output: config.go compiles with 3 new fields; Tool has InputFlag; tools.lock at 26 entries (including dnscewl); subdomains package doc created.
</objective>

<execution_context>
@/Users/six2dez/Tools/reconftw/.claude/get-shit-done/workflows/execute-plan.md
@/Users/six2dez/Tools/reconftw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/six2dez/Tools/reconftw/.planning/ROADMAP.md
@/Users/six2dez/Tools/reconftw/.planning/phases/04-subdomains-e2e-axiom-integration/04-CONTEXT.md

<interfaces>
From internal/core/config/config.go (EXACT existing field names — read before editing):
  SubBrute struct { Enabled bool `koanf:"enabled"` }
  SubPermut struct { Enabled bool; LimitBytes int64; IAEnabled bool; RegexEnabled bool; WordlistMode string; ShortThreshold int }
  AxiomConfig struct { Enabled bool; FleetName string; FleetCount int; FleetRegions string; ShutdownOnEnd bool; AutoFixHostkey bool; FleetLaunch bool; ExtraArgs string }
  AdvToolAxiom struct { ResolversPath string; ResolversTrustedPath string; PostStart string }
  — NOTE: Axiom credentials (SSH key, fleet token) are NOT in Config; they come from env at Boot time.
  — NOTE: There is NO cfg.Subdomains.Geo struct — SubGeoTask.Enabled uses cfg.Subdomains.Enabled directly.

From internal/core/backend/backend.go (EXACT existing Tool struct):
  Tool { Name string; Path string; Version string; DefaultArgs []string; Timeout time.Duration; Critical bool }
  — InputFlag string field DOES NOT EXIST YET — add it here.

From internal/core/backend/tools.lock (existing 10-entry TOML format):
  schema_version = "1.0"
  [[tools]] name/kind/go_module/timeout/critical pattern
  — Expand to 26 entries with input_flag per tool where applicable.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing config fields to config.go</name>
  <read_first>
    - internal/core/config/config.go (read the FULL file — especially SubBrute, SubPermut, AxiomConfig, AdvToolAxiom structs at exact line ranges to edit)
    - reconftw.cfg (lines with PUREDNS_TRUSTED_RESOLVERS, PERMUTATIONS_LIMIT, AXIOM_FLEET_COUNT — v1 source for defaults)
    - .planning/decisions/0002-architecture-v2.md §2 (TOML key governance — any new field must have a koanf tag)
  </read_first>
  <action>
    Edit `internal/core/config/config.go` to add the following fields (do NOT remove or rename any existing fields — additions only):

    In `SubBrute` struct, add after `Enabled bool`:
      MinResolvers int `koanf:"min_resolvers" validate:"min=0,max=10000"`
    Default semantics: brute aborts if the active resolver file has fewer than MinResolvers lines (per SUBD-02). Suggested default: 10 (matches v1 safety intent).

    In `SubPermut` struct, add after `ShortThreshold int`:
      MinFreeMemGB int `koanf:"min_free_mem_gb" validate:"min=0,max=1024"`
    Default semantics: permutation Tasks skip (wait/chunk) when OS available memory is below MinFreeMemGB GB (per SUBD-03). Suggested default: 1 (conservative; operator can raise).

    In `AxiomConfig` struct, add after `ExtraArgs string`:
      FailoverThreshold int `koanf:"failover_threshold" validate:"min=1,max=100"`
    Default semantics: FailoverBackend flip kill-switch after this many consecutive *AxiomFailure errors (per AXIOM-08 / D-06). Suggested default: 3.

    IMPORTANT: Do NOT add a SubGeo struct or cfg.Subdomains.Geo field. SubGeoTask.Enabled is implemented as `cfg.Subdomains.Enabled` (parent flag) — there is no separate geo flag in v1 and none should be invented here.

    No other changes to config.go. Preserve all existing koanf tags, validate constraints, and the file header comment block verbatim.
  </action>
  <verify>
    <automated>go build -o /dev/null ./internal/core/config/... 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `go build -o /dev/null ./internal/core/config/...` exits 0
    - `grep "MinResolvers" internal/core/config/config.go` returns a match inside SubBrute struct
    - `grep "MinFreeMemGB" internal/core/config/config.go` returns a match inside SubPermut struct
    - `grep "FailoverThreshold" internal/core/config/config.go` returns a match inside AxiomConfig struct
    - `grep "SubGeo\|subdomains.geo" internal/core/config/config.go` returns no matches (no invented Geo struct)
    - `grep -c "koanf:" internal/core/config/config.go` is higher than before (3 new koanf tags added)
    - No existing koanf tag or field is removed (diff shows only insertions)
  </acceptance_criteria>
  <done>Three new config fields added; config package builds; all new fields have koanf tags and validate constraints; no SubGeo struct invented.</done>
</task>

<task type="auto">
  <name>Task 2: Add Tool.InputFlag + expand tools.lock to 26 entries + create subdomains doc.go</name>
  <read_first>
    - internal/core/backend/backend.go (Tool struct — add InputFlag field)
    - internal/core/backend/tools.lock (existing 10-entry TOML — append 16 new entries)
    - modules/axiom.sh lines 1-55 (axiom-scan CLI surface — confirms module names and input conventions)
    - modules/subdomains.sh lines 507-553 (sub_passive — source tool list for passive tasks)
    - .planning/codebase/STACK.md (tool inventory for go_module paths)
    - internal/modules/demo/noop.go (read pattern; then this file will be deleted in plan-01)
  </read_first>
  <action>
    Step A — Edit `internal/core/backend/backend.go`: in the `Tool` struct, add after the `Critical bool` field:
      InputFlag string
    Doc comment: "InputFlag is the CLI flag (or empty string for positional last arg) that receives the input file in Axiom fleet splits. E.g. puredns uses InputFlag='' (positional), dnsx uses InputFlag='-l', tlsx uses InputFlag='-l', s3scanner uses InputFlag='--bucket-file'. Empty string = positional last argument. Used by AxiomBackend.Exec to split the correct argument; prevents the extractInputFile heuristic bug (REVIEWS finding #5)."
    No other changes to backend.go.

    Step B — Edit `internal/core/backend/tools.lock`: append 16 new [[tools]] entries after the existing 10. Add a comment `# Phase 4 expansion — plan-00` before the first new entry. Use the exact TOML schema already in the file. Per-tool entries:

      github-subdomains: go_module="github.com/gwen001/github-subdomains", timeout_seconds=120, critical=false, input_flag=""
      gitlab-subdomains: go_module="github.com/gwen001/gitlab-subdomains", timeout_seconds=120, critical=false, input_flag=""
      tlsx: go_module="github.com/projectdiscovery/tlsx/cmd/tlsx", timeout_seconds=300, critical=false, input_flag="-l"
      dnstake: go_module="github.com/pwnesia/dnstake", timeout_seconds=600, critical=false, input_flag=""
      regulator: kind="python", pip_package="git+https://github.com/cramppet/regulator", timeout_seconds=600, critical=false, input_flag=""
      hakip2host: go_module="github.com/hakluke/hakip2host", timeout_seconds=120, critical=false, input_flag=""
      analyticsrelationships: go_module="github.com/Josue87/analyticsrelationships", timeout_seconds=120, critical=false, input_flag=""
      favirecon: go_module="github.com/edoardottt/favirecon/cmd/favirecon", timeout_seconds=300, critical=false, input_flag=""
      subjs: go_module="github.com/lc/subjs", timeout_seconds=120, critical=false, input_flag=""
      jsluice: go_module="github.com/BishopFox/jsluice/cmd/jsluice", timeout_seconds=300, critical=false, input_flag=""
      massdns: kind="system", timeout_seconds=0, critical=false, input_flag="" # build from source
      urlfinder: go_module="github.com/projectdiscovery/urlfinder/cmd/urlfinder", timeout_seconds=180, critical=false, input_flag=""
      unfurl: go_module="github.com/tomnomnom/unfurl", timeout_seconds=30, critical=false, input_flag=""
      axiom-scan: kind="system", timeout_seconds=0, critical=false, input_flag="" # part of axiom installation
      puredns: go_module="github.com/d3mondev/puredns/v2", timeout_seconds=3600, critical=true, input_flag="" # positional: puredns bruteforce <wordlist> -d <domain> OR puredns resolve <domains>
      dnscewl: go_module="github.com/codingo/dnscewl", timeout_seconds=600, critical=false, input_flag="-f" # DNS permutation wordlist generator (B1/W1 — SUBD-03 third permut tool named in D-01); install ambiguity noted — verify go_module path against pkg before installing

    NOTE: puredns already exists in the seed (critical=true); update its entry to add input_flag="" (positional). Do not create a duplicate — check existing entries before appending. Total entries after expansion: 26.

    Step C — Create `internal/modules/subdomains/doc.go` with the following content (exact — do not abbreviate):
      package subdomains
      // Package subdomains implements reconFTW v2's subdomain enumeration pipeline.
      //
      // STAGING CONTRACT (addresses REVIEWS finding #2 + #3 — OutputTree.Append=REPLACE):
      // Tasks do NOT call app.Tree.Append("subdomains", ...) directly during Run.
      // Instead, each Task writes its raw output (one hostname per line) to a private
      // staging file at:   workspaces/<target>/inputs/passive.<tool>.txt
      //                    workspaces/<target>/inputs/resolved.<tool>.txt
      //                    workspaces/<target>/inputs/brute.<tool>.txt
      // The command layer (newSubsCmd in cmd/reconftw/stub_subcommands.go) calls
      // MergeStage(app, stageName) after each RunStage call returns. MergeStage:
      //   1. Reads all inputs/<stage>.*.txt files
      //   2. Deduplicates (anew-equivalent, case-insensitive, sorted)
      //   3. Converts each hostname to SubdomainRecord{subdomain, source="merged", first_seen}
      //   4. Calls app.Tree.Append("subdomains", records) ONCE as the single writer
      // This ensures REPLACE semantics are safe (one writer per artefact per stage).
      //
      // CONFIG NOTE — SubGeoTask.Enabled (W5):
      // SubGeoTask.Enabled returns cfg.Subdomains.Enabled (the parent flag). There is NO
      // separate geo flag in v1 and NO cfg.Subdomains.Geo struct exists or should be added.
      // Executors MUST NOT invent a cfg.Subdomains.Geo field. Geo always runs when the
      // subdomains module is enabled.
      //
      // FINDINGS STAGING CONTRACT (B2):
      // TakeoverSubzyTask and TakeoverDNSTakeTask do NOT call app.Tree.Append("findings", ...)
      // directly during Run. Each writes its own staging file:
      //   inputs/takeover.subzy.jsonl and inputs/takeover.dnstake.jsonl
      // The command layer reads both files and calls app.Tree.Append("findings", merged) ONCE
      // after the enrichment RunStage completes (single-writer-per-artefact invariant).
      // Similarly: buckets, asns, hosts artefacts each have exactly ONE writer per RunStage.
      //
      // All Tasks in this package self-register via init() calls. Blank-import
      // this package in cmd/reconftw/modules.go to trigger registration.
  </action>
  <verify>
    <automated>go build -o /dev/null ./... 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `go build -o /dev/null ./...` exits 0 (Tool struct change is additive — zero-value "" is safe)
    - `grep "InputFlag" internal/core/backend/backend.go` returns match inside Tool struct
    - `grep -c "^\[\[tools\]\]" internal/core/backend/tools.lock` outputs 26 (10 seed + 16 new; check existing count before asserting exact number — 10 seed → 26 total)
    - `grep "dnscewl" internal/core/backend/tools.lock` returns match (B1/W1 — SUBD-03 third permut tool)
    - `internal/modules/subdomains/doc.go` exists with `package subdomains` and the staging contract comment
    - `grep "STAGING CONTRACT\|SubGeoTask.Enabled\|cfg.Subdomains.Enabled\|FINDINGS STAGING CONTRACT" internal/modules/subdomains/doc.go` returns matches
    - `grep "SubGeo\|Subdomains.Geo" internal/core/config/config.go` returns no matches (no invented Geo struct)
    - `go vet ./...` exits 0
  </acceptance_criteria>
  <done>Tool.InputFlag added; tools.lock at 26 entries including dnscewl; subdomains staging contract + SubGeoTask.Enabled invariant + findings staging contract documented; build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config struct → all Tasks | New MinResolvers/MinFreeMemGB/FailoverThreshold fields consumed without validation bypass |
| Tool.InputFlag → AxiomBackend.Exec | Axiom splits the field named by InputFlag — malicious InputFlag value in tools.lock could redirect fleet input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-00-01 | Tampering | Tool.InputFlag value in tools.lock | mitigate | InputFlag values are set at build time in tools.lock (version-controlled); executor validates InputFlag matches known CLI flag syntax `^(-[a-zA-Z-]+)?$` before use |
| T-04-00-02 | Denial of Service | MinFreeMemGB=0 disables back-pressure | accept | Zero disables the gate (operator choice); documented in field comment; non-zero is the recommended production value |
| T-04-00-03 | Tampering | FailoverThreshold=1 causes immediate kill-switch | accept | Minimum validate:"min=1" enforced; threshold=1 is valid emergency mode; not an attack surface since it only affects axiom fallback behavior |
| T-04-00-SC | Tampering | tools.lock new entries (16 Go/Python installs including dnscewl) | mitigate | Executor must verify module paths against STACK.md before appending; dnscewl go_module path flagged with install-ambiguity note — requires human checkpoint per slopcheck protocol; Phase 11 adds SHA-256 pins |
</threat_model>

<verification>
- `go build -o /dev/null ./...` exits 0
- `go vet ./...` exits 0
- `grep "MinResolvers\|MinFreeMemGB\|FailoverThreshold" internal/core/config/config.go` returns 3 matches
- `grep "InputFlag" internal/core/backend/backend.go` returns 1 match
- `grep -c "^\[\[tools\]\]" internal/core/backend/tools.lock` equals 26
- `grep "dnscewl" internal/core/backend/tools.lock` returns match
- `grep "STAGING CONTRACT\|SubGeoTask" internal/modules/subdomains/doc.go` returns matches
</verification>

<success_criteria>
- All 3 missing config fields present with koanf tags and validate constraints
- Tool.InputFlag field on Tool struct enables per-tool Axiom input-file declaration
- tools.lock at 26 entries covering all Phase 4 subdomain tools including dnscewl (B1/W1 fix)
- Subdomains package doc declares the staging contract, SubGeoTask.Enabled invariant (W5), and findings staging contract (B2 traceability)
- Full build and vet green
</success_criteria>

<output>
Create `.planning/phases/04-subdomains-e2e-axiom-integration/04-00-SUMMARY.md` when done.
</output>
