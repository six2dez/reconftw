---
phase: 05-web-pipeline-e2e
plan: 10
type: execute
wave: 2
gap_closure: true
depends_on:
  - "05-09"
files_modified:
  - cmd/reconftw/stub_subcommands.go
autonomous: true
requirements:
  - WEB-01
  - WEB-04
  - WEB-06
  - WEB-07
  - WEB-14
  - WEB-16

must_haves:
  truths:
    - "Stage definitions split so that wafw00f/cdncheck precede nuclei (DependsOn web.wafw00f honored)"
    - "Stage definitions split so that katana/urlfinder/waymore precede urldedup (DependsOn honored)"
    - "Stage definitions split so that urldedup precedes subjs/jsluice/jsa/mantra (DependsOn honored)"
    - "Stage definitions split so that subjs/sourcemapper precede jsluice (DependsOn honored)"
    - "MergeStage is called after EVERY stage that has multi-writer participants (waf, urls, findings)"
    - "MergeAllWebArtefacts call at end removed or left as a final safety sweep only"
    - "go build ./... exits 0"
    - "go test ./... exits 0"
  artifacts:
    - path: "cmd/reconftw/stub_subcommands.go"
      provides: "split stage definitions with sequential ordering that honors DependsOn"
      contains: "analysis-waf"
  key_links:
    - from: "stub_subcommands.go:stageSpec slice"
      to: "web.nuclei DependsOn web.wafw00f"
      via: "analysis-waf stage precedes analysis stage"
      pattern: "analysis-waf"
    - from: "stub_subcommands.go:stageSpec slice"
      to: "web.urldedup DependsOn [web.katana,web.urlfinder,web.waymore]"
      via: "urls-fetch stage precedes urls-dedup stage"
      pattern: "urls-fetch"
---

<objective>
Fix the DependsOn violations caused by intra-stage ordering (CR-04). Wave 2 — blocked on 05-09
(staging contract must be in place before stage split, since urldedup now reads inputs/urls.*.jsonl
that only populate after the urls-fetch stage).

RunStage fires every task in a slice CONCURRENTLY and does NOT enforce DependsOn ordering;
ordering comes from SEQUENTIAL stage calls. The current stage layout puts mutually-dependent
tasks in the same concurrent stage.

Split the existing 4 stages into 8 stages that honor all declared DependsOn edges:

  probe           → [web.httpx]
  analysis-waf    → [web.wafw00f, web.cdncheck]           (must complete before nuclei reads waf.jsonl)
  analysis        → [web.nuclei, web.screenshot, web.ffuf, web.favirecon, web.vhostfinder,
                     web.hakoriginfinder, web.csprecon]     (nuclei DependsOn wafw00f ✓)
  urls-fetch      → [web.katana, web.urlfinder, web.waymore]
  urls-dedup      → [web.urldedup]                         (urldedup DependsOn katana/urlfinder/waymore ✓)
  js-extract      → [web.subjs, web.sourcemapper]          (subjs DependsOn urldedup ✓)
  js-analyze      → [web.jsluice, web.jsa, web.mantra]     (jsluice DependsOn subjs/sourcemapper ✓;
                                                             jsa/mantra DependsOn subjs ✓)
  bypass          → [web.nomore403, web.shortscan, web.gxss, web.arjun]

Wire MergeStage calls after each stage that has multi-writer participants. Specifically:
  - after analysis-waf: MergeStage(ctx, app, "waf")
  - after analysis: MergeStage(ctx, app, "findings")
  - after js-analyze: MergeStage(ctx, app, "urls") to absorb jsa/jsluice/mantra url staging files
  - after bypass: MergeStage(ctx, app, "findings") for nomore403/shortscan/gxss/arjun

NOTE: stub_subcommands.go also has a printWebDryRun function with the same stage list — update
BOTH the live-run stages slice and the dry-run stages slice to stay consistent.

Purpose: Ensure declared DependsOn ordering is actually honored in execution.
Output: Modified cmd/reconftw/stub_subcommands.go (stage definitions + merge calls).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-web-pipeline-e2e/05-REVIEW.md
@.planning/phases/05-web-pipeline-e2e/05-09-SUMMARY.md
@internal/core/scheduler/scheduler.go
@internal/modules/web/merge.go
@internal/modules/web/nuclei.go
@internal/modules/web/jsluice.go
@internal/modules/web/urldedup.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split stage definitions and wire per-stage MergeStage calls in stub_subcommands.go</name>
  <files>cmd/reconftw/stub_subcommands.go</files>
  <read_first>
    cmd/reconftw/stub_subcommands.go lines 690-795 — current stage definitions (stageSpec slice + RunStage loop + MergeAllWebArtefacts call)
    internal/core/scheduler/scheduler.go lines 132-170 — RunStage runs all tasks CONCURRENTLY; ordering is via sequential stage calls only
    .planning/phases/05-web-pipeline-e2e/05-REVIEW.md §CR-04 — exact DependsOn violations and proposed fix
    internal/modules/web/nuclei.go:91 — DependsOn ["web.httpx", "web.wafw00f"]
    internal/modules/web/jsluice.go:65 — DependsOn ["web.subjs","web.sourcemapper"]
    internal/modules/web/urldedup.go:55 — DependsOn ["web.katana","web.urlfinder","web.waymore"]
    internal/modules/web/merge.go:40 — MergeStage signature (ctx, app, stage string)
    internal/modules/web/merge.go:127 — MergeAllWebArtefacts signature
  </read_first>
  <action>
    In newWebCmd() RunE function, find the stageSpec slice (currently 4 stages) and the
    accompanying for range loop that calls RunStage.

    REPLACE the 4-stage stageSpec slice with 8 stages (probe + analysis-waf + analysis +
    urls-fetch + urls-dedup + js-extract + js-analyze + bypass):

    stages := []stageSpec{
      {name: "probe",        prefixes: ["web.httpx"]},
      {name: "analysis-waf", prefixes: ["web.wafw00f", "web.cdncheck"]},
      {name: "analysis",     prefixes: ["web.nuclei", "web.screenshot", "web.ffuf",
                                         "web.favirecon", "web.vhostfinder",
                                         "web.hakoriginfinder", "web.csprecon"]},
      {name: "urls-fetch",   prefixes: ["web.katana", "web.urlfinder", "web.waymore"]},
      {name: "urls-dedup",   prefixes: ["web.urldedup"]},
      {name: "js-extract",   prefixes: ["web.subjs", "web.sourcemapper"]},
      {name: "js-analyze",   prefixes: ["web.jsluice", "web.jsa", "web.mantra"]},
      {name: "bypass",       prefixes: ["web.nomore403", "web.shortscan", "web.gxss", "web.arjun"]},
    }

    In the RunStage loop, ADD per-stage MergeStage calls AFTER each relevant stage:
    - After analysis-waf stage: call web.MergeStage(ctx, app, "waf") — merges wafw00f+cdncheck staging
    - After analysis stage: call web.MergeStage(ctx, app, "findings") — merges nuclei staging
    - After urls-dedup stage: urldedup calls app.Tree.Append("urls") directly (single writer per 05-09)
      — NO separate MergeStage needed for urls here; urldedup IS the merge step
    - After js-analyze stage: call web.MergeStage(ctx, app, "urls") — merges jsa/jsluice staging
    - After bypass stage: call web.MergeStage(ctx, app, "findings") — merges nomore403/shortscan/gxss/arjun

    Keep MergeAllWebArtefacts at the end as a safety sweep (it only processes non-empty globs,
    so already-merged artefacts are no-ops). Alternatively, replace it with targeted per-artefact
    calls if cleaner — either is acceptable.

    ALSO update the stageSpec slice in printWebDryRun() to match the new 8-stage structure for
    consistency (the dry-run is documentation; keep it synchronized).

    Preserve the progress.StageStart / progress.StageDone calls per stage (may need to add them
    for the new stages — keep the same pattern as existing stages).

    Preserve the best_effort error logging pattern: each RunStage error is logged + continued.
    Each MergeStage error is logged at Warn + continued.

    Do NOT modify any Task file in this plan — stub_subcommands.go only.
  </action>
  <verify>
    <automated>cd /Users/six2dez/Tools/reconftw &amp;&amp; go build ./... 2>&amp;1 | head -20 &amp;&amp; go test ./... 2>&amp;1 | tail -10</automated>
  </verify>
  <done>
    go build ./... exits 0.
    grep 'analysis-waf' cmd/reconftw/stub_subcommands.go — finds the new stage name in BOTH the live RunE and the dry-run function.
    grep 'MergeStage' cmd/reconftw/stub_subcommands.go | wc -l — at least 4 calls (after analysis-waf, after analysis, after js-analyze, after bypass).
    grep 'urls-fetch\|urls-dedup\|js-extract\|js-analyze' cmd/reconftw/stub_subcommands.go — all 4 new stage names present.
    go test ./... exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stage ordering → task execution | Sequential stage calls enforce DependsOn; concurrent tasks within a stage share no files after 05-09 staging fix |
| MergeStage calls → app.Tree.Append | Single-writer invariant per artefact per stage; scope re-validated at Append |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10-01 | Tampering | Race condition between concurrent same-stage Append calls (CR-04) | mitigate | Split stages enforce ordering; after 05-09, same-artefact writers are in different stages or write staging files |
| T-05-10-02 | Denial of Service | Extra stages increase total sequential execution time | accept | Sequential stage calls are the designed mechanism; 8 stages vs 4 adds minimal overhead (stage orchestration is microseconds) |
| T-05-10-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies |
</threat_model>

<verification>
go build ./... — exits 0.
grep -c 'analysis-waf' cmd/reconftw/stub_subcommands.go — shows the new stage in both live and dry-run.
go test ./... — exits 0 (no regressions; default gate GREEN).
</verification>

<success_criteria>
Stage definitions split into 8 sequential stages that honor all declared DependsOn edges.
Per-stage MergeStage calls placed after multi-writer stages.
MergeAllWebArtefacts remains as final safety sweep.
printWebDryRun reflects the same 8-stage structure.
Default go test ./... is GREEN at end of this wave.
</success_criteria>

<output>
Create .planning/phases/05-web-pipeline-e2e/05-10-SUMMARY.md when done.
</output>
