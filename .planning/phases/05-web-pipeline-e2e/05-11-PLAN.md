---
phase: 05-web-pipeline-e2e
plan: 11
type: execute
wave: 3
gap_closure: true
depends_on:
  - "05-09"
  - "05-10"
files_modified:
  - internal/modules/web/jsa.go
  - internal/modules/web/hakoriginfinder.go
  - internal/modules/web/hakoriginfinder_test.go
  - internal/modules/web/mantra.go
  - internal/modules/web/gxss.go
  - internal/modules/web/nomore403.go
  - internal/modules/web/shortscan.go
  - internal/modules/web/screenshot.go
  - internal/modules/web/httpx.go
  - internal/modules/web/cdncheck.go
  - internal/extract/waf/waf.go
  - internal/extract/favicon/web.go
  - internal/extract/js/secrets.go
  - internal/modules/web/sourcemapper.go
  - internal/modules/web/favirecon.go
autonomous: true
requirements:
  - WEB-01
  - WEB-02
  - WEB-04
  - WEB-07
  - WEB-08
  - WEB-12
  - WEB-13
  - WEB-15

must_haves:
  truths:
    - "jsa.go invokes python3 via direct exec.Command (same pattern as mantra/nomore403) not app.Tools.Run with absolute path (CR-03)"
    - "jsa.go has no var _ = strings.TrimSpace blank suppression (IN-02)"
    - "hakoriginfinder.go parses real tool output format (host:origin_ip from tool lines) with Confidence:low not index-based fabrication (CR-06)"
    - "hakoriginfinder_test.go tests TestHakoriginfinderPerHostAttribution: correct host↔IP pairing, not index-based (CR-06)"
    - "hakoriginfinder_test.go tests that jsa.go Run() uses exec.Command not app.Tools.Run (CR-03 behavioral test)"
    - "go test ./internal/modules/web/... passes for hakoriginfinder_test.go (CR-03 + CR-06 coverage)"
    - "Every raw exec.CommandContext in nomore403/mantra/gxss/hakoriginfinder/shortscan wraps ctx with context.WithTimeout using tool's tools.lock Timeout (CR-07)"
    - "screenshot.go uses stdin pipe not -l flag (WR-02), or vice versa — whichever matches DoD-1 golden — with an explicit log.Warn when count==0 and hosts non-empty"
    - "favicon/web.go field name corrected (Domain renamed to Host, json:\"host\") and favirecon.go updated to use r.Host not r.Domain — both files compile together (WR-03)"
    - "favirecon.go has no reference to WebResult.Domain — grep 'r\\.Domain' internal/modules/web/favirecon.go exits 1 (no matches)"
    - "jsa.go / sourcemapper.go fan-out limit derived from cfg.Concurrency.MaxJobs not hardcoded 5 (WR-04)"
    - "waf/waf.go uses strings.SplitN(line, ' : ', 2) not LastIndex (WR-05)"
    - "hakoriginfinder.go readIPsFromJSONL / readHostnamesFromJSONL use bufio.Scanner not os.ReadFile+strings.Split (WR-06)"
    - "mantra.go strips ANSI escapes before tokenizing; doc/code match (WR-07)"
    - "httpx.go checkHostsFileReadable validates file existence and IsRegular only; doc comment states operator-trust assumption and WR-08 removal reason (WR-08)"
    - "cdncheck.go stale stdin workaround comment deleted (WR-09)"
    - "js/secrets.go doc comment shows 'kind' not 'type' (IN-01)"
    - "go test ./... exits 0"
  artifacts:
    - path: "internal/modules/web/jsa.go"
      provides: "direct exec.Command invocation for venv python3"
      contains: "exec.CommandContext"
    - path: "internal/modules/web/hakoriginfinder.go"
      provides: "real output parsing or per-host run with Confidence:low"
      contains: "Confidence"
    - path: "internal/modules/web/hakoriginfinder_test.go"
      provides: "TestHakoriginfinderPerHostAttribution + CR-03 behavioral test for jsa"
      contains: "TestHakoriginfinderPerHostAttribution"
    - path: "internal/extract/waf/waf.go"
      provides: "SplitN on ' : '"
      contains: "SplitN"
    - path: "internal/modules/web/httpx.go"
      provides: "checkHostsFileReadable with operator-trust doc comment"
      contains: "checkHostsFileReadable"
    - path: "internal/extract/favicon/web.go"
      provides: "WebResult.Host field (renamed from Domain)"
      contains: "Host.*json:\"host\""
    - path: "internal/modules/web/favirecon.go"
      provides: "FaviconRecord uses r.Host not r.Domain (updated to match WR-03 rename)"
      contains: "r.Host"
  key_links:
    - from: "jsa.go:runJSAForURL"
      to: "exec.CommandContext(ctx, jsaPython, ...)"
      via: "direct exec not app.Tools.Run registry lookup"
      pattern: "exec\\.CommandContext"
    - from: "hakoriginfinder.go:parseHakoriginfinderOutput"
      to: "line-level host:ip parsing"
      via: "real output format not index assumption"
      pattern: "Confidence.*low"
    - from: "nomore403/mantra/gxss/hakoriginfinder/shortscan"
      to: "context.WithTimeout"
      via: "tools.lock Timeout bound"
      pattern: "WithTimeout"
    - from: "hakoriginfinder_test.go:TestHakoriginfinderPerHostAttribution"
      to: "hakoriginfinder.go:Run()"
      via: "table-driven host↔IP pairs"
      pattern: "PerHostAttribution"
    - from: "internal/modules/web/favirecon.go:FaviconRecord assignment"
      to: "internal/extract/favicon/web.go:WebResult.Host"
      via: "Domain→Host rename (WR-03)"
      pattern: "r\\.Host"
---

<objective>
Fix the remaining localized blockers and all warnings that are co-located with blocker/warning
fixes in this wave. Does NOT touch stub_subcommands.go or merge.go (owned by 05-09/05-10).

Covers: CR-03 (jsa tool invocation), CR-06 (hakoriginfinder wrong attribution),
CR-07 (missing per-tool timeouts on 5 raw exec.Command tasks), WR-02 (screenshot arg doc/code
mismatch), WR-03 (favicon Host field + delete item() — ALSO update favirecon.go which reads
r.Domain from WebResult), WR-04 (jsa/sourcemapper fan-out from config), WR-05 (waf SplitN fix),
WR-06 (bufio.Scanner in hakoriginfinder), WR-07 (mantra ANSI strip + doc fix), WR-08 (httpx
validateHostsPath → checkHostsFileReadable: remove misleading .. check, document operator-trust
assumption), WR-09 (cdncheck stale comment), IN-01 (js/secrets.go doc fix), IN-02 (jsa.go blank
import suppression).

New behavioral tests: hakoriginfinder_test.go adds TestHakoriginfinderPerHostAttribution (CR-06
correct host↔IP pairing) and a test that verifies jsa.go uses direct exec.Command not
app.Tools.Run (CR-03). CR-07 timeout remains grep-verified.

Purpose: Complete the gap closure after the systemic staging-contract and stage-ordering fixes.
Output: 13 modified files (including favirecon.go for WR-03 co-change) + 1 new test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-web-pipeline-e2e/05-REVIEW.md
@.planning/phases/05-web-pipeline-e2e/05-10-SUMMARY.md
@internal/modules/web/jsa.go
@internal/modules/web/hakoriginfinder.go
@internal/modules/web/mantra.go
@internal/core/backend/tools.lock
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CR-03 + CR-07 — Fix jsa tool invocation; add per-tool timeouts to 5 raw exec tasks</name>
  <files>internal/modules/web/jsa.go, internal/modules/web/mantra.go, internal/modules/web/gxss.go, internal/modules/web/nomore403.go, internal/modules/web/shortscan.go, internal/modules/web/hakoriginfinder.go</files>
  <read_first>
    internal/modules/web/jsa.go lines 142-179 — current buggy app.Tools.Run(ctx, jsaPython, args) in runJSAForURL
    internal/modules/web/nomore403.go lines 92-112 — canonical direct exec.Command pattern to mirror
    internal/modules/web/mantra.go lines 91-110 — current exec.CommandContext (needs timeout)
    internal/modules/web/gxss.go lines 55-90 — current exec.CommandContext (needs timeout)
    internal/modules/web/shortscan.go lines 55-130 — current exec.LookPath + exec.CommandContext
    internal/modules/web/hakoriginfinder.go lines 118-140 — current exec.CommandContext
    internal/core/backend/tools.lock — timeout_seconds values: shortscan=300, nomore403=300, mantra=300, Gxss=120, hakoriginfinder=120
    .planning/phases/05-web-pipeline-e2e/05-REVIEW.md §CR-03 — exact fix: direct exec.Command for repo-clone venv tools
    .planning/phases/05-web-pipeline-e2e/05-REVIEW.md §CR-07 — exact fix: context.WithTimeout from tool Timeout
  </read_first>
  <behavior>
    CR-03 fix for jsa.go:
    In runJSAForURL, DELETE the app.Tools.Run(ctx, jsaPython, args) call.
    REPLACE with a direct exec.CommandContext pattern:
      - Derive a per-invocation timeout context: toolTimeout = 30 * time.Second (conservative
        per-URL cap; no JSA per-URL entry in tools.lock)
      - Use: cmdCtx, cancel := context.WithTimeout(ctx, toolTimeout); defer cancel()
      - cmd := exec.CommandContext(cmdCtx, jsaPython, append([]string{jsaScript, "-f", jsURL}...)...)
      - cmd.Stdout = &bytes.Buffer{}; cmd.Stderr = io.Discard (or log to app.Log.Debug)
      - Capture stdout, run cmd.Run(), handle non-zero exit as non-fatal
    Remove the (now unused) app.Tools reference from runJSAForURL args, or keep app *appctx.AppContext
    for logging. jsaPython/jsaScript come from Run() outer scope.

    Also for jsa.go (IN-02): Delete the line "var _ = strings.TrimSpace" and the corresponding
    strings import if strings is not used elsewhere in jsa.go.

    Also for jsa.go (WR-04): Replace "const maxConcurrency = 5" with:
      maxConcurrency := cfg.Concurrency.MaxJobs
      if maxConcurrency <= 0 { maxConcurrency = 5 }

    NOTE: The fan-out collector fix (all goroutines write to allRecords in-memory, WriteJSONL
    called ONCE) was applied in 05-09. This plan only changes the runJSAForURL exec pattern.
    The collector pattern in Run() is already correct — do not change it here.

    CR-07 fix for mantra.go:
    Before "cmd := exec.CommandContext(ctx, mantraBin, args...)", derive a bounded context:
      toolTimeout := 300 * time.Second  // matches tools.lock mantra.timeout_seconds=300
      cmdCtx, cancel := context.WithTimeout(ctx, toolTimeout)
      defer cancel()
    Change exec.CommandContext(ctx, ...) to exec.CommandContext(cmdCtx, ...).

    CR-07 fix for gxss.go:
    toolTimeout := 120 * time.Second  // tools.lock Gxss.timeout_seconds=120
    Same pattern: derive cmdCtx, use it in exec.CommandContext.

    CR-07 fix for nomore403.go:
    toolTimeout := 300 * time.Second  // tools.lock nomore403.timeout_seconds=300
    Same pattern.

    CR-07 fix for shortscan.go:
    toolTimeout := 300 * time.Second  // tools.lock shortscan.timeout_seconds=300
    Apply context.WithTimeout per-invocation in runShortscan.

    CR-07 fix for hakoriginfinder.go:
    toolTimeout := 120 * time.Second  // tools.lock hakoriginfinder.timeout_seconds=120
    Apply to the exec.CommandContext call.

    Add time and context imports to each file where context.WithTimeout is added.
    context is likely already imported in all these files (it's in the Run signature).
    Add time import only where needed.
  </behavior>
  <action>
    jsa.go: in runJSAForURL, remove app.Tools.Run call, add direct exec.CommandContext with
    per-URL 30s timeout context. Remove "var _ = strings.TrimSpace" and unused strings import.
    Replace "const maxConcurrency = 5" with cfg-derived value.

    mantra.go: in Run, wrap exec.CommandContext(ctx, ...) with a 300s timeout context derived
    from context.WithTimeout.

    gxss.go: in Run, wrap exec.CommandContext(ctx, ...) with a 120s timeout context.

    nomore403.go: in Run, wrap exec.CommandContext(ctx, ...) with a 300s timeout context.

    shortscan.go: in runShortscan, wrap exec.CommandContext with a 300s timeout context
    (per-invocation; shortscan is called in a loop per IIS target).

    hakoriginfinder.go: in Run, wrap exec.CommandContext(ctx, hakoBin, args...) with a 120s
    timeout context. Also apply WR-06 (see Task 2 for hakoriginfinder bufio.Scanner).

    Import addition: add "time" to import blocks where needed. Verify context is already imported.
  </action>
  <verify>
    <automated>cd /Users/six2dez/Tools/reconftw &amp;&amp; go build ./internal/modules/web/... 2>&amp;1 | head -20</automated>
  </verify>
  <done>
    go build ./internal/modules/web/... exits 0.
    grep 'Tools.Run' internal/modules/web/jsa.go | wc -l outputs 0 (no more app.Tools.Run in jsa).
    grep 'exec.CommandContext' internal/modules/web/jsa.go outputs the new direct invocation.
    grep 'TrimSpace' internal/modules/web/jsa.go | grep 'var _' | wc -l outputs 0 (blank suppression gone).
    grep -l 'context.WithTimeout' internal/modules/web/mantra.go internal/modules/web/gxss.go internal/modules/web/nomore403.go internal/modules/web/shortscan.go internal/modules/web/hakoriginfinder.go — all 5 files listed.
    grep 'MaxJobs' internal/modules/web/jsa.go — shows maxConcurrency derived from cfg.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CR-06 + WR-06 + behavioral tests — Fix hakoriginfinder output parsing; add hakoriginfinder_test.go</name>
  <files>internal/modules/web/hakoriginfinder.go, internal/modules/web/hakoriginfinder_test.go</files>
  <read_first>
    internal/modules/web/hakoriginfinder.go lines 187-224 — current buggy index-based attribution
    .planning/phases/05-web-pipeline-e2e/05-REVIEW.md §CR-06 — exact defect + fix description
    .planning/phases/05-web-pipeline-e2e/05-REVIEW.md §WR-06 — bufio.Scanner for readIPsFromJSONL/readHostnamesFromJSONL
    internal/modules/web/nuclei.go lines 328-329 — canonical bufio.Scanner with 4MiB buffer pattern
    internal/modules/web/jsa.go lines 142-179 — post-Task-1 state: now uses exec.CommandContext directly
  </read_first>
  <behavior>
    CR-06 fix for hakoriginfinder.go (per-host attribution):
    Replace parseHakoriginfinderOutput (index-based) with per-host run approach:
    - Instead of feeding ALL IPs as stdin in one batch, loop over each host individually
    - For each host: run hakoriginfinder per-host, parse output for origin IP
    - Write OriginRecord{Host: host, OriginIP: ip, Method: "hakoriginfinder", Confidence: "low"}
    - This eliminates index-based fabrication: each host unambiguously produces its own result

    Implement in Run(): refactor to use a combined JSONL read that pairs Host+IP from each record:
      type hostIPPair struct { host, ip string }
    Read hosts.jsonl once and collect pairs. Loop over pairs, run per-host with
    runHakoriginfinderForHost(ctx, hakoBin, host, ip, timeout). Write OriginRecord on match.

    WR-06 fix for readIPsFromJSONL and readHostnamesFromJSONL:
    Both currently use os.ReadFile + strings.Split(string(data), "\n"). Replace with bufio.Scanner:
      f, _ := os.Open(hostsPath)
      scanner := bufio.NewScanner(f)
      scanner.Buffer(make([]byte, 0, 64*1024), 4*1024*1024)
      for scanner.Scan() { ... json.Unmarshal(scanner.Bytes()) ... }
    Matching the pattern in nuclei.go lines 328-329 and cdncheck.go's readIPsFromHostsJSONL.

    Behavioral tests (WARNING-4): Create internal/modules/web/hakoriginfinder_test.go with:

    TestHakoriginfinderPerHostAttribution (CR-06):
    - Table-driven test with 3 hostIPPair entries: host1/ip1, host2/ip2, host3/ip3
    - Mock exec function returns per-host output: "host1 -> ip1_origin", "host2 -> ip2_origin", etc.
    - Assert that each OriginRecord.Host matches the host from the pair, NOT the index
    - Specifically: verify that swapping two pairs in the input does not silently swap their origins
    - Use the exported runHakoriginfinderForHost function (or the test may call the internal logic
      directly via a table of (host, mockOutput) → (expectedOriginIP, expectedHost) assertions)
    - If runHakoriginfinderForHost is unexported, expose a testable parseHakoriginOutput(output, host string) string
      helper and test that directly

    TestJSAUsesDirectExec (CR-03):
    - Verify that jsa.go does NOT import "github.com/six2dez/reconftw/internal/core/backend" or
      call app.Tools.Run — use a source-code reflection approach: grep the jsa.go source file text
      for "Tools.Run" and assert len == 0
    - Alternative (preferred): check that exec.CommandContext is called, by creating a thin mock
      that replaces the exec call and confirming it is invoked, not app.Tools.Run
    - Simplest acceptable form: a test that compiles jsa_test.go in the web package and asserts
      that JsaTask.Run() completes without calling any method named "Run" on a Tools object
      (verified by providing a panic-on-call mock Tools implementation)

    Add go test ./internal/modules/web/... to the verify block.
  </behavior>
  <action>
    In hakoriginfinder.go: replace parseHakoriginfinderOutput with a per-host function:
    runHakoriginfinderForHost(ctx context.Context, hakoBin, targetHost, inputIP string,
      timeout time.Duration) (originIP string, err error).
    Call this from Run() in a loop over hostIPPairs.

    In readIPsFromJSONL and readHostnamesFromJSONL: replace os.ReadFile+strings.Split with
    bufio.Scanner using 4MiB buffer per the canonical pattern from nuclei.go.

    Create internal/modules/web/hakoriginfinder_test.go in package web_test (or web for access
    to unexported helpers). Include:

    1. TestHakoriginfinderPerHostAttribution: table-driven test verifying that OriginRecord.Host
       matches the input host from the pair, not the line index. Use a mock exec.Cmd or a
       parseHakoriginOutput(output, host string) string test helper that accepts raw stdout and
       the known host. Assert correctness for all 3 table rows including a swap scenario.

    2. TestJSAUsesDirectExec: use a test AppContext with a MockTools implementation whose Run()
       method panics (or records a call). Invoke JsaTask.Run() with a temp directory containing
       a fake jsaPython/jsaScript. Assert that MockTools.Run() was never called. This confirms
       CR-03: jsa bypasses the registry and calls exec directly. Skip the test if jsaPython stat
       fails (tool not installed) — the test verifies the code path, not actual execution.

    Add bufio import to hakoriginfinder.go if not already present.
  </action>
  <verify>
    <automated>cd /Users/six2dez/Tools/reconftw &amp;&amp; go build ./internal/modules/web/... 2>&amp;1 | head -20 &amp;&amp; go test ./internal/modules/web/... -run 'TestHakoriginfinder|TestJSA' -v 2>&amp;1 | tail -30</automated>
  </verify>
  <done>
    go build ./internal/modules/web/... exits 0.
    grep 'hosts\[i\]' internal/modules/web/hakoriginfinder.go | wc -l outputs 0 (index-based attribution gone).
    grep 'Confidence.*low' internal/modules/web/hakoriginfinder.go — present.
    grep 'strings.Split.*\\n' internal/modules/web/hakoriginfinder.go | wc -l outputs 0 (strings.Split line-split gone).
    grep 'bufio.Scanner' internal/modules/web/hakoriginfinder.go — present (4MiB scanner pattern).
    hakoriginfinder_test.go exists and contains TestHakoriginfinderPerHostAttribution.
    hakoriginfinder_test.go contains TestJSAUsesDirectExec.
    go test ./internal/modules/web/... -run 'TestHakoriginfinder|TestJSA' exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 3: WR-02..WR-09 + IN-01 + WR-03 co-change — Fix remaining warnings and info findings across extractor/task files; update favirecon.go for Domain→Host rename</name>
  <files>internal/modules/web/screenshot.go, internal/modules/web/mantra.go, internal/extract/waf/waf.go, internal/extract/favicon/web.go, internal/extract/js/secrets.go, internal/modules/web/httpx.go, internal/modules/web/cdncheck.go, internal/modules/web/sourcemapper.go, internal/modules/web/favirecon.go</files>
  <read_first>
    .planning/phases/05-web-pipeline-e2e/05-REVIEW.md §WR-02 — screenshot stdin vs -l doc/code mismatch
    .planning/phases/05-web-pipeline-e2e/05-REVIEW.md §WR-03 — favicon Domain→Host + delete item()
    .planning/phases/05-web-pipeline-e2e/05-REVIEW.md §WR-04 — jsa/sourcemapper fan-out hardcoded 5
    .planning/phases/05-web-pipeline-e2e/05-REVIEW.md §WR-05 — waf SplitN not LastIndex
    .planning/phases/05-web-pipeline-e2e/05-REVIEW.md §WR-07 — mantra ANSI strip + doc/code mismatch
    .planning/phases/05-web-pipeline-e2e/05-REVIEW.md §WR-08 — httpx validateHostsPath real traversal
    .planning/phases/05-web-pipeline-e2e/05-REVIEW.md §WR-09 — cdncheck stale comment
    .planning/phases/05-web-pipeline-e2e/05-REVIEW.md §IN-01 — js/secrets.go doc fix
    internal/modules/web/screenshot.go lines 14-17 and 134-140 — doc vs code mismatch
    internal/extract/waf/waf.go lines 59-64 — LastIndex bug
    internal/extract/favicon/web.go lines 103,126,139 — Domain field + item() helper
    internal/modules/web/favirecon.go lines 41-43 and 141-148 — FaviconRecord.Domain field + r.Domain assignment that reads WebResult.Domain (BLOCKER: must update when web.go renames Domain→Host)
    internal/modules/web/mantra.go lines 177-190 — extractURLFromMantraLine doc/code + ANSI
    internal/modules/web/httpx.go lines 328-340 — validateHostsPath traversal check
    internal/modules/web/cdncheck.go lines 73-84 — stale comment block
    internal/modules/web/sourcemapper.go lines 66-107 — hardcoded maxConcurrency = 5
  </read_first>
  <action>
    WR-02 (screenshot.go): The code uses -l hostsFile but the doc comment claims stdin.
    The DoD-1 smoke test passed with -l, so -l is the correct form. UPDATE the doc comment
    to match the code: "nuclei -headless -id screenshot -V dir=&lt;dir&gt; -l &lt;hosts_file&gt;". Also add
    an explicit app.Log.Warn("web.screenshot: no screenshots produced but hosts were non-empty —
    verify nuclei headless + templates", "hosts", len(hosts)) when count==0 and non-empty input.

    WR-03 (favicon/web.go + favirecon.go co-change — BOTH files must be updated atomically):
    (1) In internal/extract/favicon/web.go: Rename WebResult.Domain to Host (json:"host") since
    the value is the per-record hostname not the apex domain. Update the struct tag:
      Domain string `json:"domain"`  →  Host string `json:"host"`
    Also update the field assignment inside ExtractWeb (line ~126):
      Domain: host,  →  Host: host,
    (2) Delete the item() identity helper function (lines 136-139). Replace
    "for _, item := range item(items)" with "for _, fi := range items" (rename loop variable
    to avoid shadowing). The loop body is identical; update "item.getString" to "fi.getString".
    (3) In internal/modules/web/favirecon.go: update ALL references to WebResult.Domain to use
    WebResult.Host. Specifically:
      - FaviconRecord struct (line ~42): "Domain string `json:\"domain\"`" — KEEP this field
        name in FaviconRecord (it is the output schema, not the input struct), the value just
        comes from r.Host now.
      - In the FaviconRecord assignment loop (line ~147): change "Domain: r.Domain" to
        "Domain: r.Host" — reads the renamed field from WebResult.
    After this change: grep 'r\.Domain' internal/modules/web/favirecon.go must return 0 matches.
    grep 'r\.Host' internal/modules/web/favirecon.go must show the updated assignment.

    WR-04 (sourcemapper.go): Replace "maxConcurrency = 5" with cfg-derived value:
      maxConcurrency := app.Cfg.Concurrency.MaxJobs
      if maxConcurrency <= 0 { maxConcurrency = 5 }
    Note: jsa.go WR-04 was handled in Task 1 above.

    WR-05 (waf/waf.go): In ExtractWafw00f, replace:
      idx := strings.LastIndex(line, ":")
      host := strings.TrimSpace(line[:idx])
      wafName := strings.TrimSpace(line[idx+1:])
    WITH:
      parts := strings.SplitN(line, " : ", 2)
      if len(parts) != 2 { continue }
      host := strings.TrimSpace(parts[0])
      wafName := strings.TrimSpace(parts[1])
    This correctly splits on " : " (whitespace-colon-whitespace) as wafw00f's actual separator.

    WR-07 (mantra.go): In extractURLFromMantraLine:
    (1) Strip ANSI escape codes before tokenizing. Add at package level:
      var ansiRE = regexp.MustCompile(`\x1b\[[0-9;]*[A-Za-z]`)
    Strip: line = ansiRE.ReplaceAllString(line, "")
    (2) Fix the doc comment: "Returns empty string if no http(s):// prefix token is found"
    (matching actual behavior; not "Returns the full line if no URL separator found").
    (3) Add fallback: scan all fields for the first http(s):// token:
      for _, f := range fields {
        if strings.HasPrefix(f, "http://") || strings.HasPrefix(f, "https://") { return f }
      }

    WR-08 (httpx.go): Rename validateHostsPath to checkHostsFileReadable. Remove the
    strings.Contains(filepath.Clean(path), "..") guard entirely. Keep only:
      info, err := os.Stat(path)
      if err != nil { return error }
      if !info.Mode().IsRegular() { return error }
    Add doc comment to checkHostsFileReadable:
      // checkHostsFileReadable checks that path is accessible and is a regular file.
      // The --hosts flag is operator-supplied on the operator's local machine;
      // arbitrary absolute paths are allowed by design — the operator controls the
      // machine running reconftw. The ".." substring check was removed (WR-08): it
      // was over-inclusive (blocked legitimate filenames containing "..") and under-
      // inclusive (did not prevent absolute path reads, which are the actual exposure).
    Update all call sites of the old name validateHostsPath within httpx.go to use
    checkHostsFileReadable.

    WR-09 (cdncheck.go): Delete lines 73-84 (the 12-line stale comment block about stdin
    workaround). Replace with a single comment: "// cdncheck reads IPs via -i &lt;file&gt; flag."

    IN-01 (js/secrets.go): Find the header example showing {"type":"AWS_ACCESS_KEY",...}.
    Change "type" to "kind" in the doc comment example to match the actual jsluice output
    format and the working parse struct tag (json:"kind").
  </action>
  <verify>
    <automated>cd /Users/six2dez/Tools/reconftw &amp;&amp; go build ./... 2>&amp;1 | head -20 &amp;&amp; go test ./... 2>&amp;1 | tail -10</automated>
  </verify>
  <done>
    go build ./... exits 0.
    grep 'LastIndex.*":"' internal/extract/waf/waf.go | wc -l outputs 0 (LastIndex removed).
    grep 'SplitN.*" : "' internal/extract/waf/waf.go — present.
    grep 'func item(' internal/extract/favicon/web.go | wc -l outputs 0 (item() helper deleted).
    grep 'Domain.*json:"domain"' internal/extract/favicon/web.go | wc -l outputs 0 (WebResult field renamed).
    grep 'Host.*json:"host"' internal/extract/favicon/web.go — present (new WebResult field name).
    grep 'r\.Domain' internal/modules/web/favirecon.go | wc -l outputs 0 (co-change: old field reference gone).
    grep 'r\.Host' internal/modules/web/favirecon.go — present (reads renamed WebResult.Host field).
    grep 'ansiRE' internal/modules/web/mantra.go — present (ANSI strip pattern).
    grep 'func checkHostsFileReadable' internal/modules/web/httpx.go — present (renamed + doc comment).
    grep 'validateHostsPath' internal/modules/web/httpx.go | wc -l outputs 0 (old name gone).
    grep 'strings.Contains.*filepath.Clean.*\.\.' internal/modules/web/httpx.go | wc -l outputs 0 (misleading guard removed).
    grep '"type".*AWS' internal/extract/js/secrets.go | wc -l outputs 0 (doc fixed to "kind").
    go test ./... exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator --hosts path → filesystem read | checkHostsFileReadable: removed misleading .. guard; documented trust assumption (operator machine). Real exposure was absolute path read — now explicitly accepted as operator-controlled. |
| jsa.go absolute jsaPython path → exec | jsaPython validated by os.Stat before exec; path derived from cfg.Paths.DataDir (validated at config load per T-05-14). Not from user-supplied input. |
| mantra ANSI-stripped output → artefact | Only the URL field is written; secret value always replaced with "***" (XCUT-07 preserved). ANSI strip does not change the redaction guarantee. |
| favicon/web.go WebResult → favirecon.go FaviconRecord | Domain→Host rename is a pure Go field rename inside the internal package; no external contract changed. favirecon.go FaviconRecord.Domain (output schema) keeps the name "domain" — only the source field reference changes from r.Domain to r.Host. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11-01 | Information Disclosure | operator --hosts arbitrary-file-read (WR-08) | accept | --hosts is operator-supplied on operator's machine; absolute paths are intentional. checkHostsFileReadable doc comment states trust assumption explicitly. |
| T-05-11-02 | Denial of Service | Raw exec.Command without timeout stalls stage (CR-07) | mitigate | context.WithTimeout wraps every raw exec.CommandContext; derived from tools.lock Timeout values |
| T-05-11-03 | Tampering | hakoriginfinder wrong host attribution (CR-06) | mitigate | Per-host run ensures unambiguous Host↔OriginIP attribution; Confidence:"low" signals uncertainty |
| T-05-11-04 | Tampering | JSA absolute-path registry bypass (CR-03) | mitigate | Direct exec.Command with validated path; registry lookup removed (was the failure vector) |
| T-05-11-05 | Information Disclosure | mantra ANSI prefix leaks to URL field | mitigate | ANSI strip before tokenizing; XCUT-07 redaction unchanged (value field always "***") |
| T-05-11-06 | Tampering | favirecon.go compile failure after WebResult rename (WR-03 BLOCKER) | mitigate | favirecon.go updated atomically with favicon/web.go in same task; grep gate confirms r.Domain = 0 matches |
| T-05-11-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies |
</threat_model>

<verification>
go build ./... — exits 0.
go test ./internal/modules/web/... — exits 0 (includes TestHakoriginfinderPerHostAttribution + TestJSAUsesDirectExec).
go test ./... — exits 0.
grep -r 'Tools.Run.*jsa\|jsaPython' internal/modules/web/jsa.go | grep -v '//' | wc -l outputs 0.
grep -c 'context.WithTimeout' internal/modules/web/mantra.go internal/modules/web/gxss.go internal/modules/web/nomore403.go internal/modules/web/shortscan.go internal/modules/web/hakoriginfinder.go — each file shows at least 1.
grep 'SplitN.*" : "' internal/extract/waf/waf.go — present.
grep 'func item(' internal/extract/favicon/web.go | wc -l — 0.
grep 'func checkHostsFileReadable' internal/modules/web/httpx.go — present.
grep 'validateHostsPath' internal/modules/web/httpx.go | wc -l — 0.
grep 'r\.Domain' internal/modules/web/favirecon.go | wc -l — 0 (BLOCKER resolved: no stale .Domain read).
grep 'r\.Host' internal/modules/web/favirecon.go — present (reads renamed WebResult.Host).
</verification>

<success_criteria>
All 7 critical blockers (CR-01..CR-07) addressed across the 05-09/05-10/05-11 gap plans.
All 9 warnings (WR-01..WR-09) addressed; WR-03 co-change covers BOTH favicon/web.go AND favirecon.go.
IN-01, IN-02 addressed (co-located with blocker files).
Behavioral tests for CR-03 and CR-06 in hakoriginfinder_test.go — pass GREEN.
go build ./... and go test ./... both exit 0.
Multi-writer integration test from 05-09 remains GREEN.
favirecon.go compiles after WebResult.Domain→Host rename: grep 'r\.Domain' internal/modules/web/favirecon.go returns 0 matches.
</success_criteria>

<output>
Create .planning/phases/05-web-pipeline-e2e/05-11-SUMMARY.md when done.
</output>
